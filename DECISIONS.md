# DECISIONS.md — Design Decision Log

One line per consequential decision + rationale, so later sessions don't re-litigate or contradict earlier choices. Numbered, append-only; superseding a decision gets a new entry referencing the old one.

- **D-001 Bootstrap exception to "nothing directly on main":** the `.gitignore` root commit lands directly on `main` (GITHUB-WORKFLOW.md §1 explicitly orders it first); everything else reaches `main` only via `dev` promotions. Rationale: a branch cannot be cut from an empty repo.
- **D-002 Python toolchain = uv + Python 3.12:** uv manages the venv, lockfile (`uv.lock`, committed) and Python pin; CI uses `astral-sh/setup-uv`. Rationale: fastest reproducible installs, single tool for venv+lock+run.
- **D-003 Tests on SQLite (aiosqlite), runtime on PostgreSQL:** models are written PostgreSQL-first with SQLite-compatible type fallbacks (e.g. JSON variant for JSONB, Numeric for money); the full suite and CI run against in-memory/file SQLite, while docker-compose runs Postgres. Rationale: the prompt requires PostgreSQL-first + SQLite-compatible demo; SQLite keeps CI fast and dependency-free. DB-level financial constraints must be expressed portably (CHECK constraints, not Postgres-only features) or get Postgres-only guards documented per case.
- **D-004 Frontend package manager = npm:** lockfile committed; no pnpm/yarn. Rationale: zero extra tooling on contributor machines.
- **D-005 CI check names are stable from PR #1:** single workflow `ci.yml` with jobs `backend` and `frontend`; each job no-ops green while its directory doesn't exist yet. Rationale: branch protection requires named status checks; names must exist before the code does.
- **D-006 Makefile and docker-compose.yml are deferred to the first backend PR:** a Makefile whose targets point at directories that don't exist would violate the no-orphan-files rule in spirit. Rationale: every committed file must work the day it lands.
- **D-007 Tenancy via non-bypassable session filter:** a `do_orm_execute` hook adds `with_loader_criteria(TenantMixin, ...)` reading a `current_tenant_id` ContextVar to every ORM statement (lazy loads and bulk writes included), fail-closed when unset unless `system_context()` is active (exactly four call sites); `before_flush` stamps/validates tenant_id; composite `(tenant_id, id)` FKs + SQLite `PRAGMA foreign_keys=ON` are the DB backstop. Only mechanism with zero cooperation from query authors. Full spec: docs/architecture.md.
- **D-008 JWT access+refresh with CAS rotation and 10 s grace window:** HS256 with `ATLAS_JWT_SECRET`; 15-min stateless access token with `ver` checked against `core_users.token_version`; 14-day refresh JWT in an httpOnly cookie scoped to /api/v1/auth, rotated via compare-and-swap on hashed jti in `core_refresh_sessions` with reuse → family revocation, prev-jti accepted within 10 s so multi-tab races don't force logouts; argon2id (t=3, m=65536, p=4) off-thread. Full spec: docs/architecture.md.
- **D-009 RBAC as data + partial-bound WrapSerializer masking:** code-defined `core_permissions` catalog, tenant-scoped `core_roles` + join tables; `require_permission(key)` dependency resolves effective permissions in one join query, TTL-cached 60 s keyed on (tenant, user, token_version); field masking via `Masked(tp, permission)` WrapSerializer reading a `current_permissions` ContextVar, masked fields excluded from Update schemas. Tenants can't invent keys no code checks. Full spec: docs/architecture.md.
- **D-010 Split-phase audit capture in-transaction:** `before_flush` diffs updates/deletes from attribute history, `after_flush` captures inserts (generated PKs), `after_flush_postexec` writes one Core insert to append-only `core_audit_log` in the same transaction; per-dialect triggers (ATLAS_AUDIT_APPEND_ONLY) forbid update/delete; ORM bulk writes on audited models are hard errors. Audit is exactly as atomic as the change. Full spec: docs/architecture.md.
- **D-011 In-process event bus, collect-then-dispatch in-transaction:** `publish()` buffers in session.info; `run_in_uow()` drains FIFO before commit, handlers share the session (cap 50, cycle error), any handler failure rolls back everything — goods issue without COGS can never commit. `events.py` becomes a sanctioned declarative cross-module import (STRUCTURE §5 amendment); swap path is an outbox bus behind the same Protocol. Full spec: docs/architecture.md.
- **D-012 Document registry + flow links + gapless numbering (merged):** `core_documents` registry (nullable doc_no, partial unique index) with `DocumentMixin` NOT NULL document_id, `core_doc_links` edges traversed by one recursive CTE; numbers from `core_number_sequences` via atomic UPDATE...RETURNING, claimed only in the transaction that makes a document permanent (drafts numbered at posting) — so gaplessness falls out of ACID with no burned numbers. Full spec: docs/architecture.md.
- **D-013 Idempotency by PK reservation + atomic route capture:** `core_idempotency_keys` PK(tenant, endpoint, key); guard inserts 'in_progress' in a separate committed transaction (concurrent duplicates collide), replays stored responses on completed+matching hash; the handler's `idem.capture(schema)` stages the completed UPDATE on the business session so replay record and document commit atomically — fail-closed if capture is forgotten. Required on every financial/stock-document endpoint. Full spec: docs/architecture.md.
- **D-014 Keyset pagination + one error envelope + ATLAS_ token translation:** `{items, next_cursor, limit}` with base64url cursors fingerprinting sort+filters, expanded portable seek predicate, never OFFSET; all errors as `{error: {code, message, details, request_id}}` with dot-namespaced codes; a DBAPIError translator maps trigger tokens (ATLAS_PERIOD_CLOSED etc.) to envelope codes so DB backstops surface like service checks. Full spec: docs/architecture.md.
- **D-015 Exact money via TypeDecorators (supersedes D-003's "plain Numeric"):** MoneyType/QuantityType (PG NUMERIC(18,6), SQLite INTEGER micro-units ×10^6) and RateType (20,10 / ×10^10); Decimal-only in Python, strings in JSON; quantize ROUND_HALF_UP to currency decimals at posting; single largest-remainder `allocate()` in new `core/money.py` (STRUCTURE §2 amendment); triggers do comparisons and SUMs only. Plain Numeric float-round-trips on SQLite and breaks exact trigger sums. Full spec: docs/architecture.md.
- **D-016 Custom fields owned by core, not industry:** `core_custom_field_defs` registry + `validate_custom_fields()` + JSONB/JSON `custom_fields` column helper live in new `core/custom_fields.py` (STRUCTURE §2 amendment) — industry-module ownership would force upward imports from finance/inventory; templates and admin CRUD write defs through core services; flat scalar keys only, DECIMAL as strings, portable `.as_string()`-style JSON paths. Full spec: docs/architecture.md.
- **D-017 Universal journal: one-side CHECK, two-flush posting, reversal-only:** `fin_journal_entries` (nullable entry_number claimed at posting) + `fin_journal_lines` with all CO dimensions, transaction+functional amount pairs, `ck_fin_journal_lines_one_side`; balance trigger on DRAFT→POSTED (ATLAS_UNBALANCED_ENTRY), immutability triggers (ATLAS_POSTED_IMMUTABLE) allowing only the reversal status mutation; posting mutates loaded lines then header across two flushes; residual functional cent absorbed largest-remainder (no rounding line). Full spec: docs/architecture.md.
- **D-018 Period close enforced by per-dialect triggers + service check:** `fin_fiscal_periods`; `trg_fin_entries_period_open` re-derives the period from posting_date on DRAFT→POSTED (plus an INSERT twin) and aborts ATLAS_PERIOD_CLOSED when no OPEN period covers the date — catching wrong-period-id service bugs; service raises 422 'finance.period_closed' first; close refuses while drafts exist in the period. Triggers are the only cross-table guard on both engines. Full spec: docs/architecture.md.
- **D-019 FX: posting-time translation, realized at clearing, auto-reversing revaluation:** `fin_exchange_rates` (SPOT/CLOSING), `get_rate()` fails loud on missing rates; functional amounts frozen at posting (largest-remainder balancing); realized gain/loss posted inside the clearing entry via purpose-keyed `fin_posting_defaults`; `run_fx_revaluation` posts one FX_REVAL entry per (currency, account) and its next-period auto-reversal, validating up front that the next period is open. Full spec: docs/architecture.md.
- **D-020 Inventory costing: moving average + FIFO, same-transaction COGS, no negative stock:** `inv_item_valuations` updated under `with_for_update` (SQLite no-ops it; single-writer lock suffices, pg-marked tests prove the PG path); FIFO via `inv_cost_layers` + `inv_layer_consumptions` with exact reversal replay; the event bus posts the COGS journal entry in the SAME transaction as the move — stock and GL can never diverge; negative stock forbidden by service + CHECKs. Full spec: docs/architecture.md.
- **D-021 Statements are pure projections of `fin_journal_lines`:** five account types + cash_flow_category on `fin_accounts`; trial balance/P&L/balance sheet/indirect cash flow all derive from ONE aggregate over lines (denormalized is_posted/posting_date, no header join); retained earnings computed cumulatively on the fly; no stored totals anywhere; partial covering index `ix_fin_journal_lines_proj` keeps it sub-second to ~10M lines. Full spec: docs/architecture.md.
- **D-022 Single Alembic chain on both engines; migrations are the only schema source:** `create_all` banned even in tests (triggers live only in migrations); batch_alter_table everywhere + MetaData naming_convention; per-dialect trigger DDL side by side, re-executed after any batch-alter of a trigger-bearing table (SQLite copy-rebuild silently drops triggers); CI runs full suite on SQLite plus downgrade/upgrade and `-m pg` DB-guard subset on postgres:16. Full spec: docs/architecture.md.
- **D-023 Hand-written frontend types with a compile-time drift gate:** per-module `types.ts` mirrors Pydantic schemas (Decimal/date/UUID → string, enums → literal unions, Masked → `T | null`); committed `backend/openapi.json` (freshness pytest-asserted) feeds a gitignored openapi-typescript output checked by per-module `types.contract.ts` Equal-assertions in `npm run typecheck` — drift fails CI in both directions with zero runtime cost. Full spec: docs/architecture.md.
- **D-024 Frontend auth: memory-only access token + boot refresh + cross-tab single-flight:** access token never persisted; httpOnly refresh cookie drives a boot-time silent refresh; proactive renewal at exp−60 s, reactive single-flight retry on 401; `navigator.locks` + BroadcastChannel ensure exactly one tab rotates and the rest adopt the token — eliminating the multi-tab races the backend grace window only softens. Full spec: docs/architecture.md.
- **D-025 Tests on per-test template copies, not SAVEPOINTs:** a session-scoped fixture migrates ONE template DB (alembic upgrade head); each test gets a file copy (or PG `CREATE DATABASE ... TEMPLATE`) so real commits — idempotency reservations, run_in_uow, numbering — work as in production and triggers exist; factories provision through real services; raw-SQL DB-guard tests pin the ATLAS_ tokens on both engines; tenancy suite auto-covers new mappers. Full spec: docs/architecture.md.
- **D-026 Seed drives the real service layer, deterministically:** `seed.py` provisions five demo tenants from industry templates under system_context, then generates 3 months of interlinked P2P/O2C/production/FX flows through real services in `run_in_uow()` (RNG seeded per template+anchor); months 1-2 closed via the real close service; self-verifies trial balance, docflow chains, and inventory-GL reconciliation; `--reset` is the only sanctioned hard-delete, guarded by the `demo-` slug prefix. Seed doubles as a CI integration smoke gate. Full spec: docs/architecture.md.
- **D-027 Core platform tables and the auth router live in `core/`:** `User` (`core_users`) and `RefreshSession` (`core_refresh_sessions`) are defined in `core/models.py` and the login/refresh/logout/me endpoints in a new `core/security_router.py` (prefix `/api/v1/auth`). Rationale: auth/identity are cross-cutting platform concerns belonging to no business module (STRUCTURE §2 litmus — not a business concept), so they stay in `core/` rather than inventing a `modules/auth` package the module list doesn't sanction. `core/auth.py` stays ORM-free (crypto + JWT primitives only). Implements D-008. **Follow-on (PLAN 3.7, same precedent):** the document-flow read endpoint lives in a new `core/docflow_router.py` (`GET /api/v1/documents/{document_id}/chain`, guarded by `get_current_user`, tenant-scoped) — the document registry is platform infrastructure owned by no module, so its router joins `security_router.py` as a sanctioned core router wired in `create_app()`. Deviations from D-012's draft, all behaviour-preserving: route path is `/api/v1/documents/{id}/chain` (the binding PLAN-task path) not the draft's `/api/v1/core/documents/{id}/flow`; the D-012 numbering/docflow ORM models (`NumberSequence`, `Document`, `DocumentLink`) live in their concern files `core/numbering.py` / `core/docflow.py` (re-exported at the tail of `core/models.py` so they register on `Base.metadata`) rather than in `models.py`, to keep `models.py` under the ~350-line cap — STRUCTURE §2 lists both concern files and §8.5 ("filename promises content") favours co-locating each model with its claim/traversal logic. `core_number_sequences` keys on `(tenant_id, name)` with a `year_reset`/`current_year` pair (the binding task's column spec) rather than the draft's `(tenant_id, doc_type, year)` PK; semantics (gapless in-transaction claim, partial unique index, both-direction recursive CTE) are identical. `claim_number` uses the atomic `UPDATE ... RETURNING` path — verified working on the project's aiosqlite (3.53, >= 3.35), so the sanctioned SELECT-then-UPDATE fallback is unused; `WITH RECURSIVE` likewise verified on aiosqlite.
- **D-028 Idempotency + keyset pagination placement and behaviour (PLAN 3.8, implements D-013/D-014):** the `core_idempotency_keys` model + `reserve`/`IdempotencyContext`/`Idempotent` guard live in a new `core/idempotency.py`; the table uses the NATURAL composite PK `(tenant_id, endpoint, key)` exactly per D-013 (tenant_id overrides TenantMixin to join the PK, keeps `index=True` so the D-007 mapper-enumeration invariant holds). Migration `0007_idempotency`. Deviations, all behaviour-preserving except where noted: (1) the model is registered on `Base.metadata` from the BOTTOM of `core/db.py` (not the `core/models.py` trailing import that numbering/docflow use) because `core/idempotency` imports `core/db` for its session dependencies and `core/db` imports `core/audit`→`core/models`, so a trailing import in models would dead-lock the cycle. (2) The `Idempotent` guard reads `tenant_id` from the D-007 `current_tenant_id` ContextVar (set by `get_current_user`, which guarded routes declare first) rather than importing `CurrentUserDep`, to avoid the `deps`→`db`→`idempotency` import cycle. (3) `capture()` is called INSIDE the `run_in_uow` work so the completion UPDATE and the business document commit in one transaction (Atlas commits via `run_in_uow` inside the handler, not a deferred session-dependency commit). Consequent fail-closed deviation: on a business EXCEPTION the guard's teardown deletes the dangling IN_PROGRESS reservation and re-raises (handlers render the envelope) — fully fail-closed; on a FORGOTTEN `capture()` (handler returned 2xx) the teardown deletes the reservation and logs a server error but cannot convert the already-sent 2xx to a 500, because the `run_in_uow` commit precedes teardown — a documented relaxation of D-013's literal "teardown rolls back" (which assumed a deferred commit). Replay short-circuit: `reserve` raises `IdempotencyReplay` (an AtlasError subtype) whose handler in `app.main` re-emits the stored status+body with `Idempotency-Replayed: true`, so a completed-key replay never runs the handler body. **Keyset pagination** lives in a NEW flat core file `core/pagination.py` (`paginate`, `OrderKey`/`SortDirection`, base64url cursor codec with sort+filter fingerprint, `CursorParams`/`cursor_params` with limit cap default 50 / max 200) — STRUCTURE §2 amendment, same precedent as `core/money.py`/`core/custom_fields.py`; this DEVIATES from D-014's letter (which puts `paginate` in `core/db.py` and the codec in `core/schemas.py`) because the keyset machinery is substantial and cross-cutting (§8.5 one-concept-per-file). The `Page` envelope stays in `core/schemas.py`. The seek predicate is written in the EXPANDED portable OR-form unconditionally (verified: this project's aiosqlite 3.53 DOES support uniform-direction row-value tuples, but not MIXED-direction, and Atlas lists mix ASC/DESC, so the expansion is the only universally-correct form on both engines per D-014). Full spec: docs/architecture.md D-013/D-014.
- **D-029 AP/AR sub-ledger is keyed by an opaque `partner_id`, not a finance-owned partner master:** finance is the bottom dependency (everyone may read `finance/queries`; finance imports no module), but the vendor master lives in procurement (PLAN 6) and the customer master in sales (PLAN 7) — both *above* finance. So finance AP/AR open items, payments, dunning and aging are keyed by an opaque `partner_id: UUID` plus a denormalized `partner_name` carried on each document; finance never FK-references a partner table. The vendor/customer masters own the identity (their PK *is* the partner_id) and call finance services to create bills/invoices and run payments. This honors the parity doc's "separate vendor and customer masters, no unified Business Partner" while keeping the dependency direction legal. Realized FX is computed at clearing here (D-019). Trade-off vs a finance-owned `BusinessPartner`: no referential FK from open items to the master (the owning module guarantees the id), accepted for the clean layering.
- **D-031 AR (PLAN 4.6) placement + shared realized-FX clearing helper + dunning (implements D-029/D-019/D-030):** the four AR tables live in `finance/models/receivables.py` (re-exported from `models/__init__`); AR schemas in a NEW sibling `finance/receivables_schemas.py` and the AR endpoints in a NEW sibling `finance/ar_router.py`, both mirroring the sanctioned AP siblings (D-030) and mounting into the finance router via `include_router` (one surface at `/api/v1/finance`). AR is the AP shape with the **sign flipped** (Dr AR control gross / Cr revenue net / Cr output tax at invoice posting; receipts Cr AR / Dr bank) PLUS **dunning**. Service split: `service/customer_invoices.py` (draft+post), `service/customer_receipts.py` (receipt document + clearing), `service/ar_aging.py` (projection), `service/dunning.py`. **Generalized AP's clearing into a shared helper:** AP's `service/payment_clearing.py` was NOT reusable as-is (it took `VendorBill` objects + AP error codes), but the realized-FX MATH is identical at the customer side — so the engine-agnostic core (compute functional amounts, build the balanced clearing journal lines with explicit functional amounts via `skip_translation`, the realized-FX gain/loss line) was factored into a NEW `service/clearing_fx.py` keyed on plain `ClearedItem` tuples + a `control_is_debit` flag (AP debits the control to clear, AR credits it — the gain/loss direction inverts: AP realized = func_control − func_bank, AR realized = func_bank − func_control). `payment_clearing.py` was refactored to adapt bills into `ClearedItem`s and delegate; AR's receipts do the same with invoices — ONE FX implementation, no duplication. **Dunning** (the AR-only escalation, no AP mirror): `DUNNING_THRESHOLDS = (7, 30, 60)` days map to levels 1/2/3 via `dunning_level_for(days_overdue)`; `run_dunning(as_of)` scans OPEN overdue invoices, and when the level their age earns EXCEEDS the invoice's current `dunning_level` it raises it + stamps `last_dunned_date = as_of`, returning the notice list — it posts NO journal (dunning is state only, D-029) and is idempotent-ish per day (a re-run advances nothing already at its earned level). Permissions `finance.ar.read`/`.manage`/`.collect` (collect = receipts + dunning). `queries.py` adds `get_open_customer_invoices` + `customer_open_balance` (the latter for Sales' future credit-limit block, partner-keyed by the opaque `partner_id`). Migration `0013_finance_ar` creates four tables and alters nothing (no trigger-bearing table touched, D-022); autogenerate confirms zero drift on SQLite AND Postgres. **Deviation (PG identifier cap):** the D-022 auto FK names for the two `fin_customer_receipt_allocations` composite FKs would be 67 chars (> PG's 63), so the model + migration share explicit abbreviated names (`fk_fin_cust_receipt_allocs_tenant_id_{receipts,invoices}`) — same precedent as AP's abbreviated allocation UNIQUE name (D-030). Finance stays the bottom dependency (imports no module above it; verified); `partner_id` stays opaque (plain Uuid, no FK).
- **D-030 AP (PLAN 4.5) placement + the journal `skip_translation` clearing hook (implements D-029/D-019):** the four AP tables live in `finance/models/payables.py` (re-exported from `models/__init__`); AP request/response schemas live in a NEW sibling `finance/payables_schemas.py` and the AP endpoints in a NEW sibling `finance/ap_router.py` — both siblings (not a `schemas/`/`router/` package) exactly mirror the sanctioned `fx_router.py`/`tax_router.py` precedent, because `schemas.py`/`router.py` are at the STRUCTURE §3 400-line cap and the AP surface is self-contained; `ap_router` mounts into the finance router via the same `include_router` one-surface pattern (no second mount in main.py). The AP service is split across `service/vendor_bills.py` (draft + post), `service/payment_clearing.py` (open-item validation + realized-FX line construction), `service/vendor_payments.py` (payment document + run), and `service/ap_aging.py` (the projection) to keep every file under the cap. **Realized FX at clearing (D-019):** computed per cleared bill as `functional-at-bill-rate − functional-at-payment-rate` over the cleared transaction amount (the bill's frozen rate is read from its posted AP-control journal line, not re-looked-up), posted to the `fx_realized_gain`/`fx_realized_loss` posting-default account inside the SAME payment entry. This required a behaviour-preserving extension to `service/journal.post_entry`: a new keyword `skip_translation: bool = False` — when the caller has already set explicit per-line functional amounts (the only case where functional ≠ transaction × a single entry rate), the engine validates those balance instead of re-translating. The realized-FX line is denominated in the functional currency and carries a positive transaction amount equal to its functional amount, so the per-line one-side CHECK (D-017, on transaction amounts) holds while the balance trigger (functional sums only) balances — no zero/zero line. Open items are partner-keyed by the opaque `partner_id` stamped on the AP control line (D-029); finance imports no module above it (verified). AP documents register in `core_documents` and link bill→journal (`posts`) and payment→bills (`pays`) so the document-flow chain spans the AP flow. Migration `0012_finance_ap` creates four tables and alters nothing (no trigger-bearing table touched, D-022); autogenerate confirms zero model drift on SQLite.
- **D-032 Background-job core (PLAN 4P.5, closes #26): code-defined registry + in-process asyncio runner behind a scheduler seam; `core_jobs` not audited:** `core/jobs.py` owns the `Job` model (`core_jobs`: job_type/status/payload/result/error/submitted_by/started+finished, indexes `(tenant_id, status)` + `(tenant_id, job_type, created_at)`; migration 0017), a `@register_job` registry (code-defined like permissions), `submit_job` (PENDING row flushed inside the endpoint's uow so it commits atomically with the D-013 idempotency capture — a replayed key returns the SAME job id), and the runner: scheduled via `schedule_job(job_id, session_factory)` strictly AFTER the uow commit (no visibility race), it opens its own session from the factory the router's `get_session_factory` dependency supplied (production = app factory; tests = the per-test-engine override, which is how the runner lands in the right DB in both worlds), restores `tenant_context(tenant_id)` + the D-010 actor ContextVar from `submitted_by_user_id`, marks RUNNING (own commit), runs the handler inside `run_in_uow` with COMPLETED+result set in the same uow (business writes and terminal status are atomic; events + audit behave exactly as in-request), and on any exception commits only FAILED + the truncated error. Concurrency capped at 4 via per-event-loop semaphores (asyncio primitives bind to their first loop; tests run one loop per test); `wait_for_jobs()` makes tests deterministic. SWAP SEAM: submitters call `schedule_job`, delegating to the module `scheduler: JobScheduler` Protocol singleton (the D-011 bus pattern) — a queue runner (arq/celery) rebinds it, ignoring the session-factory argument, with zero submitter changes. Polling: `core/jobs_router.py` (`GET /api/v1/jobs[/{id}]`, auth-only + tenant-scoped, paginated — the D-027 core-router precedent). `core_jobs` is deliberately NOT audited: high-churn control rows (PENDING→RUNNING→terminal per job), same documented exclusion class as `core_refresh_sessions`/`core_idempotency_keys`; the handler's BUSINESS writes are audited normally with the submitting user as actor. The #26 fix: `POST /finance/fx-revaluation-runs` and `POST /finance/payment-runs` now submit `finance.fx_revaluation`/`finance.payment_run` jobs and return `202 {job_id, status}` (service functions unchanged; the retired sync `PaymentRunResult` wrapper was deleted — payment ids/numbers ride the job result). Model registration deviation: `core/jobs` imports `core/audit` (actor ContextVar), so like idempotency it registers on `Base.metadata` from the BOTTOM of `core/db.py`, not models.py's trailing imports (a trailing import dead-locks on the audit->models cycle).
- **D-033 Bank reconciliation (PLAN 4.9) placement + statement-as-external-document + match-rule boundary:** the two bank tables live in `finance/models/bank.py`; the service splits along concerns (`service/bank_csv.py` CSV contract, `service/bank_import.py` import/job/progress, `service/bank_reconcile.py` matching/clearing) and the HTTP surface is the sanctioned sibling pair `bank_schemas.py`/`bank_router.py` (D-030 precedent), all under the STRUCTURE cap. `BankStatement` gets DocumentMixin (the task sketch omitted it) because the required docflow statement->'posts'->clearing-entry link (D-012) needs a registry row — but NO gapless number is claimed: D-012 numbering covers documents Atlas issues, and a statement is an EXTERNAL document identified by bank account + date + source_filename, so its registry doc_number stays NULL. `matched_journal_line_id` is an opaque Uuid (read-only reference into the immutable journal, the dimension-id precedent); `cleared_journal_entry_id` is a composite tenant FK (the entry is created BY this flow). `import_job_id` is a plain nullable Uuid (the core_jobs attribution precedent), stamped via the router writing the job id back into the job payload after submit_job (a handler cannot know its own job id). Bulk insert uses the ORM-enabled `insert(Model)` executemany with explicit per-row tenant_id — sanctioned: it is an ORM statement (not a Core Table insert), inserts are not tenant-filtered by D-007 (values carry tenant_id; composite FKs backstop), and the grep gate bans only attribute-call inserts/raw SQL. Match rules v1 = exact amount+date, then exact amount ±3 days (nearest-wins, single consumption tenant-wide); the rule-3 doc-number-in-description heuristic, partial/many-to-one matching and tolerance bands are documented boundary (parity doc already lists MT940/CAMT + auto-clearing rules as laters). Statement lists sort (statement_date DESC, id) WITHOUT a created_at seek key: SQLite stores server-default CURRENT_TIMESTAMP without fractional seconds while a re-bound cursor datetime renders with `.000000`, so a created_at key lexicographically re-includes the boundary row — a pre-existing core/pagination.py issue affecting every created_at-sorted list on SQLite (PG unaffected), tracked for a core-level fix.
- **D-034 Asset accounting lite (PLAN 4.10) placement + the depreciation-run shape + the finance constants package split:** the three tables live in `finance/models/assets.py`; the service splits along concerns (`service/assets.py` register lifecycle, `service/depreciation.py` formula + posting run + job handler, `service/depreciation_read.py` reads + the register projection — the journal.py/journal_read.py cap-split precedent) and the HTTP surface is the sanctioned sibling pair `assets_schemas.py`/`assets_router.py` (D-030 precedent), mounted one-surface. **Idempotency backbone:** `UNIQUE(tenant_id, asset_id, fiscal_period_id)` on `fin_depreciation_entries` — an asset depreciates once per period EVER, so overlapping/concurrent runs collide at the DB; the run engine selects eligible assets with a NOT EXISTS anti-join, reads prior accumulated+count in ONE grouped aggregate, bulk-inserts entries with ONE executemany, and posts ONE journal entry with lines GROUPED per (expense account, cost centre)/per accumulated account (PERFORMANCE §2: the SQL statement count is constant in the asset count, query-counter pinned). A re-run for a finished period returns the existing POSTED run unchanged (the allocation idempotency pattern); the endpoint is additionally D-013 Idempotent and routes >100 eligible assets to a `finance.depreciation_run` job (202, the bank-import threshold pattern). **Exactness:** STRAIGHT_LINE uses the drift-free cumulative formulation (amount(n) = quantize(depreciable×n/life) − prior_accumulated; final period absorbs the residual) so schedules sum to cost−salvage EXACTLY; DECLINING_BALANCE floors at salvage (the crossing/exhaust period takes NBV−salvage, landing exactly on salvage). `compute_depreciation` takes a 1-based schedule `period_index` (prior entry count + 1) rather than a period object — the formula depends on schedule position, not calendar identity. NO stored NBV: the register endpoint recomputes accumulated/NBV from SUM over entries bounded by periods ending ≤ as_of (D-021 spirit); the entries' `*_after` columns are per-entry audit trail only. AssetStatus is DRAFT|ACTIVE|FULLY_DEPRECIATED only — DISPOSED is NOT in the enum (no dead values; disposal/transfer/parallel areas are recorded parity laters). The AST number is claimed at ACTIVATION (the asset's claim-at-permanence moment, D-012; partial-unique backstop), the DEP number at run posting. Three composite FKs from fin_assets to fin_accounts carry explicit abbreviated names (the D-022 column-0 convention would collide three ways). Migration `0019_finance_assets` creates three tables and alters nothing (no trigger-bearing table touched, D-022); zero autogen drift on both engines. **Constants package split (STRUCTURE §8.4):** `finance/constants.py` hit the 400-line hard cap when 4.10's enums/sequences/permissions landed, so it split into the `finance/constants/` package (`enums.py`, `documents.py`, `permissions.py`) with `__init__.py` re-exporting every name — the same §3 package rule models/ and service/ used; zero import-site changes. Test-side: `_FINANCE_KEYS` in tests factories is now derived from `core.rbac.catalog_keys()` (every registered `finance.*` key), self-extending like the tenancy mapper enumeration; the 4.10 builders live in `tests/modules/finance/factories_assets.py` because factories.py is at the cap.
- **D-035 Conditional requests on slow-changing reference data (PLAN 4P.6, closes #28): collection-level WEAK ETag in a new `core/conditional.py`:** a justified core flat file (the `core/pagination.py` precedent) holds `collection_etag` (ONE cheap aggregate `SELECT COUNT(id), MAX(updated_at)` over the model — auto tenant-scoped by the D-007 `do_orm_execute` listener because the `func` aggregate references the model's mapped columns, verified), `check_not_modified` (RFC 7232 weak comparison, handles the `W/` prefix and `*`), and `conditional_response` (the 304-or-200 epilogue: on an `If-None-Match` hit it returns a bare starlette `Response(304, {ETag})` WITHOUT awaiting the page builder — so FastAPI's `response_model=Page[T]` is bypassed and the page query never runs, making the 304 path cheaper than 200: 2 queries vs 3, query-counter pinned). Format `W/"<count>-<max_updated_micros>-<tenant8>-<request_fingerprint>"`. **Collection-level, not row-level**, because a list endpoint returns a page of a collection and `(count, max_updated)` is the cheapest sound "did anything change?" validator — any insert moves count, any update moves `MAX(updated_at)` (TimestampMixin.onupdate), any delete moves count; trade-off is that any change invalidates every page's validator, accepted for the small reference sets this covers. **The `tenant8` component** makes a cross-tenant 304 impossible even though the aggregate is already tenant-filtered. **The `request_fingerprint`** (cursor+limit+filters, reusing `pagination.filter_fingerprint`) is the key correctness point: it is folded into the ETag so a 304 can only ever be served for the IDENTICAL page request and never returns the wrong slice. Applied to the seven slow-changing reference lists ONLY (`accounts`, `account-groups`, `currencies`, `tax-codes`, `fiscal-years`, `fiscal-periods`, `posting-defaults`); transactional lists (journal-entries, bills, invoices, receipts, payments, exchange-rates, bank-statements, depreciation/reval runs, jobs) deliberately carry none. **Structure deviation:** adding `request`/`response` + the etag computation pushed `finance/router.py` over the 400-line cap, so the journal-entry endpoints moved to a new sanctioned sibling `finance/journal_router.py` (the `fx_router.py`/`tax_router.py` one-surface precedent) — a behaviour-preserving extraction, journal is a distinct aggregate from the COA/fiscal reference data; router.py is now 322 lines.
- **D-036 Inventory stock model (PLAN 5.2): the move ledger is the quantity SSOT, the quant is a maintained projection, negative stock is forbidden at service + CHECK on both engines:** `inv_stock_moves` is an **append-only, POSTED-at-creation, immutable** ledger (no draft phase — unlike journals which are DRAFT→POSTED — because a move IS the ledger; corrections are reversing moves, never edits, the D-017 reversal philosophy applied to stock). `quantity` is **always positive**; `move_type` (RECEIPT/ISSUE/TRANSFER/ADJUSTMENT) decides which of from_bin/to_bin participate via the greppable `constants.MOVE_BIN_SIDES` table (RECEIPT→to only, ISSUE→from only, TRANSFER→both-distinct, ADJUSTMENT→exactly-one-side), so direction is structural not a sign — the journal one-sided-amount philosophy. A move carries `DocumentMixin` and claims its **gapless `STK-` number at creation** (D-012 claim-at-permanence, the orders/receipts branch). **On-hand is a MAINTAINED projection, not a re-derived query:** `inv_stock_quants` (UNIQUE(tenant,item,bin,lot)) holds current on-hand per (item,bin,lot) and is updated in the SAME transaction as every move (decrement from_bin, increment to_bin) — the moving-average `inv_item_valuations` precedent (D-020), so on-hand reads are an indexed point lookup, not an unbounded SUM over move history (PERFORMANCE §1). The ledger stays the SSOT/audit trail; the quant is **reconcilable from it** and a quant reaching exactly 0 is **deleted** (the projection holds only live stock, so `on_hand_by_bin` never lists empty bins). **Negative stock is forbidden outright (D-020), enforced twice:** the service raises `InsufficientStockError`→422 `inventory.insufficient_stock` pre-flight, and a portable **`CHECK(on_hand_qty >= 0)`** on `inv_stock_quants` is the bypass-proof DB backstop — a single-column comparison, exact on PG NUMERIC and SQLite micro-unit INTEGER (D-015/D-022), proven on real Postgres by the `-m pg` guard tests. Quant rows are locked `with_for_update` in deterministic bin-id order (PG row lock; SQLite no-op per D-020; deadlock avoidance). **Lot/serial:** a RECEIPT of a tracked item creates the `inv_lots`/`inv_serials` master instance on the fly (5.1 deferred instance creation to receipts); ISSUE/TRANSFER must reference an existing instance; a serial moves wholesale (quantity==1 enforced). **Reversal-not-edit:** `reverse_move` posts the opposite move (from/to swapped; a RECEIPT reverses as an ISSUE) linked via docflow `reverses`. **Structure:** `models.py` split into a `models/` package (`masters.py` 5.1 + `stock.py` 5.2) at the 400-line cap; the 5.2 HTTP surface is a mounted sibling `stock_router.py` (the finance `journal_router`/`ap_router` precedent), one surface at `/api/v1/inventory`. **No `events.py` yet:** a 5.2 move publishes nothing cross-module (on-hand is intra-module; costing/COGS-via-event arrives in 5.3). Migration `0021_inventory_stock` creates four tables, alters nothing (no trigger-bearing table touched, D-022), reversible, zero autogen drift on both engines.

- **D-037 Inventory costing (PLAN 5.3): valuation is per (item, warehouse); the move ledger + quants are the quantity SSOT, valuation/layers are the value SSOT, all updated in ONE transaction; COGS posts via the event bus in that same transaction:** Which engine a `(item, warehouse)` uses is the item's `costing_method` (MOVING_AVERAGE | FIFO), defaulted from its category. **Moving average** — `inv_item_valuations` (UNIQUE(tenant,item,warehouse), `CHECK(on_hand_qty >= 0)`) holds `on_hand_qty`, full-precision `avg_unit_cost`, `total_value`, locked `with_for_update` (PG row lock; SQLite FOR-UPDATE no-op + single-writer lock, D-020): receipt adds `qty×unit_cost` and recomputes `avg = total_value/on_hand` **unrounded**; issue subtracts `quantize(qty×avg, currency dp, HALF_UP)` and, when on_hand hits **exactly 0**, **flushes the residual `total_value` to the price-difference account** so value and quantity never disagree (the rounding-drift absorber — value lands at exactly 0 even for non-terminating averages). **FIFO** — `inv_cost_layers` (one per receipt, `CHECK(0 <= remaining_qty <= original_qty)`) consumed oldest-first by `(received_at, created_at, id)` under `with_for_update` (created_at is the insertion-order tiebreaker when dates tie — uuid4 ids are not monotonic), with one `inv_layer_consumptions` row per touched layer (the per-layer COGS audit trail AND the **exact-reversal** record). **Cost input:** `inv_stock_moves.unit_cost` (MoneyType nullable, batch-added) — RECEIPT/positive-ADJUSTMENT REQUIRE it (the entry cost); ISSUE/negative-ADJUSTMENT IGNORE it and the engine computes/writes it back; TRANSFER carries the current valuation. **Same-transaction COGS (the inventory↔finance seam, D-011/D-020):** `create_move` updates valuation/layers right after the quant update, then `publish(StockValued(...))` (buffered) carrying the value Δ, the three GL account ids (inventory/COGS/price-difference, opaque per D-029) AND the **chosen offset account** (ISSUE→COGS, RECEIPT/ADJUSTMENT→price-difference, reversal→the opposite of the original's offset); `run_in_uow` drains it before commit and `finance/handlers.py` (the **first real cross-module handler**) posts the journal **through the finance posting service** (`create_draft_entry` + `post_entry`, never raw inserts) in the shared session — so one commit = move + quant + valuation + journal + docflow `posts` link, and **any handler failure rolls the whole transaction back** (a stock move can never exist without its journal entry). The COGS journal posts with `move_date`, so a move dated into a **closed period** fails (the period trigger fires in the same transaction → the move rolls back; you cannot move stock into a closed period — correct by construction). **Postings:** RECEIPT/ADJUSTMENT-up Dr inventory / Cr price-difference (standalone offset; procurement GR overrides later, 6.x); ISSUE Dr COGS / Cr inventory; ADJUSTMENT-down Dr price-difference / Cr inventory; **TRANSFER within one inventory account is value-neutral → NO event, NO journal**; a reversal posts the exact reverse of the original's entry. **Reversal is replay, not recompute:** reversing an ISSUE replays its consumptions backward onto the exact layers / re-adds MAV value at the original cost; reversing a RECEIPT zeros its layer (only if unconsumed, else error) / removes MAV value. **Read surface:** `GET /stock-valuations` + `GET /items/{id}/cost-layers` (permission `inventory.valuation.read`); `queries.item_value`/`valuation_summary` for the inventory-value KPI sum qty×cost in **Python** (typed Decimals), never `func.sum(qty×cost)` — two scaled-integer columns multiply to ×10^12 on SQLite which MoneyType cannot un-scale (D-015 discipline). **Structure:** costing service split into `costing.py` (orchestration), `costing_mav.py`, `costing_fifo.py`, `costing_reversal.py` (all <400 lines); handler registered at the app factory `register_event_handlers` (the deterministic D-011 seam, re-runnable + idempotent so the test harness re-registers after `clear_subscriptions`). Migration `0022_inventory_costing` creates three tables + batch-adds `unit_cost` to non-trigger-bearing `inv_stock_moves`, reversible, zero autogen drift; the `CHECK`s + `with_for_update` proven on real Postgres by the `-m pg` guard tests.
- **D-038 Inventory physical & cycle counts (PLAN 5.4): a count posts its variances EXCLUSIVELY through stock ADJUSTMENT moves (never a bespoke journal), and the system_qty is RE-VALIDATED against live on-hand at post time so a concurrent move can't post a wrong variance:** A count (`inv_stock_counts` — `DocumentMixin` + gapless `CNT-` number claimed at creation, `count_type` PHYSICAL|CYCLE, `warehouse_id`, `status` DRAFT→COUNTING→POSTED|CANCELLED, `count_date`) snapshots one line per in-scope quant into `inv_stock_count_lines` (`system_qty` snapshot, `counted_qty` nullable, and at post `variance_qty` / `adjustment_move_id` / `unit_cost`; UNIQUE(tenant,count,item,bin,lot)). PHYSICAL enumerates **every** quant in the warehouse, CYCLE only the chosen `item_ids`/`bin_ids` — type only changes the snapshot scope, not the post path. **Variance posts via ADJUSTMENT moves, NOT a bespoke journal:** for each line `post_count` computes `variance = counted − live-system`; a non-zero variance posts ONE ADJUSTMENT move through `stock_moves.create_move` (positive → into the bin, negative → out of it), which already runs the 5.3 costing engine + publishes `StockValued` so the price-difference journal posts via the event bus **in the same transaction** — the count inherits every costing/GL/audit invariant for free (the reuse-the-existing-path principle; a count never touches the journal directly). The count's document links to each adjustment move via docflow (`counts`); a **zero-variance line posts no move** (`adjustment_move_id` NULL). The positive-adjustment **unit cost** is sourced from `queries.current_unit_cost` (the moving-average `avg_unit_cost`, or the FIFO live-layer weighted average — the same book cost a value-neutral transfer carries), so value added = book cost; an item the system thinks is empty enters at cost 0 (quantity-only correction). **Re-validation at post (the concurrency-safety crux):** `system_qty` is only the variance-PREVIEW baseline; `post_count` **re-reads live on-hand** for each line as the authoritative system qty, so a move landing between snapshot and post can never post a wrong variance — the resulting on-hand always equals the **counted** qty, not `counted − stale`. The whole post runs in `run_in_uow`, so all variance moves' journals + the count commit as one transaction; a **closed-period `count_date`** trips the period trigger via the adjustment's journal and rolls the **whole** post back (D-018). **`POSTED` is terminal** — a status guard rejects re-post (no double adjustment, D-013 idempotency on the post endpoint) and a posted count is never cancelled; corrections are new counts/adjustments (the append-only ledger philosophy). Posting requires **every** line counted (else 422). **Background path (PERFORMANCE §3):** a post whose snapshot shows > `COUNT_POST_SYNC_MAX_VARIANCES` (200) variance lines is submitted as an `inventory.count_post` job (202 `{job_id}`); at or below it the post runs inline (200) — the depreciation-run (100) / bank-import (1000) threshold precedent; the job handler delegates to the same `post_count` engine. **Structure:** writes (`service/counts.py`) split from reads (`service/count_reads.py`) from the start (the prior 566-line single file was unacceptable — both now <400), the count schemas split to a sibling `count_schemas.py` (the `assets_schemas.py` precedent, keeping `schemas.py` under cap), and the job handler in `service/count_jobs.py`; `count_router.py` is a sibling mounted into `router.py` (the one-surface stock_router precedent). Migration `0023_inventory_counts` creates two tables + indexes, no trigger-bearing alters, reversible, portable both engines.

- **D-039 Vendor master (PLAN 6.1) opens procurement, owns the entity finance AP refers to by opaque id, and models payment terms as a net-days integer:** Procurement is the third business module and is placed ABOVE inventory in the dependency order — it may import `finance/queries` AND `inventory/queries` downward, and exposes its own `procurement/queries.py` as the only file modules above it import (STRUCTURE §5). **(a) The vendor IS finance AP's `partner_id` (D-029 made concrete).** Finance owns no vendor table; every AP bill/payment already stores its vendor as an opaque `partner_id` (a plain `Uuid`, no FK) + a denormalized `partner_name`. PLAN 6.1 makes `Vendor.id` exactly that `partner_id`: procurement owns the entity, finance refers to it by opaque id, and `queries.get_vendor_for_partner` resolves a bill's `partner_id` back to a vendor for AP aging/reporting. Finance can never FK to `proc_vendors` (it is below procurement) — the relationship is by id only, the inventory-item/GL-account precedent. **(b) Payment terms = a `payment_terms_days` integer on the vendor, not a terms master.** A net-days value (30 = NET30, CHECK ≥0, default 30) drives AP due dates as `bill_date + days` — the simplest model that matches how AP already computes them. Richer term schedules (2/10 NET30 early-payment discounts, multi-instalment plans) are deferred per the s4hana-parity Procurement section and would arrive as a separate terms entity referenced from the vendor; the integer is forward-compatible. **(c) Vendor codes are user-supplied, unique per tenant, no auto-sequence** (the item_code/account-code precedent — a master carries a code, not a gapless document number; the P2P documents in 6.2+ claim gapless numbers, the master does not). **(d) Approved items are an info-record-lite.** `proc_vendor_approved_items` captures the vendor↔item link (with the vendor's own SKU) but NO time-dependent price/lead-time/valid-from-to — exactly the parity "purchasing info records = PARTIAL" cut. The `item_id` is an OPAQUE inventory id (validated via `inventory/queries.item_exists`, no cross-module FK, D-029), while `vendor_id` is an intra-module composite tenant_fk. **(e) Vendor status (ACTIVE/BLOCKED/INACTIVE) has no terminal state** — all three transitions are allowed, because a block/retire must be reversible and a vendor's AP history must stay referenceable; 6.2 reads the status to refuse new POs against non-ACTIVE vendors (a soft policy at the chain, not a master-level lock). **(f) Sanctioned cross-module query added:** `finance/queries.currency_exists` (the by-code existence check the vendor's `default_currency_code` validates against, mirroring `account_exists`); inventory/queries needed nothing new. No events/handlers ship in 6.1 — vendor master drives no cross-module effects; those arrive with the P2P documents in 6.2+.
- **D-040 Procurement P2P chain (PLAN 6.2): requisition → RFQ → PO numbered at CREATION, gated by a single-characteristic value-threshold rule, with approved-items ENFORCED on POs:** The three procurement documents (`PurchaseRequisition`/`PurchaseRequisitionLine`, `Rfq`/`RfqLine`, `PurchaseOrder`/`PurchaseOrderLine`) are each `DocumentMixin` header+line pairs registered in `core_documents` with predecessor/successor docflow edges (`"sourced_by"` requisition→rfq, `"ordered_by"` rfq→po and requisition→po). **(a) Numbering claimed AT CREATION, not posting.** Unlike finance (which numbers a bill/journal at *posting*), procurement claims the gapless `PR-`/`RFQ-`/`PO-` number when the document is *created* — a DRAFT requisition already prints a number the sourcing/ordering chain quotes, so the document is referenceable immediately (the orders/receipts claim-timing branch of D-012, the inventory stock-move precedent). Gaplessness still holds: creation IS the committing transaction, so the counter increment and the row insert commit-or-roll-back together. **(b) The approval rule is data-driven, single-characteristic (amount), single-step, single-currency** (`proc_approval_rules`, UNIQUE(tenant, document_type ∈ {REQUISITION, PURCHASE_ORDER}), `threshold_amount` CHECK ≥0). `service.requires_approval(document_type, amount, currency)` returns True iff `amount >= threshold` AND the rule's currency matches the document's; **no active rule, an inactive rule, or a currency mismatch ⇒ no gate** (a tenant that hasn't configured thresholds runs without approval; cross-currency thresholds need FX, deferred). This is exactly the parity "approval/release strategies = PARTIAL, data-driven value-threshold rules only" cut — multi-characteristic / multi-step release strategies are the documented later. The submit step (requisition, on the estimated total) and the send step (PO, on `total_amount`) consult it: at/above ⇒ stay SUBMITTED / go PENDING_APPROVAL awaiting the distinct `.approve` key; below ⇒ auto-approve. **(c) Approved-items are ENFORCED on POs, not advisory.** A PO line item MUST be in the vendor's approved-items list (an inactive approval counts as not approved) else 422 `procurement.item_not_approved`; the vendor must be ACTIVE else 422 `procurement.vendor_not_active` — the v1 source-control rule, read through `procurement/queries` (never a cross-module FK). **(d) An RFQ targets ONE vendor in v1** (multi-bidder comparison deferred); a PO **snapshots** the vendor's `payment_terms_days` + currency at create so a later vendor edit cannot rewrite an open PO's due-date math (the 6.4 bill reads the snapshot). **(e) PO line columns staged for 6.3/6.4 now:** `received_quantity` (default 0, raised by 6.3 goods receipts; `po_line_open_quantity` = ordered − received) and the opaque `tax_code_id` (for the 6.4 bill) land in the 6.2 schema so GR + match need no migration. **(f) NO events in 6.2** — nothing in the chain posts to finance/inventory (a PO is a commitment, not a posting); the first procurement event is the 6.3 goods receipt (→ inventory move + GR/IR journal), so `events.py`/`handlers.py` stay absent (no-stub rule). **(g) Behaviour-preserving package splits** at the 400-line cap (the finance/inventory precedent, re-exported so import sites are unchanged): `models/`, `schemas/`, `service/` packages + sibling `requisition_router`/`rfq_router`/`po_router`/`approval_rule_router` mounted into `router.py`. Migration `0025_procurement_documents` creates seven tables + indexes, no trigger-bearing alters, reversible, portable both engines (zero autogen drift, applies on PG to head).
- **D-041 Procurement goods receipt (PLAN 6.3): a GR posts via the event bus into inventory (Dr Inventory / Cr GR-IR) using the inventory valuation-offset override; the GR↔move link is docflow, not a cross-module FK; the whole chain (GR + moves + journals + PO update) is one atomic transaction:** A `GoodsReceipt`/`GoodsReceiptLine` (`proc_goods_receipts`/`_lines`, `DocumentMixin` + gapless `GR-` number claimed at creation, `purchase_order_id` composite FK, `vendor_id` + per-line `item_id`/`unit_cost` snapshot from the PO line, opaque `warehouse_id`/`bin_id` validated via `inventory/queries`) records receipt of PO goods. **Lifecycle DRAFT → POSTED** (chosen over post-at-creation): a GR is a document a clerk builds line-by-line then posts, unlike a stock move which is permanent at creation; DRAFT can CANCEL, **POSTED is terminal** (corrected by a reversing GR / return — Phase 7 RMA; v1 ships no reverse-GR). **(a) The GR/IR three-way flow.** Posting a GR posts **Dr Inventory / Cr GR-IR clearing** (`gr_ir_clearing` is a new per-tenant posting default — a LIABILITY/clearing account; `finance/queries.gr_ir_clearing_account` resolves it and raises 422 `finance.posting_default_unmapped` when unmapped, so a GR can't post without it). The 6.4 bill posts Dr GR-IR / Cr AP, clearing it — GR/IR holds received-not-invoiced value. **(b) The valuation-offset override (the inventory integration point).** `inventory/service/stock_moves.create_move` gains an optional `valuation_offset_account_id`; for a RECEIPT it OVERRIDES the costing engine's standard price-difference offset — the engine sets `StockValued.offset_account_id` to it and `finance/handlers.post_stock_valuation_journal` (already generic over the offset) credits GR/IR instead. The override threads end-to-end `create_move → costing.apply_costing → _apply_inbound → StockValued.offset_account_id → finance/handlers`, changes only the Cr leg never the costing math, is ignored on non-receipt types and on reversals, and defaults None ⇒ the standalone-receipt behaviour (and its tests) is unchanged. **(c) The cross-module mechanism is the EVENT BUS, not a direct call (the STRUCTURE §5 crux).** Procurement must NOT import inventory's service (modules never import each other's service.py/models.py). So `post_goods_receipt` publishes `procurement/events.GoodsReceiptPosted` (a plain typed event carrying the per-line receipts — item/bin/qty/unit_cost/lot/serial/inspection — plus the resolved GR/IR account, move_date, and the GR document id), and **`inventory/handlers.py`** (new — the first inventory handler) subscribes and calls its OWN `stock_moves.create_move` per line with `valuation_offset_account_id`=GR/IR. Those moves in turn publish `StockValued`, which finance handles — a **two-hop, same-transaction chain** (procurement → inventory → finance), the exact mirror of the existing inventory→finance COGS pattern (publisher exposes events; subscriber's handlers.py imports the publisher's events.py + the link-type constant, the sanctioned §5 events allowance — finance/handlers importing inventory.constants is the precedent). Verified: `grep` confirms procurement imports ONLY `finance/queries` + `inventory/queries` (no service/models); inventory/handlers imports only procurement `events` + the `moved_by` link constant. **(d) The GR↔move linkage is DOCFLOW, not a cross-module FK.** Inventory owns the move, so a GR line carries NO `stock_move_id` column; instead the handler writes a docflow edge GR→`moved_by`→move (and procurement writes PO→`received_by`→GR), so the chain is queryable without procurement FK-ing into inventory's table. **(e) The whole post is ONE atomic transaction** in `run_in_uow`: GR status + `posted_at` + the PO `received_quantity` raise + PO status advance (PARTIALLY_RECEIVED if any line open, RECEIVED if all fully received) are procurement's own writes; the N moves + N inventory-debit/GR-IR journals are the handler's; all commit together or roll back together. A **closed-period `receipt_date`** trips a move's journal period trigger inside the same transaction and rolls the **entire** post back (no move, no journal, GR stays DRAFT, PO untouched) — correct by construction. **(f) Over-receipt is REJECTED in v1** (422 `procurement.over_receipt`) — a receipt line cannot exceed its PO line's open quantity (ordered − received); a configurable tolerance is a 6.4/later refinement. The PO must be APPROVED/SENT/PARTIALLY_RECEIVED to receive (else 422 `procurement.po_not_receivable`). **(g) The inspection hook is a FLAG ONLY in v1.** `requires_inspection` (per GR line, default False) flags the received line and is passed through to the stock move for traceability — it does NOT route to a QI bin, block downstream use, or gate the move; the inspection-lot disposition (accept/reject/rework) is Phase 9 Quality. **(h) Inline, no background path** — a GR is a single delivery (small), so the post runs inline (O(lines)), unlike bulk count posts / payment runs. **Structure:** writes (`service/goods_receipts.py`, 373 lines) split from reads (`service/goods_receipt_reads.py`) at the cap; `models/goods_receipts.py` + `schemas/goods_receipts.py` re-exported from the packages; sibling `goods_receipt_router.py` mounted into `router.py` (the one-surface precedent); `inventory/service/stock_moves.py` re-trimmed to 399 lines after the override param. Migration `0026_procurement_goods_receipts` creates two tables + indexes, NO trigger-bearing alters (the `gr_ir_clearing` purpose is data, the valuation-offset is a service param — neither is schema), reversible, portable both engines, applies on PG to head.
- **D-042 Procurement 3-way match → AP bill + reorder-point auto-requisitions (PLAN 6.4): the matched bill DEBITS GR/IR at PO cost (clearing the receipt credit to ZERO) + routes the invoice-price difference to PPV, created in FINANCE via the event bus; the reorder scan creates procurement requisitions from an inventory query — completes Phase 6 / procurement:** An `InvoiceMatch`/`InvoiceMatchLine` (`proc_invoice_matches`/`_lines`, `DocumentMixin` + a gapless `MATCH-` number claimed at creation, `purchase_order_id` composite FK, `vendor_id` = the opaque finance partner_id snapshot, `vendor_invoice_ref` free text, `gr_ir_account_id` snapshot, opaque `tax_code_id`) compares a vendor invoice against the PO (price) and goods receipt (quantity). **(a) The cross-module mechanism is the EVENT BUS (the STRUCTURE §5 crux, mirroring D-041's GR→inventory bridge):** procurement must NOT import finance/service, so `post_invoice_match` PUBLISHES `procurement/events.InvoiceMatched` (a plain typed event carrying per-line the GR/IR portion at PO cost + the price variance + the resolved GR/IR, PPV and AP-control account ids + the vendor/partner_id/name/due-date) and **`finance/handlers.create_bill_for_match`** subscribes and creates+posts the AP vendor bill through the finance AP service (`create_vendor_bill` + `post_vendor_bill`, never raw inserts) in the SAME `run_in_uow` transaction — finance handling its OWN bill posting, the event published by procurement (the inventory→finance COGS precedent inverted). Verified: procurement imports ONLY `finance/queries` + `inventory/queries` (no service/models); `finance/handlers` imports only procurement `events` + the `billed_by` link constant (the sanctioned events.py §5 allowance — finance/handlers importing inventory.events/constants is the D-041 precedent). **(b) The GR/IR-clears-at-PO-cost + PPV accounting (the 3-way-match subtlety):** the goods receipt credited GR/IR at PO cost at receipt (D-041); the matched bill builds vendor-bill lines whose accounts are the **GR/IR clearing** account (net = matched_qty × PO cost) + the **purchase-price-variance** account (net = (invoiced − PO price) × qty), so the posted journal is **Dr GR/IR (at exactly the receipt PO cost) + Dr/Cr PPV + Dr input tax / Cr AP control (at the vendor-invoiced gross)**. GR/IR therefore nets to **zero** once a PO is fully received and billed (the end-to-end test asserts the account balance is 0 after receipt+bill, with PPV holding any in-tolerance price difference — the procure-to-pay loop closes). New finance posting-default purposes (DATA, not schema): `purchase_price_variance` + `ap_control` (the match-triggered bill resolves the AP control account from a default since procurement holds no GL account; a finance-entered bill still supplies its own `ap_account_id`), resolved via `finance/queries.purchase_price_variance_account` / `ap_control_account` (raise 422 `posting_default_unmapped` when unmapped). **(c) The over-billing 3-way constraint:** a `billed_quantity` column on `proc_purchase_order_lines` (batch-added, default 0, like 6.2's `received_quantity`) gives `po_line_open_to_bill = received − billed`; a match line's matched_quantity cannot exceed received − already-billed → 422 `procurement.over_billing` (you can never bill beyond goods receipt). The PO must be PARTIALLY_RECEIVED/RECEIVED to match. **(d) Tolerance model = a single-per-tenant `proc_match_tolerances` row (price% + quantity%), the ApprovalRule precedent;** absent ⇒ strict 0% defaults (any price difference is an exception, so a tenant must opt into a band — a price change should be deliberate). A match is MATCHED if every line's price deviation is within the band, EXCEPTION if any exceeds it; an **EXCEPTION blocks posting until OVERRIDDEN** (the invoice-release control — a clerk cannot silently bill an unagreed price); override → MATCHED. Per-vendor tolerance GROUPS + multi-step release + ERS are deferred per the parity doc. **(e) ONE atomic transaction + idempotent + closed-period rollback:** `post_invoice_match` (only a MATCHED match posts) raises each PO line's billed_quantity, advances the PO to **CLOSED** when every line is fully received AND billed, links docflow PO→`matched_by`→match + GR→`matched_by`→match + (finance writes) match→`billed_by`→bill, sets the match POSTED + publishes — all commit-or-roll-back together; a closed invoice-date period trips the bill's journal period trigger inside the same transaction and rolls the WHOLE post back (match stays MATCHED, no bill). A POSTED match is terminal (credit-memo/reversal is Phase 7). **(f) Reorder-point auto-requisitions (Part B):** inventory OWNS `reorder_point`/`reorder_quantity` (5.1) and exposes `items_below_reorder_point` (a set-based LEFT JOIN + GROUP BY + HAVING over the on-hand quant projection, no N+1); the DRAFT requisition is a PROCUREMENT document (6.2), so `service/reorder.run_reorder_scan` reads that inventory query downward (never importing inventory models/service) and creates ONE draft requisition with a line per below-reorder item (qty = reorder_quantity, base UoM) via the existing 6.2 create, so it flows through the normal approval chain. **Idempotent dedup:** `open_requisition_item_ids` collects items already on a DRAFT/SUBMITTED/APPROVED (un-converted) requisition line and the scan skips them, so a second scan the same day adds no duplicate; returns None (→ 200 null) when nothing is below reorder. Inline in v1 (modest item counts); a volume past the sync budget moves it behind the `procurement.reorder_scan` job (documented). **(g) Structure:** writes split `service/invoice_matches.py` (create + validation/variance/tolerance + the shared `po_lines_for`) from `service/invoice_match_post.py` (post/override/cancel) and `service/invoice_match_reads.py` (reads + tolerance config) at the 400-line cap (the goods_receipts precedent); `models/invoice_matches.py` (2 tables + `MatchTolerance`) + `schemas/invoice_matches.py` re-exported; sibling `invoice_match_router.py` (matches + tolerances + the reorder-scan endpoint) mounted into `router.py`. Migration `0027_procurement_invoice_matches` creates three tables + batch-adds `billed_quantity`, NO trigger-bearing alters, reversible, zero autogenerate drift on SQLite AND Postgres, applies on PG to head. **Phase 6 / Procurement is COMPLETE.**
- **D-043 Sales customer master + condition-style pricing (PLAN 7.1, opens Phase 7): the customer's id IS finance AR's opaque partner_id; credit_limit is a non-null MoneyType (0 = cash-only, no NULL/unlimited sentinel); price lists resolve deterministically by priority → specificity → latest valid_from:** Opens the sales module with the customer master + the condition-style pricing engine. Four tables (migration `0028_sales_customers_pricing`, down_revision 0027, no trigger-bearing alters, reversible, portable, ≤63-char ids): `sales_customer_groups` (lean grouping master), `sales_customers`, `sales_price_lists`, `sales_price_list_items`. **(a) The customer ↔ AR partner_id link (D-029, the vendor mirror).** Sales OWNS the customer entity; finance is below it and owns no customer table — every AR invoice/receipt stores its customer as an OPAQUE `partner_id` (a plain Uuid, NO FK) + a denormalized `partner_name`, and that `partner_id` IS `Customer.id`. `sales/queries.get_customer_for_partner` resolves it back (an intent-named alias over `get_customer`), the exact mirror of procurement's `get_vendor_for_partner`. **(b) credit_limit semantics.** `Customer.credit_limit` is a **non-null `MoneyType` defaulting to 0** = the maximum outstanding AR the customer may carry: **0 = CASH-ONLY** (no open credit; 7.2's order confirmation blocks any positive outstanding AR), **a positive value = the credit ceiling**. Deliberately **no NULL/"unlimited" sentinel** — "unlimited" is an explicit large limit — so the column stays non-null and 7.2's check is a single unconditional comparison with no NULL branch; a DB `CHECK (credit_limit >= 0)` backs it. This is the **static** credit-limit block the parity Sales section scopes to v1 (no exposure aggregation across open orders/deliveries/receivables, no finance-owned exposure ledger — a documented later). `sales/queries.customer_credit_limit` exposes it. **(c) Customer codes user-supplied + unique per tenant** (`UNIQUE(tenant, customer_code)`, the vendor-code/item-code precedent — a master carries a code, not a gapless document number; the O2C documents in 7.2+ DO claim gapless numbers). **(d) Customer group = a MASTER table, not a free string.** A customer optionally belongs to ONE `CustomerGroup` (`sales_customer_groups`, tenant+code+name); a master table is chosen because **pricing keys on the group** — a `PriceList` targets a group by id (a composite tenant FK), so the customer and the price list reference the same rows, the name is edited in one place, and a typo'd free-string group can never silently exclude a customer from its price list. The group carries no pricing of its own; `customer_group_id` is nullable on both the customer and the price list (NULL on a price list = a GENERAL list). **(e) The condition-style price model.** A `PriceList` is a condition header (currency + optional `customer_group_id` + inclusive `[valid_from, valid_to]` window with `valid_to` NULL = open-ended + `status` ACTIVE/INACTIVE + a `priority` integer) carrying `PriceListItem` rows — **ONE base `unit_price` per (list, item)**, `UNIQUE(tenant, price_list_id, item_id)`, with a `min_quantity` floor (default 0; v1's only scale knob). `item_id` is OPAQUE (validated via `inventory/queries.item_exists`, no FK); the list/item currencies validate via `finance/queries.currency_exists`. This is the parity-PARTIAL condition model — NOT the generalized access-sequence/pricing-procedure engine, multi-tier quantity scales, or freight/tax condition types (all deferred). **(f) The DETERMINISTIC resolution order (`service/price_resolution.resolve_price`, exposed via `sales/queries.resolve_price` — the public entry 7.2's order entry prices each line through).** A list APPLIES when ALL of: status ACTIVE; currency matches; `valid_from <= on_date` and (`valid_to IS NULL` or `valid_to >= on_date`); the list is general (`customer_group_id IS NULL`) OR targets the customer's group (a list targeting a different group, or any group when the customer has none, is excluded); it has a `PriceListItem` for the item with `min_quantity <= quantity`. Among applying lists the winner is picked by, in STRICT order: **1) highest `priority`; 2) most specific (group-targeted beats general); 3) latest `valid_from`; 4) price-list `id`** as a final stable tiebreaker (so the result never depends on row order). No applying list → `matched=False` (no price; 7.2 then requires a manual price/override). **NO discount is applied** here — the price list yields the base price only; line discounts (`DiscountType` PERCENT/AMOUNT, defined now) are a per-order-line concern in 7.2. **(g) The resolver is BOUNDED to two queries** regardless of list count (PERFORMANCE §6, no N+1): one fetches the candidate ACTIVE lists matching currency+date+group (index `ix_sales_price_lists_resolver` on `(tenant, currency, customer_group_id, valid_from)`); one fetches those lists' items for the item meeting the floor (`ix_sales_price_list_items_tenant_id_item_id`); the winner is picked in Python over that small set. **(h) Structure:** a `models/` package (customers.py + pricing.py) and a `service/` package (customers/customer_groups/pricing/price_resolution) from the start (the 400-line cap); a single schemas.py + queries.py; `router.py` (customers + groups) mounts a sibling `pricing_router.py` (price lists + nested items + the `GET /price-quote` resolver endpoint) under one `/api/v1/sales` surface; mounted after procurement in main.py (the D-011 handler order). Reference lists (customers, groups, price lists) carry collection ETags (D-035) + the ≤3 query budget. **7.1 publishes NO domain events** (masters + pricing drive no cross-module effects; orders in 7.2 will). Permissions: `sales.customer.read/.manage` (groups ride customer.manage) + `sales.pricelist.read/.manage`. **Phase 7 / Sales is OPEN; 7.2–7.4 add quote→order→delivery→invoice.**
- **D-044 Sales quote → order with simple ATP (availability + backorder-flag, NOT a block) + a HARD credit-limit block at confirmation (PLAN 7.2): the committed-quantity reservation is a query over confirmed-undelivered order lines; exposure = open AR + open confirmed orders + this order:** Extends Sales with the O2C spine — `Quote`/`QuoteLine` (`sales_quotes`/`_lines`, `DocumentMixin` + gapless `QUO-` number claimed at creation) and `SalesOrder`/`SalesOrderLine` (`sales_orders`/`_lines`, `DocumentMixin` + gapless `SO-` number) — mirrored on the procurement requisition/PO shape (sales order ≈ purchase order). Migration `0029_sales_orders` (down_revision 0028): four tables + (tenant,status) / (tenant,customer_id,status) indexes, NO trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG. **(a) Quote lifecycle** (`QuoteStatus` DRAFT→SENT→ACCEPTED/REJECTED, DRAFT/SENT→EXPIRED on `valid_until` lapse, ACCEPTED→CONVERTED, +CANCELLED — all set IN 7.2, a quote is wholly sales-internal): no committing gate, so MANAGE covers every action (the RFQ-manage precedent). **Expiry is LAZY** — `mark_expired_if_lapsed` is a service check the read paths call (no background sweep in v1), documented. Quote lines default `unit_price` from the price resolver (D-043, overridable) and apply an optional per-line discount (`DiscountType` PERCENT = % off gross, AMOUNT = per-unit off), `line_amount` = qty×unit_price − discount quantized to currency, `total_amount` maintained. **(b) Order lifecycle** (`SalesOrderStatus`): DRAFT → (confirm) **CONFIRMED** or **CREDIT_BLOCKED**, + CANCELLED set in 7.2; PARTIALLY_DELIVERED/DELIVERED (7.3), INVOICED (7.4), CLOSED declared now as the full lifecycle, transitions land later. An order is created from scratch OR by converting an ACCEPTED quote (copy lines with the quote's frozen prices + discounts, link docflow quote→`converted_to`→order, set `source_quote_id`, advance the quote to CONVERTED — the procurement convert precedent, one shared writer for both paths). The customer must be ACTIVE at create (422 `sales.customer_not_active` — the soft block, distinct from the credit block). `payment_terms_days` is snapshot from the customer (the 7.4 invoice reads the snapshot). **(c) ATP = INFORMATIONAL availability + a backorder FLAG, NOT a block (the parity v1 scope).** `sales/queries.atp_check` computes **availability = on-hand − committed + on-order**: on-hand = `inventory/queries.total_on_hand` (the maintained quant projection); on-order = a NEW sanctioned cross-module read `procurement/queries.open_incoming_quantity` (ordered − received summed per item over APPROVED/SENT/PARTIALLY_RECEIVED POs, set-based, no N+1); committed = the reservation (see d). A line whose requested qty > availability is a **backorder** — but ATP does **NOT block**: parity scopes v1 to "simple ATP = an availability check with manual backorders", so `confirm_order` evaluates ATP per line, records the per-line `backordered` snapshot on the `ConfirmResult`, and **proceeds** (the order still CONFIRMS). A `POST /orders/atp` (line set) + `GET /orders/atp` (single item) endpoint exposes the availability preview for the order UI. **(d) The committed-quantity reservation = a QUERY, not a table.** `sales/queries.committed_quantity(item_id)` sums ordered − delivered over CONFIRMED/PARTIALLY_DELIVERED sales order lines for the item (set-based, no N+1) — so confirming an order COMMITS its undelivered quantity against ATP for subsequent orders, and 7.3's delivery (raising `delivered_quantity`) shrinks the commitment automatically, with no separate reservation table to reconcile. Confirm excludes the order's own demand (`exclude_order_id`) so a line is checked net of OTHER commitments. **(e) Credit = the HARD block at confirmation (parity "static credit-limit block at order confirmation").** Exposure = the customer's **open AR** (`sales/queries.customer_open_ar` → finance's existing `customer_open_balance`, the sum of `open_amount` on POSTED customer invoices keyed by the opaque partner_id = Customer.id, D-029 — REUSED, no finance change) + the value of the customer's **other open confirmed orders** (`open_confirmed_order_value`, CONFIRMED/PARTIALLY_DELIVERED/DELIVERED orders excluding this one) + **this order's total**. If exposure > `credit_limit` (0 = cash-only, D-043) the order is set **CREDIT_BLOCKED** / `credit_check_status` BLOCKED and is NOT confirmed; within → **CONFIRMED** / PASSED. This is the v1 scope: no exposure aggregation beyond open AR + open confirmed orders, no recheck at delivery, no scoring (all deferred per parity). **(f) Credit release.** A user with `sales.order.credit_release` calls `release_credit` → `credit_check_status` RELEASED, then re-confirms (a RELEASED order SKIPS the credit gate) → CONFIRMED. `confirm` is a distinct permission from `manage` (it runs the ATP + credit gates — the journal.post/po.approve precedent); `credit_release` is distinct again (an approval-like override past the limit — a clerk who can confirm cannot silently override a block). Confirm is **idempotent** (re-confirming a CONFIRMED order is a no-op return; the create/convert/confirm/release endpoints carry D-013 idempotency). **(g) NO events.py yet.** A confirmed order is a COMMITMENT (like a PO) with no finance/inventory posting — 7.3's delivery is the first sales event (stock issue → COGS). So sales ships no `events.py`/`handlers.py` (the no-empty-file rule). **(h) Structure:** `models/orders.py` re-exported; `schemas.py`→`schemas/` package (masters.py + orders.py, split at the cap, re-exported so import sites are unchanged); `service/` adds `_shared.py` (validation + numbering + the line-pricing/discount helper), `quotes.py`, `orders.py`, `order_confirm.py` (the ATP+credit gate, focused <250 lines), `conversions.py` (quote→order); `router.py` mounts sibling `quote_router.py` + `order_router.py` under one `/api/v1/sales` surface. **Cross-module additions (sanctioned downward reads, STRUCTURE §5):** `procurement/queries.open_incoming_quantity` (NEW — the on-order side of ATP); `sales/queries` reuses `finance/queries.customer_open_balance` + `inventory/queries.total_on_hand` (no new finance/inventory queries). Sales imports ONLY finance/inventory/procurement QUERIES, never their service (grep-verified).
