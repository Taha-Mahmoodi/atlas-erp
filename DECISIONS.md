# DECISIONS.md — Design Decision Log

One line per consequential decision + rationale, so later sessions don't re-litigate or contradict earlier choices. Numbered, append-only; superseding a decision gets a new entry referencing the old one.

- **D-001 Bootstrap exception to "nothing directly on main":** the `.gitignore` root commit lands directly on `main` (GITHUB-WORKFLOW.md §1 explicitly orders it first); everything else reaches `main` only via `dev` promotions. Rationale: a branch cannot be cut from an empty repo.
- **D-002 Python toolchain = uv + Python 3.12:** uv manages the venv, lockfile (`uv.lock`, committed) and Python pin; CI uses `astral-sh/setup-uv`. Rationale: fastest reproducible installs, single tool for venv+lock+run.
- **D-003 Tests on SQLite (aiosqlite), runtime on PostgreSQL:** models are written PostgreSQL-first with SQLite-compatible type fallbacks (e.g. JSON variant for JSONB, Numeric for money); the full suite and CI run against in-memory/file SQLite, while docker-compose runs Postgres. Rationale: the prompt requires PostgreSQL-first + SQLite-compatible demo; SQLite keeps CI fast and dependency-free. DB-level financial constraints must be expressed portably (CHECK constraints, not Postgres-only features) or get Postgres-only guards documented per case.
- **D-004 Frontend package manager = npm:** lockfile committed; no pnpm/yarn. Rationale: zero extra tooling on contributor machines.
- **D-005 CI check names are stable from PR #1:** single workflow `ci.yml` with jobs `backend` and `frontend`; each job no-ops green while its directory doesn't exist yet. Rationale: branch protection requires named status checks; names must exist before the code does.
- **D-006 Makefile and docker-compose.yml are deferred to the first backend PR:** a Makefile whose targets point at directories that don't exist would violate the no-orphan-files rule in spirit. Rationale: every committed file must work the day it lands.
- **D-007 Tenancy via non-bypassable session filter:** a `do_orm_execute` hook adds `with_loader_criteria(TenantMixin, ...)` reading a `current_tenant_id` ContextVar to every ORM statement (lazy loads and bulk writes included), fail-closed when unset unless `system_context()` is active (exactly four call sites); `before_flush` stamps/validates tenant_id; composite `(tenant_id, id)` FKs + SQLite `PRAGMA foreign_keys=ON` are the DB backstop. Only mechanism with zero cooperation from query authors. Full spec: docs/architecture.md.
- **D-008 JWT access+refresh with CAS rotation and 10 s grace window:** HS256 with `ATLAS_JWT_SECRET`; 15-min stateless access token with `ver` checked against `core_users.token_version`; 14-day refresh JWT in an httpOnly cookie scoped to /api/v1/auth, rotated via compare-and-swap on hashed jti in `core_refresh_sessions` with reuse → family revocation, prev-jti accepted within 10 s so multi-tab races don't force logouts; argon2id (t=3, m=65536, p=4) off-thread. Full spec: docs/architecture.md.
- **D-009 RBAC as data + partial-bound WrapSerializer masking:** code-defined `core_permissions` catalog, tenant-scoped `core_roles` + join tables; `require_permission(key)` dependency resolves effective permissions in one join query, TTL-cached 60 s keyed on (tenant, user, token_version); field masking via `Masked(tp, permission)` WrapSerializer reading a `current_permissions` ContextVar, masked fields excluded from Update schemas. Tenants can't invent keys no code checks. Full spec: docs/architecture.md.
- **D-010 Split-phase audit capture in-transaction:** `before_flush` diffs updates/deletes from attribute history, `after_flush` captures inserts (generated PKs), `after_flush_postexec` writes one Core insert to append-only `core_audit_log` in the same transaction; per-dialect triggers (ATLAS_AUDIT_APPEND_ONLY) forbid update/delete; ORM bulk writes on audited models are hard errors. Audit is exactly as atomic as the change. Full spec: docs/architecture.md.
- **D-011 In-process event bus, collect-then-dispatch in-transaction:** `publish()` buffers in session.info; `run_in_uow()` drains FIFO before commit, handlers share the session (cap 50, cycle error), any handler failure rolls back everything — goods issue without COGS can never commit. `events.py` becomes a sanctioned declarative cross-module import (STRUCTURE §5 amendment); swap path is an outbox bus behind the same Protocol. Full spec: docs/architecture.md.
- **D-012 Document registry + flow links + gapless numbering (merged):** `core_documents` registry (nullable doc_no, partial unique index) with `DocumentMixin` NOT NULL document_id, `core_doc_links` edges traversed by one recursive CTE; numbers from `core_number_sequences` via atomic UPDATE...RETURNING, claimed only in the transaction that makes a document permanent (drafts numbered at posting) — so gaplessness falls out of ACID with no burned numbers. Full spec: docs/architecture.md.
- **D-013 Idempotency by PK reservation + atomic route capture:** `core_idempotency_keys` PK(tenant, endpoint, key); guard inserts 'in_progress' in a separate committed transaction (concurrent duplicates collide), replays stored responses on completed+matching hash; the handler's `idem.capture(schema)` stages the completed UPDATE on the business session so replay record and document commit atomically — fail-closed if capture is forgotten. Required on every financial/stock-document endpoint. Full spec: docs/architecture.md.
- **D-014 Keyset pagination + one error envelope + ATLAS_ token translation:** `{items, next_cursor, limit}` with base64url cursors fingerprinting sort+filters, expanded portable seek predicate, never OFFSET; all errors as `{error: {code, message, details, request_id}}` with dot-namespaced codes; a DBAPIError translator maps trigger tokens (ATLAS_PERIOD_CLOSED etc.) to envelope codes so DB backstops surface like service checks. Full spec: docs/architecture.md.
- **D-015 Exact money via TypeDecorators (supersedes D-003's "plain Numeric"):** MoneyType/QuantityType (PG NUMERIC(18,6), SQLite INTEGER micro-units ×10^6) and RateType (20,10 / ×10^10); Decimal-only in Python, strings in JSON; quantize ROUND_HALF_UP to currency decimals at posting; single largest-remainder `allocate()` in new `core/money.py` (STRUCTURE §2 amendment); triggers do comparisons and SUMs only. Plain Numeric float-round-trips on SQLite and breaks exact trigger sums. Full spec: docs/architecture.md.
- **D-016 Custom fields owned by core, not industry:** `core_custom_field_defs` registry + `validate_custom_fields()` + JSONB/JSON `custom_fields` column helper live in new `core/custom_fields.py` (STRUCTURE §2 amendment) — industry-module ownership would force upward imports from finance/inventory; templates and admin CRUD write defs through core services; flat scalar keys only, DECIMAL as strings, portable `.as_string()`-style JSON paths. Full spec: docs/architecture.md.
- **D-017 Universal journal: one-side CHECK, two-flush posting, reversal-only:** `fin_journal_entries` (nullable entry_number claimed at posting) + `fin_journal_lines` with all CO dimensions, transaction+functional amount pairs, `ck_fin_journal_lines_one_side`; balance trigger on DRAFT→POSTED (ATLAS_UNBALANCED_ENTRY), immutability triggers (ATLAS_POSTED_IMMUTABLE) allowing only the reversal status mutation; posting mutates loaded lines then header across two flushes; residual functional cent absorbed largest-remainder (no rounding line). Full spec: docs/architecture.md.
- **D-018 Period close enforced by per-dialect triggers + service check:** `fin_fiscal_periods`; `trg_fin_entries_period_open` re-derives the period from posting_date on DRAFT→POSTED (plus an INSERT twin) and aborts ATLAS_PERIOD_CLOSED when no OPEN period covers the date — catching wrong-period-id service bugs; service raises 422 'finance.period_closed' first; close refuses while drafts exist in the period. Triggers are the only cross-table guard on both engines. Full spec: docs/architecture.md.
- **D-019 FX: posting-time translation, realized at clearing, auto-reversing revaluation:** `fin_exchange_rates` (SPOT/CLOSING), `get_rate()` fails loud on missing rates; functional amounts frozen at posting (largest-remainder balancing); realized gain/loss posted inside the clearing entry via purpose-keyed `fin_posting_defaults`; `run_fx_revaluation` posts one FX_REVAL entry per (currency, account) and its next-period auto-reversal, validating up front that the next period is open. Full spec: docs/architecture.md.
- **D-020 Inventory costing: moving average + FIFO, same-transaction COGS, no negative stock:** `inv_item_valuations` updated under `with_for_update` (SQLite no-ops it; single-writer lock suffices, pg-marked tests prove the PG path); FIFO via `inv_cost_layers` + `inv_layer_consumptions` with exact reversal replay; the event bus posts the COGS journal entry in the SAME transaction as the move — stock and GL can never diverge; negative stock forbidden by service + CHECKs. Full spec: docs/architecture.md.
- **D-021 Statements are pure projections of `fin_journal_lines`:** five account types + cash_flow_category on `fin_accounts`; trial balance/P&L/balance sheet/indirect cash flow all derive from ONE aggregate over lines (denormalized is_posted/posting_date, no header join); retained earnings computed cumulatively on the fly; no stored totals anywhere; partial covering index `ix_fin_journal_lines_proj` keeps it sub-second to ~10M lines. Full spec: docs/architecture.md.
- **D-022 Single Alembic chain on both engines; migrations are the only schema source:** `create_all` banned even in tests (triggers live only in migrations); batch_alter_table everywhere + MetaData naming_convention; per-dialect trigger DDL side by side, re-executed after any batch-alter of a trigger-bearing table (SQLite copy-rebuild silently drops triggers); CI runs full suite on SQLite plus downgrade/upgrade and `-m pg` DB-guard subset on postgres:16. Full spec: docs/architecture.md.
- **D-023 Hand-written frontend types with a compile-time drift gate:** per-module `types.ts` mirrors Pydantic schemas (Decimal/date/UUID → string, enums → literal unions, Masked → `T | null`); committed `backend/openapi.json` (freshness pytest-asserted) feeds a gitignored openapi-typescript output checked by per-module `types.contract.ts` Equal-assertions in `npm run typecheck` — drift fails CI in both directions with zero runtime cost. Full spec: docs/architecture.md.
- **D-024 Frontend auth: memory-only access token + boot refresh + cross-tab single-flight:** access token never persisted; httpOnly refresh cookie drives a boot-time silent refresh; proactive renewal at exp−60 s, reactive single-flight retry on 401; `navigator.locks` + BroadcastChannel ensure exactly one tab rotates and the rest adopt the token — eliminating the multi-tab races the backend grace window only softens. Full spec: docs/architecture.md.
- **D-025 Tests on per-test template copies, not SAVEPOINTs:** a session-scoped fixture migrates ONE template DB (alembic upgrade head); each test gets a file copy (or PG `CREATE DATABASE ... TEMPLATE`) so real commits — idempotency reservations, run_in_uow, numbering — work as in production and triggers exist; factories provision through real services; raw-SQL DB-guard tests pin the ATLAS_ tokens on both engines; tenancy suite auto-covers new mappers. Full spec: docs/architecture.md.
- **D-026 Seed drives the real service layer, deterministically:** `seed.py` provisions five demo tenants from industry templates under system_context, then generates 3 months of interlinked P2P/O2C/production/FX flows through real services in `run_in_uow()` (RNG seeded per template+anchor); months 1-2 closed via the real close service; self-verifies trial balance, docflow chains, and inventory-GL reconciliation; `--reset` is the only sanctioned hard-delete, guarded by the `demo-` slug prefix. Seed doubles as a CI integration smoke gate. Full spec: docs/architecture.md.
- **D-027 Core platform tables and the auth router live in `core/`:** `User` (`core_users`) and `RefreshSession` (`core_refresh_sessions`) are defined in `core/models.py` and the login/refresh/logout/me endpoints in a new `core/security_router.py` (prefix `/api/v1/auth`). Rationale: auth/identity are cross-cutting platform concerns belonging to no business module (STRUCTURE §2 litmus — not a business concept), so they stay in `core/` rather than inventing a `modules/auth` package the module list doesn't sanction. `core/auth.py` stays ORM-free (crypto + JWT primitives only). Implements D-008. **Follow-on (PLAN 3.7, same precedent):** the document-flow read endpoint lives in a new `core/docflow_router.py` (`GET /api/v1/documents/{document_id}/chain`, guarded by `get_current_user`, tenant-scoped) — the document registry is platform infrastructure owned by no module, so its router joins `security_router.py` as a sanctioned core router wired in `create_app()`. Deviations from D-012's draft, all behaviour-preserving: route path is `/api/v1/documents/{id}/chain` (the binding PLAN-task path) not the draft's `/api/v1/core/documents/{id}/flow`; the D-012 numbering/docflow ORM models (`NumberSequence`, `Document`, `DocumentLink`) live in their concern files `core/numbering.py` / `core/docflow.py` (re-exported at the tail of `core/models.py` so they register on `Base.metadata`) rather than in `models.py`, to keep `models.py` under the ~350-line cap — STRUCTURE §2 lists both concern files and §8.5 ("filename promises content") favours co-locating each model with its claim/traversal logic. `core_number_sequences` keys on `(tenant_id, name)` with a `year_reset`/`current_year` pair (the binding task's column spec) rather than the draft's `(tenant_id, doc_type, year)` PK; semantics (gapless in-transaction claim, partial unique index, both-direction recursive CTE) are identical. `claim_number` uses the atomic `UPDATE ... RETURNING` path — verified working on the project's aiosqlite (3.53, >= 3.35), so the sanctioned SELECT-then-UPDATE fallback is unused; `WITH RECURSIVE` likewise verified on aiosqlite.
- **D-028 Idempotency + keyset pagination placement and behaviour (PLAN 3.8, implements D-013/D-014):** the `core_idempotency_keys` model + `reserve`/`IdempotencyContext`/`Idempotent` guard live in a new `core/idempotency.py`; the table uses the NATURAL composite PK `(tenant_id, endpoint, key)` exactly per D-013 (tenant_id overrides TenantMixin to join the PK, keeps `index=True` so the D-007 mapper-enumeration invariant holds). Migration `0007_idempotency`. Deviations, all behaviour-preserving except where noted: (1) the model is registered on `Base.metadata` from the BOTTOM of `core/db.py` (not the `core/models.py` trailing import that numbering/docflow use) because `core/idempotency` imports `core/db` for its session dependencies and `core/db` imports `core/audit`→`core/models`, so a trailing import in models would dead-lock the cycle. (2) The `Idempotent` guard reads `tenant_id` from the D-007 `current_tenant_id` ContextVar (set by `get_current_user`, which guarded routes declare first) rather than importing `CurrentUserDep`, to avoid the `deps`→`db`→`idempotency` import cycle. (3) `capture()` is called INSIDE the `run_in_uow` work so the completion UPDATE and the business document commit in one transaction (Atlas commits via `run_in_uow` inside the handler, not a deferred session-dependency commit). Consequent fail-closed deviation: on a business EXCEPTION the guard's teardown deletes the dangling IN_PROGRESS reservation and re-raises (handlers render the envelope) — fully fail-closed; on a FORGOTTEN `capture()` (handler returned 2xx) the teardown deletes the reservation and logs a server error but cannot convert the already-sent 2xx to a 500, because the `run_in_uow` commit precedes teardown — a documented relaxation of D-013's literal "teardown rolls back" (which assumed a deferred commit). Replay short-circuit: `reserve` raises `IdempotencyReplay` (an AtlasError subtype) whose handler in `app.main` re-emits the stored status+body with `Idempotency-Replayed: true`, so a completed-key replay never runs the handler body. **Keyset pagination** lives in a NEW flat core file `core/pagination.py` (`paginate`, `OrderKey`/`SortDirection`, base64url cursor codec with sort+filter fingerprint, `CursorParams`/`cursor_params` with limit cap default 50 / max 200) — STRUCTURE §2 amendment, same precedent as `core/money.py`/`core/custom_fields.py`; this DEVIATES from D-014's letter (which puts `paginate` in `core/db.py` and the codec in `core/schemas.py`) because the keyset machinery is substantial and cross-cutting (§8.5 one-concept-per-file). The `Page` envelope stays in `core/schemas.py`. The seek predicate is written in the EXPANDED portable OR-form unconditionally (verified: this project's aiosqlite 3.53 DOES support uniform-direction row-value tuples, but not MIXED-direction, and Atlas lists mix ASC/DESC, so the expansion is the only universally-correct form on both engines per D-014). Full spec: docs/architecture.md D-013/D-014.
- **D-029 AP/AR sub-ledger is keyed by an opaque `partner_id`, not a finance-owned partner master:** finance is the bottom dependency (everyone may read `finance/queries`; finance imports no module), but the vendor master lives in procurement (PLAN 6) and the customer master in sales (PLAN 7) — both *above* finance. So finance AP/AR open items, payments, dunning and aging are keyed by an opaque `partner_id: UUID` plus a denormalized `partner_name` carried on each document; finance never FK-references a partner table. The vendor/customer masters own the identity (their PK *is* the partner_id) and call finance services to create bills/invoices and run payments. This honors the parity doc's "separate vendor and customer masters, no unified Business Partner" while keeping the dependency direction legal. Realized FX is computed at clearing here (D-019). Trade-off vs a finance-owned `BusinessPartner`: no referential FK from open items to the master (the owning module guarantees the id), accepted for the clean layering.
- **D-031 AR (PLAN 4.6) placement + shared realized-FX clearing helper + dunning (implements D-029/D-019/D-030):** the four AR tables live in `finance/models/receivables.py` (re-exported from `models/__init__`); AR schemas in a NEW sibling `finance/receivables_schemas.py` and the AR endpoints in a NEW sibling `finance/ar_router.py`, both mirroring the sanctioned AP siblings (D-030) and mounting into the finance router via `include_router` (one surface at `/api/v1/finance`). AR is the AP shape with the **sign flipped** (Dr AR control gross / Cr revenue net / Cr output tax at invoice posting; receipts Cr AR / Dr bank) PLUS **dunning**. Service split: `service/customer_invoices.py` (draft+post), `service/customer_receipts.py` (receipt document + clearing), `service/ar_aging.py` (projection), `service/dunning.py`. **Generalized AP's clearing into a shared helper:** AP's `service/payment_clearing.py` was NOT reusable as-is (it took `VendorBill` objects + AP error codes), but the realized-FX MATH is identical at the customer side — so the engine-agnostic core (compute functional amounts, build the balanced clearing journal lines with explicit functional amounts via `skip_translation`, the realized-FX gain/loss line) was factored into a NEW `service/clearing_fx.py` keyed on plain `ClearedItem` tuples + a `control_is_debit` flag (AP debits the control to clear, AR credits it — the gain/loss direction inverts: AP realized = func_control − func_bank, AR realized = func_bank − func_control). `payment_clearing.py` was refactored to adapt bills into `ClearedItem`s and delegate; AR's receipts do the same with invoices — ONE FX implementation, no duplication. **Dunning** (the AR-only escalation, no AP mirror): `DUNNING_THRESHOLDS = (7, 30, 60)` days map to levels 1/2/3 via `dunning_level_for(days_overdue)`; `run_dunning(as_of)` scans OPEN overdue invoices, and when the level their age earns EXCEEDS the invoice's current `dunning_level` it raises it + stamps `last_dunned_date = as_of`, returning the notice list — it posts NO journal (dunning is state only, D-029) and is idempotent-ish per day (a re-run advances nothing already at its earned level). Permissions `finance.ar.read`/`.manage`/`.collect` (collect = receipts + dunning). `queries.py` adds `get_open_customer_invoices` + `customer_open_balance` (the latter for Sales' future credit-limit block, partner-keyed by the opaque `partner_id`). Migration `0013_finance_ar` creates four tables and alters nothing (no trigger-bearing table touched, D-022); autogenerate confirms zero drift on SQLite AND Postgres. **Deviation (PG identifier cap):** the D-022 auto FK names for the two `fin_customer_receipt_allocations` composite FKs would be 67 chars (> PG's 63), so the model + migration share explicit abbreviated names (`fk_fin_cust_receipt_allocs_tenant_id_{receipts,invoices}`) — same precedent as AP's abbreviated allocation UNIQUE name (D-030). Finance stays the bottom dependency (imports no module above it; verified); `partner_id` stays opaque (plain Uuid, no FK).
- **D-030 AP (PLAN 4.5) placement + the journal `skip_translation` clearing hook (implements D-029/D-019):** the four AP tables live in `finance/models/payables.py` (re-exported from `models/__init__`); AP request/response schemas live in a NEW sibling `finance/payables_schemas.py` and the AP endpoints in a NEW sibling `finance/ap_router.py` — both siblings (not a `schemas/`/`router/` package) exactly mirror the sanctioned `fx_router.py`/`tax_router.py` precedent, because `schemas.py`/`router.py` are at the STRUCTURE §3 400-line cap and the AP surface is self-contained; `ap_router` mounts into the finance router via the same `include_router` one-surface pattern (no second mount in main.py). The AP service is split across `service/vendor_bills.py` (draft + post), `service/payment_clearing.py` (open-item validation + realized-FX line construction), `service/vendor_payments.py` (payment document + run), and `service/ap_aging.py` (the projection) to keep every file under the cap. **Realized FX at clearing (D-019):** computed per cleared bill as `functional-at-bill-rate − functional-at-payment-rate` over the cleared transaction amount (the bill's frozen rate is read from its posted AP-control journal line, not re-looked-up), posted to the `fx_realized_gain`/`fx_realized_loss` posting-default account inside the SAME payment entry. This required a behaviour-preserving extension to `service/journal.post_entry`: a new keyword `skip_translation: bool = False` — when the caller has already set explicit per-line functional amounts (the only case where functional ≠ transaction × a single entry rate), the engine validates those balance instead of re-translating. The realized-FX line is denominated in the functional currency and carries a positive transaction amount equal to its functional amount, so the per-line one-side CHECK (D-017, on transaction amounts) holds while the balance trigger (functional sums only) balances — no zero/zero line. Open items are partner-keyed by the opaque `partner_id` stamped on the AP control line (D-029); finance imports no module above it (verified). AP documents register in `core_documents` and link bill→journal (`posts`) and payment→bills (`pays`) so the document-flow chain spans the AP flow. Migration `0012_finance_ap` creates four tables and alters nothing (no trigger-bearing table touched, D-022); autogenerate confirms zero model drift on SQLite.
- **D-032 Background-job core (PLAN 4P.5, closes #26): code-defined registry + in-process asyncio runner behind a scheduler seam; `core_jobs` not audited:** `core/jobs.py` owns the `Job` model (`core_jobs`: job_type/status/payload/result/error/submitted_by/started+finished, indexes `(tenant_id, status)` + `(tenant_id, job_type, created_at)`; migration 0017), a `@register_job` registry (code-defined like permissions), `submit_job` (PENDING row flushed inside the endpoint's uow so it commits atomically with the D-013 idempotency capture — a replayed key returns the SAME job id), and the runner: scheduled via `schedule_job(job_id, session_factory)` strictly AFTER the uow commit (no visibility race), it opens its own session from the factory the router's `get_session_factory` dependency supplied (production = app factory; tests = the per-test-engine override, which is how the runner lands in the right DB in both worlds), restores `tenant_context(tenant_id)` + the D-010 actor ContextVar from `submitted_by_user_id`, marks RUNNING (own commit), runs the handler inside `run_in_uow` with COMPLETED+result set in the same uow (business writes and terminal status are atomic; events + audit behave exactly as in-request), and on any exception commits only FAILED + the truncated error. Concurrency capped at 4 via per-event-loop semaphores (asyncio primitives bind to their first loop; tests run one loop per test); `wait_for_jobs()` makes tests deterministic. SWAP SEAM: submitters call `schedule_job`, delegating to the module `scheduler: JobScheduler` Protocol singleton (the D-011 bus pattern) — a queue runner (arq/celery) rebinds it, ignoring the session-factory argument, with zero submitter changes. Polling: `core/jobs_router.py` (`GET /api/v1/jobs[/{id}]`, auth-only + tenant-scoped, paginated — the D-027 core-router precedent). `core_jobs` is deliberately NOT audited: high-churn control rows (PENDING→RUNNING→terminal per job), same documented exclusion class as `core_refresh_sessions`/`core_idempotency_keys`; the handler's BUSINESS writes are audited normally with the submitting user as actor. The #26 fix: `POST /finance/fx-revaluation-runs` and `POST /finance/payment-runs` now submit `finance.fx_revaluation`/`finance.payment_run` jobs and return `202 {job_id, status}` (service functions unchanged; the retired sync `PaymentRunResult` wrapper was deleted — payment ids/numbers ride the job result). Model registration deviation: `core/jobs` imports `core/audit` (actor ContextVar), so like idempotency it registers on `Base.metadata` from the BOTTOM of `core/db.py`, not models.py's trailing imports (a trailing import dead-locks on the audit->models cycle).
- **D-033 Bank reconciliation (PLAN 4.9) placement + statement-as-external-document + match-rule boundary:** the two bank tables live in `finance/models/bank.py`; the service splits along concerns (`service/bank_csv.py` CSV contract, `service/bank_import.py` import/job/progress, `service/bank_reconcile.py` matching/clearing) and the HTTP surface is the sanctioned sibling pair `bank_schemas.py`/`bank_router.py` (D-030 precedent), all under the STRUCTURE cap. `BankStatement` gets DocumentMixin (the task sketch omitted it) because the required docflow statement->'posts'->clearing-entry link (D-012) needs a registry row — but NO gapless number is claimed: D-012 numbering covers documents Atlas issues, and a statement is an EXTERNAL document identified by bank account + date + source_filename, so its registry doc_number stays NULL. `matched_journal_line_id` is an opaque Uuid (read-only reference into the immutable journal, the dimension-id precedent); `cleared_journal_entry_id` is a composite tenant FK (the entry is created BY this flow). `import_job_id` is a plain nullable Uuid (the core_jobs attribution precedent), stamped via the router writing the job id back into the job payload after submit_job (a handler cannot know its own job id). Bulk insert uses the ORM-enabled `insert(Model)` executemany with explicit per-row tenant_id — sanctioned: it is an ORM statement (not a Core Table insert), inserts are not tenant-filtered by D-007 (values carry tenant_id; composite FKs backstop), and the grep gate bans only attribute-call inserts/raw SQL. Match rules v1 = exact amount+date, then exact amount ±3 days (nearest-wins, single consumption tenant-wide); the rule-3 doc-number-in-description heuristic, partial/many-to-one matching and tolerance bands are documented boundary (parity doc already lists MT940/CAMT + auto-clearing rules as laters). Statement lists sort (statement_date DESC, id) WITHOUT a created_at seek key: SQLite stores server-default CURRENT_TIMESTAMP without fractional seconds while a re-bound cursor datetime renders with `.000000`, so a created_at key lexicographically re-includes the boundary row — a pre-existing core/pagination.py issue affecting every created_at-sorted list on SQLite (PG unaffected), tracked for a core-level fix.
- **D-034 Asset accounting lite (PLAN 4.10) placement + the depreciation-run shape + the finance constants package split:** the three tables live in `finance/models/assets.py`; the service splits along concerns (`service/assets.py` register lifecycle, `service/depreciation.py` formula + posting run + job handler, `service/depreciation_read.py` reads + the register projection — the journal.py/journal_read.py cap-split precedent) and the HTTP surface is the sanctioned sibling pair `assets_schemas.py`/`assets_router.py` (D-030 precedent), mounted one-surface. **Idempotency backbone:** `UNIQUE(tenant_id, asset_id, fiscal_period_id)` on `fin_depreciation_entries` — an asset depreciates once per period EVER, so overlapping/concurrent runs collide at the DB; the run engine selects eligible assets with a NOT EXISTS anti-join, reads prior accumulated+count in ONE grouped aggregate, bulk-inserts entries with ONE executemany, and posts ONE journal entry with lines GROUPED per (expense account, cost centre)/per accumulated account (PERFORMANCE §2: the SQL statement count is constant in the asset count, query-counter pinned). A re-run for a finished period returns the existing POSTED run unchanged (the allocation idempotency pattern); the endpoint is additionally D-013 Idempotent and routes >100 eligible assets to a `finance.depreciation_run` job (202, the bank-import threshold pattern). **Exactness:** STRAIGHT_LINE uses the drift-free cumulative formulation (amount(n) = quantize(depreciable×n/life) − prior_accumulated; final period absorbs the residual) so schedules sum to cost−salvage EXACTLY; DECLINING_BALANCE floors at salvage (the crossing/exhaust period takes NBV−salvage, landing exactly on salvage). `compute_depreciation` takes a 1-based schedule `period_index` (prior entry count + 1) rather than a period object — the formula depends on schedule position, not calendar identity. NO stored NBV: the register endpoint recomputes accumulated/NBV from SUM over entries bounded by periods ending ≤ as_of (D-021 spirit); the entries' `*_after` columns are per-entry audit trail only. AssetStatus is DRAFT|ACTIVE|FULLY_DEPRECIATED only — DISPOSED is NOT in the enum (no dead values; disposal/transfer/parallel areas are recorded parity laters). The AST number is claimed at ACTIVATION (the asset's claim-at-permanence moment, D-012; partial-unique backstop), the DEP number at run posting. Three composite FKs from fin_assets to fin_accounts carry explicit abbreviated names (the D-022 column-0 convention would collide three ways). Migration `0019_finance_assets` creates three tables and alters nothing (no trigger-bearing table touched, D-022); zero autogen drift on both engines. **Constants package split (STRUCTURE §8.4):** `finance/constants.py` hit the 400-line hard cap when 4.10's enums/sequences/permissions landed, so it split into the `finance/constants/` package (`enums.py`, `documents.py`, `permissions.py`) with `__init__.py` re-exporting every name — the same §3 package rule models/ and service/ used; zero import-site changes. Test-side: `_FINANCE_KEYS` in tests factories is now derived from `core.rbac.catalog_keys()` (every registered `finance.*` key), self-extending like the tenancy mapper enumeration; the 4.10 builders live in `tests/modules/finance/factories_assets.py` because factories.py is at the cap.
- **D-035 Conditional requests on slow-changing reference data (PLAN 4P.6, closes #28): collection-level WEAK ETag in a new `core/conditional.py`:** a justified core flat file (the `core/pagination.py` precedent) holds `collection_etag` (ONE cheap aggregate `SELECT COUNT(id), MAX(updated_at)` over the model — auto tenant-scoped by the D-007 `do_orm_execute` listener because the `func` aggregate references the model's mapped columns, verified), `check_not_modified` (RFC 7232 weak comparison, handles the `W/` prefix and `*`), and `conditional_response` (the 304-or-200 epilogue: on an `If-None-Match` hit it returns a bare starlette `Response(304, {ETag})` WITHOUT awaiting the page builder — so FastAPI's `response_model=Page[T]` is bypassed and the page query never runs, making the 304 path cheaper than 200: 2 queries vs 3, query-counter pinned). Format `W/"<count>-<max_updated_micros>-<tenant8>-<request_fingerprint>"`. **Collection-level, not row-level**, because a list endpoint returns a page of a collection and `(count, max_updated)` is the cheapest sound "did anything change?" validator — any insert moves count, any update moves `MAX(updated_at)` (TimestampMixin.onupdate), any delete moves count; trade-off is that any change invalidates every page's validator, accepted for the small reference sets this covers. **The `tenant8` component** makes a cross-tenant 304 impossible even though the aggregate is already tenant-filtered. **The `request_fingerprint`** (cursor+limit+filters, reusing `pagination.filter_fingerprint`) is the key correctness point: it is folded into the ETag so a 304 can only ever be served for the IDENTICAL page request and never returns the wrong slice. Applied to the seven slow-changing reference lists ONLY (`accounts`, `account-groups`, `currencies`, `tax-codes`, `fiscal-years`, `fiscal-periods`, `posting-defaults`); transactional lists (journal-entries, bills, invoices, receipts, payments, exchange-rates, bank-statements, depreciation/reval runs, jobs) deliberately carry none. **Structure deviation:** adding `request`/`response` + the etag computation pushed `finance/router.py` over the 400-line cap, so the journal-entry endpoints moved to a new sanctioned sibling `finance/journal_router.py` (the `fx_router.py`/`tax_router.py` one-surface precedent) — a behaviour-preserving extraction, journal is a distinct aggregate from the COA/fiscal reference data; router.py is now 322 lines.
- **D-036 Inventory stock model (PLAN 5.2): the move ledger is the quantity SSOT, the quant is a maintained projection, negative stock is forbidden at service + CHECK on both engines:** `inv_stock_moves` is an **append-only, POSTED-at-creation, immutable** ledger (no draft phase — unlike journals which are DRAFT→POSTED — because a move IS the ledger; corrections are reversing moves, never edits, the D-017 reversal philosophy applied to stock). `quantity` is **always positive**; `move_type` (RECEIPT/ISSUE/TRANSFER/ADJUSTMENT) decides which of from_bin/to_bin participate via the greppable `constants.MOVE_BIN_SIDES` table (RECEIPT→to only, ISSUE→from only, TRANSFER→both-distinct, ADJUSTMENT→exactly-one-side), so direction is structural not a sign — the journal one-sided-amount philosophy. A move carries `DocumentMixin` and claims its **gapless `STK-` number at creation** (D-012 claim-at-permanence, the orders/receipts branch). **On-hand is a MAINTAINED projection, not a re-derived query:** `inv_stock_quants` (UNIQUE(tenant,item,bin,lot)) holds current on-hand per (item,bin,lot) and is updated in the SAME transaction as every move (decrement from_bin, increment to_bin) — the moving-average `inv_item_valuations` precedent (D-020), so on-hand reads are an indexed point lookup, not an unbounded SUM over move history (PERFORMANCE §1). The ledger stays the SSOT/audit trail; the quant is **reconcilable from it** and a quant reaching exactly 0 is **deleted** (the projection holds only live stock, so `on_hand_by_bin` never lists empty bins). **Negative stock is forbidden outright (D-020), enforced twice:** the service raises `InsufficientStockError`→422 `inventory.insufficient_stock` pre-flight, and a portable **`CHECK(on_hand_qty >= 0)`** on `inv_stock_quants` is the bypass-proof DB backstop — a single-column comparison, exact on PG NUMERIC and SQLite micro-unit INTEGER (D-015/D-022), proven on real Postgres by the `-m pg` guard tests. Quant rows are locked `with_for_update` in deterministic bin-id order (PG row lock; SQLite no-op per D-020; deadlock avoidance). **Lot/serial:** a RECEIPT of a tracked item creates the `inv_lots`/`inv_serials` master instance on the fly (5.1 deferred instance creation to receipts); ISSUE/TRANSFER must reference an existing instance; a serial moves wholesale (quantity==1 enforced). **Reversal-not-edit:** `reverse_move` posts the opposite move (from/to swapped; a RECEIPT reverses as an ISSUE) linked via docflow `reverses`. **Structure:** `models.py` split into a `models/` package (`masters.py` 5.1 + `stock.py` 5.2) at the 400-line cap; the 5.2 HTTP surface is a mounted sibling `stock_router.py` (the finance `journal_router`/`ap_router` precedent), one surface at `/api/v1/inventory`. **No `events.py` yet:** a 5.2 move publishes nothing cross-module (on-hand is intra-module; costing/COGS-via-event arrives in 5.3). Migration `0021_inventory_stock` creates four tables, alters nothing (no trigger-bearing table touched, D-022), reversible, zero autogen drift on both engines.

- **D-037 Inventory costing (PLAN 5.3): valuation is per (item, warehouse); the move ledger + quants are the quantity SSOT, valuation/layers are the value SSOT, all updated in ONE transaction; COGS posts via the event bus in that same transaction:** Which engine a `(item, warehouse)` uses is the item's `costing_method` (MOVING_AVERAGE | FIFO), defaulted from its category. **Moving average** — `inv_item_valuations` (UNIQUE(tenant,item,warehouse), `CHECK(on_hand_qty >= 0)`) holds `on_hand_qty`, full-precision `avg_unit_cost`, `total_value`, locked `with_for_update` (PG row lock; SQLite FOR-UPDATE no-op + single-writer lock, D-020): receipt adds `qty×unit_cost` and recomputes `avg = total_value/on_hand` **unrounded**; issue subtracts `quantize(qty×avg, currency dp, HALF_UP)` and, when on_hand hits **exactly 0**, **flushes the residual `total_value` to the price-difference account** so value and quantity never disagree (the rounding-drift absorber — value lands at exactly 0 even for non-terminating averages). **FIFO** — `inv_cost_layers` (one per receipt, `CHECK(0 <= remaining_qty <= original_qty)`) consumed oldest-first by `(received_at, created_at, id)` under `with_for_update` (created_at is the insertion-order tiebreaker when dates tie — uuid4 ids are not monotonic), with one `inv_layer_consumptions` row per touched layer (the per-layer COGS audit trail AND the **exact-reversal** record). **Cost input:** `inv_stock_moves.unit_cost` (MoneyType nullable, batch-added) — RECEIPT/positive-ADJUSTMENT REQUIRE it (the entry cost); ISSUE/negative-ADJUSTMENT IGNORE it and the engine computes/writes it back; TRANSFER carries the current valuation. **Same-transaction COGS (the inventory↔finance seam, D-011/D-020):** `create_move` updates valuation/layers right after the quant update, then `publish(StockValued(...))` (buffered) carrying the value Δ, the three GL account ids (inventory/COGS/price-difference, opaque per D-029) AND the **chosen offset account** (ISSUE→COGS, RECEIPT/ADJUSTMENT→price-difference, reversal→the opposite of the original's offset); `run_in_uow` drains it before commit and `finance/handlers.py` (the **first real cross-module handler**) posts the journal **through the finance posting service** (`create_draft_entry` + `post_entry`, never raw inserts) in the shared session — so one commit = move + quant + valuation + journal + docflow `posts` link, and **any handler failure rolls the whole transaction back** (a stock move can never exist without its journal entry). The COGS journal posts with `move_date`, so a move dated into a **closed period** fails (the period trigger fires in the same transaction → the move rolls back; you cannot move stock into a closed period — correct by construction). **Postings:** RECEIPT/ADJUSTMENT-up Dr inventory / Cr price-difference (standalone offset; procurement GR overrides later, 6.x); ISSUE Dr COGS / Cr inventory; ADJUSTMENT-down Dr price-difference / Cr inventory; **TRANSFER within one inventory account is value-neutral → NO event, NO journal**; a reversal posts the exact reverse of the original's entry. **Reversal is replay, not recompute:** reversing an ISSUE replays its consumptions backward onto the exact layers / re-adds MAV value at the original cost; reversing a RECEIPT zeros its layer (only if unconsumed, else error) / removes MAV value. **Read surface:** `GET /stock-valuations` + `GET /items/{id}/cost-layers` (permission `inventory.valuation.read`); `queries.item_value`/`valuation_summary` for the inventory-value KPI sum qty×cost in **Python** (typed Decimals), never `func.sum(qty×cost)` — two scaled-integer columns multiply to ×10^12 on SQLite which MoneyType cannot un-scale (D-015 discipline). **Structure:** costing service split into `costing.py` (orchestration), `costing_mav.py`, `costing_fifo.py`, `costing_reversal.py` (all <400 lines); handler registered at the app factory `register_event_handlers` (the deterministic D-011 seam, re-runnable + idempotent so the test harness re-registers after `clear_subscriptions`). Migration `0022_inventory_costing` creates three tables + batch-adds `unit_cost` to non-trigger-bearing `inv_stock_moves`, reversible, zero autogen drift; the `CHECK`s + `with_for_update` proven on real Postgres by the `-m pg` guard tests.
- **D-038 Inventory physical & cycle counts (PLAN 5.4): a count posts its variances EXCLUSIVELY through stock ADJUSTMENT moves (never a bespoke journal), and the system_qty is RE-VALIDATED against live on-hand at post time so a concurrent move can't post a wrong variance:** A count (`inv_stock_counts` — `DocumentMixin` + gapless `CNT-` number claimed at creation, `count_type` PHYSICAL|CYCLE, `warehouse_id`, `status` DRAFT→COUNTING→POSTED|CANCELLED, `count_date`) snapshots one line per in-scope quant into `inv_stock_count_lines` (`system_qty` snapshot, `counted_qty` nullable, and at post `variance_qty` / `adjustment_move_id` / `unit_cost`; UNIQUE(tenant,count,item,bin,lot)). PHYSICAL enumerates **every** quant in the warehouse, CYCLE only the chosen `item_ids`/`bin_ids` — type only changes the snapshot scope, not the post path. **Variance posts via ADJUSTMENT moves, NOT a bespoke journal:** for each line `post_count` computes `variance = counted − live-system`; a non-zero variance posts ONE ADJUSTMENT move through `stock_moves.create_move` (positive → into the bin, negative → out of it), which already runs the 5.3 costing engine + publishes `StockValued` so the price-difference journal posts via the event bus **in the same transaction** — the count inherits every costing/GL/audit invariant for free (the reuse-the-existing-path principle; a count never touches the journal directly). The count's document links to each adjustment move via docflow (`counts`); a **zero-variance line posts no move** (`adjustment_move_id` NULL). The positive-adjustment **unit cost** is sourced from `queries.current_unit_cost` (the moving-average `avg_unit_cost`, or the FIFO live-layer weighted average — the same book cost a value-neutral transfer carries), so value added = book cost; an item the system thinks is empty enters at cost 0 (quantity-only correction). **Re-validation at post (the concurrency-safety crux):** `system_qty` is only the variance-PREVIEW baseline; `post_count` **re-reads live on-hand** for each line as the authoritative system qty, so a move landing between snapshot and post can never post a wrong variance — the resulting on-hand always equals the **counted** qty, not `counted − stale`. The whole post runs in `run_in_uow`, so all variance moves' journals + the count commit as one transaction; a **closed-period `count_date`** trips the period trigger via the adjustment's journal and rolls the **whole** post back (D-018). **`POSTED` is terminal** — a status guard rejects re-post (no double adjustment, D-013 idempotency on the post endpoint) and a posted count is never cancelled; corrections are new counts/adjustments (the append-only ledger philosophy). Posting requires **every** line counted (else 422). **Background path (PERFORMANCE §3):** a post whose snapshot shows > `COUNT_POST_SYNC_MAX_VARIANCES` (200) variance lines is submitted as an `inventory.count_post` job (202 `{job_id}`); at or below it the post runs inline (200) — the depreciation-run (100) / bank-import (1000) threshold precedent; the job handler delegates to the same `post_count` engine. **Structure:** writes (`service/counts.py`) split from reads (`service/count_reads.py`) from the start (the prior 566-line single file was unacceptable — both now <400), the count schemas split to a sibling `count_schemas.py` (the `assets_schemas.py` precedent, keeping `schemas.py` under cap), and the job handler in `service/count_jobs.py`; `count_router.py` is a sibling mounted into `router.py` (the one-surface stock_router precedent). Migration `0023_inventory_counts` creates two tables + indexes, no trigger-bearing alters, reversible, portable both engines.

- **D-039 Vendor master (PLAN 6.1) opens procurement, owns the entity finance AP refers to by opaque id, and models payment terms as a net-days integer:** Procurement is the third business module and is placed ABOVE inventory in the dependency order — it may import `finance/queries` AND `inventory/queries` downward, and exposes its own `procurement/queries.py` as the only file modules above it import (STRUCTURE §5). **(a) The vendor IS finance AP's `partner_id` (D-029 made concrete).** Finance owns no vendor table; every AP bill/payment already stores its vendor as an opaque `partner_id` (a plain `Uuid`, no FK) + a denormalized `partner_name`. PLAN 6.1 makes `Vendor.id` exactly that `partner_id`: procurement owns the entity, finance refers to it by opaque id, and `queries.get_vendor_for_partner` resolves a bill's `partner_id` back to a vendor for AP aging/reporting. Finance can never FK to `proc_vendors` (it is below procurement) — the relationship is by id only, the inventory-item/GL-account precedent. **(b) Payment terms = a `payment_terms_days` integer on the vendor, not a terms master.** A net-days value (30 = NET30, CHECK ≥0, default 30) drives AP due dates as `bill_date + days` — the simplest model that matches how AP already computes them. Richer term schedules (2/10 NET30 early-payment discounts, multi-instalment plans) are deferred per the s4hana-parity Procurement section and would arrive as a separate terms entity referenced from the vendor; the integer is forward-compatible. **(c) Vendor codes are user-supplied, unique per tenant, no auto-sequence** (the item_code/account-code precedent — a master carries a code, not a gapless document number; the P2P documents in 6.2+ claim gapless numbers, the master does not). **(d) Approved items are an info-record-lite.** `proc_vendor_approved_items` captures the vendor↔item link (with the vendor's own SKU) but NO time-dependent price/lead-time/valid-from-to — exactly the parity "purchasing info records = PARTIAL" cut. The `item_id` is an OPAQUE inventory id (validated via `inventory/queries.item_exists`, no cross-module FK, D-029), while `vendor_id` is an intra-module composite tenant_fk. **(e) Vendor status (ACTIVE/BLOCKED/INACTIVE) has no terminal state** — all three transitions are allowed, because a block/retire must be reversible and a vendor's AP history must stay referenceable; 6.2 reads the status to refuse new POs against non-ACTIVE vendors (a soft policy at the chain, not a master-level lock). **(f) Sanctioned cross-module query added:** `finance/queries.currency_exists` (the by-code existence check the vendor's `default_currency_code` validates against, mirroring `account_exists`); inventory/queries needed nothing new. No events/handlers ship in 6.1 — vendor master drives no cross-module effects; those arrive with the P2P documents in 6.2+.
- **D-040 Procurement P2P chain (PLAN 6.2): requisition → RFQ → PO numbered at CREATION, gated by a single-characteristic value-threshold rule, with approved-items ENFORCED on POs:** The three procurement documents (`PurchaseRequisition`/`PurchaseRequisitionLine`, `Rfq`/`RfqLine`, `PurchaseOrder`/`PurchaseOrderLine`) are each `DocumentMixin` header+line pairs registered in `core_documents` with predecessor/successor docflow edges (`"sourced_by"` requisition→rfq, `"ordered_by"` rfq→po and requisition→po). **(a) Numbering claimed AT CREATION, not posting.** Unlike finance (which numbers a bill/journal at *posting*), procurement claims the gapless `PR-`/`RFQ-`/`PO-` number when the document is *created* — a DRAFT requisition already prints a number the sourcing/ordering chain quotes, so the document is referenceable immediately (the orders/receipts claim-timing branch of D-012, the inventory stock-move precedent). Gaplessness still holds: creation IS the committing transaction, so the counter increment and the row insert commit-or-roll-back together. **(b) The approval rule is data-driven, single-characteristic (amount), single-step, single-currency** (`proc_approval_rules`, UNIQUE(tenant, document_type ∈ {REQUISITION, PURCHASE_ORDER}), `threshold_amount` CHECK ≥0). `service.requires_approval(document_type, amount, currency)` returns True iff `amount >= threshold` AND the rule's currency matches the document's; **no active rule, an inactive rule, or a currency mismatch ⇒ no gate** (a tenant that hasn't configured thresholds runs without approval; cross-currency thresholds need FX, deferred). This is exactly the parity "approval/release strategies = PARTIAL, data-driven value-threshold rules only" cut — multi-characteristic / multi-step release strategies are the documented later. The submit step (requisition, on the estimated total) and the send step (PO, on `total_amount`) consult it: at/above ⇒ stay SUBMITTED / go PENDING_APPROVAL awaiting the distinct `.approve` key; below ⇒ auto-approve. **(c) Approved-items are ENFORCED on POs, not advisory.** A PO line item MUST be in the vendor's approved-items list (an inactive approval counts as not approved) else 422 `procurement.item_not_approved`; the vendor must be ACTIVE else 422 `procurement.vendor_not_active` — the v1 source-control rule, read through `procurement/queries` (never a cross-module FK). **(d) An RFQ targets ONE vendor in v1** (multi-bidder comparison deferred); a PO **snapshots** the vendor's `payment_terms_days` + currency at create so a later vendor edit cannot rewrite an open PO's due-date math (the 6.4 bill reads the snapshot). **(e) PO line columns staged for 6.3/6.4 now:** `received_quantity` (default 0, raised by 6.3 goods receipts; `po_line_open_quantity` = ordered − received) and the opaque `tax_code_id` (for the 6.4 bill) land in the 6.2 schema so GR + match need no migration. **(f) NO events in 6.2** — nothing in the chain posts to finance/inventory (a PO is a commitment, not a posting); the first procurement event is the 6.3 goods receipt (→ inventory move + GR/IR journal), so `events.py`/`handlers.py` stay absent (no-stub rule). **(g) Behaviour-preserving package splits** at the 400-line cap (the finance/inventory precedent, re-exported so import sites are unchanged): `models/`, `schemas/`, `service/` packages + sibling `requisition_router`/`rfq_router`/`po_router`/`approval_rule_router` mounted into `router.py`. Migration `0025_procurement_documents` creates seven tables + indexes, no trigger-bearing alters, reversible, portable both engines (zero autogen drift, applies on PG to head).
- **D-041 Procurement goods receipt (PLAN 6.3): a GR posts via the event bus into inventory (Dr Inventory / Cr GR-IR) using the inventory valuation-offset override; the GR↔move link is docflow, not a cross-module FK; the whole chain (GR + moves + journals + PO update) is one atomic transaction:** A `GoodsReceipt`/`GoodsReceiptLine` (`proc_goods_receipts`/`_lines`, `DocumentMixin` + gapless `GR-` number claimed at creation, `purchase_order_id` composite FK, `vendor_id` + per-line `item_id`/`unit_cost` snapshot from the PO line, opaque `warehouse_id`/`bin_id` validated via `inventory/queries`) records receipt of PO goods. **Lifecycle DRAFT → POSTED** (chosen over post-at-creation): a GR is a document a clerk builds line-by-line then posts, unlike a stock move which is permanent at creation; DRAFT can CANCEL, **POSTED is terminal** (corrected by a reversing GR / return — Phase 7 RMA; v1 ships no reverse-GR). **(a) The GR/IR three-way flow.** Posting a GR posts **Dr Inventory / Cr GR-IR clearing** (`gr_ir_clearing` is a new per-tenant posting default — a LIABILITY/clearing account; `finance/queries.gr_ir_clearing_account` resolves it and raises 422 `finance.posting_default_unmapped` when unmapped, so a GR can't post without it). The 6.4 bill posts Dr GR-IR / Cr AP, clearing it — GR/IR holds received-not-invoiced value. **(b) The valuation-offset override (the inventory integration point).** `inventory/service/stock_moves.create_move` gains an optional `valuation_offset_account_id`; for a RECEIPT it OVERRIDES the costing engine's standard price-difference offset — the engine sets `StockValued.offset_account_id` to it and `finance/handlers.post_stock_valuation_journal` (already generic over the offset) credits GR/IR instead. The override threads end-to-end `create_move → costing.apply_costing → _apply_inbound → StockValued.offset_account_id → finance/handlers`, changes only the Cr leg never the costing math, is ignored on non-receipt types and on reversals, and defaults None ⇒ the standalone-receipt behaviour (and its tests) is unchanged. **(c) The cross-module mechanism is the EVENT BUS, not a direct call (the STRUCTURE §5 crux).** Procurement must NOT import inventory's service (modules never import each other's service.py/models.py). So `post_goods_receipt` publishes `procurement/events.GoodsReceiptPosted` (a plain typed event carrying the per-line receipts — item/bin/qty/unit_cost/lot/serial/inspection — plus the resolved GR/IR account, move_date, and the GR document id), and **`inventory/handlers.py`** (new — the first inventory handler) subscribes and calls its OWN `stock_moves.create_move` per line with `valuation_offset_account_id`=GR/IR. Those moves in turn publish `StockValued`, which finance handles — a **two-hop, same-transaction chain** (procurement → inventory → finance), the exact mirror of the existing inventory→finance COGS pattern (publisher exposes events; subscriber's handlers.py imports the publisher's events.py + the link-type constant, the sanctioned §5 events allowance — finance/handlers importing inventory.constants is the precedent). Verified: `grep` confirms procurement imports ONLY `finance/queries` + `inventory/queries` (no service/models); inventory/handlers imports only procurement `events` + the `moved_by` link constant. **(d) The GR↔move linkage is DOCFLOW, not a cross-module FK.** Inventory owns the move, so a GR line carries NO `stock_move_id` column; instead the handler writes a docflow edge GR→`moved_by`→move (and procurement writes PO→`received_by`→GR), so the chain is queryable without procurement FK-ing into inventory's table. **(e) The whole post is ONE atomic transaction** in `run_in_uow`: GR status + `posted_at` + the PO `received_quantity` raise + PO status advance (PARTIALLY_RECEIVED if any line open, RECEIVED if all fully received) are procurement's own writes; the N moves + N inventory-debit/GR-IR journals are the handler's; all commit together or roll back together. A **closed-period `receipt_date`** trips a move's journal period trigger inside the same transaction and rolls the **entire** post back (no move, no journal, GR stays DRAFT, PO untouched) — correct by construction. **(f) Over-receipt is REJECTED in v1** (422 `procurement.over_receipt`) — a receipt line cannot exceed its PO line's open quantity (ordered − received); a configurable tolerance is a 6.4/later refinement. The PO must be APPROVED/SENT/PARTIALLY_RECEIVED to receive (else 422 `procurement.po_not_receivable`). **(g) The inspection hook is a FLAG ONLY in v1.** `requires_inspection` (per GR line, default False) flags the received line and is passed through to the stock move for traceability — it does NOT route to a QI bin, block downstream use, or gate the move; the inspection-lot disposition (accept/reject/rework) is Phase 9 Quality. **(h) Inline, no background path** — a GR is a single delivery (small), so the post runs inline (O(lines)), unlike bulk count posts / payment runs. **Structure:** writes (`service/goods_receipts.py`, 373 lines) split from reads (`service/goods_receipt_reads.py`) at the cap; `models/goods_receipts.py` + `schemas/goods_receipts.py` re-exported from the packages; sibling `goods_receipt_router.py` mounted into `router.py` (the one-surface precedent); `inventory/service/stock_moves.py` re-trimmed to 399 lines after the override param. Migration `0026_procurement_goods_receipts` creates two tables + indexes, NO trigger-bearing alters (the `gr_ir_clearing` purpose is data, the valuation-offset is a service param — neither is schema), reversible, portable both engines, applies on PG to head.
- **D-042 Procurement 3-way match → AP bill + reorder-point auto-requisitions (PLAN 6.4): the matched bill DEBITS GR/IR at PO cost (clearing the receipt credit to ZERO) + routes the invoice-price difference to PPV, created in FINANCE via the event bus; the reorder scan creates procurement requisitions from an inventory query — completes Phase 6 / procurement:** An `InvoiceMatch`/`InvoiceMatchLine` (`proc_invoice_matches`/`_lines`, `DocumentMixin` + a gapless `MATCH-` number claimed at creation, `purchase_order_id` composite FK, `vendor_id` = the opaque finance partner_id snapshot, `vendor_invoice_ref` free text, `gr_ir_account_id` snapshot, opaque `tax_code_id`) compares a vendor invoice against the PO (price) and goods receipt (quantity). **(a) The cross-module mechanism is the EVENT BUS (the STRUCTURE §5 crux, mirroring D-041's GR→inventory bridge):** procurement must NOT import finance/service, so `post_invoice_match` PUBLISHES `procurement/events.InvoiceMatched` (a plain typed event carrying per-line the GR/IR portion at PO cost + the price variance + the resolved GR/IR, PPV and AP-control account ids + the vendor/partner_id/name/due-date) and **`finance/handlers.create_bill_for_match`** subscribes and creates+posts the AP vendor bill through the finance AP service (`create_vendor_bill` + `post_vendor_bill`, never raw inserts) in the SAME `run_in_uow` transaction — finance handling its OWN bill posting, the event published by procurement (the inventory→finance COGS precedent inverted). Verified: procurement imports ONLY `finance/queries` + `inventory/queries` (no service/models); `finance/handlers` imports only procurement `events` + the `billed_by` link constant (the sanctioned events.py §5 allowance — finance/handlers importing inventory.events/constants is the D-041 precedent). **(b) The GR/IR-clears-at-PO-cost + PPV accounting (the 3-way-match subtlety):** the goods receipt credited GR/IR at PO cost at receipt (D-041); the matched bill builds vendor-bill lines whose accounts are the **GR/IR clearing** account (net = matched_qty × PO cost) + the **purchase-price-variance** account (net = (invoiced − PO price) × qty), so the posted journal is **Dr GR/IR (at exactly the receipt PO cost) + Dr/Cr PPV + Dr input tax / Cr AP control (at the vendor-invoiced gross)**. GR/IR therefore nets to **zero** once a PO is fully received and billed (the end-to-end test asserts the account balance is 0 after receipt+bill, with PPV holding any in-tolerance price difference — the procure-to-pay loop closes). New finance posting-default purposes (DATA, not schema): `purchase_price_variance` + `ap_control` (the match-triggered bill resolves the AP control account from a default since procurement holds no GL account; a finance-entered bill still supplies its own `ap_account_id`), resolved via `finance/queries.purchase_price_variance_account` / `ap_control_account` (raise 422 `posting_default_unmapped` when unmapped). **(c) The over-billing 3-way constraint:** a `billed_quantity` column on `proc_purchase_order_lines` (batch-added, default 0, like 6.2's `received_quantity`) gives `po_line_open_to_bill = received − billed`; a match line's matched_quantity cannot exceed received − already-billed → 422 `procurement.over_billing` (you can never bill beyond goods receipt). The PO must be PARTIALLY_RECEIVED/RECEIVED to match. **(d) Tolerance model = a single-per-tenant `proc_match_tolerances` row (price% + quantity%), the ApprovalRule precedent;** absent ⇒ strict 0% defaults (any price difference is an exception, so a tenant must opt into a band — a price change should be deliberate). A match is MATCHED if every line's price deviation is within the band, EXCEPTION if any exceeds it; an **EXCEPTION blocks posting until OVERRIDDEN** (the invoice-release control — a clerk cannot silently bill an unagreed price); override → MATCHED. Per-vendor tolerance GROUPS + multi-step release + ERS are deferred per the parity doc. **(e) ONE atomic transaction + idempotent + closed-period rollback:** `post_invoice_match` (only a MATCHED match posts) raises each PO line's billed_quantity, advances the PO to **CLOSED** when every line is fully received AND billed, links docflow PO→`matched_by`→match + GR→`matched_by`→match + (finance writes) match→`billed_by`→bill, sets the match POSTED + publishes — all commit-or-roll-back together; a closed invoice-date period trips the bill's journal period trigger inside the same transaction and rolls the WHOLE post back (match stays MATCHED, no bill). A POSTED match is terminal (credit-memo/reversal is Phase 7). **(f) Reorder-point auto-requisitions (Part B):** inventory OWNS `reorder_point`/`reorder_quantity` (5.1) and exposes `items_below_reorder_point` (a set-based LEFT JOIN + GROUP BY + HAVING over the on-hand quant projection, no N+1); the DRAFT requisition is a PROCUREMENT document (6.2), so `service/reorder.run_reorder_scan` reads that inventory query downward (never importing inventory models/service) and creates ONE draft requisition with a line per below-reorder item (qty = reorder_quantity, base UoM) via the existing 6.2 create, so it flows through the normal approval chain. **Idempotent dedup:** `open_requisition_item_ids` collects items already on a DRAFT/SUBMITTED/APPROVED (un-converted) requisition line and the scan skips them, so a second scan the same day adds no duplicate; returns None (→ 200 null) when nothing is below reorder. Inline in v1 (modest item counts); a volume past the sync budget moves it behind the `procurement.reorder_scan` job (documented). **(g) Structure:** writes split `service/invoice_matches.py` (create + validation/variance/tolerance + the shared `po_lines_for`) from `service/invoice_match_post.py` (post/override/cancel) and `service/invoice_match_reads.py` (reads + tolerance config) at the 400-line cap (the goods_receipts precedent); `models/invoice_matches.py` (2 tables + `MatchTolerance`) + `schemas/invoice_matches.py` re-exported; sibling `invoice_match_router.py` (matches + tolerances + the reorder-scan endpoint) mounted into `router.py`. Migration `0027_procurement_invoice_matches` creates three tables + batch-adds `billed_quantity`, NO trigger-bearing alters, reversible, zero autogenerate drift on SQLite AND Postgres, applies on PG to head. **Phase 6 / Procurement is COMPLETE.**
- **D-043 Sales customer master + condition-style pricing (PLAN 7.1, opens Phase 7): the customer's id IS finance AR's opaque partner_id; credit_limit is a non-null MoneyType (0 = cash-only, no NULL/unlimited sentinel); price lists resolve deterministically by priority → specificity → latest valid_from:** Opens the sales module with the customer master + the condition-style pricing engine. Four tables (migration `0028_sales_customers_pricing`, down_revision 0027, no trigger-bearing alters, reversible, portable, ≤63-char ids): `sales_customer_groups` (lean grouping master), `sales_customers`, `sales_price_lists`, `sales_price_list_items`. **(a) The customer ↔ AR partner_id link (D-029, the vendor mirror).** Sales OWNS the customer entity; finance is below it and owns no customer table — every AR invoice/receipt stores its customer as an OPAQUE `partner_id` (a plain Uuid, NO FK) + a denormalized `partner_name`, and that `partner_id` IS `Customer.id`. `sales/queries.get_customer_for_partner` resolves it back (an intent-named alias over `get_customer`), the exact mirror of procurement's `get_vendor_for_partner`. **(b) credit_limit semantics.** `Customer.credit_limit` is a **non-null `MoneyType` defaulting to 0** = the maximum outstanding AR the customer may carry: **0 = CASH-ONLY** (no open credit; 7.2's order confirmation blocks any positive outstanding AR), **a positive value = the credit ceiling**. Deliberately **no NULL/"unlimited" sentinel** — "unlimited" is an explicit large limit — so the column stays non-null and 7.2's check is a single unconditional comparison with no NULL branch; a DB `CHECK (credit_limit >= 0)` backs it. This is the **static** credit-limit block the parity Sales section scopes to v1 (no exposure aggregation across open orders/deliveries/receivables, no finance-owned exposure ledger — a documented later). `sales/queries.customer_credit_limit` exposes it. **(c) Customer codes user-supplied + unique per tenant** (`UNIQUE(tenant, customer_code)`, the vendor-code/item-code precedent — a master carries a code, not a gapless document number; the O2C documents in 7.2+ DO claim gapless numbers). **(d) Customer group = a MASTER table, not a free string.** A customer optionally belongs to ONE `CustomerGroup` (`sales_customer_groups`, tenant+code+name); a master table is chosen because **pricing keys on the group** — a `PriceList` targets a group by id (a composite tenant FK), so the customer and the price list reference the same rows, the name is edited in one place, and a typo'd free-string group can never silently exclude a customer from its price list. The group carries no pricing of its own; `customer_group_id` is nullable on both the customer and the price list (NULL on a price list = a GENERAL list). **(e) The condition-style price model.** A `PriceList` is a condition header (currency + optional `customer_group_id` + inclusive `[valid_from, valid_to]` window with `valid_to` NULL = open-ended + `status` ACTIVE/INACTIVE + a `priority` integer) carrying `PriceListItem` rows — **ONE base `unit_price` per (list, item)**, `UNIQUE(tenant, price_list_id, item_id)`, with a `min_quantity` floor (default 0; v1's only scale knob). `item_id` is OPAQUE (validated via `inventory/queries.item_exists`, no FK); the list/item currencies validate via `finance/queries.currency_exists`. This is the parity-PARTIAL condition model — NOT the generalized access-sequence/pricing-procedure engine, multi-tier quantity scales, or freight/tax condition types (all deferred). **(f) The DETERMINISTIC resolution order (`service/price_resolution.resolve_price`, exposed via `sales/queries.resolve_price` — the public entry 7.2's order entry prices each line through).** A list APPLIES when ALL of: status ACTIVE; currency matches; `valid_from <= on_date` and (`valid_to IS NULL` or `valid_to >= on_date`); the list is general (`customer_group_id IS NULL`) OR targets the customer's group (a list targeting a different group, or any group when the customer has none, is excluded); it has a `PriceListItem` for the item with `min_quantity <= quantity`. Among applying lists the winner is picked by, in STRICT order: **1) highest `priority`; 2) most specific (group-targeted beats general); 3) latest `valid_from`; 4) price-list `id`** as a final stable tiebreaker (so the result never depends on row order). No applying list → `matched=False` (no price; 7.2 then requires a manual price/override). **NO discount is applied** here — the price list yields the base price only; line discounts (`DiscountType` PERCENT/AMOUNT, defined now) are a per-order-line concern in 7.2. **(g) The resolver is BOUNDED to two queries** regardless of list count (PERFORMANCE §6, no N+1): one fetches the candidate ACTIVE lists matching currency+date+group (index `ix_sales_price_lists_resolver` on `(tenant, currency, customer_group_id, valid_from)`); one fetches those lists' items for the item meeting the floor (`ix_sales_price_list_items_tenant_id_item_id`); the winner is picked in Python over that small set. **(h) Structure:** a `models/` package (customers.py + pricing.py) and a `service/` package (customers/customer_groups/pricing/price_resolution) from the start (the 400-line cap); a single schemas.py + queries.py; `router.py` (customers + groups) mounts a sibling `pricing_router.py` (price lists + nested items + the `GET /price-quote` resolver endpoint) under one `/api/v1/sales` surface; mounted after procurement in main.py (the D-011 handler order). Reference lists (customers, groups, price lists) carry collection ETags (D-035) + the ≤3 query budget. **7.1 publishes NO domain events** (masters + pricing drive no cross-module effects; orders in 7.2 will). Permissions: `sales.customer.read/.manage` (groups ride customer.manage) + `sales.pricelist.read/.manage`. **Phase 7 / Sales is OPEN; 7.2–7.4 add quote→order→delivery→invoice.**
- **D-044 Sales quote → order with simple ATP (availability + backorder-flag, NOT a block) + a HARD credit-limit block at confirmation (PLAN 7.2): the committed-quantity reservation is a query over confirmed-undelivered order lines; exposure = open AR + open confirmed orders + this order:** Extends Sales with the O2C spine — `Quote`/`QuoteLine` (`sales_quotes`/`_lines`, `DocumentMixin` + gapless `QUO-` number claimed at creation) and `SalesOrder`/`SalesOrderLine` (`sales_orders`/`_lines`, `DocumentMixin` + gapless `SO-` number) — mirrored on the procurement requisition/PO shape (sales order ≈ purchase order). Migration `0029_sales_orders` (down_revision 0028): four tables + (tenant,status) / (tenant,customer_id,status) indexes, NO trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG. **(a) Quote lifecycle** (`QuoteStatus` DRAFT→SENT→ACCEPTED/REJECTED, DRAFT/SENT→EXPIRED on `valid_until` lapse, ACCEPTED→CONVERTED, +CANCELLED — all set IN 7.2, a quote is wholly sales-internal): no committing gate, so MANAGE covers every action (the RFQ-manage precedent). **Expiry is LAZY** — `mark_expired_if_lapsed` is a service check the read paths call (no background sweep in v1), documented. Quote lines default `unit_price` from the price resolver (D-043, overridable) and apply an optional per-line discount (`DiscountType` PERCENT = % off gross, AMOUNT = per-unit off), `line_amount` = qty×unit_price − discount quantized to currency, `total_amount` maintained. **(b) Order lifecycle** (`SalesOrderStatus`): DRAFT → (confirm) **CONFIRMED** or **CREDIT_BLOCKED**, + CANCELLED set in 7.2; PARTIALLY_DELIVERED/DELIVERED (7.3), INVOICED (7.4), CLOSED declared now as the full lifecycle, transitions land later. An order is created from scratch OR by converting an ACCEPTED quote (copy lines with the quote's frozen prices + discounts, link docflow quote→`converted_to`→order, set `source_quote_id`, advance the quote to CONVERTED — the procurement convert precedent, one shared writer for both paths). The customer must be ACTIVE at create (422 `sales.customer_not_active` — the soft block, distinct from the credit block). `payment_terms_days` is snapshot from the customer (the 7.4 invoice reads the snapshot). **(c) ATP = INFORMATIONAL availability + a backorder FLAG, NOT a block (the parity v1 scope).** `sales/queries.atp_check` computes **availability = on-hand − committed + on-order**: on-hand = `inventory/queries.total_on_hand` (the maintained quant projection); on-order = a NEW sanctioned cross-module read `procurement/queries.open_incoming_quantity` (ordered − received summed per item over APPROVED/SENT/PARTIALLY_RECEIVED POs, set-based, no N+1); committed = the reservation (see d). A line whose requested qty > availability is a **backorder** — but ATP does **NOT block**: parity scopes v1 to "simple ATP = an availability check with manual backorders", so `confirm_order` evaluates ATP per line, records the per-line `backordered` snapshot on the `ConfirmResult`, and **proceeds** (the order still CONFIRMS). A `POST /orders/atp` (line set) + `GET /orders/atp` (single item) endpoint exposes the availability preview for the order UI. **(d) The committed-quantity reservation = a QUERY, not a table.** `sales/queries.committed_quantity(item_id)` sums ordered − delivered over CONFIRMED/PARTIALLY_DELIVERED sales order lines for the item (set-based, no N+1) — so confirming an order COMMITS its undelivered quantity against ATP for subsequent orders, and 7.3's delivery (raising `delivered_quantity`) shrinks the commitment automatically, with no separate reservation table to reconcile. Confirm excludes the order's own demand (`exclude_order_id`) so a line is checked net of OTHER commitments. **(e) Credit = the HARD block at confirmation (parity "static credit-limit block at order confirmation").** Exposure = the customer's **open AR** (`sales/queries.customer_open_ar` → finance's existing `customer_open_balance`, the sum of `open_amount` on POSTED customer invoices keyed by the opaque partner_id = Customer.id, D-029 — REUSED, no finance change) + the value of the customer's **other open confirmed orders** (`open_confirmed_order_value`, CONFIRMED/PARTIALLY_DELIVERED/DELIVERED orders excluding this one) + **this order's total**. If exposure > `credit_limit` (0 = cash-only, D-043) the order is set **CREDIT_BLOCKED** / `credit_check_status` BLOCKED and is NOT confirmed; within → **CONFIRMED** / PASSED. This is the v1 scope: no exposure aggregation beyond open AR + open confirmed orders, no recheck at delivery, no scoring (all deferred per parity). **(f) Credit release.** A user with `sales.order.credit_release` calls `release_credit` → `credit_check_status` RELEASED, then re-confirms (a RELEASED order SKIPS the credit gate) → CONFIRMED. `confirm` is a distinct permission from `manage` (it runs the ATP + credit gates — the journal.post/po.approve precedent); `credit_release` is distinct again (an approval-like override past the limit — a clerk who can confirm cannot silently override a block). Confirm is **idempotent** (re-confirming a CONFIRMED order is a no-op return; the create/convert/confirm/release endpoints carry D-013 idempotency). **(g) NO events.py yet.** A confirmed order is a COMMITMENT (like a PO) with no finance/inventory posting — 7.3's delivery is the first sales event (stock issue → COGS). So sales ships no `events.py`/`handlers.py` (the no-empty-file rule). **(h) Structure:** `models/orders.py` re-exported; `schemas.py`→`schemas/` package (masters.py + orders.py, split at the cap, re-exported so import sites are unchanged); `service/` adds `_shared.py` (validation + numbering + the line-pricing/discount helper), `quotes.py`, `orders.py`, `order_confirm.py` (the ATP+credit gate, focused <250 lines), `conversions.py` (quote→order); `router.py` mounts sibling `quote_router.py` + `order_router.py` under one `/api/v1/sales` surface. **Cross-module additions (sanctioned downward reads, STRUCTURE §5):** `procurement/queries.open_incoming_quantity` (NEW — the on-order side of ATP); `sales/queries` reuses `finance/queries.customer_open_balance` + `inventory/queries.total_on_hand` (no new finance/inventory queries). Sales imports ONLY finance/inventory/procurement QUERIES, never their service (grep-verified).
- **D-045 Sales delivery → stock issue via the event bus mirroring 6.3's goods receipt; COGS is the default ISSUE offset (no GR/IR-style override); delivered_quantity advances the order; backorders = undelivered open lines; the whole post is one atomic transaction (PLAN 7.3):** Adds the **delivery** — the outbound twin of the procurement goods receipt (6.3) — as the first sales document that moves stock and posts to the GL. `Delivery`/`DeliveryLine` (`sales_deliveries`/`_lines`, `DocumentMixin` + gapless `DN-` number claimed at creation): built DRAFT against a CONFIRMED (or already PARTIALLY_DELIVERED) order — `sales_order_id` composite FK, `customer_id` snapshot, `warehouse_id` opaque inventory id — with lines naming the order line shipped against, the SOURCE `bin_id`, the shipped quantity, and optional lot/serial codes; `item_id` is **snapshot from the order line** (the create payload names only the order line, so the client cannot rewrite the ordered item). Migration `0030_sales_deliveries` (down_revision 0029): two tables + (tenant,status) / (tenant,sales_order_id) indexes, NO trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG. **(a) Stock issue → COGS via the event bus (§5).** Sales must NOT call inventory's service — `post_delivery` publishes `DeliveryShipped` (in `sales/events.py`, the first sales event) carrying the per-line issues; **inventory's `handlers.py`** subscribes and creates the stock **ISSUE** moves, which publish `StockValued` so finance posts **Dr COGS / Cr Inventory** at the moving-average issue cost — a two-hop, same-transaction chain (sales → inventory → finance), the exact mirror of 6.3's receipt chain. **(b) NO valuation-offset override (vs 6.3's GR/IR override).** A delivery sets no offset account on the event: an ISSUE move's **default** offset is the item-category COGS account, so **COGS *is* the issue offset** (D-045/D-020) — no GR/IR-style intermediary on the outbound side. **(c) delivered_quantity advances the order.** A post raises each order line's `delivered_quantity` (over-delivery beyond open-to-deliver is rejected 422 `sales.over_delivery` in v1) and advances the order to **PARTIALLY_DELIVERED** (any line still open) or **DELIVERED** (all lines fully delivered); because `committed_quantity` is confirmed-undelivered demand, the 7.2 ATP commitment **shrinks** by exactly the delivered amount. **(d) Backorders = undelivered open order lines** — no separate backorder table; a follow-up delivery against the same (now PARTIALLY_DELIVERED) order completes them. **(e) Delivery↔move link = docflow, not a cross-module FK.** Inventory owns the move, so the delivery line carries no `stock_move_id`; the chain records order →`delivered_by`→ delivery →`moved_by`→ each stock move (the DocFlow endpoint renders order → delivery → move). **(f) One atomic transaction.** The whole post — delivery + N issue moves + N COGS journals + the order `delivered_quantity` raise + status advance + docflow link — commits atomically or rolls back together (D-011): a **closed-period** `delivery_date` trips a move's journal period trigger and an **insufficient-stock** bin trips the move's no-negative guard; either rolls the entire post back (delivery stays DRAFT). **(g) Lifecycle DRAFT → POSTED, POSTED terminal** — a DRAFT cancels; a POSTED delivery has issued stock + posted COGS, so it is corrected by a return / RMA (7.4), never cancelled (v1 ships no reverse-delivery). Post is idempotent (D-013); re-posting a POSTED delivery is rejected. RBAC splits three authorities (D-009): read / create-cancel by `sales.delivery.manage` / post by the distinct `sales.delivery.post`. **(h) Structure:** `models/deliveries.py` + `schemas/deliveries.py` + `events.py` re-exported; `service/` adds `deliveries.py` (create/cancel), `delivery_post.py` (the post heart + tracking/status helpers, split at the 400-line cap), `delivery_reads.py` (list + point reads); `router.py` mounts sibling `delivery_router.py`. Sales imports ONLY inventory/finance QUERIES (`bin_exists`, `on_hand`, `lot_id_for_code`, `serial_id_for_code`), never their service (grep-verified). Issue #53 filed for an aiosqlite-connection caveat: a handler-raised post failure (insufficient stock) can leave the test connection unusable, so API tests assert the 422 cases at CREATE validation and the happy post at success + advanced state.
- **D-046 Sales billing → AR customer invoice via the event bus (mirroring 6.4's match → AP bill, sign-flipped); RMA returns reverse BOTH the delivery's COGS and the billing's revenue via two event-bus legs; the order-to-cash GL nets to zero on a full return; over-billing ≤ delivered, over-return ≤ invoiced (PLAN 7.4 — completes Phase 7 / Sales):** Adds the two documents that close the order-to-cash loop. **(A) Billing.** `SalesBilling`/`SalesBillingLine` (`sales_billings`/`_lines`, `DocumentMixin` + gapless `BIL-` number at creation) — the AR mirror of the procurement invoice match (6.4). Built DRAFT against an at-least-partially-delivered order (customer + `payment_terms_days` snapshot from the order; per line item/`unit_price`/discount/`tax_code_id` snapshot from the order line); each billed qty ≤ **delivered − invoiced** (over-billing → 422 `sales.over_billing` — you invoice what shipped). A `bill_all_delivered` convenience flag bills every delivered-not-invoiced line. **Post → AR via the event bus (§5):** sales resolves the AR control + sales-revenue accounts from `finance/queries` up front (a missing posting default fails the post before any state change), publishes `BillingInvoiced`; **finance's `handlers.py`** creates + posts the AR customer invoice **Dr AR control gross / Cr sales-revenue per line + Cr output tax** (partner_id = customer id D-029, due = billing_date + terms) via the existing `create_customer_invoice`+`post_customer_invoice` path (no hand-rolled journal). The post raises `invoiced_quantity` and advances the order **INVOICED** (fully invoiced) / **CLOSED** (fully delivered AND invoiced). The finance AR invoice claims its OWN `INV-` number — two numbers, the sales billing triggers the finance doc (mirroring match→bill). **(B) Returns / RMA.** `SalesReturn`/`SalesReturnLine` (`sales_returns`/`_lines`, `DocumentMixin` + gapless `RMA-` number) — a delivery run backwards. Built DRAFT against a delivered+invoiced order (receiving `bin_id` + priced fields snapshot per line); each returned qty ≤ **invoiced − returned** (over-return → 422 `sales.over_return`). **The cap is INVOICED, not delivered:** a credit note must reduce a real invoice, so the customer cannot be credited for more than was invoiced — tracked by a new `returned_quantity` column on `sales_order_lines`. **Post → TWO atomic event-bus legs:** `ReturnReceived` → inventory's handler creates a stock **RECEIPT** move per line with `valuation_offset_account_id` = the item-category **COGS** account (the override, mirroring 6.3's GR/IR override) so the costing posts **Dr Inventory / Cr COGS** — reversing the issue (goods re-enter at current book cost); `ReturnCredited` → finance's handler posts an AR **credit note** **Dr sales-revenue / Cr AR control + reverse output tax** — reversing the billing. **(C) Credit notes in finance: added (4.6 had none).** Finance shipped no credit-note path, so 7.4 adds `finance/service/credit_notes.create_and_post_customer_credit_note`: a credit note is a `CustomerInvoice` row (no new model) whose POSTED journal carries `document_type` **AR_CREDIT_NOTE** and the SIGN-FLIPPED directions of an invoice; it claims a `CN-` number and `open_amount`=0 (a reduction, not an open receivable to dun). The draft build (tax+totals) is shared verbatim via the extracted `create_ar_document_draft` builder — the only differences are the doc type + journal direction. The journal one-side CHECK requires positive amounts, so a credit note posts the reversal on the *flipped* sides (positive), NOT negative amounts. **(D) The order-to-cash GL:** delivery posts Dr COGS / Cr Inventory; billing posts Dr AR / Cr Revenue (+ tax); a full return reverses BOTH — inventory back UP, COGS back DOWN, revenue back DOWN, AR back DOWN — so the AR/revenue/COGS accounts **net to zero** (proven via the finance account-balance projection, the 6.4 GR/IR-clears-to-zero mirror); inventory holds the returned goods' value (they are physically back in stock). **(E) Migration `0031_sales_billing_returns`** (down_revision 0030): four tables + (tenant,status)/(tenant,sales_order_id) indexes + the `returned_quantity` batch-alter column on `sales_order_lines`; NO trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG. **(F) Structure:** `constants.py`→`constants/` package (enums/permissions/documents, split at the cap, re-exported); `models/billing.py`+`returns.py`, `schemas/billing.py`+`returns.py`, `events.py` (adds `BillingInvoiced`/`ReturnReceived`/`ReturnCredited`); `service/` adds `billing.py`+`billing_post.py`+`billing_reads.py` and `returns.py`+`returns_post.py`+`returns_reads.py`; `router.py` mounts sibling `billing_router.py`+`return_router.py`. Finance adds the `ar_control`/`sales_revenue` posting defaults + `finance/queries.ar_control_account`/`sales_revenue_account` + the two handlers (registered in `app.main.register_event_handlers`). Lifecycle DRAFT→POSTED, POSTED terminal (corrected by a return/credit note, never cancelled); post idempotent-rejected; RBAC read/manage/the distinct `.post`. Sales imports ONLY finance/inventory QUERIES + events, never their service (grep-verified §5). Per issue #53, the rejection tests use create-time 422s; the happy path asserts success + state.
- **D-047 Manufacturing opens (PLAN 8.1) with the PP master data; BOM/routing identity is `(item_id, version)`; the activation model is DRAFT-editable-then-ACTIVE-frozen with one default-active version per item; multi-level BOMs are "via references" resolved at 8.3 explosion time:** Manufacturing is the fifth business module and sits ABOVE inventory — it imports `finance/queries` AND `inventory/queries` DOWNWARD (D-029) and exposes `manufacturing/queries.py` as the only file modules above it (8.2/8.3) import (STRUCTURE §5). **(a) Identity `(item_id, version)`.** A BOM AND a routing are keyed by `(tenant_id, item_id, version)` — the item produced/made plus a user-supplied version string — NOT a standalone code (S/4HANA's material+alternative keying), so BOM and routing identity read identically and a routing is looked up exactly like a BOM. **Work centres** carry a user-supplied `code` unique per tenant (the item_code/vendor_code/customer_code master precedent). **Masters carry no gapless document number** — a code/version is a stable handle, not a posted-document number; the production ORDERS in 8.2 will claim gapless numbers (D-012), the masters do not. **(b) Activation model (DRAFT→ACTIVE→INACTIVE).** A version is born DRAFT and **editable** (header + components/operations may be added/changed/removed). **Activating** freezes it (components/operations immutable — corrections are a NEW version, the append-only-master philosophy), requires ≥1 component/operation, and makes it the item's **single ACTIVE default**: activation demotes the previously-default ACTIVE version's `is_default` so exactly one default-active version exists per item — the one `get_active_bom_for_item`/`get_active_routing_for_item` resolve (8.2/8.3 read this). Deactivating an ACTIVE version → INACTIVE (kept for history, never deleted). Editing/adding/deleting on a non-DRAFT version → 409. **(c) Multi-level = via references.** The SCHEMA is single-level-per-BOM: a `Bom` header lists DIRECT `BomComponent` lines of one parent. "Multi-level" emerges because a component item can itself be the parent of its OWN `Bom`; the tree is resolved by EXPLOSION at MRP time (8.3), which walks component→its active BOM→its components recursively (visited set + depth cap, the `docflow.get_chain` precedent). The masters reject only a DIRECT self-component (a component whose item IS the BOM's parent — 422 `manufacturing.bom_self_component`); deeper cycles (A→B→A) are an 8.3 explosion-time concern, not expressible on a single-level row. `BomComponent.scrap_percent` is the per-component waste allowance; `Bom.base_quantity` is the parent yield, component `quantity_per` is per base_quantity. **(d) Work centres + routings.** `WorkCenter.capacity_hours_per_day` feeds 8.3's rough capacity check; `efficiency_percent` (default 100) scales throughput; `cost_center_id` is an OPAQUE finance id (D-029, nullable, validated via `finance/queries.cost_center_exists`) for later activity-rate costing. `RoutingOperation` pins a step to a work centre (`work_center_id` is an INTRA-module composite tenant FK) with `setup_time_minutes` (fixed/order) + `run_time_minutes_per_unit` (per unit) — MINUTES as `QuantityType` scale-6 (fractional minutes, exact on both engines, D-015 — never plain Numeric); `operation_number` (10/20/30…) unique per routing, auto-appended as the next multiple of 10. **(e) Opaque cross-module ids + the new `inventory/queries.uom_exists`.** BOM/routing item ids + the BOM UoM ids are opaque inventory ids (no FK to inv_items/inv_uoms), validated via `inventory/queries.item_exists` and a NEW `inventory/queries.uom_exists` (the UoM analogue of `item_exists` — a UoM is a distinct inventory entity, so it could not be folded into `item_exists`; added in 8.1). The work centre's cost_center_id validates via `finance/queries.cost_center_exists` (already present). No new finance query was needed. **(f) Migration `0032_manufacturing_masters`** (down_revision 0031): five `mfg_` tables (work centres, BOM header+components, routing header+operations) + the (tenant,code)/(tenant,item_id,status)/(tenant,is_active) indexes + the per-table read indexes; CHECKs (capacity≥0, efficiency>0, base_quantity>0, quantity_per>0, scrap≥0, setup/run≥0); NO trigger-bearing alters (D-022), reversible, portable both engines, the longest id is exactly 63 chars (PG cap), zero autogenerate drift on SQLite + PG. **(g) Structure:** `constants.py` (single file under the cap), `models/` package (workcenters/boms/routings), `schemas/` package, `service/` package (work_centers/boms/routings), `queries.py`, `router.py`+sibling `bom_router.py`+`routing_router.py` (mounted into one `/api/v1/manufacturing` surface, the finance/inventory include precedent). NO events.py/handlers.py (masters drive no cross-module effects; production orders in 8.2 will). RBAC read/manage per master (manage covers activate/deactivate — no GL effect, so no separate `.post`-style key). Manufacturing imports ONLY finance/inventory QUERIES downward (grep-verified §5).
- **D-048 Production orders (PLAN 8.2): reserve → issue components to WIP (Dr WIP / Cr Inventory) → finish to stock (Dr Inventory / Cr WIP) through the EVENT BUS with a valuation-offset OVERRIDE to a WIP clearing account; WIP nets to ZERO per fully-issued+finished order; accumulated_wip_cost tracked on the order; residual flushes to a production-variance account; single-level explosion per order:** A production order (`mfg_production_orders`, prefix `MO-` — distinct from purchase-order `PO-`) turns components into a finished parent item across the manufacturing↔inventory↔finance seam, mirroring the 6.3 GR/IR override and 7.3 delivery COGS. Three tables (migration `0033_manufacturing_production`, down_revision 0032, no trigger-bearing alters, reversible, portable, ≤63-char ids): `mfg_production_orders` (DocumentMixin header + composite FKs to mfg_boms/mfg_routings), `mfg_production_order_components` (the exploded reservations), `mfg_production_order_operations` (the routing snapshot for 8.3 capacity load). **(a) Status lifecycle.** DRAFT (created + active BOM EXPLODED into component rows + routing snapshotted) → RELEASED (materials reserved — the component rows ARE the reservation, v1 ATP-style; release does NOT block on availability, a shortage is informational) → IN_PROGRESS (≥1 component issued to WIP) → FINISHED (parent fully produced, WIP nets to zero) | CANCELLED (DRAFT/RELEASED only — once a component is issued the order is POSTED-ish and must FINISH; issued stock + WIP cannot strand, no reverse-issue in v1). **(b) Single-level explosion.** `required_quantity = quantity_per × order_qty / base_quantity × (1 + scrap_percent/100)`, quantized to the QuantityType scale; a sub-assembly component is produced by its OWN order (multi-level via references, D-047), this order issues it as a finished material, it does not recurse. No active default BOM (and none supplied) → 422 `manufacturing.no_active_bom`. **(c) The §5 event-bus mechanism (the binding constraint).** Manufacturing imports ONLY `inventory/queries` + `finance/queries` + its own events — NEVER inventory/finance SERVICE (grep-verified). `issue_components` publishes `ComponentsIssued` → inventory's `handlers.issue_production_components` creates an ISSUE move per component with `valuation_offset_account_id` = the WIP account (the 6.3 GR/IR-override pattern applied to an ISSUE — required THREADING the override through the costing engine's outbound path `_apply_outbound`, which previously hardcoded the COGS offset) → the StockValued costing event posts Dr WIP / Cr Inventory. `finish_order` publishes `OrderFinished` → inventory's `handlers.receive_finished_order_move` creates a RECEIPT move (`unit_cost` = accumulated WIP / ordered qty, WIP offset) → Dr Inventory / Cr WIP. Both handlers register at `app.main.register_event_handlers` (the deterministic D-011 seam; the manufacturing test conftest re-registers after the per-test reset). **(d) accumulated_wip_cost tracking.** The order carries `accumulated_wip_cost` (MoneyType, the WIP-nets-to-zero SSOT): the issue flow raises it by the component's current book cost × qty (read pre-publish from `inventory/queries.current_unit_cost`, which equals the MAV issue cost the move will post — exact for moving-average), and the finish flow drains it. Maintained on the header (not recomputed from the journal) so the finish flow derives the finished unit cost in hand. Exposed via `manufacturing/queries.order_wip_balance`. **(e) The WIP-nets-to-zero invariant + the variance flush.** The finished goods enter at a UNIFORM per-unit cost = accumulated WIP / ordered qty (quantized); on the FINAL finish any residual (the quantization difference between accumulated WIP and the total received, or genuine over/under-absorption) is carried on `OrderFinished` as a SIGNED `variance_amount`, and finance's `post_production_variance` handler posts Dr/Cr WIP / Cr/Dr the production-variance account (the MAV zero-quantity-flush analogue) so the WIP clearing account nets to EXACTLY zero at completion. Routing the variance through a finance HANDLER (a second OrderFinished subscriber) — not a manufacturing-posted journal — keeps manufacturing free of any finance/service import (§5). Proven end-to-end: WIP balance == 0, the finished item's inventory value == the issued component cost, the residual holds in the variance account — read off the trial balance (the 6.4 GR/IR-clears-to-zero / 7.4 nets-to-zero precedent). **(f) Posting defaults.** `wip_clearing` (ASSET) + `production_variance` (EXPENSE) per-tenant posting defaults, resolved via NEW `finance/queries.wip_clearing_account` / `.production_variance_account` (the gr_ir/ppv mirror); an unmapped WIP account fails an issue/finish loud (422 `finance.posting_default_unmapped`). The existing inventory→finance StockValued handler posts both WIP journals unchanged (it is a generic Dr/Cr switch over the event's offset). **(g) Over-issue/closed-period.** v1 over-issue policy: issued ≤ required (beyond → 422 `manufacturing.over_issue`). A closed period trips the WIP journal's period trigger in the same transaction and rolls the whole issue/finish back; insufficient stock trips the inventory move guard and rolls back (tests assert the rolled-back state via FRESH reads, never post-failure ORM state — issue #53). **(h) Permissions + numbering + structure.** `manufacturing.production_order` `.read` / `.manage` (create+cancel) / `.release` / `.execute` (issue+finish folded under one key — both POST stock+journals, the shop-floor action). MO- gapless number claimed at creation (D-012). New files: `models/production.py`, `events.py`, `schemas/production.py`, `service/production_orders.py` + `service/production_post.py` (split at the cap, the delivery_post precedent), `production_order_router.py` (sibling, mounted). Docflow: order → `issued_to` → ISSUE moves, order → `finished_to` → finished RECEIPT move + variance entry. Idempotent create/issue/finish (D-013).
- **D-049 MRP run (PLAN 8.3, COMPLETES Phase 8 / Manufacturing): a deterministic, set-based, level-ordered planning pass — run ALWAYS as a `manufacturing.mrp_run` BACKGROUND JOB — that nets DEMAND (open sales-order demand + reorder-point shortfall) against SUPPLY (on-hand + open POs + open production orders + firmed/converted planned orders), EXPLODES MAKE items' BOMs into dependent component demand with a cycle guard, and writes PLANNED ORDERS (MAKE/BUY) + a rough per-work-centre CAPACITY load; planned orders convert MAKE→production order (intra-module) / BUY→requisition (via an event):** Three tables (migration `0034_manufacturing_mrp`, down_revision 0033, no trigger-bearing alters, reversible, portable, ≤63-char ids): `mfg_mrp_runs` (DocumentMixin header, prefix `MRP-` gapless number at creation — the depreciation-run precedent), `mfg_planned_orders` (the regenerated planning output, no number — ephemeral per-run output keyed by `mrp_run_id`), `mfg_capacity_loads` (the per-work-centre load report). **(a) The algorithm.** DEMAND per item (level 0, independent) = open undelivered sales-order quantity (`sales/queries.open_demand_item_ids` + `committed_quantity` — the ATP reservation sum) + reorder-point shortfall (`inventory/queries.items_below_reorder_point` → demand = `reorder_quantity`). SUPPLY per item = `inventory/queries.total_on_hand` + `procurement/queries.open_incoming_quantity` (open POs) + `manufacturing/queries.open_production_order_supply` (un-finished open production orders) + `manufacturing/queries.planned_make_supply` (still-open FIRMED/CONVERTED planned orders — the regeneration policy keeps committed proposals as supply). `net_requirement = max(0, demand − supply)`, quantized to the QuantityType scale (D-015, summed in Python so the exact decimal round-trips on both engines). **(b) MAKE vs BUY is STRUCTURAL via active-BOM presence.** An item with an ACTIVE default BOM (`get_active_bom_for_item`) is MAKE — a planned PRODUCTION order, and its BOM is EXPLODED; an item with no active BOM is BUY — a planned PURCHASE order (a leaf the explosion stops at). This is how Atlas distinguishes manufactured from purchased items — documented in `docs/modules/manufacturing.md` + `docs/modules/inventory.md`. **(c) Multi-level explosion + cycle guard.** LEVEL-ORDERED: level 0 (independently-demanded items) nets first; each MAKE item's net requirement explodes its BOM into dependent demand for components at the next level (`dependent = quantity_per × parent_net / base_quantity × (1 + scrap_percent/100)`, the create_production_order math), accumulating across all parents (a component used by two parents sums). Each level batches its active BOMs (ONE query) + their components (ONE query); the explosion runs IN MEMORY. The guard: a `netted` set (low-level-code — a component re-appearing deeper is folded into its earlier net, never re-planned) + a `MRP_MAX_EXPLOSION_LEVELS=20` depth cap (the docflow get_chain precedent), so a masters-rejected-but-defensive cycle (A↔B across separate BOMs) terminates cleanly rather than hanging. **(d) Run-as-job (PERFORMANCE §3).** The run scans every planning-relevant item, so it is ALWAYS a job: `POST /api/v1/manufacturing/mrp/runs` submits a `manufacturing.mrp_run` job and returns 202 `{job_id}` (the depreciation-job pattern, minus the inline branch); the submit is idempotent (D-013 — a replayed key returns the same job id). Each run is its own `MrpRun` snapshot (multiple runs allowed); a re-run REGENERATES — deletes the tenant's prior PLANNED (un-firmed) rows then writes a fresh plan; FIRMED/CONVERTED/CANCELLED rows survive (FIRMED/CONVERTED net as supply). **(e) Rough capacity check (parity capacity = PARTIAL, evaluation only).** For the run's planned MAKE orders (their items' active routings, batched) PLUS the tenant's open production orders (their snapshot operations' precomputed `planned_minutes`, ONE grouped query), sum each work centre's LOAD = Σ(`setup_time_minutes + run_time_minutes_per_unit × qty`); AVAILABLE = `capacity_hours_per_day × efficiency_percent/100 × horizon_days × 60`; `utilization_percent = load/available × 100`; `is_overloaded = load > available`. NO leveling / finite scheduling — a rough infinite-capacity load picture. Persisted per run (a stable report). **(f) Conversion + §5.** `convert_planned_order`: a MAKE order calls manufacturing's OWN `create_production_order` (intra-module, fine) and records `converted_document_id` + a docflow run → 'planned_to' → order edge; a BUY order PUBLISHES `PlannedBuyConverted` → procurement's `handlers.create_requisition_for_planned_buy` creates the DRAFT requisition in the SAME transaction + links run → 'planned_to' → requisition (the §5-clean cross-module WRITE, the billing→AR-invoice precedent — manufacturing NEVER imports sales/inventory/procurement SERVICE). Firm (PLANNED→FIRMED) / cancel (→CANCELLED, a CONVERTED order can't be cancelled) round out the lifecycle. **(g) Cross-module READ directions added + no-cycle confirmation (§5).** MRP reads, downward/sibling, three module `queries.py`: manufacturing→sales/queries (`open_demand_item_ids`, `committed_quantity` — the latter pre-existing from 7.2, `open_demand_item_ids` ADDED for 8.3), manufacturing→procurement/queries (`open_incoming_quantity`, pre-existing), manufacturing→inventory/queries + manufacturing→finance/queries (pre-existing). Sales and procurement are SIBLINGS of manufacturing; verified NO bidirectional cycle: neither sales nor procurement imports manufacturing/queries (manufacturing is the newest module — grep-confirmed), so manufacturing→sales/queries and manufacturing→procurement/queries are one-directional, allowed (STRUCTURE §5 bans only BIDIRECTIONAL query imports). Inventory/finance are below everything. The planned-BUY→requisition WRITE is an event, never a service import. **(h) Permissions + structure.** `manufacturing.mrp.read` / `.mrp.run` (segregated — running the engine is a planning-controller act), `manufacturing.planned_order.read` / `.planned_order.manage` (firm/convert/cancel). New files: `models/mrp.py`, `schemas/mrp.py`, `service/mrp.py` (the engine + job handler) + `service/mrp_capacity.py` (the rough check) + `service/planned_orders.py` (lifecycle + reads), `mrp_router.py` (sibling, mounted), `procurement/handlers.py` (NEW — the planned-buy subscriber); `sales/queries.open_demand_item_ids` added. Idempotent run-submit + convert (D-013); paginated reads ≤3-query budget (D-014/PERFORMANCE §6).
- **D-050 Quality opens (PLAN 9.1, the sixth business module): the DELIBERATELY SMALL QM core — goods-receipt inspection FLAG → inspection LOT → accept/reject usage DECISION with a stock DISPOSITION (SCRAP write-off / BLOCK transfer) — created event-driven off procurement's `GoodsReceiptPosted`, dispositioned event-driven through inventory; accepted stock needs NO move; no cycle:** Opens Phase 9 with the minimal QM the parity doc scopes; everything else QM (inspection plans, master inspection characteristics, results recording, usage-decision code catalogs, quality notifications/CAPA, certificates, non-GR lot origins) is OUT of v1 (recorded in `docs/research/s4hana-parity.md`). ONE table (migration `0035_quality_inspection`, down_revision 0034, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG, applies on PG to head): `qm_inspection_lots` (`InspectionLot`, DocumentMixin header + gapless `QL-` number claimed at creation — a permanent document at creation, the orders/receipts branch). **(a) Event-driven lot creation off the goods receipt (the §5 crux).** Quality must NOT import procurement's service, so it SUBSCRIBES to procurement's `GoodsReceiptPosted` (the sanctioned declarative-event import, D-011): `quality/handlers.create_inspection_lots_for_receipt` creates ONE OPEN `InspectionLot` per GR line flagged `requires_inspection=True` (an unflagged line creates none), snapshotting the line's opaque item/bin/quantity, in the SAME transaction as the GR post. `GoodsReceiptPosted` now has TWO subscribers on one event key: inventory's `receive_goods_receipt_moves` (registered FIRST — creates the RECEIPT moves + the lot/serial master instance) then quality's lot creator (registered SECOND so it can resolve the GR line's lot/serial CODE to the instance id inventory just created, via `inventory/queries.lot_id_for_code`/`serial_id_for_code`). The GR→lot link is docflow (GR document → `inspected_by` → lot document), not a cross-module FK. **(b) The usage decision (accept/reject).** `decide(lot_id, accepted_quantity, rejected_quantity, disposition?)` (permission `quality.inspection.decide`, DISTINCT from `.manage` because it moves stock + posts a journal — the journal.post precedent) validates `accepted + rejected == lot quantity` (v1: a single decision covers the whole lot, else 422 `quality.decision_quantity_mismatch`). The lot lands ACCEPTED (rejected == 0) or REJECTED (rejected > 0) with both quantities recorded — any rejection (including a partial accept/reject) lands REJECTED; NO `PARTIALLY_ACCEPTED` status in v1 (documented). Idempotent (D-013): a decided lot is terminal (re-decide → 409 `quality.lot_not_open`). `cancel_lot` discards an OPEN lot (moves no stock). **(c) The disposition stock effect goes through the EVENT BUS (§5).** A reject PUBLISHES `quality/events.InspectionDispositioned` → `inventory/handlers.disposition_rejected_stock` creates the move via inventory's OWN service: **SCRAP** = an ADJUSTMENT-out from the receiving bin (`from_bin` set, no `to_bin`) — the costing engine offsets an ADJUSTMENT-down to the price-difference / inventory-adjustment account (the write-off), so Dr inventory-adjustment / Cr Inventory at book value, total on-hand DROPS; **BLOCK** = a TRANSFER from the receiving bin to the tenant's designated blocked/QI bin (`blocked_bin_id` on the decide request, validated via `inventory/queries.bin_exists`) — value-neutral (a within-warehouse transfer publishes no costing journal), total on-hand UNCHANGED but the stock leaves the usable bin. **RETURN_TO_VENDOR** is DECLARED in the `RejectDisposition` enum but NOT implemented in v1 (it needs the vendor-return chain — parity-doc "later"; choosing it → 422 `quality.disposition_not_implemented`). The lot → `dispositioned_by` → move docflow edge is written by inventory's handler (it owns the move). Atomic (D-011): a SCRAP write-off into a CLOSED period trips the move's journal period trigger and rolls the WHOLE decision back (the lot stays OPEN — asserted via FRESH reads, issue #53). **(d) ACCEPTED needs NO stock move (the rationale).** A v1 inspection lot does NOT hold stock in a separate quality-inspection bucket — the received stock is already on hand and usable the moment the GR posts. So an ACCEPT records the outcome and moves nothing; only a REJECT moves stock (SCRAP out / BLOCK aside). (The documented v1 simplification vs S/4HANA's quality-inspection stock type.) **(e) Cross-module directions + no cycle (§5).** quality → procurement/events (subscribe to `GoodsReceiptPosted`); quality → inventory/queries (downward: `bin_exists`, `lot_id_for_code`, `serial_id_for_code`); quality PUBLISHES `InspectionDispositioned` → inventory/handlers moves the stock. Procurement and inventory are OLDER modules and import NOTHING from quality (grep-confirmed: quality is the newest module), so all quality cross-module imports are ONE-directional — NO bidirectional cycle (STRUCTURE §5 bans only bidirectional query imports). `inventory/handlers.py` importing `quality.events`/`quality.constants` is the sanctioned "upward" handler import (D-011: events.py/constants.py are declarative, no behaviour — the existing inventory/handlers importing sales.events + manufacturing.events precedent). Quality imports NO procurement/inventory/finance SERVICE (grep-verified). **(f) Permissions + structure.** `quality.inspection.read` / `.manage` (cancel an OPEN lot) / `.decide` (the accept/reject usage decision — distinct). NO `.create` key: lots come from the GR handler (already authorised as a procurement action). Files: `constants.py`, `models.py` (one table), `schemas.py`, `events.py`, `handlers.py`, `service.py`, `queries.py` (the only file a later module imports — `get_inspection_lot`/`open_lots_for_item`/`lots_for_goods_receipt`), `router.py` (list + point read + `/decide` + `/cancel` under `/api/v1/quality`, NO create endpoint). Mounted after manufacturing in main.py (the D-011 handler order). `inventory/handlers.disposition_rejected_stock` added + both subscriptions registered in `app.main.register_event_handlers` (quality's GR subscriber AFTER inventory's; the disposition subscriber on `InspectionDispositioned`). Idempotent decide (D-013); paginated list ≤3-query budget (D-014/PERFORMANCE §6). **Phase 9 / Quality is OPEN; 9.2 adds maintenance (equipment + maintenance orders) as a separate module.**
- **D-051 Maintenance opens + closes Phase 9 (PLAN 9.2, the SEVENTH business module): the DELIBERATELY SMALL PM core — an EQUIPMENT register + CORRECTIVE (ad-hoc) and PREVENTIVE (interval-plan-generated) maintenance orders; an interval PLAN's generation RUN with the generate-one-advance-to-next-future overdue rule; v1 cost is RECORD-ONLY (no GL posting); no cross-module event:** Opens the Plant-Maintenance half of the parity QM/PM area and CLOSES Phase 9 (Quality & Maintenance). Everything else PM (functional-location hierarchies, maintenance notifications, measurement points/counters, counter/strategy-based scheduling, task lists, spare-part stock issue) is OUT of v1 (recorded in `docs/research/s4hana-parity.md`). THREE tables (migration `0036_maintenance`, down_revision 0035, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG, applies on PG to head): `pm_equipment` (`Equipment`), `pm_maintenance_plans` (`MaintenancePlan`, DocumentMixin), `pm_maintenance_orders` (`MaintenanceOrder`, DocumentMixin + gapless number). **(a) The three entities.** `Equipment` is a FLAT register keyed by a USER-SUPPLIED `code` unique per tenant (the work-centre master precedent — no auto-number); `status` ACTIVE|INACTIVE|RETIRED; `location` is a FREE-TEXT label NOT a functional-location hierarchy (out of v1); `cost_center_id` is an OPAQUE finance cost-centre id (nullable, validated via `finance/queries.cost_center_exists` when set — D-029, no cross-module FK) for cost attribution. `MaintenanceOrder` is a posted document claiming a gapless **`MNT-`** number at creation — prefix **`MNT-`, NOT `MO-`** (already manufacturing's production-order prefix; `WO-` was the alternative, `MNT-` reads unambiguously); `order_type` CORRECTIVE (ad-hoc) | PREVENTIVE (plan-generated, carries `maintenance_plan_id` + a plan→order docflow edge); lifecycle DRAFT → SCHEDULED → IN_PROGRESS → COMPLETED (terminal; records `actual_cost` + `completed_date`) plus → CANCELLED. `MaintenancePlan` is keyed by a user code, mixes in DocumentMixin so it is the docflow PREDECESSOR of the orders it generates (the only master here that is a docflow source); `interval_value × interval_unit` (DAYS|WEEKS|MONTHS, time-based only — counter/strategy intervals need measurement points, out of v1); `next_due_date` + `last_generated_date` drive generation; status ACTIVE|INACTIVE. **(b) The interval model + generation RUN.** First `next_due_date = start_date (or today) + one interval`; MONTHS uses calendar arithmetic clamped to the month's last valid day (Jan 31 + 1mo = Feb 28, the `finance/periods._add_months` pattern re-declared module-locally — a private service helper is not imported across modules). Changing a plan's interval applies to the NEXT advance — it does NOT retro-shift the current `next_due_date`. `run_preventive_maintenance(tenant, as_of)` (endpoint `POST /maintenance-plans/run-preventive?as_of=`, default today): a SET-BASED scan (`queries.due_plans` — ONE query, PERFORMANCE §2, no per-plan N+1 in the scan) of ACTIVE plans with `next_due_date <= as_of`; per due plan creates ONE PREVENTIVE order (SCHEDULED, scheduled at the plan's CURRENT `next_due_date`), writes the plan→`generates`→order docflow edge, sets `last_generated_date` = that due date, and advances `next_due_date`. **(c) The OVERDUE-ADVANCE rule (the chosen policy).** A plan overdue by MULTIPLE intervals generates exactly ONE order and advances `next_due_date` by whole intervals until it is STRICTLY AFTER `as_of` (**generate-one-advance-to-next-future** — the simpler of the two options; the alternative "one order per missed interval" was rejected to avoid order spam). **(d) Idempotency (two ways).** The run endpoint is D-013 idempotent (an `Idempotency-Key` replay returns the captured result), AND it is NATURALLY idempotent — after a plan generates, its `next_due_date` is strictly `> as_of`, so a same-day re-run finds nothing due and generates nothing. **(e) v1 cost handling = RECORD-ONLY (the chosen policy, NOT a GL posting).** Completing an order records `actual_cost` ON THE ORDER ROW with NO GL posting — the parity scope is "equipment register + corrective/preventive orders", NOT full work-order costing; a maintenance-expense journal (Dr maintenance-expense / Cr a clearing or accrual, optionally through the equipment's cost centre) is a DOCUMENTED LATER, to be wired via an event the way other modules post through the bus. So there is NO `events.py` / `handlers.py` in the module (an empty event file would be a dead file — STRUCTURE §8.3); maintenance publishes/subscribes to NO cross-module event in v1. **(f) Inline vs job (D-032).** The run executes INLINE at v1 scale — the 6.4 reorder-scan precedent (a tenant has tens, not thousands, of plans; the scan is one query + a bounded per-plan order create). A background job (the D-032 runner) is the documented later if plan counts grow. **(g) Permissions + structure.** `maintenance.equipment.read/.manage`; `maintenance.order.read/.manage/.complete` (`.complete` distinct because it records the actual cost — the value-bearing transition, the journal.post precedent); `maintenance.plan.read/.manage/.run` (`.run` distinct for segregation of duties — running the generator vs authoring a plan). Files: `constants.py`, `models.py` (three tables, one concern, under the 400-line cap), `schemas.py`, `service/` package (`equipment.py`, `orders.py`, `plans.py` — each <400 lines, the manufacturing precedent), `queries.py` (the only file a later module imports — `get_equipment`/`equipment_exists`/`get_maintenance_order`/`orders_for_equipment`/`get_maintenance_plan`/`due_plans`), `router.py` + `order_router.py` + `plan_router.py` (ONE surface at `/api/v1/maintenance`, sub-routers mounted in `router.py` — the manufacturing bom_router precedent; `/run-preventive` declared before `/{plan_id}` so the literal path matches first). Equipment + plan lists carry the D-035 conditional-GET ETag (reference data); idempotent order create + complete + run (D-013); paginated lists ≤3-query budget (D-014/PERFORMANCE §6). The `maintenance_plan_id` composite FK is spelled with an explicit shortened name (`fk_pm_mnt_orders_maintenance_plan_id_pm_maintenance_plans`) because the D-022 auto-convention would render 65 chars > the PG 63-char cap. **(h) Cross-module + no cycle (§5).** The only downward read is `finance/queries.cost_center_exists`; finance is older and imports nothing from maintenance, so the import is one-directional (no cycle). Mounted after quality in main.py (the deterministic module import order). **Phase 9 / Quality & Maintenance is COMPLETE.**
- **D-052 HR opens Phase 10 (PLAN 10.1, the EIGHTH business module): the DELIBERATELY SMALL HCM core — the EMPLOYEE master with MASKED compensation/PII, DEPARTMENTS, POSITIONS, and the reporting ORG CHART; the FIRST real use of the D-009 field-level read-masking serializer; the department↔manager circular reference resolved by a plain-uuid soft link; hierarchy + reporting cycle guards; no cross-module event:** Opens Phase 10 (Human Resources) with the minimal HCM the parity doc scopes. Everything else HCM (date-effective history + formal hire/transfer/terminate actions, leave/absence, timesheet/CATS, jurisdiction-compliant payroll, pay-grade structures + comp-review cycles, talent/recruiting/benefits, ESS/MSS) is OUT of v1 (leave/time/payroll-lite arrive in later 10.x plans; talent/benefits stay out — recorded in `docs/research/s4hana-parity.md`). THREE tables (migration `0037_hr`, down_revision 0036, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG, applies on PG to head): `hr_departments` (`Department`), `hr_positions` (`Position`), `hr_employees` (`Employee`). All three are MASTERS keyed by a USER-SUPPLIED `code` unique per tenant (the item-code master precedent — no gapless number, no DocumentMixin: HR masters are not posted documents). **(a) THE D-009 MASKING (the headline).** The employee's compensation/PII columns store REAL values but are READ-MASKED in the schema: `EmployeeRead` declares `base_salary` (money), `currency_code`, `national_id`, `tax_id`, `date_of_birth`, `bank_account` as `Masked(tp, "hr.employee.read_compensation")`. At serialization the `Masked` WrapSerializer (core/rbac.py) reads the `current_permissions` ContextVar (set per request by `get_current_user`) and emits the real value only if the key is present, else `None`. Masking is PER REQUEST, not stored (one row serializes pay for a compensation-holder and `None` for everyone else, same process, no extra query — the ≤3-query list budget holds); name/code/department/status etc. are always visible (field-level, not row-level); outside a request the ContextVar defaults empty so masking FAILS CLOSED. The columns are in `Employee.__audit_exclude__` so raw pay/PII never lands in an audit diff (the password_hash precedent; the audit-viewer masking with the same machinery is a documented later). **(b) THE COMPENSATION-WRITE PATH (D-009 write-side convention).** The masked fields are EXCLUDED from `EmployeeUpdate` entirely (so a partial update can never silently null pay). They are written by exactly two paths: `EmployeeCreate` (initial values — the `POST /employees` endpoint is guarded by BOTH `hr.employee.manage` AND `hr.employee.read_compensation`, so a manage-only user cannot seed pay), and the dedicated `PATCH /employees/{id}/compensation` (`EmployeeCompensationUpdate`, guarded by `hr.employee.read_compensation`, `exclude_unset` so salary updates without touching PII). A holder of `.manage` but NOT `.read_compensation` edits non-pay fields, gets MASKED reads, and is 403 on the compensation endpoint. **(c) THE DEPARTMENT↔MANAGER CIRCULAR REFERENCE (the chosen resolution).** A department has a manager employee; an employee belongs to a department — a hard composite FK each way is a circular table dependency. Resolution: `Department.manager_employee_id` is a PLAIN nullable `Uuid` (opaque, validated in the service against `hr_employees`), NOT a composite FK; the employee→department side IS a real composite FK. So the DDL dependency is one-directional (employee → department) and the manager link is a service-validated soft reference (the OTHER option — both sides nullable composite FK + deferrable — was rejected as heavier; the plain-uuid soft link is the simplest that fully works). The employee `manager_id` reporting line and the department `parent_id` hierarchy ARE intra-table SELF composite FKs (not circular). **(d) THE CYCLE GUARDS.** Both hierarchies are guarded in the service before any write by walking the would-be ancestor/reporting chain UP and rejecting a self-reference or a loop back to the edited node, bounded by `MAX_HIERARCHY_DEPTH = 64`: `_assert_no_parent_cycle` (422 `hr.department_cycle`), `_assert_no_manager_cycle` (422 `hr.manager_cycle`). The org-chart BUILD (`service/org_chart.py`) loads every tenant employee in ONE query (`queries.org_chart_for` — no per-node N+1, PERFORMANCE §6) and assembles the nested tree in memory, also bounded by the depth cap + a `visited` set; the chart carries name/code/title only (no compensation), so any `hr.employee.read` holder may view it (endpoint `GET /employees/org-chart?root_employee_id=`, declared BEFORE `/employees/{employee_id}` so the literal path matches first). **(e) OPAQUE cross-module/soft ids (D-029).** `Department.cost_center_id` is an opaque finance cost-centre id (nullable, validated via `finance/queries.cost_center_exists`, no FK); `Employee.user_id` is an opaque core users id (nullable, validated via a `core_users` probe — an employee MAY also be a system user, never a hard FK to core_users). **(f) Permissions + structure.** `hr.employee.read` / `.manage` / `.read_compensation` (the sensitive gate); `hr.department.read` / `.manage`; `hr.position.read` / `.manage`. Files: `constants.py`, `models.py` (three tables, one concern, under the 400-line cap), `schemas.py`, `service/` package (`departments.py`, `positions.py`, `employees.py`, `org_chart.py` — each <400 lines, the maintenance precedent), `queries.py` (the only file a later module imports — `get_employee`/`employee_exists`/`get_department`/`department_employees`/`employee_manager_chain`/`org_chart_for`), `router.py` + `position_router.py` + `employee_router.py` (ONE surface at `/api/v1/hr`, sub-routers mounted in `router.py` — the maintenance precedent). Department + position lists carry the D-035 conditional-GET ETag (reference data); paginated lists ≤3-query budget (D-014/PERFORMANCE §6). There is NO `events.py` / `handlers.py` (an empty event file would be a dead file — STRUCTURE §8.3): HR masters drive no cross-module effect in v1; payroll (10.4) will post a journal through the bus. **(g) Cross-module + no cycle (§5).** The only downward reads are `finance/queries.cost_center_exists` + a core `core_users` probe; finance and core are older and import nothing from HR, so the imports are one-directional (no cycle). Mounted after maintenance in main.py (the deterministic module import order). **Phase 10 / Human Resources is OPEN.**
- **D-053 HR leave (PLAN 10.2): leave TYPES with periodic ACCRUAL + a per-employee-per-type running BALANCE + a leave REQUEST whose APPROVE decrements the balance and whose cancel-of-APPROVED restores it; the accrual run is period-keyed idempotent; insufficient balance blocks approval:** The second HCM plan (s4hana-parity §HCM "Leave and absence management" = Full) — leave types, accruals, and the request approval flow, extending the hr module. THREE tables (migration `0038_hr_leave`, down_revision 0037, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG, applies on PG to head): `hr_leave_types` (`LeaveType`), `hr_leave_balances` (`LeaveBalance`), `hr_leave_requests` (`LeaveRequest`). The hr `models.py` became a PACKAGE (`models/org.py` = the 10.1 masters moved verbatim + `models/leave.py`) and `service/` gained `leave.py` (request lifecycle) + `leave_config.py` (type CRUD + balance reads) + `leave_accrual.py` (the run) — each <400 lines (the cap forced the split; behaviour-preserving move). **(a) THE LEAVE MODEL.** `LeaveType` is a MASTER keyed by a user `code` unique per tenant (no number, no DocumentMixin): `accrual_frequency` (MONTHLY|ANNUAL), `accrual_amount` (QuantityType — days/period, so 1.67/month is exact, D-015, CHECK ≥0), `max_balance` (QuantityType nullable — accrual cap), `unit` (LeaveUnit = DAYS in v1; half-day is expressible as a fractional `days` 0.5 with no new unit — a dedicated HALF_DAY unit + shift math is the documented later), `is_paid`, `is_active`. `LeaveBalance` is the RUNNING balance per (employee, type) — composite tenant FKs, UNIQUE(tenant, employee_id, leave_type_id): `balance_days` (available now), `accrued_to_date`, `taken_to_date` (all QuantityType, CHECK ≥0 on the two totals), and `last_accrual_period` (the accrual idempotency guard). `LeaveRequest` is the request DOCUMENT — composite tenant FKs, claims a gapless **`LV-`** `request_number` AT CREATION (the procurement-requisition claim-at-create precedent, D-040; `ensure_sequence`+`claim_number`, year_reset) but is NOT a docflow document (no DocumentMixin — a leave request has no successor doc in v1): `start_date`/`end_date` (Date, stored for reference), `days` (QuantityType, caller-supplied, CHECK >0 — calendar/business-day computation FROM the dates is the documented later, the simplest-that-works choice), `status`, `reason`/`notes`, `approved_by` (OPAQUE core users id, nullable, no FK — the Employee.user_id precedent), `decided_at`. Index (tenant, employee_id, status). **(b) THE ACCRUAL RUN (the maintenance preventive-generation analogue, set-based — PERFORMANCE §2).** `accrue_leave(session, tenant, as_of, frequency)` (endpoint `POST /leave-balances/accrue?frequency=&as_of=`, default today) derives the PERIOD KEY (YYYY-MM for MONTHLY, YYYY for ANNUAL), then for every (ACTIVE employee × ACTIVE leave type of that frequency) grants `accrual_amount` to the pair's balance — TWO set-based queries (active employees, active types) + one for the existing balances feed an in-memory cross-product (no per-pair N+1). **THE IDEMPOTENCY GUARD:** each balance carries `last_accrual_period`; the run grants a pair only when its `last_accrual_period` != the run period, then stamps it — so a same-period re-run finds every balance already stamped and grants 0 (the generate-once-per-period guarantee, the maintenance next-due-date idempotency analogue). The endpoint is ALSO D-013 idempotent (Idempotency-Key replay). **THE CAP:** when `max_balance` is set the grant is clamped so `balance_days` never exceeds the cap (0 when already at/over, partial to lift exactly to the cap); a capped balance is still stamped so it is not re-granted later; `accrued_to_date` records only what was actually granted. The run is gated by `hr.leave_type.manage` (a config-owner action — no separate `.run` key, since accrual is set-based over the type config). **(c) THE REQUEST LIFECYCLE (the procurement requisition submit→approve→reject precedent, D-040, WITHOUT a value threshold — every SUBMITTED request awaits an approver).** DRAFT (create; validates employee + type exist, days >0, end ≥ start) → SUBMITTED (submit) → APPROVED/REJECTED, or DRAFT/SUBMITTED/APPROVED → CANCELLED. **APPROVE DECREMENTS the balance** by `days` (raising `taken_to_date`); if the available balance < days → **422 `hr.insufficient_leave_balance`** (v1 BLOCKS negative balances — an allow-negative leave type is the documented later; a missing balance row counts as 0 available). REJECT has no balance effect. **CANCEL of an APPROVED request RESTORES the balance** (adds `days` back, lowers `taken_to_date`); cancel from DRAFT/SUBMITTED has no balance effect; a terminal (REJECTED/CANCELLED) request cannot be cancelled. Only a DRAFT is editable. Create/submit/approve/reject are D-013 idempotent. **(d) Permissions (D-009; the request vs approve split).** `hr.leave_type.read` / `.manage` (config + the accrual run); `hr.leave.read` (requests + balances); `hr.leave.request` (file/update/submit/cancel — an employee/manager files); `hr.leave.approve` (the DISTINCT approval authority — approve/reject, the value-bearing transition that moves the balance). A `.request` holder can submit but is 403 on approve. **(e) NO cross-module effect / no events (v1).** Leave is intra-HR; payroll (10.4) may READ leave balances/requests via `hr/queries` (`get_leave_request`/`leave_requests_for_employee`/`get_leave_balance`/`leave_balances_for_employee`), but leave drives no cross-module event in v1, so no `events.py`/`handlers.py` (STRUCTURE §8.3). **(f) Structure.** `leave_router.py` mounted into `router.py` (ONE surface at `/api/v1/hr`; `/leave-balances/accrue` and the literal leave-type/request routes declared correctly); leave-type list carries the D-035 conditional-GET ETag (config = reference data); paginated lists ≤3-query budget (D-014/PERFORMANCE §6).
- **D-054 HR time tracking (PLAN 10.3): a TIMESHEET header + TIME-ENTRY lines with project & cost-centre allocation; HEADER-level approval (submit→approve/reject, cancel reopens to draft); a MAINTAINED total_hours; `cost_center_id` VALIDATED against finance, `project_id` OPAQUE/UNVALIDATED until the projects module exists (Phase 11); allocation aggregates over APPROVED entries feed project costing + CO; no cross-module event:** The third HCM plan (s4hana-parity §HCM "Time recording with account assignment (CATS-style timesheet)" = Full), extending the hr module. TWO tables (migration `0039_hr_time`, down_revision 0038, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG, applies on PG to head): `hr_timesheets` (`Timesheet`), `hr_time_entries` (`TimeEntry`). `models/time.py` joins the hr models package; `service/` gains `timesheets.py` (header + entry CRUD), `timesheet_lifecycle.py` (the approval transitions), `time_reads.py` (list + pagination), `time_allocation.py` (the aggregates), and the timesheet schemas live in a sibling `time_schemas.py` — each <400 lines (the cap forced the splits; the leave-module precedent). **(a) THE TIMESHEET + ENTRY MODEL (the clean SAP-like model: a header groups time entries over a period and is approved as a whole; chosen over per-row TimeEntry status).** `Timesheet` is the period HEADER — composite tenant FK to `hr_employees`; claims a gapless **`TS-`** `timesheet_number` AT CREATION (the leave-request claim-at-create precedent, D-040/D-053; `ensure_sequence`+`claim_number`, year_reset) but is NOT a docflow document (no DocumentMixin — a timesheet has no successor document in v1; payroll 10.4 + project costing 11 READ its approved entries via `hr/queries`, not via docflow): `period_start`/`period_end` (Date, CHECK `period_end >= period_start`), `status` (`TimesheetStatus` DRAFT|SUBMITTED|APPROVED|REJECTED), `total_hours` (QuantityType, CHECK ≥0 — the MAINTAINED sum of the entry hours, kept in step by the service on every line add/update/remove, a denormalized running total the stock-balance precedent), `submitted_at`/`approved_at`/`approved_by` (OPAQUE core users id, nullable, no FK — the leave `approved_by` precedent), `notes`. UNIQUE(tenant, employee_id, period_start) — one timesheet per employee per period; UNIQUE(tenant, timesheet_number); index (tenant, employee_id, status). `TimeEntry` is a LINE — composite tenant FK to `hr_timesheets`, NOT separately audited (the header is the audited unit — the journal-line precedent): `entry_date` (Date, validated WITHIN the header period), `hours` (QuantityType, CHECK >0), `task_description`, `is_billable` (bool default false). Entries can repeat per day/project, so there is NO unique constraint on the line — just index (tenant, timesheet_id), plus (tenant, cost_center_id) + (tenant, project_id) for the allocation aggregates. **(b) THE ALLOCATION — `cost_center_id` VALIDATED, `project_id` OPAQUE UNTIL PHASE 11 (the headline decision).** A time entry's `cost_center_id` is an OPAQUE finance cost-centre id (nullable) VALIDATED via `finance/queries.cost_center_exists` when set (D-029, no cross-module FK) — a bad id is a 422 `hr.cost_center_not_found`. A time entry's `project_id` is a NULLABLE OPAQUE `Uuid` STORED AS-IS and NOT validated in v1: the PROJECTS module is **Phase 11 (NOT YET BUILT)**, so there is no table to validate against and **NO forward dependency on projects is created**. The validation hook wires up when `projects/queries` exists in Phase 11 — `add_time_entry`/`update_time_entry` will then call a projects existence probe exactly as they call `cost_center_exists` today, and `hr/queries.approved_hours_for_project` (already shipped) is the per-project hook Phase-11 project costing reads. **(c) THE APPROVAL FLOW (HEADER-level, the leave-request submit→approve/reject precedent, D-053, applied to the SAP CATS model).** Lines are editable ONLY while the header is a DRAFT. DRAFT (create; employee exists, `end >= start`, TS- number claimed) → SUBMITTED (submit; stamps `submitted_at`, lines frozen) → APPROVED/REJECTED (record `approved_by` + `approved_at`). A SUBMITTED timesheet → **cancel = reopen to DRAFT** so the filer can edit and re-submit; APPROVED/REJECTED is terminal (cannot reopen). **APPROVED is the VALUE-BEARING state**: only entries of APPROVED timesheets feed the allocation aggregates. Create/submit/approve/reject are D-013 idempotent. **(d) THE ALLOCATION AGGREGATES (the cost/project allocation deliverable).** `hours_by_cost_center` / `hours_by_project` are SET-BASED GROUP-BY SUMs over APPROVED time entries ONLY (DRAFT/SUBMITTED/REJECTED time is provisional and never costed), each ONE query bounded by an entry-date range (PERFORMANCE §2), the unallocated bucket keyed `None`. They back `GET /timesheets/allocation?by=cost_center|project&from=&to=` and feed project costing (Phase 11) + CO reporting. The `hr/queries` companions `approved_hours_for_project(project_id)` / `approved_hours_for_cost_center(...)` are the per-dimension hooks a later module reads. **(e) Permissions (D-009; the manage vs approve split, the leave precedent).** `hr.timesheet.read` (timesheets + entries + the allocation report); `hr.timesheet.manage` (create / edit draft entries / submit / cancel — the filer); `hr.timesheet.approve` (the DISTINCT approval authority — approve/reject). A `.manage` holder can submit but is 403 on approve. **(f) NO cross-module effect / no events (v1).** Time is intra-HR; payroll (10.4) + project costing (11) READ approved time via `hr/queries`, but time drives no cross-module event in v1, so no `events.py`/`handlers.py` (STRUCTURE §8.3). **(g) Structure.** `timesheet_router.py` mounted into `router.py` (ONE surface at `/api/v1/hr`: timesheets CRUD + submit/approve/reject/cancel; nested `GET/POST /timesheets/{id}/time-entries` + PATCH/DELETE for a line; `/timesheets/allocation` declared BEFORE `/{timesheet_id}` so the literal path wins); paginated lists ≤3-query budget (D-014/PERFORMANCE §6). **Later:** validate `project_id` once projects exists (Phase 11); work-schedule rules + overtime/premium evaluation; per-entry approval; a labour-cost / maintenance-expense journal from approved time. **Phase 10 PLAN 10.3 is COMPLETE.**
- **D-055 HR payroll (PLAN 10.4, the FINAL Phase 10 plan — completes HR): a SIMPLISTIC, NON-JURISDICTION-COMPLIANT flat-tax gross→net payroll RUN that posts ONE consolidated finance journal via the EVENT BUS (HR's FIRST cross-module event); salary expense carries the cost-centre dimension; `base_salary` = period gross with no proration; closed-period rollback; idempotent post:** The fourth + final HCM plan (s4hana-parity §HCM "Payroll (localized gross-to-net)" = Partial / "Payroll posting to finance (FI/CO)" = Full), extending hr. **(a) THE NON-COMPLIANCE FLAG (binding scope, flagged prominently in `docs/modules/hr.md`).** v1 payroll is a SIMPLISTIC gross→net: a SINGLE flat withholding rate and nothing else — NO tax brackets, NO social security / pension / Medicare, NO employer-side taxes, NO deductions, NO retro accounting, NO payment files, NO statutory reporting. The parity doc explicitly scopes payroll to this reduced form. Suitable for demos + the payroll-to-GL integration; NEVER for paying real employees. **(b) THE GROSS→NET FLAT-TAX MODEL.** Per covered ACTIVE employee with a `base_salary`: `gross = base_salary` (**taken AS the period gross with NO proration** — the simplest v1 assumption: base_salary IS the per-period/monthly gross; mid-period hire/term not prorated), `tax = quantize(gross × tax_rate_percent / 100)` (the FLAT rate, supplied PER RUN, defaulting to per-tenant `DEFAULT_PAYROLL_TAX_RATE_PERCENT = 20`), `net = gross − tax`. So **`gross == tax + net` per line AND at the header totals** — THE balancing invariant the journal relies on. Employees with NO `base_salary` are SKIPPED (no line, not counted); a run matching no salaried employee is a 422 `hr.payroll_no_payable_employees`. **(c) TWO tables** (migration `0040_hr_payroll`, down_revision 0039, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogenerate drift on SQLite + PG, applies on PG to head): `hr_payroll_runs` (`PayrollRun`, the period HEADER — mixes in `DocumentMixin`: it IS a posted document, registers in core_documents at creation + claims a gapless **`PAY-`** `run_number` AT POSTING, NULLABLE column with a partial-unique (tenant, run_number) index so an abandoned/cancelled draft burns no number — the journal-entry `entry_number` precedent; `status` DRAFT|POSTED|CANCELLED, `period_start`/`period_end`/`pay_date` Date, `tax_rate_percent` + `total_gross`/`total_tax`/`total_net` MoneyType maintained at creation, `employee_count`, `currency_code`, `journal_entry_id` nullable opaque finance id no FK, `posted_at`, CHECKs period-order + non-negative totals/rate/count) and `hr_payroll_run_lines` (`PayrollRunLine` — composite tenant FKs to `hr_payroll_runs` + `hr_employees`, UNIQUE(tenant, run, employee); `gross_amount`/`tax_amount`/`net_amount` MoneyType CHECK ≥0, `cost_center_id` nullable opaque finance id no FK = the employee's department cost centre for the salary-expense allocation; NOT separately audited — the header is the audited unit, the journal-line precedent). `models/payroll.py` joins the hr models package. **(d) THE EVENT-BUS POSTING (§5-clean cross-module write — HR's FIRST cross-module event; the 6.4 invoice-match→AP-bill / 7.4 sales-billing→AR-invoice precedent).** `post_payroll_run` claims the PAY- number, flips the run POSTED, and PUBLISHES `PayrollPosted` (new `hr/events.py` — HR's first event file) carrying the run totals + the three RESOLVED posting-account ids (read from `finance/queries`) + the per-cost-centre salary-expense allocation. `finance/handlers.create_payroll_journal` SUBSCRIBES and posts ONE consolidated journal (document_type `PAYROLL`) in the SAME transaction via the finance posting service (`create_draft_entry`+`post_entry`, NEVER raw inserts): **Dr salary-expense SPLIT per cost centre at total gross** (each Dr line carries the cost-centre dimension so CO cost-centre reports include labour; a None bucket posts undimensioned), **Cr payroll-tax-payable at total tax**, **Cr wages-payable at total net** — balanced because gross = tax + net. The handler links **payroll-run → 'posts' → journal** (docflow) and sets the run's `journal_entry_id` (importing `PayrollRun` LAZILY in the function body so finance — below hr — never depends on hr at module load). **§5 verified: HR imports only `finance/queries` + the `PayrollPosted` event — NEVER `finance/service`.** **(e) Posting accounts (per-tenant defaults, D-019).** Three new purposes added in finance: `salary_expense` (EXPENSE), `wages_payable` (LIABILITY, net pay owed — "net_pay_clearing" in the parity doc), `payroll_tax_payable` (LIABILITY, withheld tax owed); resolved via new `finance/queries` helpers `salary_expense_account`/`wages_payable_account`/`payroll_tax_payable_account`; a tenant MUST map all three or post is a 422 `finance.posting_default_unmapped`. **(f) ATOMICITY + lifecycle.** Closed-period rollback: a `pay_date` in a closed fiscal period trips the journal's period trigger inside the same transaction, rolling the WHOLE post back — run stays DRAFT, no number, no journal (the COGS / production-finish atomicity precedent; tested per issue #53 with fresh scalar reads). Re-posting a POSTED run is a conflict `hr.payroll_run_not_draft` (a run posts ONCE; correct a posted run by REVERSING its journal in finance — the D-017 reversal-only principle). cancel is DRAFT-only → CANCELLED. create/post are D-013 idempotent. **(g) Permissions (D-009; the manage vs post split, the leave/timesheet `.approve` precedent).** `hr.payroll.read` (runs + lines); `hr.payroll.manage` (create + cancel — the authoring authority); `hr.payroll.post` (the DISTINCT GL-posting authority). A `.manage` holder computes a run but is 403 on post. **(h) Structure (each <400 lines).** `constants.py` additions (PayrollRunStatus, the three perms, PAY- sequence + doc type + 'posts' docflow link + `hr.payroll.posted` event key + the flat-tax-default constant); `payroll_schemas.py` (sibling, the cap); `service/payroll.py` (create + reads + cancel) + `service/payroll_post.py` (the publish-the-event post flow); `events.py`; `models/payroll.py`; `payroll_router.py` mounted into `router.py` (ONE surface at `/api/v1/hr`: CRUD-ish create/list/detail-with-lines/post/cancel, paginated ≤3-query budget). The finance `handlers.py` (489 lines after this) and `queries.py` (427) crossed the 400-line cap and were SPLIT into PACKAGES per STRUCTURE §8.4 (the models/+service/ precedent), behaviour-preserving + re-exported from `__init__.py`: `finance/handlers/` (inventory_cogs / procure_to_pay / order_to_cash / production / payroll / _shared) and `finance/queries/` (`__init__` + `posting_accounts.py` for the 10 posting-default resolvers). **Later:** configurable wage-type→account mapping, accrual postings, employer contributions, deductions, retro calculation, proration, certified country compliance packs. **Phase 10 PLAN 10.4 is COMPLETE — Phase 10 / Human Resources is COMPLETE.**
- **D-056 Projects / PS-lite (PLAN 11.1, the SINGLE Phase 11 plan — completes Projects): PROJECTS + a WBS-element TREE as COSTING OBJECTS; the WBS-element id is the OPAQUE project dimension a journal line / timesheet TAGS when work/purchases are "posted to a WBS"; the PROJECT COST REPORT is a BOUNDED finance-journal PROJECTION by that dimension + approved-timesheet hours + a simple budget variance; projects posts NOTHING and reads finance/hr/sales queries DOWNWARD (one-directional, no cycle):** The eighth business module (s4hana-parity §PS "Project definitions and WBS structures" = Full / "Time recording to projects" = Full / "Procurement and expense postings to projects" = Partial / "Project cost reporting" = Partial), sitting at the TOP of the dependency order. **(a) THE OPAQUE-WBS-DIMENSION MODEL (the load-bearing idea, D-017/D-029).** A `WbsElement.id` IS the costing object: when work/purchases are "posted to a WBS", a finance JOURNAL LINE (`fin_journal_lines.project_id`) or a HR TIME ENTRY (`hr_time_entries.project_id`) simply carries that WBS-element id as its OPAQUE project dimension. **Finance and HR do NOT validate that id against projects** — it stays an opaque tag so FINANCE STAYS AT THE BOTTOM (D-029): finance/hr never import projects. **Projects posts NOTHING itself** — it owns the WBS masters + the report and reads the journal projection + the timesheet aggregate downward. So "purchases postable to a WBS" required NO new column and NO schema change: the journal line has carried a `project_id` dimension since D-017 / migration 0009, `JournalLineCreate` already exposes it, and the journal service already copies it onto the persisted line; HR timesheets already allocate to an opaque `project_id` (D-054). `projects/queries.wbs_element_exists` is exposed so a FUTURE projects-owned posting gate could validate a WBS dimension — but today no one calls it (the tag is opaque). **(b) TWO masters** (migration `0041_projects`, down_revision 0040, **NO trigger-bearing alters**, reversible, portable both engines, ≤63-char ids, ZERO autogenerate drift on SQLite + PG, applies on PG to head; it does NOT add a journal-line project dimension — that column already exists): `ps_projects` (`Project` — user-supplied `code` UNIQUE per tenant [no auto-number, the master-data precedent]; `status` PLANNING|ACTIVE|CLOSED|CANCELLED [INFORMATIONAL — does NOT gate posting, since finance/hr never consult it]; `customer_id` OPAQUE sales id [nullable, validated via `sales/queries.customer_exists` when set]; `cost_center_id` OPAQUE finance id [nullable, validated via `finance/queries.cost_center_exists`]; `start_date`/`end_date` Date; `budget_amount` MoneyType nullable = a SIMPLE budget figure for the report's variance, NOT budget control / availability check [no posting-time funds check in v1]; `is_active`; (tenant,status) index + UNIQUE(tenant,code)) and `ps_wbs_elements` (`WbsElement` — the costing-object tree: composite tenant FK to `ps_projects`; `code` UNIQUE per (tenant, PROJECT) [not per tenant — same code may recur under a different project, the account-group-within-chart precedent]; `parent_id` nullable SELF composite tenant FK = the WBS tree, CYCLE-GUARDED in the service [walks the ancestor chain bounded by `MAX_WBS_DEPTH`, also rejects a parent in a DIFFERENT project, the department-hierarchy precedent D-052]; `status` OPEN|CLOSED [ADVISORY in v1 — finance/hr tag opaquely and do NOT check WBS status before posting; a posting-time block would need a projects-owned gate, a later]; `is_billable`; `budget_amount` MoneyType nullable per-WBS budget; (tenant,project_id,status) tree index + (tenant,parent_id) parent-walk index + UNIQUE(tenant,project_id,code)). Single `models.py` (well under the cap). **(c) THE PROJECT COST REPORT (`GET /api/v1/projects/{id}/cost-report`, perm `projects.report.read`, optional `as_of`).** Per WBS element: `budget` (the element's `budget_amount`), `actual_cost` (SUM of POSTED journal lines tagged with that WBS id — the journal projection, D-021: CO derives from journal lines, never a stored total), `hours` (APPROVED timesheet hours allocated to that WBS id — only APPROVED count, D-054), `variance` (budget − actual). Lines roll up to a project total: `total_actual_cost`/`total_hours` sum the lines, `total_budget` = the project's own `budget_amount` when set else the SUM of the WBS budgets, `total_variance` = total_budget − total_actual. A WBS with no postings shows ZERO. `as_of` bounds the actuals cumulatively. **(d) BOUNDED, NEVER N+1 (PERFORMANCE §6).** The report loads the WBS structure ONCE, then runs ONE finance projection over ALL the WBS ids + ONE hr aggregate over ALL the WBS ids — a fixed query count regardless of WBS count; the per-WBS roll-up is in-memory dict lookups. A regression test doubles the WBS count (2→4) and asserts the query count does not grow. **(e) SANCTIONED finance/queries ADDITION (§5).** `finance/queries.costs_by_project_dimension(session, tenant, project_dimension_ids, *, date_to=None) -> dict[uuid, Decimal]` — SUM over POSTED journal lines whose `project_id` is in the ids, of (functional debit − functional credit), grouped by that dimension, optionally bounded by `date_to`; returns only ids with postings (caller defaults the rest to zero); empty ids → `{}` no query. Mirrors `cost_center_balance` exactly. Companion `hr/queries.approved_hours_by_project(...)` (set-based form of `approved_hours_for_project`) returns approved hours grouped by dimension for a set of ids, so the report's hours are ONE query. Finance/hr own these projections; projects reads them downward by the opaque dimension — finance never imports projects, so finance stays at the bottom. **(f) §5 + NO CYCLE (verified).** Projects reads DOWNWARD only: `finance/queries` (cost_center_exists + costs_by_project_dimension), `hr/queries` (approved_hours_by_project), `sales/queries` (customer_exists). finance/hr/sales are OLDER and import nothing from projects → one-directional, no cycle. NO events.py/handlers.py — projects is masters + a READ report, triggers no cross-module write (an empty event file would be a dead file, STRUCTURE §8.3). **(g) Permissions (D-009).** `projects.project.read/.manage`, `projects.wbs.read/.manage`, `projects.report.read` (the report split out so a controller can read cost without WBS-edit rights). **(h) Structure (each <400 lines).** `constants.py`, `models.py`, `schemas.py`, `queries.py`, `service/` (projects/wbs/report), `router.py` + `wbs_router.py` (ONE surface at `/api/v1/projects`: projects CRUD + filter + ETag, WBS nested under a project + tree + ETag, the cost-report endpoint). **Later (parity §PS):** networks/activities, scheduling/milestones, cost planning (plan vs actual), budgeting with availability control, settlement, results analysis / event-based revenue recognition, customer-project billing, commitment columns on the report. **Phase 11 PLAN 11.1 is COMPLETE — Phase 11 / Projects is COMPLETE.**
- **D-057 CRM / CRM-lite (PLAN 12.1, the SINGLE Phase 12 plan — OPENS + COMPLETES CRM): LEADS that QUALIFY/CONVERT into OPPORTUNITIES; the opportunity STAGE IS THE KANBAN COLUMN (moving a card = moving the stage); ACTIVITIES logged against EXACTLY ONE of a lead OR an opportunity; the headline CONVERT-TO-CUSTOMER+QUOTE turns a won opportunity into a SALES Customer + Quote VIA THE EVENT BUS (CRM publishes, sales/handlers.py creates) — one-directional crm→sales/queries + sales←crm/events, NO cycle:** The tenth business module (s4hana-parity §CRM/Sales-pipeline: lead management, opportunity pipeline/kanban, activities, lead/opportunity → customer/quote conversion), sitting at the TOP of the dependency order. **(a) THE CROSS-MODULE CONVERT MODEL (the load-bearing idea, D-011/D-029/§5).** The opportunity → customer + quote conversion is a SALES write (a Customer + Quote are sales-owned). CRM MUST NOT import sales/service. So `convert_opportunity` (perm `crm.opportunity.convert`, run inside `run_in_uow`) PUBLISHES `OpportunityConverted` and SALES' `handlers.py` `create_customer_and_quote_for_conversion` (subscribed at the `app.main.register_event_handlers` D-011 seam) creates the customer (only when the opportunity is NOT already linked to one) + the quote through SALES' OWN service in the SAME transaction, then links the opportunity document → 'converted_to_quote' → quote document — the billing → AR-invoice / planned-buy → requisition precedent with the roles flipped (CRM publishes, sales creates). Atomic (any handler failure — duplicate customer code on re-convert, unknown item, unpriceable line — rolls the WHOLE convert back). **WHY CRM PRE-GENERATES IDS:** CRM records `converted_customer_id`/`converted_quote_id` on the opportunity, so it pre-generates the new customer id (for a prospect) + the quote id and passes them in the event; sales' `create_customer`/`create_quote` gained an optional `customer_id`/`quote_id` kwarg to create WITH those ids — so CRM knows both ids before the handler runs, needs NOTHING back from sales, and sales never imports crm/queries (no read-back, no cycle). The DURABLE convert link is the docflow quote edge; a sales Customer is a MASTER (not a docflow document — no core_documents entry), so there is NO opportunity→customer docflow edge, only the opaque recorded `converted_customer_id`. **CONVERT NEEDS ≥1 LINE:** a quote line must reference a real inventory item, so an opportunity with NO expected-product lines cannot convert (422 `crm.opportunity_no_lines`); the "single-line-from-estimated_value" fallback is NOT viable in v1 (a quote line needs a real item) — recorded here. Re-convert (a WON opportunity) and convert-of-LOST are rejected BY THE CRM SERVICE BEFORE any event publishes (no handler runs — heeds issue #53: no same-session post-failure-state assertions). **(b) FOUR tables** (migration `0042_crm`, down_revision 0041, **NO trigger-bearing alters**, reversible — verified PG+SQLite downgrade 0041→re-upgrade, portable both engines, ≤63-char ids, ZERO autogenerate drift on SQLite + PG, applies on PG to head): `crm_leads` (`Lead` — auto-numbered gapless `LEAD-` at creation [the procurement-document precedent — NOT a master code]; status NEW|CONTACTED|QUALIFIED|DISQUALIFIED|CONVERTED; company/contact/email/phone/source; estimated_value+currency_code [MoneyType nullable]; `owner_employee_id` OPAQUE hr id [nullable, validated via `hr/queries.employee_exists` when set]; `converted_opportunity_id` intra-module composite tenant FK to crm_opportunities, set on convert; (tenant,status) index), `crm_opportunities` (`Opportunity` — DocumentMixin [document_id → core_documents, so the convert handler can write the quote docflow edge]; auto-numbered gapless `OPP-` at creation; `stage` PROSPECTING|QUALIFICATION|PROPOSAL|NEGOTIATION|WON|LOST = THE KANBAN COLUMN; `source_lead_id` nullable composite tenant FK to crm_leads; `customer_id` OPAQUE sales id [nullable — set = existing customer (convert only quotes), NULL = prospect (convert creates the customer); validated via `sales/queries.customer_exists`]; company/contact/email; estimated_value+currency_code; probability_percent [MoneyType nullable]; expected_close_date; owner_employee_id OPAQUE; `converted_customer_id`/`converted_quote_id` recorded on convert [NOT cross-module FKs]; (tenant,stage)+(tenant,owner_employee_id) indexes), `crm_opportunity_lines` (`OpportunityLine` — OPTIONAL expected products that become the QUOTE LINES on convert: composite tenant FK opportunity_id, line_number UNIQUE per (tenant,opportunity); `item_id` OPAQUE inventory id [validated]; quantity [QuantityType, CHECK >0] + estimated_unit_price [MoneyType, CHECK ≥0]; (tenant,opportunity_id) index), `crm_activities` (`Activity` — activity_type CALL|EMAIL|MEETING|TASK|NOTE, status OPEN|COMPLETED|CANCELLED, subject/description/due_date/completed_date; nullable composite tenant FKs lead_id + opportunity_id with the `ck_crm_activities_one_parent` CHECK enforcing EXACTLY-ONE-PARENT [lead_id XOR opportunity_id]; owner_employee_id OPAQUE; (tenant,lead_id)+(tenant,opportunity_id)+(tenant,status) indexes). The crm_leads↔crm_opportunities mutual FK is handled in the migration by creating crm_leads WITHOUT its converted_opportunity_id FK, then crm_opportunities, then adding the cross-FK last in a `batch_alter_table` (SQLite cannot add an FK in place; the cycle warning at autogenerate is harmless). Single `models.py` (286 lines, under the cap). **(c) THE KANBAN = THE STAGE (`GET /api/v1/crm/opportunities/kanban`, perm `crm.opportunity.read`, optional `owner_employee_id`).** Returns opportunities grouped into a COLUMN PER STAGE in the declared order (the four open columns then WON, LOST), each with count + total_estimated_value + the (capped) cards. `move_stage` is the kanban move: any OPEN stage → any OPEN stage (forward or backward — a deal can slip) or → WON/LOST (closing it); a TERMINAL stage (WON/LOST) cannot move (the deal is closed; reopening = a new opportunity); same-stage rejected. BOUNDED, NEVER N+1 (PERFORMANCE §6): ONE query loads the opportunities (`opportunities_by_stage`, bounded by a per-column cap × 6 stages), grouped in memory. **(d) THE ACTIVITY EXACTLY-ONE-PARENT RULE.** An activity references EXACTLY ONE of a lead or an opportunity — enforced TWICE: the service validates exactly-one + the named parent exists (friendly 422 `crm.activity_parent_invalid` / `crm.lead_not_found` / `crm.opportunity_not_found`) AND the DB CHECK `ck_crm_activities_one_parent` (lead_id XOR opportunity_id) is the bypass-proof backstop. **(e) THE LEAD → OPPORTUNITY CONVERT (intra-module, no event).** `convert_lead_to_opportunity` (perm `crm.lead.manage`) on a QUALIFIED lead builds the opportunity through the opportunity service (so its OPP- number + core_documents registration happen identically), copies company/contact/email/value/currency, links `source_lead_id`, sets the lead's `converted_opportunity_id` + status CONVERTED. A lead with no currency cannot convert (422). **(f) §5 + NO CYCLE (grep-verified).** CRM reads DOWNWARD only: `finance/queries` (currency_exists), `hr/queries` (employee_exists), `inventory/queries` (item_exists + get_base_uom), `sales/queries` (customer_exists + get_customer). The ONLY reverse import is sales → `crm/events` (the typed event class) + `crm/constants` (the declarative `converted_to_quote` link string) — the sanctioned events-only/declarative allowance (STRUCTURE §5 / D-011), the same pattern every cross-module handler uses (finance importing sales/constants+sales/events). finance/inventory/hr/sales are OLDER and import nothing else from crm; sales needs nothing back from crm (ids pre-generated) → one-directional, NO cycle. NO `crm/handlers.py` — CRM publishes `OpportunityConverted` but subscribes to NOTHING (sales owns the handler). **(g) Permissions (D-009).** `crm.lead.read/.manage`, `crm.opportunity.read/.manage`, `crm.opportunity.convert` (the DISTINCT convert action — higher-privilege than editing), `crm.activity.read/.manage`. **(h) Structure (each <400 lines).** `constants.py`, `models.py`, `schemas.py`, `queries.py` (the only file another module could import), `service/` (`leads`/`opportunities`/`activities`/`convert` + `_shared`), `events.py` (`OpportunityConverted`), `router.py` + `opportunity_router.py` + `activity_router.py` (ONE surface at `/api/v1/crm`); the convert handler in `sales/handlers.py` (NEW). **Later (parity §CRM):** campaigns & marketing automation, contact/account (business-partner) hierarchies, forecasting & pipeline analytics, service tickets/cases, opportunity teams/competitors/products-with-price-books, lead scoring/assignment rules, email/calendar integration. **+55 module tests** — test_leads.py (13: create+number+defaults, value-without-currency 422, unknown owner/currency 422, update mutates-loaded, qualify→convert links source_lead_id + sets converted_opportunity_id + status, convert-unqualified/no-currency 422/409, qualify-already-converted 409, disqualify, update-converted 409, 404, status filter), test_opportunities.py (11: create+lines+number+doc, unknown item/customer 422, update replaces lines, move-stage open→open, move-to-LOST terminal 409, same-stage 409, update-closed 409, **kanban board groups by stage [empty stages included, cards in their stage]**, 404, stage filter), test_activities.py (12: create against lead/opportunity, no-parent/both-parents 422, unknown-lead 422, completed-NOTE stamps date, complete, cancel, complete-already-completed 409, update-terminal 409, 404, list scoped to parent), test_convert.py (5 — **THE key test: convert a prospect opportunity → a NEW sales customer [code CRM-OPP-…] + a quote [3×100=300] are created via the event, opportunity WON + converted ids set, docflow opportunity→'converted_to_quote'→quote link; convert-against-EXISTING-customer creates only the quote [converted_customer_id = the existing id]; re-convert rejected; no-lines rejected; convert-of-LOST rejected**), test_crm_api.py (14 — create/get lead, qualify+convert over the wire, lead list filter + ≤3 budget, create opportunity with lines, move-stage, **kanban board endpoint [6 columns, total_estimated_value]**, **convert endpoint → WON + converted ids**, opportunity list ≤3 budget, activity create+complete, activity no-parent 422, activity list scoped + ≤3 budget, RBAC manage-cannot-convert + read-only-cannot-create, tenant isolation 404). Sales `create_customer`/`create_quote` each gained an optional id kwarg (the convert path; default unchanged). SQLite total **1610 passed + 1 skipped** (`-m "not pg and not perf"`, +55 crm + net from existing; was 1543 at 11.1 close); **pg: 21 passed** (`-m pg`; PG schema reset → alembic upgrade head through 0042 [reversible, zero drift] → all finance/inventory trigger pg-tests green; crm has NO pg-specific tests — no triggers); ruff (app+alembic+tests) clean whole repo; stray atlas.db deleted. **STRUCTURE §9 self-check: PASS** — every app file <400 lines (service/opportunities.py 388, schemas.py 317, models.py 286, service/leads.py 234, opportunity_router.py 218, service/activities.py 199, constants.py 197, router.py 177, service/convert.py 150, activity_router.py 150, queries.py 124, events.py 108, service/_shared.py 98); §5 grep-verified (CRM reads only finance/hr/inventory/sales queries downward; sales imports only crm/events+crm/constants; one-directional, no cycle); single ORM definition per model (Lead/Opportunity/OpportunityLine/Activity only in models.py); no orphan/empty/dead files (no crm/handlers.py — read+publish only, the handler lives in sales); no TODO/stub; no supplier/product/sku term drift (item used everywhere; "expected products" is the deal-line phrase, fields are item_id). DECISIONS D-057 recorded; docs/modules/crm.md (NEW — the pipeline, the kanban=stage model, the exactly-one-parent rule, the convert-via-event mechanism + §5/no-cycle, out-of-scope) added; s4hana-parity.md §CRM rows = Done-with-v1-scope; PLAN 12.1 ticked. **Phase 12 / CRM is COMPLETE.**
- **D-058 Reporting / role-based dashboards (PLAN 13.1, the FIRST Phase 13 plan — OPENS Reporting): a READ-ONLY KPI AGGREGATOR over other modules' queries, with ROLE-BASED per-KPI gating by the SOURCE module's read permission, returned as ONE single-call dashboard bundle — reporting imports ONLY other modules' queries DOWNWARD (one-directional, no cycle), owns NO tables, needs NO migration:** The eleventh module (build-spec §13.1: role-based dashboards with KPI cards — cash position, AR/AP aging, inventory value, open orders, OTD%, WIP), sitting at the TOP of the dependency order (the generic report builder is §13.2). **(a) THE READ-ONLY AGGREGATOR (the load-bearing idea, D-021).** Every KPI is a PROJECTION read off another module's EXISTING `queries` — never a new stored total (D-021: reports are projections; KPIs read existing queries). Reporting is the NEWEST module and the TOP of the dependency order: it imports ONLY other modules' `queries` (downward reads) + their `constants` (the permission keys, the declarative allowance), NEVER their `service`/`models` (STRUCTURE §5). It owns NO tables — so NO `models.py`, NO MIGRATION (latest stays 0042), and NO `queries.py` (reporting is a LEAF consumer; nothing imports it, so a queries file would be an orphan, STRUCTURE §8.3); and it triggers no cross-module write, so NO `events.py`/`handlers.py` (an empty event file would be a dead file). **(b) THE KPI → SOURCE-QUERY MAP.** `cash_position` → `finance/queries.cash_position` (Σ presentation balance of `is_cash_equivalent` accounts over the POSTED journal, date-bounded), `ar_aging`/`ap_aging` → `finance/queries.ar_aging_summary`/`ap_aging_summary` (the rolled-up current/1-30/31-60/90+ bucket totals over open invoices/bills — the dashboard collapses the AR/AP REPORT's 61-90 + over-90 tail into one 90+ bucket; NO per-partner lines, the AR/AP routers serve those), `inventory_value` → `inventory/queries.valuation_summary` (Σ `inv_item_valuations.total_value` + FIFO live layers, summed in the reporting service — NO new inventory query, the existing per-item dict is summed), `open_sales_orders` → `sales/queries.open_sales_orders` (count + Σ `total_amount` of CONFIRMED/PARTIALLY_DELIVERED orders), `open_purchase_orders` → `procurement/queries.open_purchase_orders` (count + Σ `total_amount` of APPROVED/SENT/PARTIALLY_RECEIVED POs), `otd_percent` → `sales/queries.on_time_delivery` (the SIMPLE OTD, below), `wip_value` → `finance/queries.wip_balance` (the WIP-clearing account balance over the POSTED journal — the AUTHORITATIVE open-WIP figure, D-048; returns 0 when the tenant has NOT mapped a WIP-clearing posting default, so a non-manufacturing tenant shows zero WIP rather than a 422). **(c) SANCTIONED cross-module query additions (STRUCTURE §5; finance/sales/procurement are OLDER and import nothing from reporting → no cycle).** NEW `finance/queries/dashboards.py` (`cash_position`, `ar_aging_summary`, `ap_aging_summary`, `wip_balance` + the `AgingBuckets` dataclass) — the aging summaries facade `service.ar_aging.customer_aging` / `service.ap_aging.vendor_aging` (the SAME pure projection the AR/AP reports use, D-021), collapsing the 61-90 + over-90 tail into one 90+ bucket; the cash/WIP aggregates join the journal lines to their accounts and sum (functional debit − credit), date-bounded (PERFORMANCE §1). NEW `sales/queries/dashboards.py` (`open_sales_orders` tenant-wide count+value, `on_time_delivery`). `procurement/queries.py` +`open_purchase_orders` (tenant-wide count+value). All re-exported from each module's `queries` package `__init__`. **(d) OTD IS A DELIBERATELY SIMPLE, BEST-EFFORT METRIC.** `on_time_delivery` counts, of the tenant's POSTED deliveries joined to their sales order, how many shipped on or before the order's single `requested_date` (`delivery_date <= requested_date`) out of the total that HAVE a requested_date — measured at the DELIVERY level against the order's single requested date, NOT a line-level promised-date model, NOT confirmed-vs-requested, no partial-shipment weighting. Deliveries on an order with no requested_date are excluded from both numerator and denominator (no promise to measure). A rigorous per-line promised-date OTD is a documented later (parity §SD). **(e) THE ROLE-BASED GATING (D-009).** The dashboard is role-based: each KPI is gated by the SOURCE module's read permission (the `KPI_PERMISSIONS` map in `constants.py`: cash + WIP → `finance.statements.read`, ar_aging → `finance.ar.read`, ap_aging → `finance.ap.read`, inventory_value → `inventory.valuation.read`, open_sales_orders + otd_percent → `sales.order.read`, open_purchase_orders → `procurement.po.read`). The endpoint is guarded by the base `reporting.dashboard.read` key (the ONLY key reporting DECLARES — the price of admission); the service computes a KPI ONLY when the caller holds its source key, and the router's `response_model_exclude_none` drops the omitted KPIs — so THE RESPONSE SHAPE IS THE CALLER'S ROLE (a finance role sees cash/AR/AP/WIP, a sales role sees open sales orders + OTD, an inventory role sees inventory value, a buyer sees open POs). **(f) THE SINGLE-CALL DASHBOARD (PERFORMANCE §4/§6).** The CLIENT makes ONE call to `GET /api/v1/reporting/dashboard`, which internally runs a FIXED, BOUNDED set of aggregates — one (two for inventory) per PERMITTED KPI, each O(1) over its module's covering index. So §4's "one screen ≤3 calls" holds: the dashboard screen is a SINGLE endpoint call. The internal aggregate count EXCEEDS 3 BY DESIGN (~12 statements for the full 8-KPI bundle: auth user + functional currency + the per-KPI aggregates + the WIP posting-default lookup) — it is N-aggregates-for-N-KPIs, NEVER N+1; a test asserts the bundle stays under a fixed ceiling (16) so a regression into per-row queries is caught. **NO ETag** (PERFORMANCE §3, documented) — KPI cards are LIVE figures (a posted journal / shipped delivery changes them immediately), so a conditional-GET would serve stale numbers; the read-only aggregate needs no uow either. `as_of` (optional, defaults today) bounds the date-bounded figures (cash/aging/WIP). **(g) MONEY AS STRINGS (build-spec §13.1).** Dashboard money fields serialize as EXACT-DECIMAL STRINGS, not JSON numbers (the `MoneyStr` annotated type — a `Decimal` with a `PlainSerializer` rendering the decimal string), because a KPI card is a display surface read by JS clients where a float round-trip would corrupt the decimal; the value stays a `Decimal` in Python, only the wire form is a string. `OtdKpi.percent` is a plain ratio (0-100, one decimal), not money. The schemas: `MoneyKpi {value, currency}` (cash/inventory/WIP), `AgingSummary {current, d30, d60, d90plus, total, currency}` (AR/AP), `CountValueKpi {count, total, currency}` (open orders), `OtdKpi {percent, on_time, total}`; the `DashboardResponse` holds every KPI as an OPTIONAL field. **(h) §5 + NO CYCLE (grep-verified).** Reporting reads DOWNWARD only: finance/inventory/sales/procurement `queries` (the KPI aggregates) + their `constants` (the gating permission keys). NO module imports reporting — only `core/bootstrap.py` `mount_routers` includes the router (the sanctioned app-wiring file). finance/inventory/sales/procurement/manufacturing are OLDER and import nothing from reporting → one-directional, NO cycle. **(i) Structure (each <400 lines).** `__init__.py`, `constants.py` (base key + KPI catalog + the gating map), `schemas.py` (the four typed KPI sub-models + the optional-field DashboardResponse + the `MoneyStr` type), `service.py` (`dashboard_kpis(session, tenant_id, permissions, *, as_of)`), `router.py` (the single dashboard endpoint at `/api/v1/reporting/dashboard`); mounted last in `core/bootstrap.py` (nothing added to main.py — it delegates to bootstrap). **Later (§13.2 + parity):** the generic report builder (entity + columns + filters + group-by → JSON + CSV export), saved dashboard layouts, scheduled report delivery, drill-down from a KPI card to the underlying list, multi-currency KPI consolidation (v1 reports the functional currency; open-order totals are summed across transaction currencies — a single-currency-tenant simplification). **+20 module tests** — test_dashboard.py (13: cash sums is_cash_equivalent, AR/AP buckets total the open invoice/bill, WIP = WIP-clearing balance, inventory value >0 [combined] + exact [40 from one 8×5 receipt, focused], open sales count/value, open PO count/value, OTD 100% all-on-time, **the role-gate: a finance-only role sees cash/AR/AP/WIP but NOT inventory/open-orders/OTD**, **a role missing inventory.valuation.read drops inventory_value but keeps the rest**, no-source-keys → empty dashboard, **empty tenant → well-formed ZERO KPIs**), test_dashboard_api.py (7: the permitted bundle over the wire with money as strings + OTD 100%, base-permission required [403 rbac.permission_denied], **finance role sees only finance KPIs**, **sales role sees only open sales orders + OTD**, **buyer role sees only open POs**, tenant isolation [empty tenant B → zeros, never A's figures], the bundle query budget ≤16 [bounded, not N+1]). New test infra: `factories.py` (build_finance_base + the cash/WIP/AR-invoice/AP-bill seeders via the REAL finance services + ReportingPrincipal/create_reporting_principal) + `factories_crossmod.py` (the sales/procurement/inventory seeders + the all-KPIs-non-zero `build_reporting_setup` on the sales build_order_setup foundation with non-colliding finance codes) + conftest (reporting_setup, reporting_api, reporting_user_factory). SQLite total **1630 passed + 1 skipped** (`-m "not pg and not perf"`, +20 reporting; was 1610 at 12.1 close); **pg: 21 passed** (`-m pg`; PG schema reset → alembic upgrade head through 0042 → all finance/inventory trigger pg-tests green; reporting has NO pg-specific tests — no tables/triggers); ruff (app+alembic+tests) clean whole repo; stray atlas.db: none. **STRUCTURE §9 self-check: PASS** — every new app file <400 lines (service.py 148, schemas.py 83, constants.py 71, __init__.py 50, router.py 46; finance/queries/dashboards.py 161, sales/queries/dashboards.py 120; procurement/queries.py 368, inventory/queries.py 399 unchanged-at-cap); §5 grep-verified (reporting reads only finance/inventory/sales/procurement queries downward + their constants; NO module imports reporting; one-directional, no cycle); NO orphan/empty/dead files (no models/queries/events/handlers — a read-only leaf, documented); no TODO/stub; no supplier/product/sku term drift. DECISIONS D-058 recorded; docs/modules/reporting.md (NEW — the read-only aggregator, the KPI→source-query map, the role-based gating map, the single-call §4 design, money-as-strings, the simple-OTD note, projections-not-stored-totals) added; PLAN 13.1 ticked. **Phase 13 / Reporting is OPEN (13.1 done; the generic report builder 13.2 next).**
- **D-059 Reporting / generic report builder (PLAN 13.2, the FINAL Phase 13 plan — COMPLETES Reporting): an AD-HOC define-and-run query over a WHITELIST registry of reportable entities/columns, gated per entity by the SOURCE module's read permission, constructed via the ORM with TYPED BINDS (tenant auto-filtered by do_orm_execute, NO SQL injection), masked/sensitive columns EXCLUDED from the whitelist, 10k row cap for the JSON grid + STREAMING CSV export for larger sets — ad-hoc-only (no persisted report defs), NO migration:** The build-spec §13.2 scope (entity + columns + filters + group-by → JSON for the grid + CSV export; PERFORMANCE §3: 10k row cap + streaming CSV). **(a) THE WHITELIST-REGISTRY SECURITY MODEL (the load-bearing idea, §5/CRITICAL).** The builder must NOT allow querying arbitrary tables/columns (SQL-injection / data-exfiltration). So everything reportable is declared as DATA in `report_registry.py`: a `ReportableEntity` (stable `key` like `finance.journal_lines`, the ORM MODEL class, the SOURCE-module read `source_permission`, a dict of allowed `ReportColumn`s) + a `ReportColumn` (the pre-resolved ORM attribute, label, wire `type` str/number/date/bool, and three capability flags `filterable`/`groupable`/`is_aggregatable`). A request can ONLY name a REGISTERED entity, a SUBSET of its REGISTERED columns, filters/group-by over columns flagged filterable/groupable, and aggregations over is_aggregatable columns — anything else is 400 `reporting.invalid_report`. The column-name → ORM-attribute lookup goes through the registry allow-list (NEVER `getattr` on raw request input against the model); the whitelist is CLOSED (a column not in an entity's dict does not exist for the builder). **THE REGISTERED ENTITIES (the safe initial set):** `finance.journal_lines` (JournalLine, finance.journal.read), `finance.accounts` (Account, finance.account.read), `inventory.items` (Item, inventory.item.read), `inventory.stock_moves` (StockMove, inventory.move.read), `sales.orders` (SalesOrder, sales.order.read), `procurement.purchase_orders` (PurchaseOrder, procurement.po.read), `hr.employees` (Employee, hr.employee.read). **(b) THE MASKED-COLUMN EXCLUSION (D-009/D-052).** Masked / sensitive columns are EXCLUDED from the whitelist OUTRIGHT — they have NO `ReportColumn` entry, so they can never be selected/filtered/grouped/aggregated. The HR Employee entity exposes ONLY non-sensitive columns (employee_code, first_name, last_name, department_id, position_id, status, employment_type, hire_date); its compensation + PII (`base_salary`, `currency_code`, `national_id`, `tax_id`, `date_of_birth`, `bank_account`) are DELIBERATELY ABSENT — requesting one is 400 "unknown column". Masked data is not exposable through reports BY CONSTRUCTION (not in the registry at all), independent of the read-side `Masked` serializer. **(c) THE ORM TYPED-BIND CONSTRUCTION — NO INJECTION.** The builder NEVER string-concatenates SQL: it maps the spec's entity/column NAMES to the registry's pre-resolved ORM attributes, builds a SQLAlchemy `select()` over them, and applies filters as TYPED, BOUND comparisons (the value is coerced to the column's Python type — the `core/pagination._coerce_key_value` discipline — then BOUND, never interpolated). Operators from a FIXED set (`FilterOperator`: EQ NE GT GTE LT LTE IN LIKE BETWEEN IS_NULL); aggregations from a FIXED set (`Aggregation`: COUNT SUM AVG MIN MAX; SUM/AVG/MIN/MAX require an is_aggregatable column, COUNT may target any or COUNT(*)). A malicious-looking value like `"'; DROP TABLE …"` is just a string compared for equality — matches nothing, executes nothing (pinned by an injection test on BOTH engines). **(d) TENANCY AUTO-APPLIED (D-007).** The select runs through the TENANT-FILTERED session, so `core/tenancy.do_orm_execute` injects the `tenant_id == current` predicate into every ORM statement — the builder writes NO tenant WHERE itself; every whitelisted model is a TenantMixin so the listener always fires; tenant A can never see tenant B's rows (pinned at service + API level). THIS is why the builder MUST go through the ORM, not raw SQL (only ORM statements get the auto-scope). **(e) THE MODEL-IMPORT EXCEPTION (§5).** Reporting is otherwise a LEAF reading only other modules' `queries` downward (D-058); `report_registry.py` is the ONE place it imports the model classes — a READ-ONLY query-construction need (the builder selects/filters/groups over them through the tenant-filtered session; it NEVER calls their services and writes nothing). finance/inventory/sales/procurement/hr are all OLDER and import nothing from reporting → one-directional, NO cycle (grep-verified). **(f) THE 10K CAP + STREAMING CSV (PERFORMANCE §3).** `POST /api/v1/reporting/reports/run` → ReportResult JSON: the builder fetches `REPORT_ROW_CAP` (10_000) + 1 rows; the extra row → truncate to 10k + `truncated=True` (the UI then offers the CSV export); a spec `limit` can only LOWER the cap, never raise it; ORDER BY a stable key (the entity's default order column + the PK). `POST /api/v1/reporting/reports/export` → a `StreamingResponse` (text/csv, attachment): the CSV is generated LAZILY via `session.stream(...).partitions(...)` — NEVER materialized in memory — so a result larger than the 10k grid is served here; the spec is VALIDATED EAGERLY before the 200 stream begins (a malformed report fails as a normal 400 envelope, not mid-body). `GET /api/v1/reporting/reports/entities` → the whitelist catalog FILTERED to the entities the caller's role permits (so a UI builds a role-correct picker). **(g) ROLE-BASED GATING (D-009).** The endpoints are guarded by the base `reporting.report.run` key (the price of admission — the SECOND key reporting declares), then each report is gated by the named entity's SOURCE-module read permission (enforced IN-HANDLER since the entity is in the body — 403 `rbac.permission_denied` if absent): a finance role can only report on finance entities, a sales role on sales, etc. — the report surface IS the caller's role, exactly like the dashboard. **(h) AD-HOC ONLY (no persistence).** v1 is define-and-run in ONE request — NO saved report definitions, so NO new table and NO MIGRATION (latest stays 0042). **(i) Structure (each <400 lines).** `constants.py` (+the FilterOperator/Aggregation enums + REPORT_ROW_CAP + the report.run key, 114), `schemas.py` (+ReportSpec/ReportFilter/ReportAggregation/ReportResult + the entity-catalog schemas, 171), `report_registry.py` (the whitelist — the ONE model-import site, 281), `report_builder.py` (`run_report` + `stream_report_csv` + `validate_spec` — the ORM typed-bind construction, 299), `router.py` (+the three report routes, 146). **Later (parity §reporting):** saved/scheduled reports, JOINs across entities (v1 is single-entity), per-column custom labels/formatting, drill-down to the source list, multi-currency consolidation, broader whitelist coverage. **+26 module tests** — test_report_builder.py (13: select columns, typed-bound filter, **group-by + COUNT/SUM aggregation [exact per-group counts + Σ]**, unknown entity/column → 400, non-filterable/non-groupable column used as such → 400, SUM over a non-aggregatable column → 400, **MASKED HR compensation/PII not exposable [base_salary/national_id/… → 400 unknown column]**, **the INJECTION test: a `"'; DROP TABLE …"` filter value is BOUND not interpolated → 0 rows, no error, table survives**, **the 10k cap truncates + sets truncated=true [limit 2 over 4 rows]**, limit cannot raise the cap, **TENANT-SCOPED [tenant B's report sees only B's rows, do_orm_execute applies to the dynamic select]**), test_report_export.py (4: header + data rows + money-as-strings, filtered export, grouped export, validate-before-streaming 400), test_report_api.py (9: entities-list filtered to the role, run JSON grid, grouped aggregation over the wire, unknown entity 400, base-perm required 403, **per-entity source-perm required 403**, **export streams text/csv + attachment**, export source-perm 403, **tenant isolation [B sees only B's rows]**). New test infra: `factories_reportbuilder.py` (a deterministic sales-order population — 3 CONFIRMED + 1 DRAFT — via the REAL sales service). SQLite total **1656 passed + 1 skipped** (`-m "not pg and not perf"`, +26 report-builder; was 1630 at 13.1 close); **pg: 21 passed** (`-m pg`; PG schema reset → alembic upgrade head through 0042 → all finance/inventory trigger pg-tests green; the report builder has NO pg-specific tests [no tables/triggers], but its group-by/SUM-over-MoneyType/CSV/injection-safety were verified live on Postgres too — exact `CONFIRMED (3, 200.000000)` / `DRAFT (1, 30.000000)` group sums, proving SQL portability); ruff (app+alembic+tests) clean whole repo; stray atlas.db: none. **STRUCTURE §9 self-check: PASS** — every app file <400 lines (report_builder.py 299, report_registry.py 281, schemas.py 171, router.py 146, constants.py 114, service.py 148, __init__.py 65); §5 grep-verified (reporting reads finance/inventory/sales/procurement/hr `queries`+`constants` downward, and imports their MODELS in `report_registry.py` ONLY — the sanctioned read-only query-construction exception, D-059; NO module imports reporting; one-directional, no cycle); NO orphan/empty/dead files (still no migration/events/handlers — ad-hoc read-only); no TODO/stub; no supplier/product/sku term drift. DECISIONS D-059 recorded; docs/modules/reporting.md (+the Report Builder section — the whitelist-registry model, the registered entities table, the masked-column exclusion, the ORM typed-bind no-injection construction, tenancy auto-application, the 10k cap + streaming CSV, the role-based gating) updated; PLAN 13.2 ticked. **Phase 13 / Reporting is COMPLETE.**
- **D-060 Industry configuration layer (PLAN 14.1, OPENS Phase 14 / Admin+Onboarding): a YAML industry-template schema (the single source of truth) + a validating idempotent loader + five meaningfully-different shipped templates, with the cross-module setup writes (COA/tax/currencies, UoMs/categories, approval presets) applied via the EVENT BUS so the industry module never imports finance/inventory/procurement services; the CORE/ADMIN slices (custom-field defs per D-016, numbering sequences per D-012, terminology + module toggles as admin TenantSettings) applied directly; idempotent apply recorded per tenant. The product's headline differentiator (Atlas's answer to SAP industry solutions).** **(a) THE YAML SCHEMA AS SINGLE SOURCE OF TRUTH.** `industry-templates/_schema.yaml` is a JSON-Schema (Draft 2020-12, expressed in YAML) defining the template structure: name/display_name/description, terminology (canonical term -> display label, CLOSED whitelist of terms so a typo is rejected not silently ignored), chart_of_accounts (groups + accounts), tax_codes, currencies (exactly one functional), uoms, item_categories (+ default costing method), modules (enabled/disabled, closed whitelist), custom_fields (typed defs), approval_presets (PO/requisition thresholds), numbering_formats (sequence -> {prefix, padding, year_reset}). The loader validates EVERY shipped template against it before parsing. To avoid a new `jsonschema` dependency, a compact dependency-free validator (`industry/schema_validator.py`) implements exactly the keyword subset the schema uses (type/enum/required/properties/additionalProperties/items/pattern/min*/max*) and raises on any UNSUPPORTED keyword so it can never give a false PASS; the Pydantic `IndustryTemplate` (schemas.py) is the typed parse-side backstop (extra=forbid + exactly-one-functional-currency). **(b) THE EVENT-BUS PROVISIONING (the §5 load-bearing idea, D-011).** Applying a template creates OTHER modules' rows (finance accounts/tax/currencies, inventory UoMs/categories, procurement approval presets). The industry module MUST NOT import their services. So `apply_template` (run inside `run_in_uow` under `system_context`) PUBLISHES `IndustryTemplateApplying` carrying the validated `IndustryTemplate`, and each OWNING module's provisioning handler creates ITS slice IDEMPOTENTLY (skip-if-exists by code) in the SAME transaction: **finance/handlers/provisioning.py `provision_finance_for_template`** (currencies + COA groups + accounts + tax codes), **inventory/handlers.py `provision_inventory_for_template`** (UoMs + item categories), **procurement/handlers.py `provision_procurement_for_template`** (approval presets) — the inventory-COGS / billing -> AR-invoice precedent. Registered at the `app.core.bootstrap.register_event_handlers` D-011 seam. **(c) THE CORE/ADMIN SLICES APPLIED DIRECTLY.** The loader applies the slices it MAY own directly (industry imports core + admin freely): custom-field defs via `core/custom_fields.ensure_field_def` (D-016), numbering sequences via `core/numbering.ensure_sequence` (D-012), and terminology overrides + module toggles as admin `adm_tenant_settings` rows (upserted). Chosen over routing the settings through a fourth event (SIMPLEST §5-clean option — admin owns Tenant/TenantSetting and industry may import admin models). **(d) ONE-DIRECTIONAL, NO CYCLE.** Industry imports core + admin (models) and publishes the event; finance/inventory/procurement import `industry/events` (the declarative event + the typed `IndustryTemplate` — no logic) ONLY, never each other's or industry's services; nothing imports `industry/service`. grep-verified. **(e) IDEMPOTENT APPLY + THE APPLIED-TEMPLATE RECORD.** `ind_tenant_industry_configs` (the ONE industry-owned table, UNIQUE(tenant_id)) records which template a tenant applied + when — the onboarding record + idempotency anchor. Re-applying the SAME template is a no-op (the core get-or-create + the handlers' skip-if-exists make the second pass harmless); applying a DIFFERENT one raises `industry.template_conflict` (a tenant's industry is chosen once; switching would orphan the first template's COA/fields, out of v1 scope). The whole apply is ONE unit of work — any handler failure rolls EVERYTHING back (pinned by test_apply_is_one_transaction). **(f) CUSTOM FIELDS = CORE (D-016), BUILT NOW.** `core/custom_fields.py` (the STRUCTURE §2 amendment, deferred to now) ships the `core_custom_field_defs` registry (CustomFieldDef), `validate_custom_fields(defs, values)` (unknown keys rejected, required enforced, per-type coercion, DECIMAL-as-string per D-015, DATE ISO-8601, flat scalars only), `custom_fields_column()` (the JSONB/JSON opt-in column), and the get-or-create CRUD the loader writes through; types STRING|NUMBER|DECIMAL|BOOL|DATE. The report builder (13.2) + forms read defs via `list_field_defs`; models opt into the column over time (NOT retrofitted now). Custom fields are invisible to DB constraints BY DESIGN — anything in a financial invariant is a real column. **(g) THE FIVE TEMPLATES (meaningfully different).** manufacturing (ALL modules on, MOVING_AVERAGE, production terminology, WIP/Finished-Goods/variance COA), retail (manufacturing OFF, FIFO default, Store terminology, barcode + shelf-location item fields, merchandise COA), professional-services (inventory + manufacturing OFF, projects ON, Engagement/Consultant terminology, billable_rate employee field, WIP/Consulting-Fees COA), healthcare (manufacturing OFF, customer->Patient + a REQUIRED insurer/payer customer field, Encounter/Claim terminology, Patient/Insurance-Receivables + Contractual-Allowances COA), construction (manufacturing OFF, projects ON, project+production_order->Job terminology, AR retainage via a Retainage-Receivable/Payable account + a retainage_percent customer field, equipment + maintenance emphasis). Each validates against _schema.yaml; no two share terminology, modules or COA. **(h) MIGRATION 0043** (down_revision 0042): `core_custom_field_defs` (D-016 registry) + `ind_tenant_industry_configs` (the applied-template record) + their indexes; no trigger-bearing alters; portable across both engines, verified by `alembic upgrade head` on a fresh PG to 0043. **(i) API.** GET `/api/v1/industry/templates` (catalog summaries) + GET `/templates/{name}` (the parsed template), both guarded by `industry.template.read`; POST `/tenants/{tenant_id}/apply?template=` guarded by `industry.template.apply` AND tenant-scoped (own tenant only — cross-tenant provisioning is 14.2's system path; a mismatch is a 403 `rbac.tenant_mismatch` so cross-tenant existence is not probeable). 14.2's onboarding wizard wraps the apply. **(j) pyyaml DEPENDENCY** added to backend/pyproject.toml (`pyyaml>=6`); uv.lock updated + committed (CI uses --frozen). **+44 tests** — test_custom_fields.py (13: registry CRUD + ensure idempotency + soft-deactivate; validator unknown-keys/required/default/per-type-coercion/DECIMAL-string-in-out + float-rejected/nested-rejected/number-rejects-bool/tenant-scoped), test_templates_valid.py (14: every shipped template validates + parses [parametrized over SHIPPED_TEMPLATES], unknown template 404, schema rejects unknown top-level key + unknown terminology term, the five distinctives — healthcare no-mfg+patient field, retail FIFO+barcode+no-mfg, manufacturing full+MAV, construction retainage+Job terminology, prof-services no-inventory+billable_rate, all five terminologies distinct), test_apply.py (9: apply creates every slice [COA/tax/currencies via finance handler, UoMs/categories via inventory handler, approval presets via procurement handler, custom-field defs + numbering via the loader, the config record], one functional currency, terminology + module-toggle TenantSettings set, retail barcode field + FIFO categories, RE-APPLY idempotent [no duplicates], different-template rejected, tenant-scoped, the WHOLE apply is ONE transaction [a patched failing inventory handler rolls back the finance accounts + config + custom-field defs]), test_industry_api.py (8: list the five, get parsed template, unknown 404, apply instantiates [13 accounts over the wire] + idempotent [created=false], reject template switch [409], read-perm required [403], apply-perm required [403], cross-tenant apply denied [403 rbac.tenant_mismatch]). SQLite total **1706 passed + 1 skipped** (`-m "not pg and not perf"`, +44; was 1656+1 at 13.2 close, branch since grew); **pg: 21 passed** (`-m pg`; PG schema reset -> alembic upgrade head through 0043 -> all trigger pg-tests green; the industry layer has no PG-specific tests but the apply was verified LIVE on PG end-to-end — healthcare apply yields 13 accounts + 4 UoMs + the customer->Patient terminology read back from the JSONB TenantSetting, proving cross-engine portability). ruff (app+alembic+tests) clean whole repo; stray atlas.db deleted. **STRUCTURE §9 self-check: PASS** — every new app file <400 lines (loader.py ~210, schema_validator.py ~165, schemas.py ~180, router.py ~135, queries.py ~62, constants.py ~50, events.py ~40, models.py ~50, finance/handlers/provisioning.py ~165); §5 grep-verified (industry imports core+admin + publishes IndustryTemplateApplying; finance/inventory/procurement import industry/events declaratively only; no cycle); NO orphan/empty/dead files; no TODO/stub; no supplier/product/sku term drift (templates override DISPLAY labels only, never internal names — STRUCTURE §7 lock). DECISIONS D-060 recorded; docs/modules/industry.md + docs/industry-templates.md written; PLAN 14.1 ticked. **Phase 14 / Admin+Onboarding is OPEN (14.1 done; the onboarding wizard 14.2 next).**
- **D-061 Tenant onboarding wizard (PLAN 14.2, COMPLETES Phase 14 / Admin+Onboarding): a single-transaction orchestrator — company info → industry template → tenant + first admin user + the whole industry configuration — that lives in the INDUSTRY module, reuses existing services, adds NO table and NO migration:** The wizard is `industry/onboarding.py`'s `onboard_tenant(session, *, company_name, slug, template_name, admin_email, admin_password, functional_currency=None)`, a thin ORCHESTRATOR over pieces that already exist — it BUILDS nothing new business-wise. **(a) WHY IT LIVES IN INDUSTRY, NOT ADMIN (§5 import direction).** Onboarding must call BOTH the admin provisioning service (`provision_tenant`/`provision_user`/`grant_admin_role`) AND the industry loader (`apply_template`). `industry` already imports `admin.models`/`admin.service` (D-060), so `industry → admin` is the legal, already-established direction (STRUCTURE §5); putting the orchestrator in `admin` would force `admin → industry`, a cycle. So it sits in industry, at the top of the dependency order, importing downward only. **(b) THE SINGLE-TRANSACTION FLOW.** Inside the caller's `run_in_uow` (D-011): `find_tenant_by_slug(slug)` — if present, raise `ConflictError` (409, `onboarding.slug_taken`); `load_template(template_name)` up front (unknown name → 404 `industry.template_not_found`) BEFORE any write, so a bad template never leaves a half-created tenant; then `provision_tenant` → `provision_user` (it hashes the plaintext with argon2id itself, D-008 single hashing path) → `grant_admin_role` (creates/reuses the Administrator role carrying the four admin keys and assigns it) → `apply_template` (which publishes `IndustryTemplateApplying`; the finance/inventory/procurement handlers create their slices in the SAME transaction, D-060). Each provisioning call enters `system_context` itself (D-007 site 2); the orchestrator adds no outer context. Any failure — bad template, a handler blowing up — rolls tenant + role + user + every slice back together; a half-provisioned tenant can never persist (proven by the rollback test, which asserts only the raise + a FRESH `find_tenant_by_slug` = None afterward, per issue #53). **(c) IDEMPOTENT BY SLUG, NO NEW TABLE.** The idempotency key is the tenant slug via the existing `find_tenant_by_slug` — no onboarding-record table, so NO migration (latest stays 0043). The response is a small dataclass/schema: `tenant_id`, `slug`, `admin_user_id`, `template_applied`, and an `instantiated` count summary (accounts/uoms/tax_codes) read off the PARSED template (the apply is 1:1 with it on a fresh tenant — no extra count queries). **(d) SYSTEM-GUARDED ENDPOINT.** `POST /api/v1/onboarding/tenants` (its own `onboarding_router`, separate prefix from `/api/v1/industry`) is guarded by ONE new permission `onboarding.tenant.create` (registered in `industry/constants.py`, held by PLATFORM OPERATORS only) via the existing `require_permission` dep — because creating a WHOLE new tenant is a system/platform action, NOT the tenant-scoped `industry.template.apply` a tenant admin holds for their own tenant. The wizard's template picker reuses the EXISTING `GET /api/v1/industry/templates`; no new list endpoint. **(e) DELIBERATE SCOPE CUTS (ponytail).** `functional_currency` is accepted + validated shape-side and reserved, but NOT wired into the template's currency slice (the shipped template already fixes exactly one functional currency — overriding it is out of 14.2's minimal scope; marked `# ponytail:` in `onboarding.py`). `admin_email` is a plain `str` with a minimal `@` shape check, NOT `EmailStr` — matching `core/schemas.py`'s LoginRequest, which deliberately avoids the `email-validator` dependency (D-008). No new files beyond `onboarding.py` + the test; schemas/endpoint/permission appended to existing files (STRUCTURE §8.1). §9/§5 self-check: industry→admin only (no cycle), all files <400 lines, no term drift, no orphan/dead files, no TODO/stub. PLAN 14.2 ticked. **Phase 14 / Admin+Onboarding is COMPLETE (14.1 industry layer + 14.2 onboarding wizard both done).**
- **D-062 Admin endpoints (PLAN 14.3, the LAST backend task before the v0.3.0 promotion): the tenant-admin API surface at `/api/v1/admin` — user/role management, a read-only audit viewer, a read-only number-sequence viewer — built as ENDPOINTS OVER EXISTING CORE TABLES with NO new table and NO migration; exchange rates + tax codes are NOT duplicated because finance already ships them.** The admin module previously had only `__init__`/`models`/`service`; 14.3 adds `router.py` + `schemas.py` + `queries.py`. **(a) EXCHANGE RATES + TAX CODES ARE ALREADY DONE BY FINANCE — NOT RE-EXPOSED (the §5 load-bearing decision).** Finance ships FX-rate management (`fx_router.py`: `GET`/`POST /api/v1/finance/exchange-rates` + `/currencies` + `/posting-defaults` + `/fx-revaluation-runs`, guard `finance.fx.manage`, D-019) and tax-code CRUD (`tax_router.py`: `GET`/`POST /api/v1/finance/tax-codes` + `GET`/`PATCH /tax-codes/{id}`, guards `finance.tax.read`/`.manage`). These ARE the exchange-rate + tax-code management APIs. Re-exposing them under `/admin` would force `admin → finance/service` (a STRUCTURE §5 violation — admin may import core + its own module ONLY) AND duplicate a working API. So admin does NOT touch them; `docs/modules/admin.md` cross-links the finance paths instead. **(b) ALL UNDERLYING TABLES ALREADY EXIST → NO MIGRATION (confirmed).** users/roles/permissions/role_permissions/user_roles (Phase 3.3/3.4, core/models.py), the audit table (3.5, `core_audit_log` in core/models.py + core/audit.py), NumberSequence (3.8, core/numbering.py) all predate this task. 14.3 is PURE endpoints over them; latest migration stays 0043 (alembic upgrade head on a freshly-reset PG runs through 0043 with zero drift). **(c) USERS/ROLES REUSE admin.service FOR WRITES; queries.py ADDS THE READS.** Writes call the EXISTING `provision_user` / `create_role` / `assign_role` (no new write logic); `queries.py` adds the read side — `list_users`, `get_user`/`get_user_roles`, `list_roles`/`get_role`, `permission_keys_for_roles`, `list_permissions`, `list_audit_logs`, `list_number_sequences`. Endpoints (all tenant-scoped, RBAC-guarded, lists cursor-paginated per D-014 + N+1-free ≤3 queries): `GET`/`POST /users`, `GET /users/{id}`, `GET /users/{id}/roles`, `POST /users/assign-role`, `GET`/`POST /roles`, `GET /roles/{id}` (WITH permissions), `GET /permissions` (the global catalog). The role→permission and user→role attaches are ONE JOIN + in-Python grouping keyed on the parent id, so a page of roles (or one user's roles) costs one extra query, not N (pinned by query-budget tests). **(d) AUDIT VIEWER = READ-ONLY over core_audit_log.** `GET /audit-logs` guarded by `admin.audit.read`, newest-first keyset-paginated, filterable by `entity_table`/`entity_id`/`actor_user_id`/`action`/`created_from`/`created_to`, tenant-isolated by the D-007 filter (a tenant reads only its OWN rows — pinned by a two-tenant isolation test using a SEPARATE AsyncClient for tenant B, since the conftest `admin_client` and `client` are the SAME object). The filter set is folded into the cursor fingerprint (a cursor from one filtered view can't replay on another). NO extra masking beyond what capture already excludes (password_hash is never in a diff, D-010 — pinned). INDEX CHECK: both hot filter paths are covered by the composite indexes already on `core_audit_log` — `(tenant_id, entity_table, entity_id)` and `(tenant_id, created_at)` (AuditLog.__table_args__, from 4P.3) — so no new index and no issue filed. **(e) NUMBER-SEQUENCE VIEWER = READ-ONLY (deliberate, ponytail).** `GET /number-sequences` guarded by `admin.numbering.read`, lists the tenant's NumberSequence rows (name/prefix/padding/next_value/year_reset/current_year), keyset-paginated by name. NO reset/adjust-current-value write: mutating `next_value` out of band would open a gap (or duplicate) in the gapless numbering D-012 guarantees — a foot-gun with no v1 need (YAGNI), marked `# ponytail:` in the router. **(f) ONE NEW PERMISSION KEY, SEEDED + GRANTED WHERE THE CATALOG IS.** Users/roles reuse the EXISTING broad `admin.user.manage` / `admin.role.manage` keys (the permission-catalog read shares the role-manage guard, since reading the grantable set is part of role editing); the audit viewer reuses `admin.audit.read`. The number-sequence viewer needed a key that had none, so `admin.numbering.read` was added to `core/rbac.py` alongside the other admin keys (registered at import → seeded by `sync_permission_catalog`) AND added to admin.service's `_ADMIN_PERMISSION_KEYS` so `grant_admin_role` grants it to a tenant's Administrator (so the `admin_client`/`admin_user` test fixtures and RBAC resolution see it). No `.read`/`.write` splits invented for users/roles — reuse over new keys (the existing `.manage` keys already cover both, and the RBAC engine has no read/write split precedent). **(g) MOUNTED IN core/bootstrap.py.** `admin_router` included LAST (after industry/onboarding); admin imports `app.core.*` + `app.modules.admin.*` ONLY (grep-verified — NO finance/other-module import, no §5 violation). **(h) A queries.py IMPORT-TIME GOTCHA.** `queries.py` annotates its service-layer returns as `Page[User]`/`Page[Role]`/etc. (the pagination envelope over ORM rows); those must be STRING annotations (`from __future__ import annotations`) so Pydantic never eagerly builds a schema over the ORM type at import — the established finance-service pattern. **+28 tests** — tests/modules/admin/ test_users.py (7: create+list, get+assign-role+read-back roles, unknown-user 404, list/create RBAC 403, tenant isolation, list query-budget ≤3), test_roles.py (9: create-with-permissions, unknown-key 422 rbac.unknown_permission, list ordered, get-with-permissions, unknown-role 404, catalog lists the grantable keys, list/catalog RBAC 403, list query-budget ≤3), test_audit_viewer.py (8: returns rows, filter by entity_table, filter by action, password_hash never in diff, two-tenant isolation [separate client], pagination [limit=1 + cursor follow], RBAC 403, query-budget ≤3), test_number_sequences.py (4: lists [seeded via real ensure_sequence], tenant isolation, RBAC 403, query-budget ≤3). **VERIFY:** SQLite **1743 passed + 1 skipped** (`-m "not pg and not perf"`, +28; was 1715+1 at 14.2 close); **pg 21 passed** (`-m pg`; local PG on 55432 reset [DROP SCHEMA public CASCADE] → alembic upgrade head through 0043 → all trigger pg-tests green; admin has NO pg-specific tests — no new tables/triggers, pure reads/writes over already-pg-verified tables). ruff (app+alembic+tests) clean whole repo; no stray atlas.db. **STRUCTURE §9 self-check: PASS** — every new app file <400 lines (router.py 315, queries.py 204, schemas.py 97); §5 grep-verified (admin imports core + admin.service/queries ONLY, NEVER finance/other-module service — so FX/tax stay on the finance router, cross-linked in docs); NO orphan/empty/dead files; no TODO/stub; no supplier/product/sku term drift. DECISIONS D-062 recorded; docs/modules/admin.md written; PLAN 14.3 ticked. **Phase 14 / Admin+Onboarding fully done — this was the LAST backend task; all backend modules complete, ready for the v0.3.0 promotion to `main`.**
- **D-063 Leave-accrual idempotency guard moved from the single `last_accrual_period` column to the applied-periods table `hr_leave_accruals` (fixes #160):** D-053 stamped ONE `last_accrual_period` per balance, which forgot older periods — running period N, then N+1, then N again re-granted N (QA live repro: 5.25 → 10.5 double-grant); only the immediate same-period re-run was idempotent. The run now records one `hr_leave_accruals` row per granted (balance, period) — `LeaveAccrual` in `models/leave.py`; migration `0045_hr_leave_accruals` (down_revision 0044, reversible, portable both engines, ≤63-char ids, no trigger-bearing alters) creates the table AND BACKFILLS each existing balance's `last_accrual_period` into a guard row so already-run periods stay idempotent on live data — and skips any (balance, period) already recorded, so re-running ANY previously applied period grants 0. UNIQUE(tenant, balance_id, period) is the DB backstop against a concurrent same-period double-grant; index (tenant, period) serves the run's ONE set-based applied-ids read (no per-pair N+1, PERFORMANCE §2). `last_accrual_period` is KEPT, informational only (most recently granted period — the `LeaveBalanceRead` API surface is unchanged); capped-to-zero grants still record a guard row (the D-053 stamped-anyway semantics). Regression: `test_accrual_rerun_of_older_period_grants_nothing` (the N, N+1, N, N+1 sequence) + `test_0045_backfills_applied_periods_from_last_accrual_period`.
- **D-064 Frontend re-skin is a token swap, not a rewrite (porcelain, runs/atlas-console/DIRECTION.md):** the porcelain palette is applied by remapping the EXISTING token names in `frontend/src/styles.css` rather than renaming them, so ~3,900 token-class usages across 250 pages re-skinned with a one-file diff. This is only possible because the original DESIGN.md rule ("components use ONLY these tokens, never raw palette classes") was actually held — an audit found zero raw Tailwind palette classes and zero hex literals in the whole `src/`. Light and dark are one design in two palettes: `@custom-variant dark` + `@theme inline` pointing at custom properties redefined under `.dark`, so no component branches on theme. `color-scheme` is set per theme so native controls (the project uses native form controls by design) follow.
- **D-065 `ink-faint` collapses to `ink-muted` in porcelain:** porcelain is a deliberate TWO-tier text system (ink + ink2). No third tier lighter than `ink-muted` clears 4.5:1 on `surface` — the best candidate measured 4.40 — so rather than ship an inaccessible tier the token resolves to the same value. `text-ink-faint`'s 24 usages therefore render identically to `text-ink-muted`, and `border-ink-faint`'s become a stronger hover border. Superseded values, if a third tier is ever wanted, must come with a re-run of the contrast script.
- **D-066 One canonical status vocabulary, five tones (fixes #182):** every status pill in the app routes through `components/StatusPill`, whose word→tone table is the single source of truth. Before this, 53 pages each declared their own `STATUS_TONE` map and they disagreed (`CLOSED` was green in Procurement, grey in Sales; `RECEIVED` and `CLOSED` shared one colour in a single list), so colour carried no reliable meaning. The register names four variants (ok/warn/bad/mute); a fifth, `info`, is added deliberately — without it "APPROVED" and "SENT" would render amber and read as needing attention when they need none. Unknown words fall back to `mute` rather than inventing a colour. Colour is never the only signal: the label always renders and `mute` differs in shape (dashed outline, no dot).
- **D-067 4xx on a query throws to a route-level error boundary (fixes #180):** `queryClient` sets `throwOnError` for 4xx-except-401, and `AppShell` wraps its outlet in `RouteErrorBoundary` (keyed on pathname so navigation clears it). This is the single-point fix for the measured silent failures — a bad record id used to render the edit form with every field blank AND editable, and vendor bills spun on "Loading…" forever — replacing them with a designed state across all ~250 pages without per-page edits. 401 is excluded because `apiClient` owns the refresh-and-retry flow; 5xx and network errors stay inline and retryable rather than replacing the page. Mutations are unaffected, so inline mutation errors (e.g. the report builder's run) keep their existing treatment.
- **D-068 Glass is confined to floating layers:** `backdrop-filter` appears in exactly one place, the ⌘K command palette (`shell/CommandPalette.tsx`). Nothing structural ever blurs. Binding constraint carried from the Loop 2 prototype measurement: text on glass must be `ink` — secondary ink measured 3.98:1 over the worst-case underlay — so the palette's input row and keycaps carry their own opaque backing. The reduced-motion state is designed rather than disabled: the panel arrives already opaque, and blur is never animated into or out of.
- **D-069 Bearer credentials now have two shapes:** a request principal arrives as either a user access JWT or a machine API key (`atk_<tenant-uuid-hex>_<32 bytes urlsafe>`), and both converge on the same frozen `CurrentUser` inside the one principal builder (`core/deps.py`) — which is why no router, no `require_permission` call, no idempotency path and none of the 436 `CurrentUserDep` call sites changed. The tenant ref rides *inside* the key string so the D-007 ContextVar is set before any tenant-scoped lookup, keeping the sanctioned `system_context()` bypass list at exactly four (`core/tenancy.py`); the ref is the tenant UUID, so setting it costs no query at all, and a forged ref — or an inactive tenant — 401s with the same flat envelope as a forged secret. Secrets are sha256, not argon2: they are CSPRNG output rather than guessable passwords, and argon2id at the D-008 parameters costs tens of ms per request. The key row, its user and the tenant's `is_active` flag are loaded by ONE join, never as a second SELECT; measured, authentication costs the same single statement a JWT request costs, so an API-key list request spends exactly what the same request spends under a JWT and keeps whatever margin that endpoint has. **Corrected after review:** the first implementation minted on the tenant SLUG and resolved it per request. That measured 3 on `/api/v1/admin/users`, which has no ETag — and 4 on every list endpoint that also computes a `collection_etag` (`core/conditional.py`), which is most reference lists in the codebase, breaching PERFORMANCE §2's ≤3. Minting on the tenant UUID (this entry's own named escape hatch) removes the resolve; `backend/tests/core/test_api_keys.py` now pins authentication at EXACTLY one statement and `backend/tests/modules/inventory/test_items_api.py` asserts the ≤3 budget on ETag lists under key auth. The inactive-tenant check the slug resolve used to provide rides in the join instead — stricter than the JWT path, which has none, because a JWT dies in 15 minutes and a key can live a year. Corollary, per the spec: no per-request `last_used_at` write. Scopes **intersect** the user's resolved permissions and validate against `rbac.catalog_keys()` at mint time (D-009), so a key may only ever narrow, never widen, and a later role change re-narrows it without orphaning the key. `prefix` carries the scheme and tenant ref only and no leading characters of the secret — `secrets.token_urlsafe` emits `_`, so any split-the-key convention lands inside the secret about half the time and stores most of it in a column the list endpoint displays; operators tell two keys apart by `name`. `admin.apikey.manage` is as strong as the tenant's most privileged user, since its holder can bind a key to any user in the tenant; it ships bundled with `admin.user.manage`, which already permits the same escalation through role assignment, so a tenant only opens a gap by hand-crafting a role that carries the key alone. Rate limiting is nginx, not Python: `limit_req_zone $http_authorization` at 10 r/s burst 20 (`frontend/nginx.conf`), sized between Cloudbeds' 5 r/s per property and Toast's 20 r/s burst, and keyed per *credential* because a property's website is one server whose requests all share a source address. nginx does not account requests whose key is empty, so login, refresh and health stay deliberately unlimited — Atlas still has no brute-force protection on `POST /api/v1/auth/login`, and a login throttle is a different key and a different decision. **Rejected:** OAuth2 client-credentials (Toast's model, but it exists to separate credential from tenant because one client serves many restaurants, hence its extra restaurant header; a property's own website serves one property, and Mews and Cloudbeds both ship property-scoped keys instead) and a service user on the existing JWT flow (works today with zero code, but the credential becomes a password on the one unauthenticated public endpoint, every login inserts a `core_refresh_sessions` row with no purge job anywhere in the repo, and revocation granularity is the whole user so there is no zero-downtime rotation).

- **D-070 No credential may mint one wider than itself:** `POST /api/v1/admin/api-keys` is the only endpoint in Atlas that *chooses* a credential's permissions, so D-069's intersection alone did not contain a scoped key — a key scoped to `admin.apikey.manage` could issue itself a NULL-scoped key on its own bound user and walk out with that user's entire permission set, making scopes advisory rather than confining. Role assignment cannot do the same (it moves the USER's permissions and the presenting key's scopes still narrow the result), which is why D-069's "already reachable through `admin.user.manage`" argument does not cover this path. Fix: when the request principal is itself an API key (`CurrentUser.is_api_key`, the one field added to the frozen principal — every authorization check still reads `permissions` and nothing else), the mint must name an explicit `scopes` list that is a subset of the presenting key's own effective permissions; `scopes: null` is refused outright from a key presenter because "inherit the bound user" is by definition not bounded by the presenter (403 `rbac.scope_escalation`). A human JWT principal is deliberately unaffected: D-069 records that `admin.apikey.manage` is as strong as the tenant's most privileged user for its holder, and that decision is not reopened here. Costs zero extra queries — the check reads the principal that `get_current_user` already built. Regression net: `backend/tests/core/test_api_key_scope_containment.py`.

- **D-071 A D-013 idempotency key is scoped to its request TARGET, not just its body:** the guard hashed `await request.body()` alone, so on an ACTION route — `POST /{id}/fire`, `/{id}/post`, `/{id}/send`, ~half the guarded endpoints in Atlas — every resource on the route hashed the empty body identically. A key spent on one document therefore REPLAYED that document's stored response for a different one: HTTP 200 plus the first document's body for an action that never ran, and the one collision D-013's "different body -> 422 key_reuse" arm cannot see, because there is no body to differ. Found on PLAN 19's `POST /hospitality/tickets/{id}/fire` (a terminal reusing a key across two tables is told the second check is with the kitchen while it sits OPEN and no ingredient is ever depleted). Fix: `Idempotent.__call__` hashes `path?query` + `\n` + body, one line in `core/idempotency.py`, so the collision degrades to the ordinary 422 the client retries under a fresh key. The reservation PK `(tenant_id, endpoint, key)` is UNCHANGED — the endpoint string stays the coarse namespace, the hash narrows within it — and a genuine retry (same key, same target, same body) replays exactly as before; 261 idempotency-touching module tests and the core suite pass unchanged. Regression net: `tests/core/test_idempotency.py::test_a_key_spent_on_one_resource_cannot_answer_for_another` and `tests/modules/hospitality/test_website_isolation.py::test_a_fire_key_spent_on_one_ticket_cannot_answer_for_another`.

- **D-072 Ingredient depletion is aggregated and BACKGROUNDED off the sale — a RESTAURANT-MODULE decision, never a platform-wide relaxation:** `fire_ticket` publishes `RestaurantOrderFired` and a handler submits a `hospitality.deplete_ticket` job inside the fire's own uow; the ISSUE moves happen in the job's transaction, not the guest's request. **What is bought:** three measured failures on the synchronous path, all of which end with a guest who cannot be served. (1) `MAX_DISPATCHES_PER_UOW = 50` counts handler INVOCATIONS, so an 8-top ordering 8 dishes at 7 ingredients (56 raw lines) raised `EventCycleError` → **HTTP 500 while the guest waits to pay**; (2) one ingredient ISSUE move measures **38 SQL statements** (`tests/perf/test_write_budgets.py` re-measures it every run), so 12 distinct components inline is ~456 statements against the **10** a fire now costs; (3) `apply_bin_delta` raising `InsufficientStockError` rolls the WHOLE uow back under D-011, so a phantom stock-out **refuses service** on stock the industry's own benchmark says is permanently 2–5% wrong. Aggregation across the ticket's lines (a check sharing onion/oil/salt collapses ~24 raw lines to ~12 distinct items) is what makes the numbers small; chunking at `DEPLETE_MAX_COMPONENTS_PER_JOB = 40` is what makes the dispatch wall unreachable, because backgrounding alone does **not** lift it — the job runner executes its handler inside `run_in_uow` too, and the job's own uow was measured to COMPLETE at 49 components and FAIL at 50 (one dispatch per ISSUE move for the finance COGS handler, plus one for `TicketIngredientsConsumed` itself). **What is paid, stated in full rather than discovered later:** (a) between fire and job completion the ticket has **revenue with no COGS** — a trial balance run mid-service is momentarily short the COGS of every in-flight ticket, and it self-corrects only when the jobs drain; (b) a failure that used to be **loud** — a 422 with a guest standing at the table — becomes **quiet**, a FAILED `core_jobs` row nobody is watching. **This must be bought back with FAILED-job alerting; without it the change is strictly worse than today**, because a refusal at least gets counted. As partial mitigation the recorded error is deliberately self-describing (`Ticket TKT-2026-000001 (<uuid>) could not deplete item <uuid> from bin <uuid>: …`) because `core/jobs.py` keeps only `str(exc)`, and `GET /api/v1/jobs?status=FAILED` is the only surface a property has — polling, not push; (c) it lands on a **pre-existing core gap: there is no stale-PENDING sweeper**, so a job whose PENDING row committed but whose runner died stays PENDING forever. That was tolerable for a large stock count (the only prior job with a stock effect); it is **not** tolerable for something with a GL effect, and it is the second thing owed; (d) each chunk is atomic but the TICKET is not, so one short ingredient rolls back its own chunk's other ingredients (reachable only above 40 distinct components), and the kitchen re-counts that chunk. **What actually breaks is the unstated coupling "the sale and its depletion commit together" — NOT D-011**, which is untouched: the job's uow still guarantees all-or-nothing and a goods issue can still never commit without its COGS journal. **Scope fence:** the argument for accepting an eventually-consistent depletion rests entirely on restaurant theoretical-vs-actual usage being permanently 2–5% wrong, which makes refusing service on a computed stock-out the wrong answer. That reasoning does **not** transfer to the hotel side (Phase 20), to inventory, or to any other vertical, and this entry must never be cited as precedent for moving a stock or GL effect off its transaction anywhere else.

- **D-073 A collection ETag is only sound when every field in the payload comes from the aggregated table — so the website menu read deliberately ships NO validator:** D-035 is applied ASYMMETRICALLY inside one module, on purpose. `GET /api/v1/hospitality/menu/availability` keeps its ETag because it is computed over `hsp_menu_availability`, the one table hospitality owns and the only source of anything in that payload — correct by construction, and the reason Q2 stores availability instead of deriving it. `GET /api/v1/hospitality/menu` ships none, because a `collection_etag` over `Item` would be a **lying validator** once price is in the payload: the tag is `COUNT(id), MAX(updated_at)` over `inv_items`, prices live in `sales_price_list_items`, and a reprice moves neither — a revalidating website would be told 304 and serve yesterday's price indefinitely (Q2's ETag trap, one table over). Making it truthful needs a second aggregate over the price table, which puts the 200 path at five statements and breaches PERFORMANCE §2's ≤3. Instead the staleness is BOUNDED rather than validated: `Cache-Control: private, max-age=60, stale-while-revalidate=600, stale-if-error=86400` — exactly the window Q6 already contracts for menu structure and price, at zero queries, and it cannot lie. Safe only because the ORDER RESPONSE'S TOTAL IS AUTHORITATIVE: the website must display `total_amount` from `POST /orders`, never a total it computed from a cached menu price. One consequence of the availability tag being truthful: lazy expiry means a time-boxed 86 lapsing changes the answer **without changing a row**, so `COUNT/MAX` alone held still through it and served a 304 asserting a lapsed dish was still 86'd — the tag therefore carries a third component, `COUNT(CASE WHEN available_until <= now THEN 1 END)`, in the same aggregate statement (`extra_components` on `core/conditional.collection_etag`, defaulting to empty so the ~30 other callers emit byte-identical tags). It is monotone in time, so it moves once per lapse and never spuriously; the cost is a new dependency on the reading server's clock, which fails toward freshness (an extra 200) and never toward staleness. Regression net: `tests/modules/hospitality/test_availability_etag.py`.

- **D-074 CI path filtering lives at the JOB level, never on the `on:` trigger:** a docs-only PR paid the full backend job for nothing. Measured on run 31876164689: **18m04s total, of which the SQLite suite is 17m14s** and everything else — Postgres container init, the migration up/down/up round trip, the pg-marked guards, ruff, checkout — is **55s combined**. Postgres was never the expensive part, so the D-022 step-B guard subset stays exactly as it is; what was wasted was running 2,149 Python tests against a change to a `.md` file. **The obvious fix is a trap and must not be reintroduced:** `paths`/`paths-ignore` on the trigger skips the WHOLE workflow, and `backend` + `frontend` are REQUIRED status checks on `dev` and `main` — GitHub leaves a required check belonging to a skipped workflow in *Pending forever*, so the PR becomes permanently unmergeable (documented behaviour; the official workaround is a duplicate no-op workflow reusing the job names, which is worse than the problem). A job skipped by an `if:` instead reports success and satisfies the requirement, so the workflow always runs and only its jobs are gated. A `changes` job reads the PR's file list from the API (no checkout, ~10s, no new action dependency) and sets two booleans; `backend`, `frontend` and `perf-smoke` gate on them. **The classification is fail-safe by construction and both properties are load-bearing:** `*.md` is matched FIRST and skips both stacks (markdown cannot break a test, even under `backend/`), and the `*)` catch-all runs BOTH stacks so anything unanticipated — a new top-level directory, a compose file, this workflow itself — is tested rather than silently skipped; an unreadable file list and every push to `dev`/`main` also run both. The failure mode is therefore wasted CI, never skipped CI. What this does NOT fix: a code PR still pays the full 17m14s, because the suite runs single-process on a 4-vCPU runner against 5m24s on a developer Mac — parallelising it (`pytest-xdist`) is a separate change with a much larger blast radius and needs its own measurement.
