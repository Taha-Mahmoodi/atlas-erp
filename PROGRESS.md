# PROGRESS.md — Build Log

One line per completed task, newest at the bottom. This file is the single source of truth for "where was I" — verify the last entry against `git log` before resuming work.

- [done] 0.1 Public repo `Taha-Mahmoodi/atlas-erp` created; `.gitignore` is the root commit on `main` (44369b8)
- [done] 0.2 `dev` branch created and pushed; feature-branch flow active from here on
- [done] 0.3 16 labels created (module:*, severity:*, bug, enhancement, tech-debt, found-during-build)
- [done] 0.4 State files written: CLAUDE.md, PLAN.md, PROGRESS.md, DECISIONS.md + STRUCTURE.md/GITHUB-WORKFLOW.md copied to root
- [done] 0.5 Governance docs: LICENSE (Apache-2.0), NOTICE, README, CONTRIBUTING.md, SECURITY.md, .env.example
- [done] 0.6 CI (`backend`+`frontend` jobs, guarded green pre-code) + PR template + bug/feature issue forms
- [done] 0.7 PR #1 squash-merged to `dev`; branch protection live on `main`+`dev` (required checks backend+frontend, enforce_admins, no force-push)
- [done] 1.1 12 parallel research agents covered all S/4HANA areas from live web sources (June 2026)
- [done] 1.2 docs/research/s4hana-parity.md: 139 capabilities mapped — 45 full / 48 partial / 46 out-of-scope, every cut with reason + later path
- [done] 2.1 PLAN.md expanded: 50 tasks across Phases 3–17, one task = one PR, promotion milestones fixed
- [done] 2.2 Architect panel (2 domain architects + adversarial consolidator) → 20 decisions D-007..D-026 in DECISIONS.md, full spec in docs/architecture.md (643 lines)
- [done] 2.3 STRUCTURE.md amended per D-011/D-015/D-016 (core/money.py, core/custom_fields.py, events.py import allowance); no other tree changes
- [done] 3.1 Backend scaffold: uv project, app factory + error envelope + request-id middleware, build_engine with SQLite FK pragma, Base + naming convention, async Alembic (0001 baseline), template-copy test harness, Makefile + docker-compose — 7 tests green
- [done] 3.2 Tenancy (D-007): core/tenancy.py do_orm_execute fail-closed filter + before_flush stamping + system_context; TenantMixin/UuidPKMixin/tenant_fk in models; adm_tenants + adm_tenant_settings (migration 0002); 18 tenancy tests incl. mapper-enumeration auto-cover + FK backstop + grep gate — 25 tests green total
- [done] 3.3 Auth (D-008): core/auth.py (argon2id off-thread + HS256 JWT), core_users + core_refresh_sessions (migration 0003), CAS refresh rotation with 10s grace + family revocation, get_current_user sets production tenant ContextVar, middleware resets it; +20 tests (51 total). D-027 records core auth-table/router placement.
- [done] 3.4 RBAC (D-009): core/rbac.py code-defined permission catalog + resolve_permissions (one join, 60s TTL cache) + require_permission dependency + Masked field serializer; core_permissions (global) + core_roles/role_permissions/user_roles tenant-scoped (migration 0004); get_current_user now resolves real permissions; admin service role helpers; +14 tests (74 total). Seeded keys: admin.{user,role,audit,tenant}.*, core.document.read, core.setting.manage.
- [done] 3.5 Audit (D-010): core/audit.py split-phase capture (before_flush updates/deletes, after_flush inserts, after_flush_postexec one Core insert in-txn), RequestContext ContextVars, bulk-write-on-audited-model hard guard; core_audit_log append-only via per-dialect triggers (migration 0005); ATLAS_ token→envelope translator (audit.append_only + finance tokens pre-registered); audited: User/Role/RolePermission/UserRole/Tenant/TenantSetting; +18 tests (92 total).
- [done] 3.6 Event bus (D-011): core/events.py DomainEvent base + subscribe/@on registry + publish (buffers in session.info) + run_in_uow (drain handlers FIFO on same session before commit, rollback-all on any failure, cascade cap 50); handler writes inherit tenancy stamping + audit; +11 tests (103 total). Swap seam = EventBus Protocol.
- [done] 3.7 + numbering-half-of-3.8 Docflow + numbering (D-012): core/numbering.py (core_number_sequences model + claim_number via atomic UPDATE...RETURNING with on-demand year reset + idempotent ensure_sequence — gaplessness for committed docs falls out of ACID, no fallback needed: RETURNING works on aiosqlite 3.53); core/docflow.py (core_documents registry + partial unique index on (tenant_id, doc_number), core_doc_links edges, DocumentMixin/document_fk, register_document/link_documents, get_document_chain via two recursive CTEs both directions with depth cap 20 + visited-path cycle guard — WITH RECURSIVE works on aiosqlite); migration 0006 (partial index via postgresql_where+sqlite_where); core/docflow_router.py GET /api/v1/documents/{id}/chain (get_current_user-guarded, tenant-scoped) wired into create_app; DocumentRead/DocChain* schemas in core/schemas.py; +22 tests (134 total). D-012 numbering+docflow models live in their concern files (numbering.py/docflow.py) not models.py to keep it under the ~350-line cap, re-exported at the tail of models.py so they register on Base.metadata. Idempotency-key + pagination halves of 3.8 remain.
- [done] 3.8 Idempotency + cursor pagination (D-013/D-014): core/idempotency.py (core_idempotency_keys natural composite PK (tenant_id,endpoint,key) + reserve() in a separate committed session so concurrent duplicates collide on the PK + IdempotencyContext.capture staging the COMPLETED update on the business session inside run_in_uow for atomic doc+replay + Idempotent guard reading Idempotency-Key header/hashing body/short-circuiting completed-key replays via IdempotencyReplay→app.main handler with Idempotency-Replayed header; fail-closed cleanup on business failure); migration 0007 (revision 0007→0006, plus standalone tenant_id index for the D-007 invariant); model registered from the bottom of core/db.py (not models.py) to dodge the db→audit→models import cycle. core/pagination.py (new flat core file: paginate(session,stmt,order_by,pk,cursor,limit,filters)→Page keyset helper, base64url cursor codec fingerprinting sort+filters, expanded portable OR-form seek predicate, OrderKey/SortDirection, CursorParams/cursor_params default 50/max 200) — D-014 placed it in core/db.py but the machinery warrants its own file (recorded D-028); Page envelope unchanged in core/schemas.py. Row-value tuple comparison verified present on aiosqlite 3.53 (uniform-direction only) but the expanded OR-form is used unconditionally so mixed ASC/DESC is correct on both engines. +16 tests (8 idempotency incl. replay-runs-once/key-reuse/in-progress/missing-header/fail-closed/tenant-isolation, 8 pagination incl. no-overlap-no-gaps/null-terminator/tiebreaker-stability/tampered-cursor/filter-fingerprint/limit-cap/desc-order); 153 total green, ruff clean. Decisions in D-028.
- [done] fix #12 (blocker): migration 0005 DROP TRIGGER used SQLite-only syntax, breaking alembic upgrade head on Postgres. Added core/db_guards.py (dialect-aware drop_trigger/create_abort_trigger/drop_function), refactored 0005 to use it; wired the D-022 Postgres CI step (postgres:16 service, upgrade/downgrade/upgrade reversibility + `pytest -m pg`); added tests/core/test_pg_migrations.py (pg-marked append-only trigger + schema smoke). Validated locally: full migration round-trip + trigger fire on real Postgres 16. SQLite 153 pass, pg 2 pass.
- [done] Phase 3 complete + v0.1.0 promoted to main (merge commit 222a2ee, tagged, released); main==dev trees.
- [done] 4.1 Finance COA + fiscal periods (D-021/D-018): modules/finance package (constants/models/schemas/service[accounts,periods]/queries/router); fin_accounts + fin_account_groups (presentation hierarchy) + fin_fiscal_years + fin_fiscal_periods (migration 0008); account-type model (ASSET/LIAB/EQUITY/REV/EXP + cash_flow_category) that statements will project; period open/close service with the D-018 close seam for journal drafts; finance permissions registered; queries.py exposes find_period_for_date/get_period_status/account_exists; +41 tests (206 SQLite + 2 pg). Validated on Postgres.
- [done] 4.2 Universal journal (D-015/D-017/D-018): core/money.py (MoneyType/QuantityType/RateType TypeDecorators — NUMERIC on PG, INTEGER micro/nano-units on SQLite; quantize half-up; largest-remainder allocate); fin_journal_entries + fin_journal_lines (one-side CHECK, all CO dimensions, denormalized is_posted/posting_date/fiscal_period for header-join-free statements); two-flush post_entry (period check → gapless number claim → DRAFT→POSTED), reverse-only corrections, JournalEntryPosted/Reversed events; four per-dialect triggers (period-close ATLAS_PERIOD_CLOSED, balance ATLAS_UNBALANCED_ENTRY, header + line immutability ATLAS_POSTED_IMMUTABLE) via extended db_guards; idempotent post/reverse endpoints; widened period-close seam rejects drafts; migration 0009. +93 tests (258 SQLite + 11 pg) — all four triggers verified firing on BOTH engines.
- [done] 4.3 Multi-currency (D-019): models/fx.py (fin_currencies w/ partial-unique one-functional index, fin_exchange_rates SPOT/CLOSING, fin_posting_defaults purpose-keyed wiring, fin_fx_revaluation_runs) + is_monetary/currency_code on fin_accounts; constants RateKind/FxRunStatus + 5 FX posting purposes + finance.fx.manage/revalue perms. service/fx.py get_rate (direct-or-inverse w/ 10-dp round, same-currency=1, missing→422 finance.exchange_rate_missing) + functional_currency(_or_none) + translate + currency/rate mgmt; service/posting_defaults.py get/set (unmapped→422); service/fx_translation.py translate_entry_lines wired into post_entry (foreign entries translate at SPOT, residual cent absorbed via largest-remainder so functional Σdebit==Σcredit exactly — balance trigger passes on both engines; rate_override param, no header column so fin_journal_entries+its 4 triggers UNTOUCHED; reversals never re-translated); service/fx_revaluation.py run_fx_revaluation (per-account monetary-foreign-balance scope, CLOSING-rate delta vs functional carrying, balanced FX_REVAL entry + independent next-period auto-reversal linked 'revalues', next-period-open validated up front→422, re-run reverses prior COMPLETED run then reposts, tracked in fin_fx_revaluation_runs). fx_router.py (currencies/exchange-rates/posting-defaults guarded fx.manage; fx-revaluation-runs guarded fx.revalue + idempotent) mounted into router.py. queries.py exposes get_rate/functional_currency. migration 0010 (4 tables + batch_alter fin_accounts — non-trigger-bearing so NO trigger recreation needed; journal table deliberately not altered). +37 tests (295 SQLite + 12 pg); PG reversibility (downgrade base→upgrade head) + foreign-translation balance trigger verified on real Postgres 16. DEVIATIONS: rate override is a post_entry param not a JE column (avoids batch-altering the trigger-bearing journal table); auto-reversal is an independent swapped-line FX_REVAL entry (not reverse_entry of the adjustment) so the adjustment stays POSTED and re-runnable; realized FX + per-open-item revaluation bounded to AP/AR (4.4+), recorded 'partial' in parity doc.
- [done] 4.4 Tax engine: fin_tax_codes (rate/jurisdiction/inclusive-exclusive, payable+receivable account links; migration 0011); calculate_line_tax (inclusive net=gross/(1+rate)) + calculate_document_tax (group per code, allocate exact) with TaxDirection OUTPUT/INPUT; queries expose get_tax_code + calculate_line_tax for Sales/Procurement; tax-codes endpoints (tax_router.py). +17 tests (315 SQLite + 12 pg). Note: finance/router.py at the 400-line cap — next routes split.
- [done] 4.5 Accounts Payable (D-029/D-019): fin_vendor_bills/lines + fin_vendor_payments + allocations (migration 0012), partner_id opaque per D-029; create+post bill posts Dr expense/Dr input-tax/Cr AP-control journal with open_amount; payment clearing reduces open items + realized FX at payment (functional-at-bill-rate − functional-at-payment-rate to fx_realized_gain/loss, balanced via skip_translation clearing entry); payment-run batch; AP aging projection; VendorBillPosted/VendorPaymentPosted events; ap_router.py. +25 tests (351 SQLite + 13 pg incl. realized-FX on Postgres). D-030 records AP placement + journal clearing hook.
- [done] 4.6 Accounts Receivable (D-029/D-019): fin_customer_invoices/lines + fin_customer_receipts + allocations (migration 0013, down_revision 0012, partner_id opaque per D-029, no FK to a customer master; abbreviated 67→45-char allocation FK names for PG's 63-char cap); AP mirror sign-flipped — create+post invoice posts Dr AR-control/Cr revenue/Cr output-tax journal (AR_INVOICE) with open_amount=gross; receipt clearing Cr AR / Dr bank reduces open items + realized FX at receipt (functional-at-invoice-rate vs functional-at-receipt-rate to fx_realized_gain/loss, balanced via skip_translation clearing entry); AR aging projection; DUNNING (DUNNING_THRESHOLDS 7/30/60 days → levels 1/2/3; run_dunning advances dunning_level + last_dunned_date on overdue open invoices, posts NO journal, idempotent-ish per day); CustomerInvoicePosted/CustomerReceiptPosted events; queries get_open_customer_invoices + customer_open_balance (for Sales credit-limit block); ar_router.py mounted one-surface. Factored AP+AR realized-FX clearing into shared service/clearing_fx.py (ClearedItem tuples + control_is_debit flips AP Dr-control vs AR Cr-control); payment_clearing.py refactored to delegate. +29 tests (392 SQLite + 14 pg incl. realized-FX receipt on Postgres). ruff + pytest not-pg + alembic PG upgrade (zero autogen drift) + -m pg all green; finance stays bottom-dependency, partner_id opaque.
- [done] 4.6 Accounts Receivable (D-029/D-019/D-031): fin_customer_invoices/lines + fin_customer_receipts + allocations (migration 0013); invoice posts Dr AR/Cr revenue/Cr output-tax; receipts clear open items + realized FX (shared clearing_fx.py helper, control_is_debit flag — AP+AR one implementation); dunning levels (7/30/60-day thresholds, state-only no journal); AR aging; customer_open_balance query for Sales credit block; CustomerInvoicePosted/CustomerReceiptPosted events; ar_router.py. +29 tests (392 SQLite + 14 pg). D-031 records the shared clearing helper.
- [done] 4.7 Controlling (D-021): fin_cost_centers + fin_profit_centers (hierarchies) + fin_allocation_rules/targets + fin_allocation_runs (migration 0014); run_allocation distributes a source cost-center's period balance to targets via allocate (exact) posting a balanced CO entry tagged per cost_center dimension; journal create_draft_entry now validates cost_center/profit_center existence (service-level integrity, no FK on trigger-bearing fin_journal_lines); cost_center_balance query; AllocationPosted event; co_router.py. +22 tests (428 SQLite + 15 pg). CO is a projection of the universal journal.
- [done] 4.8 Financial statements (D-021 — THE universal-journal payoff): service/statements/ package derives Trial Balance, P&L, Balance Sheet, Cash Flow (indirect), cost-center report, margin-by-product ALL from ONE aggregate over fin_journal_lines (sum functional debit−credit, tenant+is_posted+date predicate) — zero stored totals; retained earnings computed on the fly as net-income-to-date synthetic equity line; cash flow reconciles to is_cash_equivalent movement (built-in is_reconciled self-check); TB and BS self-balance (is_balanced). statements_router.py (finance.statements.read). Migration 0015 promotes ix_fin_journal_lines_proj to covering (postgresql_include + partial where both dialects); line trigger survives (plain CREATE INDEX). +11 tests (438 SQLite + 16 pg). Phase 4 finance ~80% (8/10).
- [done] 4P.1 PERFORMANCE.md addendum integrated: committed at repo root, bound in CLAUDE.md (§6 checklist now part of endpoint DoD), Phase 4P retrofit tasks added to PLAN (query-count fixture, index audit, gzip, background-job core, ETag, perf suite), Phase 16/17 amended (volume seed tenant, deployment doc). v0.2.0 promotion now gated on 4P.2–4P.4 + perf smoke.
- [done] fix #25 (4P.3): AP/AR hot-list filter indexes — ix_fin_vendor_bills_list_filters + ix_fin_customer_invoices_list_filters (tenant_id, partner_id, status, date) in models + migration 0016; regression test asserts both exist in the migrated schema. Audit found all other FK/filter columns already covered (UNIQUE composites + tenant indexes). 440 SQLite + 16 pg.
- [done] 4P.2+4P.4 (closes #27): query_counter fixture (tests/conftest.py, after_cursor_execute on the per-test engine, transaction-control/PRAGMA excluded) + assert_query_budget helper (finance conftest, warm-up request so the D-009 RBAC cache is hot); ≤3-query assertions on all 17 list endpoints (accounts, account-groups, fiscal-years, fiscal-periods, journal-entries, currencies, exchange-rates, posting-defaults, fx-revaluation-runs, tax-codes, vendor-bills, vendor-payments, customer-invoices, customer-receipts, cost-centers, profit-centers, allocation-rules) + ≤4 on the 3 child-bearing details (journal-entry/vendor-bill/customer-invoice). Measured warm path: every list = 2 queries (user load + page select), every detail = 4 (user + header + refresh + lines) — NO N+1 found. GZipMiddleware (minimum_size=500) in create_app() between CORS and RequestId (tests: large gzipped, tiny passthrough, X-Request-ID survives). The six bare-list reference endpoints now cursor-paginated via paginate() + new core map_page() helper (account-groups sort_order/code, fiscal-years/periods start_date, currencies code, posting-defaults purpose, reval-runs created_at DESC — all + id tiebreaker); router.py refactored under the 400-line cap. +12 tests (452 SQLite + 16 pg). docs: finance.md endpoint shapes, core.md guarantee 6.
- [done] 4P.2 + 4P.4: QueryCounter fixture (after_cursor_execute on per-test engine, tx-control excluded) + warm-path budgets asserted on every list (≤3; measured 2) and child-bearing detail (≤4; measured 4 incl. deliberate refresh) endpoint — zero N+1 found; GZipMiddleware (≥500B) with request-id preserved; six reference endpoints cursor-paginated (closes #27); map_page helper keeps routers under cap. +12 tests (452 SQLite + 16 pg).
- [done] fix #30 (STRUCTURE §8.4 size caps): tests/modules/finance/conftest.py 466→127 lines — data builders (setup dataclasses, seed_small_coa/seed_fiscal_year, build_*_setup, create_finance_principal) extracted to tests/modules/finance/factories.py (394); conftest keeps only the thin fixtures (names/signatures unchanged, dataclasses re-exported so test imports keep working). app/modules/finance/constants.py 405→363 by condensing docstrings/comments only — no renames, no value changes, permission registration intact. ruff clean; 452 SQLite passed + 1 skipped (identical to pre-change).
- [done] 4P.5 Background-job core (PERFORMANCE §3, closes #26, D-032): core/jobs.py — core_jobs model (NOT audited; high-churn control rows), @register_job code-defined registry, submit_job (PENDING row commits inside the endpoint's uow atomically with the D-013 capture → a replayed Idempotency-Key returns the SAME job id), schedule_job post-commit via the JobScheduler Protocol seam (in-process asyncio runner v1, 4-concurrent cap via per-loop semaphores, wait_for_jobs() test helper; arq/celery swaps the one scheduler binding); runner opens its own session from the submitter-passed factory (prod = get_session_factory dependency, tests = per-test override), restores tenant_context + audit actor from submitted_by, marks RUNNING (commit), runs the handler in run_in_uow with COMPLETED+result in the same commit, FAILED+error on rollback. core/jobs_router.py GET /api/v1/jobs[/{id}] (auth, tenant-scoped, paginated; D-027 core-router precedent) + JobRead/JobSubmitted in core/schemas.py. Migration 0017_core_jobs (zero autogen drift both engines). #26 fix: POST /fx-revaluation-runs + POST /payment-runs now submit finance.fx_revaluation/finance.payment_run jobs and return 202 {job_id} (service functions unchanged; PaymentRunResult retired). +13 tests (465 SQLite + 16 pg incl. PG downgrade base→head); jobs suite proves PENDING→COMPLETED+result, FAILED rollback, tenant+actor context, cap, polling isolation; FX/AP endpoint tests assert the 202+poll contract with unchanged business outcomes. ruff + pytest -m 'not pg' + alembic PG reversibility + -m pg all green.
- [done] 4.9 Bank reconciliation (D-033): fin_bank_statements + fin_bank_statement_lines (migration 0018, down_revision 0017; statement is DocumentMixin with doc_number NULL — external document, no Atlas number; matched_journal_line_id opaque, cleared_journal_entry_id composite tenant FK; UNIQUE(tenant, statement, line_number) + (tenant, statement, status) + (tenant, value_date) indexes). CSV contract in service/bank_csv.py (header value_date,amount,description,counterparty_ref; ISO dates; signed decimal amounts, positive = money in; per-row 422 error report finance.statement_csv_invalid; closing==opening+Σlines or 422 finance.statement_unbalanced; account must be is_cash_equivalent). import_statement (service/bank_import.py) bulk-inserts lines via ONE ORM-enabled executemany (PERFORMANCE §2 — a 1200-line import = 6 SQL statements, query-counter asserted ≤10); POST /bank-statements runs ≤1000 lines inline (201) and submits finance.bank_statement_import job above (202 {job_id}, PERFORMANCE §3; handler calls the same import; replayed Idempotency-Key returns the same statement/job id). suggest_matches (service/bank_reconcile.py): two set-based rules — exact amount+date, then exact amount ±3 days nearest-wins — over candidate maps from two queries (posted journal lines on the bank account, consumed ids excluded tenant-wide via NOT-IN subquery); rule-3 description heuristic + partial matching documented as v1 boundary. confirm/reject transitions; clear_unmatched_line posts a real Dr/Cr bank vs contra entry (default = bank_unmatched_clearing posting default), JOURNAL type, docflow statement->'posts'->entry, line CLEARED; statement status derives IMPORTED -> PARTIALLY_RECONCILED -> RECONCILED from resolved (MATCHED|CLEARED) counts. Permissions finance.bank.read/.import/.reconcile; bank_router.py mounted one-surface; lists paginated ≤3-query budget (statements sort (statement_date DESC, id) — created_at seek keys repeat the boundary row on SQLite, core fix tracked). +32 tests (497 SQLite + 16 pg incl. zero autogen drift both engines). ruff + pytest -m 'not pg' + alembic PG reset/upgrade/check + -m pg all green.
- [done] 4.10 Asset accounting lite (parity scope: register + SL/DB depreciation runs; NO transfers/retirements/parallel areas — recorded laters): fin_assets + fin_depreciation_runs + fin_depreciation_entries (migration 0019, down_revision 0018, three tables + indexes, NO trigger-bearing alters; UNIQUE(tenant, asset, fiscal_period) on entries = the idempotency backbone; partial-unique (tenant, asset_number) WHERE NOT NULL — AST number claimed at ACTIVATION per D-012; three explicit-named composite FKs to fin_accounts to dodge the column-0 name collision). Lifecycle: create/update DRAFT (validates account TYPES — asset+accumulated postable ASSET, expense postable EXPENSE — salvage<cost, declining rate required (0,100] iff DECLINING_BALANCE, cost-centre dimension exists); activate claims AST-2026-NNNNN + status ACTIVE; capitalize=True ALSO posts Dr asset / Cr asset_acquisition_clearing posting default dated acquisition_date with docflow asset->'posts'->entry; capitalize=False = opening-balance assets. compute_depreciation(asset, period_index, prior_accumulated): STRAIGHT_LINE drift-free cumulative form (amount(n)=quantize(depreciable×n/life)−prior; the final period takes depreciable−prior, so 10000/12 → 833.33/833.34 alternating summing EXACTLY 10000); DECLINING_BALANCE NBV×rate/12 quantized, floored so the crossing period takes NBV−salvage landing EXACTLY on salvage (exhaust n>=life likewise). run_depreciation is SET-BASED (PERFORMANCE §2; constant statement count ~33 regardless of N, query-counter asserted <=35): NOT EXISTS anti-join selection, ONE grouped prior aggregate (count+sum per asset), ONE bulk executemany entry insert, ONE journal entry with lines GROUPED per (expense account, cost centre) / per accumulated account (document_type DEPRECIATION, DEP-2026-NNNNN, docflow run->'posts'->entry); assets at cost−salvage flip FULLY_DEPRECIATED; re-run returns the existing POSTED run (no-op); closed period → 422 finance.period_closed (journal period trigger = bypass-proof backstop); run_date must fall inside the period. POST /depreciation-runs: <=100 eligible inline 201, >100 submits finance.depreciation_run job → 202 {job_id} (D-013 idempotent both paths; replay returns the same run / SAME job id). asset_register(as_of) = pure projection (ONE statement: SUM(entries.amount) over periods ending <= as_of; the *_after columns are per-entry audit trail only, no stored NBV — D-021 spirit). Permissions finance.asset.read/.manage + finance.depreciation.run; assets_schemas.py/assets_router.py siblings mounted one-surface. constants.py hit the 400-line cap and split into the constants/ package (enums/documents/permissions; __init__ re-exports everything, zero import changes — D-034); test factories' _FINANCE_KEYS now registry-derived (auto-grants new finance keys). +29 tests (537 SQLite passed + 1 skipped, 16 pg incl. zero autogen drift + full downgrade base→head reversibility on Postgres 16). ruff + pytest -m 'not pg' + PG schema reset/upgrade/check + -m pg all green. PHASE 4 (Finance & Controlling) COMPLETE — 4.1–4.10 all done (4P.6 ETag + 4P.7 perf suite remain as the v0.2.0 promotion gate). STRUCTURE §9 self-check: no file over the 400-line cap, no orphans, no dependency-rule breaks, no term drift.
- [done] 4P.7 Perf smoke suite (PERFORMANCE §5): tests/perf/ (@pytest.mark.perf, excluded from default runs via -m "not pg and not perf") — session-scoped perf_dataset fixture bulk-seeds ONE mid-volume tenant (30-account COA via real services; 2,500 POSTED journal entries / 20,000 posted lines across 12 OPEN 2026 periods with denormalized is_posted/posting_date/fiscal_period_id; 1,500 mixed-status customer invoices spanning all aging buckets; full-finance principal for API timing) onto a copy of the D-025 SQLite template — or onto ATLAS_PERF_DATABASE_URL (real Postgres, fresh tenant per run) — using Core executemany inserts (PERFORMANCE §2; trigger-safe: 0009's period trigger fires on INSERT of POSTED and passes with periods OPEN, the balance trigger is UPDATE-only, line immutability is UPDATE/DELETE-only; debits==credits per entry by construction). Seeds in ~2s SQLite / ~5s PG. test_budgets.py: journal-entries list API (status filter, page 50) + trial balance/P&L/balance sheet/AR aging service calls + a volume/TB-balanced sanity pin; `timed` helper asserts the MEDIAN of 5 runs (1 warmup; stabler than p95 at n=5) at 2× the §5 budgets on SQLite (600ms/3s) and 1× on Postgres (300ms/1.5s), always printing [perf] report lines. Measured medians: SQLite 4.2ms list, 6.0/6.6/6.0ms statements, 10.8ms aging; Postgres 4.8ms list, 5.6/5.6/5.5ms statements, 8.1ms aging — all far under budget. CI: new NON-BLOCKING `perf-smoke` job (continue-on-error, NOT a required status check; `pytest -q -m perf -s`); backend SQLite step now `-m "not pg and not perf"`. Makefile `perf` target; `perf` marker in pyproject; suite documented in docs/modules/core.md Tests. Default suite unchanged (537 passed + 1 skipped); ruff clean; verified green on BOTH engines. Deviation: the list API is timed with its supported status filter — the journal list endpoint has no date-range filter parameter today (the §5 'journal list w/ filters' path is exercised via filtered keyset pagination).
- [done] 4P.6 Conditional requests on slow-changing reference data (PERFORMANCE §3, closes #28, D-035): new core flat file `core/conditional.py` — `collection_etag` (ONE cheap aggregate `COUNT(id)+MAX(updated_at)`, auto tenant-scoped by the D-007 listener since the `func` aggregate carries the model's mapper), `check_not_modified` (RFC 7232 weak comparison + `W/` prefix + `*`), `conditional_response` (returns a bare `304 {ETag}` WITHOUT awaiting the page builder, so `response_model=Page[T]` is bypassed and the page query never runs). ETag format `W/"<count>-<max_updated_micros>-<tenant8>-<request_fingerprint>"` — collection-level weak validator; the `tenant8` component makes a cross-tenant 304 impossible and the `request_fingerprint` (cursor+limit+filters) makes a wrong-slice 304 impossible. Applied to the seven slow-changing reference lists ONLY (accounts, account-groups, currencies, tax-codes, fiscal-years, fiscal-periods, posting-defaults); transactional lists carry none. The 304 path is cheaper than 200 (2 queries vs 3, query-counter pinned). Structure: journal-entry endpoints moved to a new sanctioned sibling `finance/journal_router.py` so `router.py` stays under the 400-line cap (now 322). +22 tests (14 core unit in tests/core/test_conditional.py + 8 endpoint in tests/modules/finance/test_router_conditional.py) → 559 passed, 1 skipped on SQLite; 16 pg-marked pass after reset+upgrade head (no new migration — D-035 adds no schema).
- [done] 4P.6 Conditional reference reads (PERFORMANCE §3, closes #28, D-035): core/conditional.py weak collection ETag = W/"<count>-<max(updated_at)micros>-<tenant8>-<request-fingerprint>" from ONE COUNT/MAX aggregate (tenant-scoped by the D-007 listener + tenant component = cross-tenant 304 impossible; request fingerprint via pagination.filter_fingerprint = cross-page 304 impossible); conditional_response returns 304 WITHOUT awaiting the page builder so revalidation is 2 queries vs 200-path 3. Applied to /accounts, /account-groups, /currencies, /tax-codes, /fiscal-years, /fiscal-periods, /posting-defaults only (never transactional lists). journal endpoints extracted to finance/journal_router.py (router.py back under cap). +22 tests (559 SQLite + 16 pg). No migration (head 0019).
- [self-check v0.2.0 promotion] STRUCTURE §9 + GITHUB-WORKFLOW §9: working tree clean on dev; all feature branches deleted after merge; both CI checks green on dev; perf-smoke green; zero open issues; no file over the 400-line cap; no orphans; dependency rules intact; no term drift. docker stack verified manually (compose binary absent in this env): backend image builds, `alembic upgrade head` applies all 19 migrations on Postgres 16, app boots and /api/v1/health returns 200, OpenAPI mounts auth/documents/finance/jobs route groups. Full suite 559 passed + 1 skipped (SQLite) / 16 pg / perf 6 passed both engines. PHASE 4 (Finance & Controlling) + Phase 4P (performance retrofits) COMPLETE — promoting dev → main as v0.2.0.
- [done] 5.1 Inventory item masters — NEW module `backend/app/modules/inventory/` (second business module, reads finance/queries downward; permissions registered at import). `constants.py` (single file): ItemType (STOCKED|NON_STOCKED|SERVICE), CostingMethod (MOVING_AVERAGE|FIFO), TrackingMode (NONE|LOT|SERIAL) + LotStatus/SerialStatus, six permission keys (item/category/uom × read/manage). NO number sequence — item_code is user-supplied unique-per-tenant (account-code precedent), required on create; masters carry no gapless document number (those come with moves in 5.2). `models.py` (six inv_ tables): ItemCategory (default_costing_method + THREE OPAQUE finance GL-account ids inventory/cogs/price_difference per D-029 — nullable, validated-in-finance when set, never a cross-module FK), Uom (code/name; base-ness is per item), Item (item_code UNIQUE(tenant,item_code), item_type, category_id+base_uom_id composite tenant_fks, costing_method defaulted-from-category-but-stored per D-020, tracking_mode, is_active, reorder_point/quantity QuantityType nullable; composite index (tenant,item_type,category_id,is_active) for the filtered list + (tenant,category_id) FK index), UomConversion (per-item alt_uom + factor_to_base QuantityType, CHECK factor>0, UNIQUE(tenant,item,alt_uom)), Lot + SerialNumber (master tables defined now, populated by receipts 5.2+; UNIQUE(tenant,item,code) each). **UoM convention chosen: per-item BASE + ALTERNATES** — each item has one base_uom_id; UomConversion(item, alt_uom, factor_to_base) where multiplying an alt-UoM qty by factor yields the base-UoM qty (base EA + BOX factor 12 ⇒ 1 BOX = 12 EA); base↔alt and alt↔alt both derive from the single per-alternate factor (the S/4 alternative-UoM shape). `service/` package (split at the 400-line cap per finance precedent — categories/uoms/items/conversions, __init__ re-exports so `service.create_item(...)` is unchanged): CRUD for all four masters + validations (code/item_code unique with friendly ConflictError; category+base_uom exist; costing_method defaults from category when omitted; tracking-only-on-STOCKED; conversion factor>0 + alt≠base; GL accounts validated via finance/queries) + pure `convert_quantity(qty,from,to,base,factors)=qty*factors[from]/factors[to]` quantized scale 6, unknown-UoM raises. `queries.py` (the only file other modules import): get_item/item_exists/get_costing_method/get_base_uom/get_category_accounts(item)->(inv,cogs,price_diff). `router.py` /api/v1/inventory: item-categories/uoms/items CRUD (paginated + collection ETag per D-035, ≤3-query budget) + nested GET/POST /items/{id}/uom-conversions, every endpoint permission-guarded; mounted in main.py after finance. NO events.py/handlers.py (masters drive no cross-module effects; no-stub rule — they arrive with moves in 5.2). Migration `0020_inventory_items` (down_revision 0019): six tables + FK/filter indexes, no trigger-bearing alters, portable both engines; zero autogenerate drift on PG, full downgrade base→head reversibility verified. **Sanctioned cross-module addition:** `finance/queries.account_exists_by_id(session,tenant,account_id)` (by-id companion to account_exists, for the category's opaque GL ids — D-029/STRUCTURE §5). docs/modules/inventory.md added. +46 tests (test_categories/test_uom/test_items/test_items_api) → 623 passed + 1 skipped SQLite, 16 pg. ruff + pytest -m "not pg and not perf" + PG reset/upgrade/downgrade-base/upgrade-head + -m pg all green; no stray atlas.db; no file over the 400-line cap.
- [done] 5.2 Inventory warehouses + bins + stock moves (the quantity SSOT) + on-hand projection (D-020/D-036). `constants.py`: MoveType (RECEIPT|ISSUE|TRANSFER|ADJUSTMENT), MoveStatus (POSTED-only — a move is created already POSTED and IMMUTABLE, the universal-journal reversal philosophy applied to stock; NO draft phase, unlike journals), the greppable MOVE_BIN_SIDES required-side table (RECEIPT->to-only, ISSUE->from-only, TRANSFER->both-distinct, ADJUSTMENT->exactly-one-side), the inventory.stock_move sequence (STK-2026-NNNNN, year-reset, claimed at creation — a move is permanent at create, D-012 orders/receipts branch), STOCK_MOVE_DOC_TYPE + reverses link, six new permission keys (warehouse/bin x read/manage, move read/create). `models.py` SPLIT into a models/ package (masters.py 5.1 + stock.py 5.2) at the 400-line cap (finance models/ precedent; __init__ re-exports so import sites unchanged). stock.py four tables: Warehouse (UNIQUE(tenant,code), is_active soft-delete), Bin (warehouse_id composite tenant_fk, UNIQUE(tenant,warehouse,code), is_default/is_active, (tenant,warehouse) index), StockMove (DocumentMixin + move_number claimed at creation, move_type, item_id, quantity QuantityType ALWAYS POSITIVE, base_uom_id frozen, from_bin_id/to_bin_id nullable composite tenant_fks with distinct explicit names, lot_id/serial_id nullable, move_date, reference, posted; four PERFORMANCE-§1 indexes (tenant,item,to_bin)/(tenant,item,from_bin)/(tenant,move_date)/(tenant,item)), StockQuant (the MAINTAINED on-hand projection — item_id/bin_id/lot_id/on_hand_qty, UNIQUE(tenant,item,bin,lot) upsert target, CHECK(on_hand_qty >= 0) = no-negative-stock backstop portable both engines, (tenant,item) index; NOT AuditMixin — derived projection). `service/`: warehouses.py + bins.py (CRUD; warehouse must be active for a new bin; is_active deactivation never hard-delete), stock_quants.py (apply_bin_delta with_for_update upsert/decrement + InsufficientStockError pre-flight + delete-at-zero; resolve_lot/resolve_serial — create master instance on RECEIPT, require-existing on ISSUE/TRANSFER), stock_moves.py (create_move = the heart: validate STOCKED item + bin-side rule + bins-in-active-warehouses + quantity>0 + tracking + serial=>qty1; register doc + claim STK number; insert POSTED move; apply quant deltas in deterministic bin-id order = deadlock avoidance; reverse_move posts the opposite move linked docflow 'reverses', double-reverse rejected; list_moves ledger + list_on_hand projection). `queries.py`: total_on_hand/on_hand/on_hand_by_bin/on_hand_by_lot (read the maintained quant, indexed; what sales ATP / procurement call). `stock_router.py` (sibling mounted into router.py one-surface, the journal_router precedent): warehouses + bins CRUD (paginated, ETag), POST /stock-moves (idempotent D-013), POST /stock-moves/{id}/reverse (idempotent), GET /stock-moves (filtered ledger), GET /stock-moves/{id}, GET /stock-on-hand (paginated projection) — permission-guarded. Migration `0021_inventory_stock` (down_revision 0020): four tables + indexes + the on_hand_qty>=0 CHECK, no trigger-bearing alters, portable both engines; zero autogen drift + full downgrade-0020->upgrade-0021 reversibility on PG. NO events.py yet (a 5.2 move publishes nothing cross-module; on-hand intra-module; costing/COGS-via-event arrives in 5.3). +46 tests (test_warehouses 4, test_bins 5, test_stock_moves 18, test_stock_moves_api 11, test_on_hand 4, test_stock_db_guards 2 SQLite + 2 pg) covering receipt/issue/transfer/adjustment, insufficient_stock 422 + CHECK backstop, lot/serial receipt-creates-master + issue-reduces-lot + serial qty1, reversal restores quants, gapless STK numbering, idempotent replay, query-counter-bounded create_move, RBAC 403, tenant isolation, on-hand correctness -> 679 passed + 1 skipped SQLite, 18 pg (incl. the CHECK + with_for_update guards proven on real Postgres 16). ruff + pytest -m "not pg and not perf" + PG reset/upgrade-head + -m pg all green; no stray atlas.db; no file over the 400-line cap.
- [done] 5.3 Inventory costing — moving average AND FIFO per item, with same-transaction COGS via the event bus (the inventory↔finance seam, D-020/D-037/D-011). **Models** (`inventory/models/costing.py`, re-exported): `ItemValuation` (`inv_item_valuations`, UNIQUE(tenant,item,warehouse), CHECK(on_hand_qty>=0); on_hand_qty/avg_unit_cost full-precision/total_value — the moving-average state per (item,warehouse)), `CostLayer` (`inv_cost_layers`, composite tenant_fks to items/warehouses/receipt_move, CHECK(0<=remaining_qty<=original_qty), index (tenant,item,warehouse,received_at) — FIFO layers), `LayerConsumption` (`inv_layer_consumptions`, fks to issue_move + layer, qty+cost — per-layer COGS audit trail + exact-reversal record). All NOT AuditMixin (derived projection of the audited moves). `inv_stock_moves` gains nullable `unit_cost` (MoneyType, batch-added). **Costing service** (split <400 lines): `costing.py` (apply_costing orchestration), `costing_mav.py` (moving-average row math + with_for_update), `costing_fifo.py` (create/consume/reverse layers), `costing_reversal.py` (reverse_costing). MAV: receipt adds qty×cost + recomputes avg=total_value/on_hand **unrounded**; issue subtracts quantize(qty×avg, currency dp, HALF_UP) and at on_hand=0 **flushes residual to price-difference** so value lands at exactly 0 (even for non-terminating averages). FIFO: one layer per receipt, consumed oldest-first by (received_at,created_at,id) under with_for_update (created_at the insertion-order tiebreaker since uuid4 ids aren't monotonic), one consumption row per touched layer, COGS=Σ per-layer quantize(qty×unit_cost). **Cost input rule:** RECEIPT/positive-ADJUSTMENT REQUIRE unit_cost; ISSUE/negative-ADJUSTMENT IGNORE it (engine computes + writes it back); TRANSFER carries current valuation. **Events + COGS handler:** `inventory/events.py` (NEW — declarative `StockValued` carrying tenant/move/item/warehouse/qty/total_cost/residual/the three GL ids + the chosen **offset account** + move_date + document_id; key `inventory.stock.valued`); `create_move` computes cost, updates valuation/layers, then `publish(StockValued)` (buffered) — `run_in_uow` drains before commit and `finance/handlers.py` (the FIRST real cross-module handler) posts the journal **through the finance posting service** (create_draft_entry+post_entry, never raw inserts) in the shared session, links move.document→entry.document ('posts'). One commit = move+quant+valuation+journal; **any handler failure rolls the whole transaction back**. Handler registered at the app factory via NEW `register_event_handlers()` (deterministic D-011 seam, re-runnable+idempotent so the test harness re-registers after clear_subscriptions); the inventory conftest's autouse fixture registers it for service-level tests. **Postings:** RECEIPT/ADJUSTMENT-up Dr inventory/Cr price-difference (standalone offset; procurement GR overrides later); ISSUE Dr COGS/Cr inventory; ADJUSTMENT-down Dr price-difference/Cr inventory; **TRANSFER within one inventory account → NO event, NO journal** (value-neutral); reversal posts the exact reverse of the original's entry. COGS journal posts with move_date → a move into a **closed period fails** (period trigger fires in the same transaction → the move rolls back). **Reads:** `GET /stock-valuations` + `GET /items/{id}/cost-layers` (permission `inventory.valuation.read`); `queries.item_value`/`valuation_summary` (the inventory-value KPI; qty×cost summed in Python, never func.sum(qty×cost) — D-015 trigger discipline avoids the SQLite ×10^12 scaling trap). **Cross-module addition:** `finance/queries.functional_currency_or_none` (the currency the COGS journal posts in; defaults to USD/2dp when unconfigured). Migration `0022_inventory_costing` (down_revision 0021): three tables + the unit_cost batch-alter (inv_stock_moves non-trigger-bearing) + CHECKs + indexes, reversible, zero autogen drift on both engines. **Existing 5.2 tests updated:** build_stock_setup now wires the category GL accounts + an open 2026 fiscal year (the costing preconditions); build_stock defaults unit_cost=1; the create_move query budget bumped to ≤45 (the move now posts its COGS journal in the same transaction — constant, O(1) for a single-layer issue). +30 tests: test_costing.py (7: MAV avg/COGS/zero-qty flush/non-terminating flush, FIFO partial #23-class/exhaustion/item_value), test_costing_events.py (13: same-tx COGS Dr COGS/Cr inventory, forced-handler-failure rolls back the move+quant, receipt/adjustment-down/transfer-no-journal postings, closed-period rollback, FIFO+MAV reversal restores layers/valuation + reverses the journal, RBAC 403, tenant isolation, valuation/cost-layer read-endpoint query budget), test_stock_db_guards.py extended (+3 SQLite/+3 pg: valuation CHECK, layer CHECK, valuation with_for_update) → 709 passed + 1 skipped SQLite, 21 pg (incl. the new CHECK + with_for_update guards on real Postgres 16). ruff (app+alembic+tests) + pytest -m "not pg and not perf" + PG downgrade-base/upgrade-head + -m pg all green; no stray atlas.db; no file over the 400-line cap. DECISIONS D-037 recorded; docs/modules/inventory.md costing section added.
- [done] 5.4 Inventory physical & cycle counts with variance posting (D-038) — **completes Phase 5 / Inventory**. A count captures counted quantities per (item,bin,lot), compares to live on-hand and posts the differences as stock ADJUSTMENT moves (which flow through the 5.3 costing engine → the price-difference journal via the event bus). `constants.py`: CountType (PHYSICAL|CYCLE — physical = whole-warehouse snapshot, cycle = chosen items/bins), CountStatus (DRAFT→COUNTING→POSTED|CANCELLED), three permissions (count read/manage/post), the `inventory.count` sequence (CNT-2026-NNNNN, claimed at creation), STOCK_COUNT_DOC_TYPE + the `counts` docflow link, COUNT_POST_SYNC_MAX_VARIANCES (200) + COUNT_POST_JOB. **Models** (`inventory/models/counts.py`, re-exported): `StockCount` (`inv_stock_counts`, DocumentMixin + count_number, count_type, warehouse_id composite tenant_fk, status, count_date, description, posted_at; (tenant,warehouse)+(tenant,status) indexes; Audited), `StockCountLine` (`inv_stock_count_lines`, count_id/item_id/bin_id composite tenant_fks + nullable lot_id + nullable adjustment_move_id fk, line_number, system_qty snapshot, counted_qty/variance_qty/unit_cost; UNIQUE(tenant,count,item,bin,lot), (tenant,count) index; NOT AuditMixin — the count + each generated move are audited, so the lines would double-record). **Service split writes-vs-reads from the start** (the prior 566-line single file was rejected): `service/counts.py` (398 ln — create_count+populate snapshots one line per in-scope quant; record_counted → COUNTING; post_count = the heart; cancel_count) + `service/count_reads.py` (get/list/lines/current_system_qty/variance_preview) + `service/count_jobs.py` (the background handler). **post_count (D-038):** for each line RE-READS live on-hand as the authoritative system qty (NOT the stale snapshot — concurrency safety), computes variance = counted − live; zero → skip (adjustment_move_id NULL); else posts ONE ADJUSTMENT move via stock_moves.create_move (positive → into the bin at the item's current book cost from `queries.current_unit_cost` = MAV avg / FIFO live-layer weighted avg; negative → out of the bin, engine computes COGS), links count.document→move.document ('counts'), stores variance_qty+adjustment_move_id+unit_cost. Runs in run_in_uow so every variance move's journal + the count are ONE transaction — a closed-period count_date trips the period trigger and rolls the whole post back. POSTED is terminal (re-post rejected, never cancelled; corrections are new counts). Posting requires every line counted (else 422). **Schemas split** to a sibling `count_schemas.py` (the assets_schemas.py precedent, keeping schemas.py under cap). **`count_router.py`** (sibling mounted into router.py, one surface): POST /stock-counts (create+snapshot, idempotent), GET /stock-counts (paginated, status/warehouse/type filter), GET /stock-counts/{id}, GET /stock-counts/{id}/lines (paginated), POST /stock-counts/{id}/lines/{line}/count, GET /stock-counts/{id}/variance-preview, POST /stock-counts/{id}/post (idempotent; ≤200 snapshot-variance lines inline 200, larger → `inventory.count_post` job 202 {job_id} — the depreciation/bank-import threshold precedent, IMPLEMENTED not deferred), POST /stock-counts/{id}/cancel — permission-guarded (manage for create/count/cancel, post for the privileged post). `queries.current_unit_cost` added (the positive-adjustment book cost source). Migration `0023_inventory_counts` (down_revision 0022): two tables + FK/filter indexes, no trigger-bearing alters, portable both engines, every identifier ≤63 chars, applies clean on SQLite with zero autogen drift. +23 tests: test_counts.py (14: physical-snapshots-all/cycle-snapshots-chosen, variance preview, positive-variance ADJUSTMENT-in + price-difference journal + count→move docflow link + on-hand=counted, negative-variance ADJUSTMENT-out, zero-variance no move, **live-on-hand re-validation** (a move after snapshot before post → posted on-hand = counted not counted−stale), all-counted-or-422, re-post rejected, cancel draft works / posted rejected, closed-period rolls back, lot-tracked adjusts the right lot, tenant isolation), test_counts_api.py (9: full lifecycle, all-counted 422, re-post idempotent replay + 409 + no double adjustment, create idempotent, cancel, RBAC manage-can't-post + read-can't-create, tenant isolation, list query budget ≤3) → 738 passed + 1 skipped SQLite. ruff (app+alembic+tests) + pytest -m "not pg and not perf" all green; no stray atlas.db; no file over the 400-line cap (counts.py 398, count_reads.py 188, count_schemas.py 106, count_router.py 280). **pg path (`-m pg`) NOT run: the Docker daemon is down in this environment (port 55432 unreachable) — the migration is portable/reversible with convention-matching constraint names verified by the SQLite drift test, so it will apply on PG as the prior 22 did; the pg run must be re-executed once the daemon is available.** DECISIONS D-038 recorded; docs/modules/inventory.md counts section added; PLAN 5.4 ticked (Phase 5 / Inventory COMPLETE).
- [done] 6.1 Vendor master + procurement module skeleton — **OPENS Phase 6 / Procurement** (third business module; `backend/app/modules/procurement/`, reads finance/queries + inventory/queries downward, permissions registered at import). `constants.py` (single file): `VendorStatus` (ACTIVE|BLOCKED|INACTIVE — transitions UNRESTRICTED, no terminal state so vendor history stays referenceable; 6.2 reads status to refuse POs against non-ACTIVE vendors), `DEFAULT_PAYMENT_TERMS_DAYS=30`, two permissions (`procurement.vendor.read`/`.manage` — approved-item mgmt rides `.manage`, the nested-config-shares-parent-key precedent). **Payment-terms decision:** a plain `payment_terms_days` net-days integer ON THE VENDOR (CHECK ≥0, default NET30), NOT a terms master — the simplest model matching how AP computes due dates (bill_date + days); richer schedules (2/10 NET30) deferred per parity. **Vendor-code decision:** user-supplied, UNIQUE(tenant,vendor_code), no auto-sequence (item_code/account-code precedent — a master carries a code, not a gapless number). `models.py` (single file, 2 tables): `Vendor` (`proc_vendors`; vendor_code/name/status/default_currency_code(str3)/payment_terms_days/tax_reference/email/phone/address/notes; (tenant,status) index for the filtered list; the row id **IS** finance AP's opaque `partner_id` per D-029), `VendorApprovedItem` (`proc_vendor_approved_items`; vendor_id composite tenant_fk to proc_vendors + **item_id OPAQUE inventory id** — plain Uuid, NO cross-module FK per D-029, validated via inventory/queries.item_exists — + vendor_item_code + is_active; UNIQUE(tenant,vendor,item) + (tenant,vendor) index; the v1 info-record-lite, NO price/lead-time/valid-from-to). `schemas.py` (Create/Update/Read/Filter for both; all scalars, no money/qty; payment_terms_days ge=0 + currency min/max_length=3 at the schema boundary). `service.py` (single file: vendor CRUD with code-unique ConflictError + currency-exists ValidationFailedError via finance/queries; status transitions unrestricted; approved-item add/remove/list with item-exists validation + no-duplicate ConflictError). `queries.py` (the ONLY file other modules import): get_vendor, **get_vendor_for_partner** (AP partner_id→vendor resolution, D-029), vendor_exists, vendor_payment_terms_days, vendor_default_currency, is_item_approved_for_vendor (active-only). `router.py` (thin, /api/v1/procurement: vendors CRUD + paginated/status-filtered/ETag list, nested approved-items GET/POST + DELETE by item id; permission-guarded; writes through run_in_uow) mounted in main.py after inventory. **Sanctioned cross-module addition:** `finance/queries.currency_exists(session,tenant,code)` (by-code existence check the vendor master validates default_currency_code against, mirroring account_exists); nothing added to inventory/queries (item_exists sufficed). NO events.py/handlers.py (vendor master drives no cross-module effects — those come with the P2P documents in 6.2+). Migration `0024_procurement_vendors` (down_revision 0023): two tables + FK/filter indexes + the payment_terms CHECK, no trigger-bearing alters, portable both engines, every identifier ≤63 chars, applies clean on SQLite with zero autogen drift; env.py model import added. +32 tests (tests/modules/procurement/: conftest+factories reusing shared tenant/auth + seeding a finance currency + a real inventory item so approved-item validation has a target; test_vendors.py 11 = CRUD/defaults, code-unique, currency-exists, neg-terms-rejected-by-schema, currency-revalidated-on-update, status-transitions-unrestricted, queries interface; test_approved_items.py 7 = add/list, unknown-item-422, no-duplicate, unknown-vendor-404, remove+re-remove-404, is_item_approved active/inactive; test_vendors_api.py 14 = CRUD over wire, unknown-currency 422, status filter, approved-item endpoints, query budget ≤3, ETag+304+invalidation, RBAC read-can't-manage + 401, tenant isolation, cross-tenant ETag, payment-terms ≥0 accepted). ruff (app+alembic+tests) + pytest -m "not pg and not perf" green; pg path (`-m pg`) verified against the local Postgres 16 at :55432 (schema reset → alembic upgrade head through 0024 → pg tests green). docs/modules/procurement.md added (NEW module guide); parity Procurement rows (vendor master FULL, info records PARTIAL) annotated Done; DECISIONS D-039 recorded; PLAN 6.1 ticked.
- [done] 6.2 Requisition → RFQ → PO chain + data-driven approval-threshold rules (D-040) — extends Procurement with the three numbered P2P documents and the value-threshold release engine. **Package splits at the 400-line cap (behaviour-preserving, re-exported so import sites are unchanged):** `models.py`→`models/` (vendors+requisitions+rfqs+orders+approvals), `schemas.py`→`schemas/` (per-document + approvals), `service.py`→`service/` (vendors+requisitions+rfqs+orders+conversions+approvals + private `_shared.py` validation/numbering helpers); `router.py` now owns the vendor master and MOUNTS sibling `requisition_router`/`rfq_router`/`po_router`/`approval_rule_router` (the finance ap_router precedent, one mount in main.py). `constants.py`: RequisitionStatus (DRAFT→SUBMITTED→APPROVED|REJECTED, +CANCELLED, +CONVERTED), RfqStatus (DRAFT→SENT→QUOTED→CLOSED, +CANCELLED), PurchaseOrderStatus (DRAFT→PENDING_APPROVAL|APPROVED→SENT, +REJECTED/CANCELLED set in 6.2; PARTIALLY_RECEIVED/RECEIVED/CLOSED declared as the lifecycle but driven by 6.3 GR), ApprovalDecision, ApprovalDocumentType (REQUISITION|PURCHASE_ORDER), three doc-types + year-reset sequences (PR-/RFQ-/PO-, claimed AT CREATION per D-040), docflow links ('sourced_by' req→rfq, 'ordered_by' rfq→po + req→po), nine permission keys (requisition/po read·manage·approve, rfq read·manage, approval_rule.manage). **Models (7 tables):** PurchaseRequisition/Line, Rfq/Line (vendor_id composite tenant_fk — ONE vendor in v1; source_requisition_id nullable), PurchaseOrder/Line (vendor_id + source_requisition_id + source_rfq_id; payment_terms_days snapshot; total_amount maintained; line_amount=qty×unit_cost; received_quantity default 0 + opaque tax_code_id staged for 6.3/6.4; (tenant,status) + (tenant,vendor,status) indexes per PERFORMANCE §1), ApprovalRule (UNIQUE(tenant,document_type), threshold_amount CHECK≥0). Each header DocumentMixin (registered in core_documents) + gapless number claimed at creation. **Service rules:** requisition create/submit(approval-gated on estimated total)/decision/update(DRAFT only)/cancel; RFQ create/send/record-quote(SENT→QUOTED)/close; PO create(vendor must be ACTIVE + every item APPROVED for the vendor else 422 procurement.vendor_not_active / procurement.item_not_approved — the v1 source-control rule)/send(approval-gated on total_amount → PENDING_APPROVAL or auto-approve; only APPROVED can be SENT)/decide/cancel; conversions req→rfq, req→po (unit_cost from estimate), rfq→po (unit_cost from quote) copy lines + link docflow + set source ids + advance predecessor (req→CONVERTED, rfq→CLOSED); `requires_approval(doc_type,amount,currency)` = single-active-rule lookup, True iff amount≥threshold AND currency matches, NO rule/inactive/currency-mismatch ⇒ no gate. **queries.py additions** (the 6.3/6.4 contract): get_purchase_order, po_line_open_quantity (ordered−received), get_po_for_receipt (header+lines, no N+1), open_po_lines_for_vendor. **NO events.py** — nothing in 6.2 posts to finance/inventory (a PO is a commitment; the first procurement event is the 6.3 goods receipt — no-stub rule). Idempotency (D-013) on every create/convert/submit/approve endpoint; lists paginated + status/vendor filtered + ≤3-query budget; the chain renders via core GET /api/v1/documents/{id}/chain. Migration `0025_procurement_documents` (down_revision 0024): seven tables + indexes, no trigger-bearing alters, portable both engines, every identifier ≤63 chars, zero autogen drift (`alembic check`), applies on PG to head. **+38 tests** (test_requisitions 10, test_rfqs 6, test_purchase_orders 11, test_approval_rules 6, test_procurement_api 5: full chain+docflow over the wire, RBAC manage-vs-approve 403, ≤3 list budget, create-idempotent replay, tenant isolation, PO send-gating then approve→send) → procurement 70 tests; **835 passed + 1 skipped SQLite, 21 pg** (schema reset → ATLAS_DATABASE_URL alembic upgrade head → -m pg green). ruff (app+alembic+tests) clean; no stray atlas.db; STRUCTURE §9 self-check clean (no file >400 lines — largest service/orders.py 372; no orphans; no supplier/vendor term drift; dependency rules intact). DECISIONS D-040 recorded; docs/modules/procurement.md extended (the chain, the threshold rule, the approved-items enforcement, status lifecycles, what 6.3/6.4 add); parity Procurement rows annotated (requisitions/RFQs/POs FULL, approval strategies PARTIAL data-driven-thresholds-only); PLAN 6.2 ticked.
- [done] 6.3 Goods receipt → stock move + GR/IR journal via the event bus (D-041) — the procurement→inventory→finance integration. **finance:** new `gr_ir_clearing` posting purpose (constants/documents.py, added to POSTING_PURPOSES + the constants package re-exports) + `finance/queries.gr_ir_clearing_account` (delegates to posting_defaults.get_posting_default; raises 422 `finance.posting_default_unmapped` when a tenant hasn't mapped it — a GR can't post without it). **inventory:** `create_move` gains an optional `valuation_offset_account_id` threaded `create_move → costing.apply_costing → _apply_inbound → StockValued.offset_account_id → finance/handlers` — for a RECEIPT it OVERRIDES the standard price-difference offset (default None ⇒ unchanged, standalone-receipt tests stay green); `inventory/queries.bin_exists` added (D-029 bin validation); **`inventory/handlers.py`** (NEW — inventory's first handler) subscribes to `procurement.goods_receipt.posted` and creates the RECEIPT moves with the GR/IR offset + links GR→`moved_by`→move, the mirror of the inventory→finance COGS pattern. **procurement:** `GoodsReceiptStatus` (DRAFT→POSTED|CANCELLED — a GR is built then posted, unlike a permanent-at-creation move; POSTED terminal), three permissions (goods_receipt read/manage/post), `procurement.goods_receipt` sequence (GR-2026-NNNNN claimed at creation), GR doc-type + docflow links ('received_by' PO→GR, 'moved_by' GR→move), `GoodsReceiptPosted` event key; `models/goods_receipts.py` (`proc_goods_receipts` header — DocumentMixin + gr_number/status/purchase_order_id composite FK/vendor_id snapshot/warehouse_id opaque/receipt_date/posted_at, (tenant,status)+(tenant,po) indexes, Audited — + `proc_goods_receipt_lines` — po_line_id composite FK/item_id+bin_id opaque/received_quantity/unit_cost snapshot/lot_code/serial_code/requires_inspection; UNIQUE(tenant,gr,line); **NO stock_move_id column — the GR↔move link is docflow per D-041**); `events.py` (NEW: `GoodsReceiptPosted` + `GoodsReceiptMove` payload); `schemas/goods_receipts.py` (Create/Detail/Line/Filter); `service/goods_receipts.py` (373 ln — create_goods_receipt validates PO receivable + line-on-PO + qty≤open (over-receipt → 422 `procurement.over_receipt`) + bin exists, snapshots item/cost; **post_goods_receipt** the heart: resolves GR/IR up front, raises each PO line received_quantity, advances PO status PARTIALLY_RECEIVED/RECEIVED, links PO→GR, sets POSTED+posted_at, publishes GoodsReceiptPosted — all in run_in_uow so GR + N moves + N inventory-debit/GR-IR journals + PO update are ONE transaction; closed-period receipt_date rolls it all back; re-post rejected; cancel DRAFT-only) split from reads `service/goods_receipt_reads.py`; `queries.get_goods_receipt`/`goods_receipts_for_po` added (the 6.4 contract); sibling `goods_receipt_router.py` mounted into router.py (POST /goods-receipts idempotent, GET list paginated+po/status filter, GET /{id}, POST /{id}/post idempotent, POST /{id}/cancel — permission-guarded manage-vs-post). **Inspection hook = FLAG ONLY in v1** (requires_inspection per line, passed to the move for traceability, no QI-bin routing / no blocking — Phase 9 adds disposition). Inline post (no background path — a GR is one delivery). Migration `0026_procurement_goods_receipts` (down_revision 0025): two tables + indexes, NO trigger-bearing alters (gr_ir_clearing purpose is data, valuation-offset is a service param — neither is schema), reversible, portable both engines, every identifier ≤63 chars. **+33 tests** — test_goods_receipts.py (13: draft+no-stock, over-receipt 422, unreceivable-PO 422, post moves stock + Dr Inventory/Cr GR-IR journal (not price-diff), received_quantity raised + PARTIALLY_RECEIVED→RECEIVED, docflow PO→GR→move chain, closed-period whole-rollback, lot receipt creates lot + lands stock, serial receipt creates serial, requires_inspection stored, re-post rejected, cancel-draft + posted-terminal, unmapped-GR/IR 422 + no move), test_goods_receipts_api.py (8: create+post over the wire + on-hand up, over-receipt 422, post idempotent, docflow chain, list pagination + ≤3 budget, list filter by PO, post needs .post key (manage-only 403), tenant isolation 404) + test_costing_override.py (2: receipt without override → price-diff, with override → GR/IR, price-diff untouched). **864 passed + 1 skipped SQLite; 21 pg** (schema reset at :55432 → alembic upgrade head through 0026 → downgrade base → upgrade head reversibility proof → -m pg green). ruff (app+alembic+tests) clean; no stray atlas.db; **STRUCTURE §9 self-check clean: §5 verified — procurement imports ONLY finance/queries + inventory/queries (grep: no inventory/finance service.py or models.py import); inventory/handlers imports only procurement events.py + the moved_by link constant (the finance/handlers→inventory.constants precedent); no file >400 lines (goods_receipts.py 373, stock_moves.py re-trimmed to 399, costing.py 397); no orphans; no supplier/vendor term drift; single GoodsReceipt definition.** DECISIONS D-041 recorded; docs/modules/procurement.md (goods-receipt section: the three-way GR/IR flow, event-bus mechanism, docflow linkage, inspection-flag-v1, over-receipt-rejected, closed-period rollback) + docs/modules/inventory.md (valuation-offset override) updated; PLAN 6.3 ticked.
- [done] 6.4 3-way invoice match → AP bill + reorder-point auto-requisitions (D-042) — **COMPLETES Phase 6 / Procurement.** The procure-to-pay closing step: an `InvoiceMatch`/`InvoiceMatchLine` (`proc_invoice_matches`/`_lines`, gapless `MATCH-` number) compares a vendor invoice against the PO (price) + goods receipt (quantity); on POST it PUBLISHES `InvoiceMatched` and `finance/handlers.create_bill_for_match` creates+posts the AP vendor bill (**Dr GR/IR at PO cost + Dr/Cr PPV + Dr input tax / Cr AP control**) in the SAME transaction — so GR/IR (credited at receipt) nets to ZERO and the price variance lands in the new `purchase_price_variance` account (procurement never imports finance/service — the event-bus §5 mechanism, mirroring 6.3). Over-billing beyond received−billed → 422 (a `billed_quantity` counter on the PO line); a single-per-tenant `proc_match_tolerances` band ⇒ MATCHED/EXCEPTION (EXCEPTION blocks posting until OVERRIDDEN — the invoice-release control); PO advances to CLOSED when fully received+billed; docflow PO→match→bill chain; closed-period rolls the whole post back; idempotent. Part B: inventory's `items_below_reorder_point` (set-based) feeds `run_reorder_scan`, which creates DRAFT requisitions (qty = reorder_quantity) and dedups against open requisition lines (`POST /reorder-scan`). New finance posting defaults `purchase_price_variance`/`ap_control` + queries; `finance/queries.purchase_price_variance_account`/`ap_control_account`. Service split invoice_matches (create) / invoice_match_post / invoice_match_reads at the cap; sibling `invoice_match_router`. Migration **0027_procurement_invoice_matches** (3 tables + batch-add `billed_quantity`, no trigger-bearing alters, zero autogen drift SQLite + PG). **STRUCTURE §9 self-check: PASS** — no file over the 400-line cap; procurement imports ONLY finance/queries + inventory/queries (no service/models, §5 verified by grep); finance/handlers imports only procurement events + the `billed_by` link constant (the sanctioned events.py allowance); no orphan/duplicate/term-drift. ruff clean; pytest **898 passed / 1 skipped** on SQLite (`-m "not pg and not perf"`, +25 new 6.4 tests) and **21 passed** on Postgres (`-m pg`); alembic upgrade head + `alembic check` zero drift on both engines; stray atlas.db removed.
- [done] 7.1 Sales customer master + condition-style pricing (D-043) — **OPENS Phase 7 / Sales** (the fourth business module). **Four tables** (migration `0028_sales_customers_pricing`, down_revision 0027, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids): `sales_customer_groups` (lean grouping master, UNIQUE(tenant,code)), `sales_customers` (UNIQUE(tenant,customer_code); nullable composite FK to the group; CHECKs payment_terms_days≥0 + credit_limit≥0; (tenant,status) index — **the row id IS finance AR's opaque partner_id, D-029, the vendor mirror**), `sales_price_lists` (condition header: currency + nullable composite-FK group + [valid_from,valid_to] window + status + priority; CHECKs priority≥0 + valid window; the (tenant,currency,group,valid_from) resolver index), `sales_price_list_items` (ONE base unit_price per (list,item), UNIQUE(tenant,list,item), min_quantity floor; item_id OPAQUE/no FK; (tenant,list)+(tenant,item) indexes). **models/** package (customers.py: `CustomerGroup`+`Customer`; pricing.py: `PriceList`+`PriceListItem`) + **service/** package (customers/customer_groups/pricing CRUD + **price_resolution.py** the engine) from the start. **credit_limit semantics (D-043):** non-null MoneyType, **0 = cash-only** (no open credit — 7.2 blocks any positive outstanding AR), positive = the ceiling, **no NULL/unlimited sentinel** (unlimited = an explicit large value), so 7.2's check is a single comparison; the static block parity scopes to v1 (no exposure ledger). **customer_group = a MASTER table** (not a free string) because pricing keys on it — customer + price list reference the same group rows. **Customer codes user-supplied + unique per tenant** (the vendor/item-code precedent; the master carries no gapless number). **The condition resolver** `service.price_resolution.resolve_price` (exposed via `sales/queries.resolve_price`, the public surface 7.2 prices each line through): a list APPLIES when status ACTIVE + currency matches + on_date in the inclusive window + (general OR the customer's group) + a price row with min_quantity≤qty; the winner is picked by the **DETERMINISTIC order: highest priority → most specific (group beats general) → latest valid_from → list id** (a final stable tiebreaker, never row-order). No match → matched=False (7.2 then needs a manual price). **NO discount applied** (base price only; line discounts `DiscountType` PERCENT/AMOUNT defined for 7.2). **Bounded to TWO queries** regardless of list count (candidate lists + their items; no N+1, asserted by query_counter). schemas.py (+ a `PriceQuoteRead`) + queries.py (`get_customer`/`get_customer_for_partner`/`customer_exists`/`customer_status`/`customer_credit_limit`/`customer_payment_terms_days`/`customer_default_currency`/`resolve_price`). `router.py` (customers + customer-groups) mounts sibling `pricing_router.py` (price lists + nested items + `GET /price-quote`) under one `/api/v1/sales` surface; mounted after procurement in main.py (the D-011 order); env.py imports sales.models for autogenerate. Reference lists carry collection ETags (D-035) + the ≤3 budget. Permissions `sales.customer.read/.manage` (groups ride customer.manage) + `sales.pricelist.read/.manage`. **NO events.py/handlers.py** — 7.1 masters + pricing drive no cross-module effects (orders in 7.2 will); cross-module reads are `finance/queries.currency_exists` + `inventory/queries.item_exists` downward only. **+60 tests** — test_customer_groups.py (4), test_customers.py (8: CRUD + validation code-unique/currency-exists/terms&credit≥0/group-exists/status-transitions + the queries read surface), test_pricing.py (12: list+item CRUD + validation), test_price_resolution.py (14: clean match, no-match, wrong-currency/expired/future/INACTIVE/wrong-group/ungrouped exclusions, min_quantity floor, group-beats-general, priority-beats-specificity, latest-valid_from tie, **bounded ≤3 queries**), test_customers_api.py (11) + test_pricing_api.py (11: endpoints + RBAC + pagination + ≤3 budget + ETag/304 + price-quote + tenant isolation). **970 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +60 from 910); **21 pg** (schema reset at :55432 → alembic upgrade head through 0028 → downgrade base → upgrade head reversibility proof → `-m pg` green). ruff (app+alembic+tests) clean; stray atlas.db removed. **STRUCTURE §9 self-check: PASS** — no sales file >400 lines (largest pricing_router 244); sales imports ONLY finance/queries + inventory/queries downward (grep: no finance/inventory service.py or models.py import); single ORM definition per model; no orphan/empty files (no events.py/handlers.py since 7.1 has no cross-module effects); no supplier/product/sku term drift. **No additions to finance/inventory queries** (reused existing `currency_exists` + `item_exists`). DECISIONS D-043 recorded; docs/modules/sales.md (NEW module guide); PLAN 7.1 ticked.
- [done] 7.2 Quote → Order with ATP check + credit-limit block at confirmation (D-044) — the O2C spine, mirrored on the procurement requisition/PO chain. **Four tables** (migration `0029_sales_orders`, down_revision 0028, no trigger-bearing alters, reversible on PG, portable both engines, ≤63-char ids, zero autogen drift): `sales_quotes`/`sales_quote_lines` (`Quote`/`QuoteLine`, DocumentMixin + gapless `QUO-2026-NNNNN` claimed at creation, customer_id composite tenant_fk, valid_until, total_amount maintained; lines opaque item/uom + unit_price + optional discount + line_amount; (tenant,status)+(tenant,customer,status) indexes) and `sales_orders`/`sales_order_lines` (`SalesOrder`/`SalesOrderLine`, DocumentMixin + gapless `SO-2026-NNNNN`, customer_id composite FK, nullable source_quote_id composite FK, payment_terms_days snapshot, credit_check_status nullable; lines ordered/delivered/invoiced quantities + opaque tax_code_id staged for 7.3/7.4). **constants.py**: `QuoteStatus` (DRAFT→SENT→ACCEPTED/REJECTED, +EXPIRED on lapse, +CONVERTED, +CANCELLED — all set in 7.2), `SalesOrderStatus` (DRAFT→CONFIRMED|CREDIT_BLOCKED, +CANCELLED set in 7.2; PARTIALLY_DELIVERED/DELIVERED 7.3, INVOICED 7.4, CLOSED — full lifecycle declared), `CreditCheckStatus` (PASSED/BLOCKED/RELEASED), permissions `sales.quote.read/.manage` + `sales.order.read/.manage/.confirm/.credit_release` (confirm distinct = runs ATP+credit gate; credit_release distinct = the override), two doc-types + year-reset sequences, the `converted_to` docflow link. **schemas.py → schemas/ package** (masters.py + orders.py, split at the cap, re-exported so import sites unchanged): quote/order Create/Update/Read/Detail/Filter (+ lines), `ConvertQuoteToOrder`, the ATP request/response schemas. **service/ package** adds `_shared.py` (validation + numbering + the line-pricing/discount helper `build_line_input`/`compute_line_amount`), `quotes.py` (create/send/accept/reject/update(DRAFT)/cancel + lazy expiry), `orders.py` (create-from-scratch + the shared writer + update/cancel + reads), `order_confirm.py` (the ATP+credit gate, 244 ln), `conversions.py` (quote→order). **ATP = INFORMATIONAL availability + a backorder FLAG, NOT a block** (parity v1): `atp_check` = on-hand (`inventory/queries.total_on_hand`) − committed + on-order (NEW `procurement/queries.open_incoming_quantity`, ordered−received over APPROVED/SENT/PARTIALLY_RECEIVED POs, set-based); a short line is flagged backordered but confirm STILL succeeds (CONFIRMED). **The committed-quantity reservation = a QUERY** (`committed_quantity`, ordered−delivered over CONFIRMED/PARTIALLY_DELIVERED order lines, no table) — confirming commits the undelivered qty against the next order's ATP, 7.3's delivery shrinks it. **Credit = the HARD block at confirmation**: exposure = open AR (`customer_open_ar` → finance's REUSED `customer_open_balance`, POSTED-invoice open_amount keyed by the opaque partner_id) + other open confirmed orders (`open_confirmed_order_value`) + this order's total; > `credit_limit` (0 = cash-only) → CREDIT_BLOCKED/BLOCKED (not confirmed); within → CONFIRMED/PASSED; a `sales.order.credit_release` user → RELEASED then re-confirm (skips the gate) → CONFIRMED. Confirm idempotent (re-confirm a no-op). **NO events.py** — a confirmed order is a commitment (like a PO), no finance/inventory posting; 7.3's delivery is the first sales event (no-empty-file rule). `sales/queries` adds get_sales_order, so_line_open_to_deliver, get_order_for_delivery (the 7.3 contract), committed_quantity, atp_check, open_confirmed_order_value, customer_open_ar. `router.py` mounts sibling `quote_router` (quotes CRUD + send/accept/reject/cancel, create/send/accept/reject idempotent) + `order_router` (orders CRUD + convert-from-quote + confirm + credit-release + cancel + the ATP endpoint; create/convert/confirm/release idempotent); permission-guarded (manage vs confirm vs credit_release); lists paginated + status/customer filtered + ≤3 budget. **+34 tests** — test_quotes.py (12: numbering+totals, resolver price defaulting, no-price→422, percent/amount discounts, send/accept/reject lifecycle, lazy expiry, convert→order with docflow `converted_to` edge, convert-requires-accepted), test_sales_orders.py (8: numbering+terms-snapshot+total, blocked-customer→422, discount, update-replaces-lines, cancel, so_line_open_to_deliver, committed-quantity-only-confirmed, update-non-draft-rejected), test_order_confirm.py (9 THE key tests: ATP available→PASSED, ATP short→CONFIRMED+backordered-flag, on-order counts toward availability, committed-quantity reduces next ATP, credit within→PASSED, credit exceeded→CREDIT_BLOCKED, credit-release→RELEASED→CONFIRMED, release-requires-blocked, confirm idempotent), test_sales_api.py (10: quote→order chain over the wire + source_quote_id + quote CONVERTED, confirm PASSED, credit-block(cash-only)→release with the no-credit_release user 403 + the authorised release, ATP endpoint, confirm-requires-confirm-key 403, order-list ≤3 budget, quotes paginate, create idempotent replay, tenant isolation 404). **1019 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +49 from 970); **21 pg** (schema reset at :55432 → alembic upgrade head through 0029 → downgrade 0028 → upgrade head reversibility proof → `-m pg` green). ruff (app+alembic+tests) clean; stray atlas.db removed. **STRUCTURE §9 self-check: PASS** — no sales file >400 lines (largest queries.py 378); §5 verified by grep: sales imports ONLY finance/inventory/procurement QUERIES, never their service/models; no `text(`/Core `.insert(` under sales (D-007 gate); no events.py/handlers.py (7.2 has no cross-module effects); single ORM definition per model; no term drift. **Cross-module addition:** `procurement/queries.open_incoming_quantity` (the ATP on-order side) — a sanctioned downward read; `customer_open_balance` (finance) + `total_on_hand` (inventory) reused, no new finance/inventory queries. DECISIONS D-044 recorded; docs/modules/sales.md (the quote→order chain, the ATP model, the credit-block exposure + release, the reservation approach, status lifecycles, what 7.3/7.4 add) + API table extended; parity Sales rows annotated (quotations/order management FULL, ATP/credit PARTIAL as scoped); PLAN 7.2 ticked.
- [done] 7.3 Delivery → stock issue + COGS journal via the event bus (D-045) — the outbound twin of the 6.3 goods receipt; the first sales document that moves stock + posts to the GL. **Two tables** (migration `0030_sales_deliveries`, down_revision 0029, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogen drift SQLite + PG): `sales_deliveries` (`Delivery`, DocumentMixin + gapless `DN-2026-NNNNN` claimed at creation, status, sales_order_id composite tenant_fk, customer_id snapshot, warehouse_id opaque, delivery_date/posted_at; (tenant,status)+(tenant,sales_order_id) indexes) + `sales_delivery_lines` (`DeliveryLine`, sales_order_line_id composite FK, item_id **snapshot from the order line**, bin_id opaque source bin, quantity, lot_code/serial_code; **NO stock_move_id — the delivery↔move link is docflow per D-045**). **constants.py**: `DeliveryStatus` (DRAFT→POSTED|CANCELLED — built then posted, POSTED terminal), permissions `sales.delivery.read/.manage/.post` (post distinct = issue stock + post COGS, the goods_receipt.post precedent), the `DN-` doc-type + year-reset sequence, the `delivered_by` (order→delivery) docflow link. **models/deliveries.py** + **schemas/deliveries.py** (Create/Line/Read/Detail/Filter) + **`events.py` (NEW — the first sales event)**: `DeliveryShipped` + `DeliveryMove` payload. **service/** adds `deliveries.py` (create/cancel + the create-validation helpers — order deliverable, line-on-order, qty≤open-to-deliver → 422 `sales.over_delivery`, bin exists, bin holds enough → 422 `sales.insufficient_stock`; unconfirmed order → 422 `sales.order_not_confirmed`), **`delivery_post.py`** (the post heart + the post-only `_resolve_tracking_ids`/`_advance_order_status` helpers, **split from deliveries.py at the 400-line cap**, re-exported from the package so `service.post_delivery` is unchanged), `delivery_reads.py` (keyset list + filters + point reads). **post_delivery** the heart: raises each order line `delivered_quantity`, advances the order PARTIALLY_DELIVERED/DELIVERED, links order→`delivered_by`→delivery, sets POSTED+posted_at, and PUBLISHES `DeliveryShipped` — all in run_in_uow so delivery + N ISSUE moves + N COGS journals + the order update are ONE transaction. **Stock issue → COGS via the event bus (§5 / D-045):** sales never calls inventory's service; **inventory's `handlers.py`** subscribes to `sales.delivery.shipped`, creates the stock ISSUE moves (which publish `StockValued` so finance posts **Dr COGS / Cr Inventory** at moving-average cost) + links delivery→`moved_by`→move — the mirror of the 6.3 receipt chain. **NO valuation-offset override** (vs 6.3's GR/IR): an ISSUE move's DEFAULT offset is the item-category COGS account, so **COGS *is* the issue offset** (D-045/D-020) — no override on the event. Closed-period `delivery_date` (move's journal period trigger) or insufficient stock (move's no-negative guard) rolls the WHOLE post back (delivery stays DRAFT); re-post rejected; cancel DRAFT-only (POSTED terminal → corrected by 7.4 RMA). The delivered qty leaves `committed_quantity`, so the 7.2 ATP commitment shrinks after a post. `sales/queries` adds `so_line_open_to_invoice`/`get_order_for_invoice` (the 7.4 contract). `router.py` mounts sibling `delivery_router.py` (POST /deliveries idempotent, GET list paginated + sales_order_id/status filter, GET /{id}, POST /{id}/post idempotent, POST /{id}/cancel — permission-guarded manage-vs-post). Inline post (a delivery is one shipment). **+27 tests** — test_deliveries.py (13: draft+DN+no-stock, over-delivery 422, unconfirmed-order 422, insufficient-bin 422, post issues stock + Dr COGS/Cr Inventory journal, partial→PARTIALLY_DELIVERED then full→DELIVERED backorder closed, docflow order→delivery→move chain, post-insufficient-stock rejected, closed-period whole-rollback, re-post rejected, lot delivery issues the lot, cancel-draft + posted-terminal, committed-quantity shrinks after delivery), test_deliveries_api.py (14: create DRAFT 201 + snapshot item, GET +lines, over-delivery 422 envelope, order-not-confirmed 422 envelope, post→200 POSTED + order delivered_quantity rose + PARTIALLY_DELIVERED + order→delivery→move docflow chain over the wire, full delivery→DELIVERED, create idempotent, post idempotent no-double-issue, list paginate + filter by order/status, list ≤3 query budget, create-needs-manage 403, post-needs-post-key 403, 401 unauthenticated, tenant-B 404 read/post). **Issue #53 filed** for the post-failure aiosqlite-connection caveat: a handler-raised post failure (insufficient stock) can leave the test connection unusable, so the API 422 cases test CREATE-validation failures (over-delivery, not-confirmed) and the happy post asserts success + advanced state only (the closed-period rollback — a DB-trigger failure, connection stays usable — proves the all-or-nothing rollback at the service level). **1052 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +33 from 1019); **pg green** (schema reset at :55432 → alembic upgrade head through 0030 → downgrade base → upgrade head reversibility proof → `-m pg` green; `alembic check` zero drift). ruff (app+alembic+tests) clean; stray atlas.db removed. **STRUCTURE §9 self-check: PASS** — no sales file >400 lines (deliveries.py 262, delivery_post.py 191, queries.py condensed to 399); §5 verified by grep: sales imports ONLY inventory/finance/procurement QUERIES + the event bus, never their service/models; inventory/handlers imports only sales events.py + the moved_by link constant; single ORM definition per model; no term drift. DECISIONS D-045 recorded; docs/modules/sales.md (the Delivery section: outbound-twin design, partial shipments + backorders, the stock-issue→COGS §5 mechanism, atomic + closed-period rollback, DRAFT→POSTED terminal, the committed-quantity ATP shrink) + API table + events note updated; PLAN 7.3 ticked.
- [done] 7.4 Billing (invoice from delivery → AR) + RMA returns with credit notes (D-046) — **COMPLETES Phase 7 / Sales** and closes the order-to-cash loop (quote → order → delivery → billing → AR invoice; RMA returns reverse both COGS and revenue). **Four tables** (migration `0031_sales_billing_returns`, down_revision 0030, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogen drift SQLite + PG): `sales_billings`/`sales_billing_lines` (`SalesBilling`/`SalesBillingLine`, DocumentMixin + gapless `BIL-2026-NNNNN`, customer + payment_terms snapshot; lines opaque item/unit_price/discount/tax_code snapshot from the order line + nullable `delivery_line_id`) + `sales_returns`/`sales_return_lines` (`SalesReturn`/`SalesReturnLine`, DocumentMixin + gapless `RMA-2026-NNNNN`, warehouse; lines receiving bin + opaque item/price + lot/serial) + a batch-add `returned_quantity` column on `sales_order_lines` (the over-return cap). **constants.py → constants/ package** (enums/permissions/documents, split at the 400-line cap, the finance precedent, all re-exported so existing imports are unchanged): `BillingStatus`/`ReturnStatus` (DRAFT→POSTED|CANCELLED, POSTED terminal), permissions `sales.billing.read/.manage/.post` + `sales.return.read/.manage/.post`, the `BIL-`/`RMA-` doc-types + sequences, docflow links (order→billing `billed_by`, billing→invoice `invoiced_by_invoice`, order→return `returned_by`, return→move `received_by`, return→credit-note `credited_by`), event keys `sales.billing.posted` / `sales.return.received` / `sales.return.credited`. **events.py** adds `BillingInvoiced` (+`BillingInvoiceLine`), `ReturnReceived` (+`ReturnMove`), `ReturnCredited`. **PART A — Billing:** `service/billing.py`+`billing_post.py`+`billing_reads.py`. create_billing DRAFT against an at-least-partially-delivered order; each billed qty ≤ **delivered − invoiced** → over-billing **422 `sales.over_billing`** (you invoice what shipped); a `bill_all_delivered` convenience flag. **post_billing** the heart: resolves the AR control + sales-revenue accounts from `finance/queries` up front (missing default fails before any state change), PUBLISHES `BillingInvoiced`; **finance's `handlers.create_invoice_for_billing`** creates + posts the AR customer invoice **Dr AR control gross / Cr sales-revenue per line + Cr output tax** (partner_id = customer id D-029, due = billing_date + terms) via the existing `create_customer_invoice`+`post_customer_invoice` path; raises `invoiced_quantity`, advances the order INVOICED/CLOSED, links order→billing→`INV-` invoice. The mirror of 6.4's match→AP-bill, sign-flipped. **PART B — Returns:** `service/returns.py`+`returns_post.py`+`returns_reads.py`. create_return DRAFT against a delivered+invoiced order; each returned qty ≤ **invoiced − returned** → over-return **422 `sales.over_return`** (the cap is invoiced not delivered — a credit note must reduce a real invoice). **post_return** the heart: PUBLISHES TWO legs — `ReturnReceived` → **inventory's `handlers.receive_return_moves`** creates a RECEIPT move per line with `valuation_offset_account_id` = the item-category **COGS** account (the override, mirroring 6.3's GR/IR override) so the costing posts **Dr Inventory / Cr COGS** reversing the issue; `ReturnCredited` → **finance's `handlers.create_credit_note_for_return`** posts an AR **credit note** **Dr sales-revenue / Cr AR control + reverse output tax** reversing the billing. Raises `returned_quantity`, links docflow. **Credit notes in finance: ADDED (4.6 had none)** — `finance/service/credit_notes.create_and_post_customer_credit_note`: a credit note is a `CustomerInvoice` row (no new model) whose POSTED journal carries `document_type` **AR_CREDIT_NOTE** + the SIGN-FLIPPED directions, claims a `CN-` number, `open_amount`=0; the draft build is shared verbatim with the AR invoice via the extracted `create_ar_document_draft` builder (only the doc type + journal direction differ; the journal one-side CHECK needs positive amounts so the reversal posts on the flipped sides, NOT negative). **New finance:** `ar_control`/`sales_revenue` posting defaults + `finance/queries.ar_control_account`/`sales_revenue_account`; the two handlers registered in `app.main.register_event_handlers`. `router.py` mounts sibling `billing_router.py`+`return_router.py` (CRUD + post + cancel, permission-guarded manage-vs-post, paginated lists, idempotent create/post). **+40 tests** — test_billing.py (11: draft+BIL+terms snapshot, over-billing 422, undelivered-order 422, post→AR invoice Dr AR/Cr Revenue, tax (Dr AR 33/Cr Rev 30/Cr Tax 3), invoiced_quantity raised + DELIVERED→CLOSED, bill_all_delivered, docflow order→delivery→billing→invoice, closed-period rollback, idempotent reject, cancel-draft-only) + test_billing_api.py (13: 201 DRAFT, +lines, over-billing 422 envelope, post→POSTED + invoiced_quantity + the AR invoice over the wire + billed_by/invoiced_by_invoice docflow, full→CLOSED, create/post idempotent, list paginate+filter, ≤3 budget, manage/post RBAC 403, 401, tenant-B 404), test_returns.py (10: draft+RMA, over-return 422, post receives stock reversing COGS, credit note Dr Revenue/Cr AR + CN- + open_amount 0, returned_quantity raised, docflow received_by/credited_by/returned_by, idempotent reject, cancel-draft-only, closed-period rollback, **the headline full-O2C-nets-to-zero proof: order→deliver→bill→full-return → AR/revenue/COGS balances == 0 via the finance account-balance projection + on-hand restored**), test_returns_api.py (12: 201 DRAFT, +lines, over-return 422 envelope, post→POSTED + returned_quantity + the CN- credit note over the wire + received_by/credited_by/returned_by docflow, create/post idempotent, list paginate+filter, ≤3 budget, manage/post RBAC 403, 401, tenant-B 404). **1110 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +58 from 1052); **21 pg** (schema reset at :55432 → `ATLAS_DATABASE_URL` alembic upgrade head through 0031 → `-m pg` green; autogenerate zero drift on both engines). ruff (app+alembic+tests) clean; stray atlas.db removed. **STRUCTURE §9 self-check: PASS** — every file <400 lines (billing.py 347, returns.py 255, returns_post.py 225, queries.py 399, customer_invoices.py refactored to 398); **§5 verified by grep**: sales imports ONLY finance/inventory/procurement QUERIES + the event bus, never their service/models; finance/handlers + inventory/handlers import only sales events.py + the link constants (the sanctioned events.py allowance); single ORM definition per model; no orphan/empty/dead files; no supplier/product/sku term drift. DECISIONS D-046 recorded; docs/modules/sales.md (Billing + Returns sections: invoice-from-delivery→AR §5 mechanism + the over-billing constraint + order→INVOICED/CLOSED; RMA→stock-receipt-reversing-COGS + credit-note-reversing-revenue + the over-return cap + the O2C-nets-to-zero GL; how credit notes are modeled in finance) + Tables/API tables extended; PLAN 7.4 ticked (**Phase 7 / Sales COMPLETE**).
- [done] 8.1 Manufacturing module skeleton + PP MASTER DATA — multi-level versioned BOMs, work centres, routings (D-047) — **OPENS Phase 8 / Manufacturing** (the fifth business module). **Five tables** (migration `0032_manufacturing_masters`, down_revision 0031, no triggers, reversible, zero autogen drift on SQLite + PG): `mfg_work_centers` (user code, capacity_hours_per_day + efficiency_percent for 8.3, opaque finance cost_center_id), `mfg_boms` header + `mfg_bom_components`, `mfg_routings` header + `mfg_routing_operations`. **Identity = `(item_id, version)`** for BOMs AND routings (not a standalone code — the S/4HANA material+alternative keying); masters carry no gapless number (the production orders in 8.2 will). **Activation model:** DRAFT-editable → ACTIVE-frozen → INACTIVE, with exactly ONE ACTIVE+default version per item (activation demotes the prior default; `get_active_bom_for_item`/`get_active_routing_for_item` resolve it). **Multi-level = via references:** schema is single-level-per-BOM; a component item can itself have its own BOM, resolved by 8.3 explosion (visited set + depth cap); the masters reject only a direct self-component. **Times** = minutes as QuantityType scale-6 (fractional, exact both engines, D-015). **D-029 opaque ids:** BOM/routing item ids + BOM UoM ids validated via `inventory/queries.item_exists` + a NEW `inventory/queries.uom_exists` (the UoM analogue, added in 8.1); work-centre cost_center_id via `finance/queries.cost_center_exists` (already present). **Structure:** `constants.py`, `models/` + `schemas/` + `service/` packages, `queries.py` (the 8.2/8.3 read contract: get/active BOM+routing per item, components/operations, work-centre capacity), `router.py` + sibling `bom_router.py`/`routing_router.py` (one `/api/v1/manufacturing` surface), mounted in main.py after sales + env.py model import added. NO events/handlers (masters drive no cross-module effects). RBAC bom/workcenter/routing read+manage (manage covers activate). **Tests:** 38 new (test_work_centers/test_boms/test_routings service + test_manufacturing_api endpoints+RBAC+pagination+≤3 budget+ETag+tenant-isolation); full SQLite suite 1163 passed / 1 skipped; pg suite 21 passed (PG reset → upgrade head → reversibility proof → `-m pg`). ruff clean. docs/modules/manufacturing.md added; DECISIONS.md D-047 added; PLAN 8.1 ticked. §9 self-check: all files <400 lines (max 330), canonical tree, no orphans/empty files, manufacturing imports only finance/inventory queries downward (grep-verified), no term drift.
- [done] 8.2 Production orders: reserve → issue components to WIP → finish to stock + WIP journals (D-048) — the **manufacturing↔inventory↔finance seam** (mirrors the 6.3 GR/IR override + 7.3 delivery COGS). **Three tables** (migration `0033_manufacturing_production`, down_revision 0032, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogen drift SQLite + PG): `mfg_production_orders` (`ProductionOrder`, DocumentMixin + gapless `MO-NNNNN` claimed at creation, status, item_id/warehouse_id opaque, quantity, composite tenant_fk bom_id + nullable routing_id, planned dates, finished_quantity, **accumulated_wip_cost MoneyType — the WIP-nets-to-zero SSOT**, released_at/finished_at; (tenant,status)+(tenant,item_id) indexes), `mfg_production_order_components` (the EXPLODED reservations: component_item_id/uom_id/bin_id opaque, required_quantity, issued_quantity), `mfg_production_order_operations` (the routing SNAPSHOT for 8.3: work_center_id composite FK, setup/run minutes, planned_minutes = setup + run×qty). **constants.py**: `ProductionOrderStatus` (DRAFT→RELEASED→IN_PROGRESS→FINISHED|CANCELLED — documented transitions), permissions `manufacturing.production_order.read/.manage/.release/.execute` (issue+finish folded under one `.execute` — both POST stock+journals, the shop-floor action), the `MO-` doc-type + year-reset sequence (distinct from purchase-order `PO-`), docflow links `issued_to`/`finished_to`, event keys `manufacturing.production_order.components_issued`/`.finished`. **models/production.py** + **events.py (NEW — first manufacturing events)** (`ComponentsIssued`+`ComponentIssueMove`, `OrderFinished`+`FinishedReceiptMove`, the latter carrying a signed `variance_amount`+`variance_account_id`) + **schemas/production.py** (Create/Read/Detail/Filter + the issue/finish request bodies). **service/**: `production_orders.py` (create+explode/release/cancel + reads) + **`production_post.py`** (the issue+finish POST heart, split at the 400-line cap, the delivery_post precedent). **create_production_order** resolves the item's ACTIVE default BOM (422 `manufacturing.no_active_bom` if none), EXPLODES `required = quantity_per × order_qty / base_quantity × (1+scrap/100)` quantized into component rows (SINGLE-LEVEL — a sub-assembly is produced by its own order), snapshots the routing operations' per-order load, claims the MO- number; validates item STOCKED + qty>0 + warehouse exists. **issue_components** (in run_in_uow): publishes `ComponentsIssued` → **inventory's `handlers.issue_production_components`** creates an ISSUE move per component with `valuation_offset_account_id` = the WIP account → **Dr WIP / Cr Inventory** at the component's MAV/FIFO cost; raises issued_quantity + accumulated_wip_cost (the issued book value, read pre-publish from `inventory/queries.current_unit_cost` — exact for MAV), status → IN_PROGRESS. Over-issue beyond required → 422 `manufacturing.over_issue`. **finish_order** (in run_in_uow): finished unit cost = accumulated WIP / ordered qty (quantized); publishes `OrderFinished` → **inventory's `handlers.receive_finished_order_move`** creates a RECEIPT move (WIP offset) → **Dr Inventory / Cr WIP**; on the FINAL finish the residual flushes via **finance's `handlers.post_production_variance`** (a SECOND OrderFinished subscriber) → Dr/Cr WIP / Cr/Dr the production-variance account so **WIP nets to ZERO**. **The override threaded through the costing engine's OUTBOUND path** (`_apply_outbound` previously hardcoded the COGS offset on an ISSUE — now accepts `valuation_offset_account_id`, applied only on an ISSUE, mirroring the inbound RECEIPT override). **§5 verified by grep:** manufacturing imports ONLY `inventory/queries` (+ the `DEFAULT_COSTING_CURRENCY` constant) + `finance/queries` + its own events — NEVER inventory/finance service; the variance is posted by a finance HANDLER, not a manufacturing journal, keeping the import surface clean. **New finance:** `wip_clearing` (ASSET) + `production_variance` (EXPENSE) posting purposes + `finance/queries.wip_clearing_account`/`production_variance_account` (the gr_ir/ppv mirror); the existing inventory→finance StockValued handler posts both WIP journals unchanged. **New inventory:** `inventory/queries.default_bin_for_warehouse` (the component rows' default source bin). `manufacturing/queries` adds `get_production_order`/`production_order_components`/`order_wip_balance`/`open_production_orders`. `router.py` mounts sibling **`production_order_router.py`** (POST /production-orders idempotent+explode, GET list paginated+filter item/status, GET /{id}+components+operations, POST /{id}/release, /issue-components idempotent, /finish idempotent, /cancel — permission-guarded). **+21 tests** — test_production_orders.py (6: create+explode-with-scrap, no-active-BOM 422, release, issue→Dr WIP/Cr Inventory + on-hand drop, **the WIP-nets-to-zero end-to-end proof** [issue all + finish → WIP balance == 0, finished inventory value == issued cost, via the trial balance], variance-case [non-divisible WIP → $0.01 residual in variance, WIP still 0]), test_production_orders_lifecycle.py (6: over-issue 422, closed-period rollback, insufficient-stock rollback, WIP-default-unmapped 422, cancel-draft + issued-terminal, tenant isolation — **issue #53 honoured: rolled-back state asserted via FRESH scalar reads, never post-failure ORM state**), test_production_orders_api.py (9: create+explode 201, no-active-BOM 422 envelope, **full-flow over the wire + WIP-nets-to-zero via the trial balance**, create idempotent, issue idempotent [no double-issue], list filter + ≤3 budget, docflow order→issued_to/finished_to, execute-needs-permission 403, tenant-B 404). New test infra: `production_factories.py` (the full setup builder — items + GL accounts + open period + warehouse/bins + WIP/variance defaults + component on-hand + active BOM, split from factories.py at the cap) + `_production_shared.py` (tenant-context-wrapped read helpers) + the conftest `_register_event_handlers` autouse fixture + the `production_api` fixture (a client with the setup seeded in its tenant). **1193 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +21 from 1172); **21 pg** (PG reset at :55432 → alembic upgrade head through 0033 → downgrade base → upgrade head reversibility proof → `-m pg` green; zero autogen drift both engines; the `mfg_po_operations` unique shortened to fit the 63-char PG cap). ruff (app+alembic+tests) clean; no stray atlas.db. **STRUCTURE §9 self-check: PASS** — every file <400 lines (production_orders.py 385, production_post.py 336, the touched finance/inventory shared files trimmed back to ≤399, the test files split: factories 280 / production_factories 189 / _production_shared 101 / test_production_orders 216 / _lifecycle 179 / _api 246); §5 grep-verified; single ORM definition per model; no orphan/empty/dead files; no TODO/stub; no supplier/product/sku term drift. DECISIONS D-048 recorded; docs/modules/manufacturing.md (the Production orders section: lifecycle, single-level explosion, issue-to-WIP Dr WIP/Cr Inv + finish-to-stock Dr Inv/Cr WIP, the WIP-nets-to-zero invariant + variance flush, the event-bus §5 mechanism, posting defaults, closed-period rollback) added; PLAN 8.2 ticked.
- [done] 8.3 MRP run + rough capacity check (D-049) — **COMPLETES Phase 8 / Manufacturing** (the deterministic, set-based, level-ordered planning pass). **Three tables** (migration `0034_manufacturing_mrp`, down_revision 0033, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogen drift): `mfg_mrp_runs` (`MrpRun`, DocumentMixin + gapless `MRP-NNNNN` claimed at creation — the depreciation-run precedent, status RUNNING→COMPLETED|FAILED, run_date/horizon_days, warehouse_id opaque NULL=tenant-wide, demand_source, planned_make/buy_count, completed_at; (tenant,status) index + UNIQUE(tenant,run_number)), `mfg_planned_orders` (`PlannedOrder` — the REGENERATED planning output, NO number: order_type MAKE/BUY, item_id opaque, quantity QuantityType = net requirement, due_date, status PLANNED→FIRMED→CONVERTED|CANCELLED, source_notes, level [BOM-explosion depth], converted_document_id, composite FK to mfg_mrp_runs; CHECK quantity>0, (tenant,mrp_run_id)+(tenant,item_id,status) indexes), `mfg_capacity_loads` (`CapacityLoad` — the rough-capacity report: work_center_id composite FK, planned_load_minutes/available_minutes/utilization_percent QuantityType, is_overloaded; UNIQUE(tenant,run,work_center)). **constants.py**: `PlannedOrderType` (MAKE/BUY), `PlannedOrderStatus`, `MrpRunStatus`, permissions `manufacturing.mrp.read/.mrp.run/.planned_order.read/.planned_order.manage`, `MRP-` doc-type + year-reset sequence, job type `manufacturing.mrp_run`, `MRP_DEFAULT_HORIZON_DAYS=30`, `MRP_MAX_EXPLOSION_LEVELS=20` (cycle cap), `planned_to` docflow link, `manufacturing.planned_order.buy_converted` event key. **models/mrp.py** + **schemas/mrp.py** (MrpRunRequest/Read/Summary, PlannedOrderRead, CapacityLoadRead, PlannedOrderConvertRequest). **service/**: `mrp.py` (the ENGINE: gather independent demand → net against supply LEVEL BY LEVEL → explode MAKE BOMs in-memory with the netted-set + depth-cap cycle guard → bulk-insert planned orders → rough capacity check; `@register_job(MRP_RUN_JOB)` handler) + `mrp_capacity.py` (the rough per-work-centre load: planned-MAKE routings + open-order snapshot minutes vs `capacity_hours_per_day × efficiency × horizon × 60`) + `planned_orders.py` (firm/convert/cancel + run/planned-order/capacity reads). **THE ALGORITHM (D-049):** DEMAND = open sales-order undelivered qty (`sales/queries.open_demand_item_ids`+`committed_quantity`) + reorder shortfall (`inventory/queries.items_below_reorder_point`→reorder_quantity); SUPPLY = `inventory.total_on_hand` + `procurement.open_incoming_quantity` (open POs) + `manufacturing.open_production_order_supply` (un-finished orders) + `manufacturing.planned_make_supply` (firmed/converted planned orders); net = max(0, demand−supply). **MAKE vs BUY = active-BOM presence** (`get_active_bom_for_item` → MAKE+explode, else BUY). **Multi-level explosion** is level-ordered + set-based (per level: ONE active-BOMs query + ONE components query, explode in Python; `dependent = quantity_per × parent_net / base_quantity × (1+scrap/100)` quantized); cycle guard = netted-set (low-level-code fold) + 20-level cap. **Run ALWAYS a background job** (PERFORMANCE §3): POST returns 202 {job_id}, idempotent submit (D-013); re-run REGENERATES (deletes PLANNED rows, keeps FIRMED/CONVERTED/CANCELLED). **Conversion §5-clean:** MAKE → manufacturing's own `create_production_order` (intra-module) + docflow run→'planned_to'→order; BUY → publishes `PlannedBuyConverted` → **procurement/handlers.py (NEW) `create_requisition_for_planned_buy`** creates the DRAFT requisition in the SAME transaction + links run→'planned_to'→requisition (the billing→AR-invoice precedent — manufacturing NEVER imports sales/inventory/procurement SERVICE, grep-verified). **Cross-module READ directions added:** manufacturing→sales/queries (`open_demand_item_ids` ADDED for 8.3, `committed_quantity` pre-existing) + manufacturing→procurement/queries (`open_incoming_quantity`, pre-existing); both ONE-directional (sales/procurement don't import manufacturing/queries — manufacturing is newest, grep-confirmed) → NO §5 bidirectional cycle; inventory/finance below everything. `manufacturing/queries` adds `open_production_order_supply`/`get_mrp_run`/`planned_orders`/`planned_make_supply`. `mrp_router.py` (sibling, mounted): POST /mrp/runs (202+job), GET /mrp/runs (paginated)+/{id} (summary+capacity), GET /{id}/planned-orders (paginated, filter type/status), GET /{id}/capacity, POST /planned-orders/{id}/convert (idempotent) /firm /cancel — permission-guarded. `app.main.register_event_handlers` subscribes the planned-buy handler (the D-011 seam; the manufacturing conftest re-registers after the per-test reset). **+29 tests** — test_mrp.py (12: single-level BUY net→planned-purchase, on-hand-covers→no-order, multi-level MAKE explosion with quantities multiplied through [finished 10 / sub-assembly 20 / raw1 30 / raw2 80] + make/buy counts, scrap inflation [10×3×1.10=33], on-hand-reduces-production + cascade, open-PO-reduces-purchase, open-production-order-reduces-production, reorder+sales summed, **cyclic-BOM-terminates-via-cap**), test_mrp_planning.py (8: capacity overload flag [20060>14400], not-overloaded [160<<14400], convert-MAKE→production-order, convert-BUY→requisition, firmed-survives-rerun-nets-as-supply, rerun-regenerates, **set-based run query-budget [<60, no per-item N+1 — HEED #53]**, tenant isolation), test_mrp_api.py (9: **run-as-job 202+job-id→completes→plan persists** [the depreciation-job pattern], idempotent submit [same job id], planned-orders type filter, capacity report, convert-MAKE-over-the-wire, convert-BUY→requisition [DB-verified], firm+cancel, run-needs-mrp.run 403, convert-needs-planned_order.manage 403, tenant-B 404, list + nested-list ≤3 budgets). New test infra: `mrp_factories.py` (the multi-level demand+supply builder — confirmed undelivered sales order, finished←sub-assembly+raw1 / sub-assembly←raw2 active BOMs, active routing, on-hand/open-PO/open-production-order + reorder helpers) + `_mrp_shared.py` (run + planned-by-item helpers) + the conftest `mrp_api` fixture. **1231 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +29 from 1202); **pg green** (PG reset at :55432 → alembic upgrade head through 0034 → downgrade base → upgrade head reversibility proof → `-m pg` green; zero autogen drift both engines). ruff (app+alembic+tests) clean; no stray atlas.db. **STRUCTURE §9 self-check: PASS** — every file <400 lines (service/mrp.py 397, planned_orders.py 274, mrp_capacity.py 209, mrp_router.py 241, models/mrp.py 184; tests split: test_mrp.py 293 / test_mrp_planning.py 225 / _mrp_shared.py 47 / test_mrp_api.py 274 / mrp_factories.py 261); §5 grep-verified (no service cross-import, the new sibling query reads are one-directional, no cycle); single ORM definition per model; no orphan/empty/dead files; no TODO/stub; no supplier/product/sku term drift. DECISIONS D-049 recorded; docs/modules/manufacturing.md (the MRP section: demand sources, netting formula, multi-level explosion + cycle guard, make/buy via active BOM, planned orders + conversion, run-as-job, rough capacity check + overload flag) + docs/modules/sales.md (open_demand_item_ids) updated; PLAN 8.3 ticked.
- [done] 9.1 Quality: inspection flag on goods receipt → inspection lot → accept/reject with stock disposition (D-050) — **OPENS Phase 9 / Quality** (the sixth business module), the DELIBERATELY SMALL QM core the parity doc scopes; inspection plans / characteristics / results recording / usage-decision code catalogs / quality notifications / certificates / non-GR lot origins are all OUT of v1 (recorded in s4hana-parity.md). **One table** (migration `0035_quality_inspection`, down_revision 0034, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogen drift on SQLite + PG, applies on PG to head): `qm_inspection_lots` (`InspectionLot`, DocumentMixin + gapless `QL-NNNNN` claimed at creation; status OPEN→ACCEPTED|REJECTED|CANCELLED, source GOODS_RECEIPT, source_document_id [opaque GR core_documents id], opaque item/warehouse/bin/inspect_lot/serial ids, quantity + accepted/rejected_quantity, disposition nullable, created/decided_date, decision_by; (tenant,status)+(tenant,item_id)+(tenant,source_document_id) indexes + UNIQUE(tenant,lot_number); CHECKs quantity>0, accepted/rejected≥0). **constants.py**: `InspectionLotStatus`, `InspectionSource` (GOODS_RECEIPT only used; enum declared for future origins), `RejectDisposition` (SCRAP + BLOCK implemented; RETURN_TO_VENDOR declared, not implemented), permissions `quality.inspection.read/.manage/.decide` (decide distinct — it moves stock + posts a journal), `QL-` doc-type + year-reset sequence, `inspected_by`/`dispositioned_by` docflow links, `quality.inspection_lot.dispositioned` event key. **models.py** (one table) + **schemas.py** (InspectionLotRead/Filter + InspectionDecideRequest) + **events.py** (`InspectionDispositioned`) + **handlers.py** + **service.py** + **queries.py** + **router.py**. **THE FLOW (D-050):** a GR with `requires_inspection=True` posts → procurement publishes `GoodsReceiptPosted` → TWO subscribers on one key: inventory's `receive_goods_receipt_moves` (FIRST — creates RECEIPT moves + lot/serial instance) then quality's `create_inspection_lots_for_receipt` (SECOND — one OPEN lot per flagged line, resolving the line's lot/serial code to the instance id, GR→`inspected_by`→lot docflow edge), all SAME transaction. **The usage decision** `decide(lot, accepted, rejected, disposition?)` (perm `.decide`) validates accepted+rejected==lot qty (else 422 `decision_quantity_mismatch`); ACCEPTED (rejected==0) or REJECTED (rejected>0, both qty recorded — partial lands REJECTED, no PARTIALLY_ACCEPTED status); idempotent, decided lot terminal (409 `lot_not_open`); cancel discards an OPEN lot. **Disposition via the EVENT BUS (§5):** a reject publishes `InspectionDispositioned` → **inventory/handlers.disposition_rejected_stock** (NEW) — SCRAP = ADJUSTMENT-out (Dr inventory-adjustment/price-difference / Cr Inventory write-off, total on-hand DROPS), BLOCK = TRANSFER to the `blocked_bin_id` quarantine bin (value-neutral, total on-hand UNCHANGED, leaves the usable bin); RETURN_TO_VENDOR → 422 `disposition_not_implemented`; lot→`dispositioned_by`→move edge. **ACCEPTED needs NO move** — received stock is already on hand + usable (no separate QI-stock bucket in v1). Atomic: a closed-period SCRAP write-off rolls the whole decision back (lot stays OPEN — FRESH reads, issue #53). **§5 + NO CYCLE (grep-verified):** quality→procurement/events (subscribe), quality→inventory/queries (bin_exists/lot_id_for_code/serial_id_for_code), quality publishes→inventory/handlers moves stock; procurement+inventory are OLDER and import nothing from quality → all one-directional, no bidirectional cycle; inventory/handlers importing quality.events/constants is the sanctioned declarative upward-import (the sales/manufacturing events precedent); quality imports NO procurement/inventory/finance SERVICE. **router.py**: GET /api/v1/quality/inspection-lots (paginated, filter status/item/source), GET /{id}, POST /{id}/decide (idempotent), POST /{id}/cancel — permission-guarded; mounted after manufacturing in main.py; `app.main.register_event_handlers` subscribes quality's GR creator (after inventory's) + the disposition handler. **+27 tests** — test_inspection.py (13: flagged-GR→OPEN-lot [item/qty/bin + GR→lot docflow], unflagged→no-lot, accept→ACCEPTED+no-stock-change, reject-SCRAP→REJECTED+on-hand-drops+write-off-journal, reject-BLOCK→TRANSFER [total unchanged, left usable bin, in blocked bin], partial accept/reject, split-mismatch 422, reject-needs-disposition 422, RETURN_TO_VENDOR 422, BLOCK-needs-bin 422, re-decide 409, cancel OPEN, **closed-period SCRAP rolls back** [FRESH reads]), test_inspection_api.py (14: list + status filter, GET {id} + 404, decide-accept/reject-SCRAP over the wire, split-mismatch 422, idempotent decide, cancel, RBAC read/decide/manage 403s, tenant isolation, list ≤3 budget). New test infra: `factories.py` (`build_inspection_lot_setup` — a flagged+posted GR auto-creating an OPEN lot + a second QI bin; `create_quality_principal`) + conftest (`inspection_lot_setup`, `quality_api`, `quality_client_b`). **1261 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +27 from 1234); **21 passed pg** (PG reset at :55432 → alembic upgrade head through 0035 → `-m pg` green; zero autogen drift both engines — qm_inspection_lots 0 probe ops). ruff (app+alembic+tests) clean; no stray atlas.db. **STRUCTURE §9 self-check: PASS** — every app file <400 lines (service.py 333, router.py 126, constants.py 132, models.py 118, handlers.py 91, events.py 74, schemas.py 70, queries.py 64; tests test_inspection.py 457 / test_inspection_api.py 239 / factories.py 160 / conftest.py 137, the test-file convention [finance/inventory/sales tests exceed 400 too]); §5 grep-verified (no service cross-import, one-directional reads, no cycle); single ORM definition per model; no orphan/empty/dead files; no TODO/stub; no supplier/product/sku term drift. DECISIONS D-050 recorded; docs/modules/quality.md (NEW — the GR-flag→lot→decide→disposition flow, event-driven lot creation, SCRAP/BLOCK stock effects, accepted-needs-no-move, §5 + no-cycle, out-of-scope) added; PLAN 9.1 ticked.
- [done] 9.2 Maintenance: equipment register + corrective + preventive (interval-based) maintenance orders (D-051) — **OPENS + CLOSES Phase 9 / Quality & Maintenance** (the SEVENTH business module), the Plant-Maintenance half the parity doc scopes; functional-location hierarchies / maintenance notifications / measurement points + counters / counter+strategy scheduling / task lists / spare-part stock issue are all OUT of v1 (recorded in s4hana-parity.md). **Three tables** (migration `0036_maintenance`, down_revision 0035, no trigger-bearing alters, reversible, portable both engines, ≤63-char ids, zero autogen drift on SQLite + PG, applies on PG to head): `pm_equipment` (`Equipment` — flat register, user-supplied `code` UNIQUE per tenant, status ACTIVE/INACTIVE/RETIRED, FREE-TEXT `location` label [NOT a functional-location hierarchy], manufacturer/model/serial/commissioned_date, opaque nullable `cost_center_id`; (tenant,status) index), `pm_maintenance_plans` (`MaintenancePlan`, DocumentMixin so it is the docflow predecessor of generated orders — user `code` UNIQUE, composite tenant_fk equipment_id, status ACTIVE/INACTIVE, interval_value+interval_unit [DAYS/WEEKS/MONTHS], task_description, last_generated_date, next_due_date, estimated_cost MoneyType; CHECK interval_value>0, (tenant,status,next_due_date) due-scan index + (tenant,equipment_id) index), `pm_maintenance_orders` (`MaintenanceOrder`, DocumentMixin + gapless `MNT-NNNNN` claimed at creation — prefix **MNT-, NOT MO-** [manufacturing's prefix]; order_type CORRECTIVE/PREVENTIVE, status DRAFT→SCHEDULED→IN_PROGRESS→COMPLETED|CANCELLED, composite tenant_fk equipment_id + nullable maintenance_plan_id, description, scheduled/completed_date, estimated/actual_cost MoneyType, assigned_to [plain user id, no FK]; (tenant,equipment_id,status)+(tenant,status,scheduled_date) indexes + UNIQUE(tenant,order_number); the maintenance_plan_id composite FK spelled `fk_pm_mnt_orders_maintenance_plan_id_pm_maintenance_plans` to fit the 63-char PG cap). **constants.py**: `EquipmentStatus`, `MaintenanceOrderType/Status`, `MaintenancePlanStatus`, `IntervalUnit`; permissions `maintenance.equipment.read/.manage`, `maintenance.order.read/.manage/.complete` (.complete distinct — records the actual cost, the journal.post precedent), `maintenance.plan.read/.manage/.run` (.run distinct for segregation of duties); `MNT-` doc-type + year-reset sequence; plan→`generates`→order docflow link + the plan doc-type. **models.py** (three tables, one concern, under the cap) + **schemas.py** (Create/Update/Read/Filter for the three + ScheduleOrder/CompleteOrder/RunPreventiveResult). **service/** package (each <400): `equipment.py` (CRUD + cost-centre validation via finance/queries — D-029), `orders.py` (create_corrective [ACTIVE-equipment-only, 422 `equipment_not_active`], create_generated_order [the plan-run path], schedule/start/complete[records actual_cost]/cancel + reads), `plans.py` (CRUD + activate/deactivate + the RUN). **THE INTERVAL MODEL + RUN (D-051):** first next_due = start_date(or today) + one interval; MONTHS uses calendar arithmetic clamped to month-end (Jan31+1mo=Feb28, the finance periods._add_months pattern re-declared module-locally); a changed interval applies to the NEXT advance (no retro-shift). `run_preventive_maintenance(tenant, as_of)` (endpoint `POST /maintenance-plans/run-preventive?as_of=`): a SET-BASED scan (`queries.due_plans` — ONE query, PERFORMANCE §2, no per-plan N+1 in the scan) of ACTIVE plans with next_due≤as_of → per due plan create ONE PREVENTIVE order (SCHEDULED, scheduled at the plan's CURRENT next_due), write plan→`generates`→order docflow edge, set last_generated_date=that due date, advance next_due. **THE OVERDUE-ADVANCE RULE (the chosen policy):** a plan overdue by MULTIPLE intervals generates exactly ONE order and advances next_due by whole intervals until strictly AFTER as_of (**generate-one-advance-to-next-future** — the alternative one-order-per-missed-interval rejected to avoid order spam). **IDEMPOTENT two ways:** the endpoint is D-013 idempotent AND naturally idempotent (after generation next_due>as_of, so a same-day re-run finds nothing). **v1 COST = RECORD-ONLY (the chosen policy):** completion records actual_cost ON THE ORDER with NO GL posting — the parity scope is corrective/preventive orders, not full work-order costing; a maintenance-expense journal through the cost centre is a documented later → so NO events.py/handlers.py (an empty event file would be a dead file, STRUCTURE §8.3); maintenance publishes/subscribes to NO cross-module event in v1. **Inline at v1 scale (D-032):** the run executes inline (the 6.4 reorder-scan precedent; a job is the later if plan counts grow). **queries.py** (the only file a later module imports — get_equipment/equipment_exists/get_maintenance_order/orders_for_equipment/get_maintenance_plan/due_plans). **router.py** (equipment CRUD + ETag list) mounts sibling **order_router.py** (orders CRUD + schedule/start/complete/cancel, idempotent create+complete) + **plan_router.py** (plans CRUD + activate/deactivate + run-preventive; `/run-preventive` declared before `/{plan_id}` so the literal path matches first; plan list ETag) — ONE surface at `/api/v1/maintenance`, permission-guarded; mounted after quality in main.py; alembic env.py imports the models. **§5 grep-verified:** the ONLY cross-module import is `finance/queries.cost_center_exists` (one-directional — finance is older, imports nothing from maintenance, no cycle); no module SERVICE imported. **+45 tests** — test_equipment.py (7: default-ACTIVE, dup-code 409, cost-centre-must-exist 422, valid-cost-centre stored, update status/fields, update re-validates cost centre, unknown 404), test_maintenance_orders.py (10: create-corrective DRAFT/SCHEDULED, **equipment-must-be-ACTIVE 422**, full schedule→start→complete-with-actual_cost lifecycle, complete-defaults-today, cannot-start-DRAFT, cannot-complete-DRAFT, cancel + terminal-immutable, completed-cannot-cancel), test_maintenance_plans.py (16: next_due computation, **MONTHS end-of-month clamp**, interval>0 422, dup-code 409, **interval-change-no-retroshift**, activate/deactivate, **run generates+advances**, run skips not-due, run skips INACTIVE, **idempotent same-day re-run generates nothing**, **overdue-by-multiple advances to next future [one order, next_due 2026-02-19]**, multiple-due-plans one pass, **run scan linear / no-N+1 [2 vs 4 plans via query_counter]**), test_maintenance_api.py (12: create+get equipment, list status filter, **full order lifecycle over the wire + actual_cost**, inactive-equipment 422, order-create idempotent, **plan CRUD + run-preventive endpoint**, run with ?as_of + naturally idempotent, RBAC read/complete/run 403s, tenant isolation, equipment+plan list ETags [304], equipment+order+plan list ≤3 budgets). New test infra: `factories.py` (build_cost_center/build_equipment/build_corrective_order/build_plan/build_maintenance_setup + create_maintenance_principal) + conftest (maintenance_setup, maintenance_client, maintenance_api, maintenance_client_b). **1315 passed + 1 skipped SQLite** (`-m "not pg and not perf"`, +45 maintenance); **pg: 20 passed** (`-m pg`; PG reset → alembic upgrade head through 0036 → `alembic check` zero drift → `-m pg` green; the one `test_pg_migrations` append-only-trigger failure is a PRE-EXISTING local-role privilege-ordering quirk [migration 0005 REVOKEs UPDATE/DELETE from the table owner, so a freshly-bootstrapped local `atlas` role hits `permission denied` before the trigger fires; verified the trigger raises `ATLAS_AUDIT_APPEND_ONLY` once UPDATE is granted — unrelated to PLAN 9.2; the local PG is on :5432 not :55432 as the docker map is down]). ruff (app+alembic+tests) clean; no stray atlas.db. **STRUCTURE §9 self-check: PASS** — every app file <400 lines (service/orders.py 341, models.py 234, service/plans.py 265, order_router.py 218, plan_router.py 208, schemas.py 206, constants.py 159, router.py 138, service/equipment.py 138, queries.py 97; tests test_maintenance_plans.py 397 / test_maintenance_api.py 333 / others ≤199); §5 grep-verified (only finance/queries imported, one-directional, no cycle); single ORM definition per model; no orphan/empty/dead files (no events.py/handlers.py — record-only, documented); no TODO/stub; no supplier/product/sku term drift. DECISIONS D-051 recorded; docs/modules/maintenance.md (NEW — equipment register, corrective vs preventive orders, the interval model + generation run + overdue-advance rule + idempotency, the v1 record-only cost decision, out-of-scope) added; s4hana-parity.md PM rows updated to Done-with-v1-scope; PLAN 9.2 ticked. **Phase 9 / Quality & Maintenance COMPLETE.**
