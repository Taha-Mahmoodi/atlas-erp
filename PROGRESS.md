# PROGRESS.md — Build Log

One line per completed task, newest at the bottom. This file is the single source of truth for "where was I" — verify the last entry against `git log` before resuming work.

- [done] 0.1 Public repo `Taha-Mahmoodi/atlas-erp` created; `.gitignore` is the root commit on `main` (44369b8)
- [done] 0.2 `dev` branch created and pushed; feature-branch flow active from here on
- [done] 0.3 16 labels created (module:*, severity:*, bug, enhancement, tech-debt, found-during-build)
- [done] 0.4 State files written: CLAUDE.md, PLAN.md, PROGRESS.md, DECISIONS.md + STRUCTURE.md/GITHUB-WORKFLOW.md copied to root
- [done] 0.5 Governance docs: LICENSE (Apache-2.0), NOTICE, README, CONTRIBUTING.md, SECURITY.md, .env.example
- [done] 0.6 CI (`backend`+`frontend` jobs, guarded green pre-code) + PR template + bug/feature issue forms
- [done] 0.7 PR #1 squash-merged to `dev`; branch protection live on `main`+`dev` (required checks backend+frontend, enforce_admins, no force-push)
- [done] 1.1 12 parallel research agents covered all S/4HANA areas from live web sources (June 2026)
- [done] 1.2 docs/research/s4hana-parity.md: 139 capabilities mapped — 45 full / 48 partial / 46 out-of-scope, every cut with reason + later path
- [done] 2.1 PLAN.md expanded: 50 tasks across Phases 3–17, one task = one PR, promotion milestones fixed
- [done] 2.2 Architect panel (2 domain architects + adversarial consolidator) → 20 decisions D-007..D-026 in DECISIONS.md, full spec in docs/architecture.md (643 lines)
- [done] 2.3 STRUCTURE.md amended per D-011/D-015/D-016 (core/money.py, core/custom_fields.py, events.py import allowance); no other tree changes
- [done] 3.1 Backend scaffold: uv project, app factory + error envelope + request-id middleware, build_engine with SQLite FK pragma, Base + naming convention, async Alembic (0001 baseline), template-copy test harness, Makefile + docker-compose — 7 tests green
- [done] 3.2 Tenancy (D-007): core/tenancy.py do_orm_execute fail-closed filter + before_flush stamping + system_context; TenantMixin/UuidPKMixin/tenant_fk in models; adm_tenants + adm_tenant_settings (migration 0002); 18 tenancy tests incl. mapper-enumeration auto-cover + FK backstop + grep gate — 25 tests green total
- [done] 3.3 Auth (D-008): core/auth.py (argon2id off-thread + HS256 JWT), core_users + core_refresh_sessions (migration 0003), CAS refresh rotation with 10s grace + family revocation, get_current_user sets production tenant ContextVar, middleware resets it; +20 tests (51 total). D-027 records core auth-table/router placement.
- [done] 3.4 RBAC (D-009): core/rbac.py code-defined permission catalog + resolve_permissions (one join, 60s TTL cache) + require_permission dependency + Masked field serializer; core_permissions (global) + core_roles/role_permissions/user_roles tenant-scoped (migration 0004); get_current_user now resolves real permissions; admin service role helpers; +14 tests (74 total). Seeded keys: admin.{user,role,audit,tenant}.*, core.document.read, core.setting.manage.
- [done] 3.5 Audit (D-010): core/audit.py split-phase capture (before_flush updates/deletes, after_flush inserts, after_flush_postexec one Core insert in-txn), RequestContext ContextVars, bulk-write-on-audited-model hard guard; core_audit_log append-only via per-dialect triggers (migration 0005); ATLAS_ token→envelope translator (audit.append_only + finance tokens pre-registered); audited: User/Role/RolePermission/UserRole/Tenant/TenantSetting; +18 tests (92 total).
- [done] 3.6 Event bus (D-011): core/events.py DomainEvent base + subscribe/@on registry + publish (buffers in session.info) + run_in_uow (drain handlers FIFO on same session before commit, rollback-all on any failure, cascade cap 50); handler writes inherit tenancy stamping + audit; +11 tests (103 total). Swap seam = EventBus Protocol.
- [done] 3.7 + numbering-half-of-3.8 Docflow + numbering (D-012): core/numbering.py (core_number_sequences model + claim_number via atomic UPDATE...RETURNING with on-demand year reset + idempotent ensure_sequence — gaplessness for committed docs falls out of ACID, no fallback needed: RETURNING works on aiosqlite 3.53); core/docflow.py (core_documents registry + partial unique index on (tenant_id, doc_number), core_doc_links edges, DocumentMixin/document_fk, register_document/link_documents, get_document_chain via two recursive CTEs both directions with depth cap 20 + visited-path cycle guard — WITH RECURSIVE works on aiosqlite); migration 0006 (partial index via postgresql_where+sqlite_where); core/docflow_router.py GET /api/v1/documents/{id}/chain (get_current_user-guarded, tenant-scoped) wired into create_app; DocumentRead/DocChain* schemas in core/schemas.py; +22 tests (134 total). D-012 numbering+docflow models live in their concern files (numbering.py/docflow.py) not models.py to keep it under the ~350-line cap, re-exported at the tail of models.py so they register on Base.metadata. Idempotency-key + pagination halves of 3.8 remain.
- [done] 3.8 Idempotency + cursor pagination (D-013/D-014): core/idempotency.py (core_idempotency_keys natural composite PK (tenant_id,endpoint,key) + reserve() in a separate committed session so concurrent duplicates collide on the PK + IdempotencyContext.capture staging the COMPLETED update on the business session inside run_in_uow for atomic doc+replay + Idempotent guard reading Idempotency-Key header/hashing body/short-circuiting completed-key replays via IdempotencyReplay→app.main handler with Idempotency-Replayed header; fail-closed cleanup on business failure); migration 0007 (revision 0007→0006, plus standalone tenant_id index for the D-007 invariant); model registered from the bottom of core/db.py (not models.py) to dodge the db→audit→models import cycle. core/pagination.py (new flat core file: paginate(session,stmt,order_by,pk,cursor,limit,filters)→Page keyset helper, base64url cursor codec fingerprinting sort+filters, expanded portable OR-form seek predicate, OrderKey/SortDirection, CursorParams/cursor_params default 50/max 200) — D-014 placed it in core/db.py but the machinery warrants its own file (recorded D-028); Page envelope unchanged in core/schemas.py. Row-value tuple comparison verified present on aiosqlite 3.53 (uniform-direction only) but the expanded OR-form is used unconditionally so mixed ASC/DESC is correct on both engines. +16 tests (8 idempotency incl. replay-runs-once/key-reuse/in-progress/missing-header/fail-closed/tenant-isolation, 8 pagination incl. no-overlap-no-gaps/null-terminator/tiebreaker-stability/tampered-cursor/filter-fingerprint/limit-cap/desc-order); 153 total green, ruff clean. Decisions in D-028.
- [done] fix #12 (blocker): migration 0005 DROP TRIGGER used SQLite-only syntax, breaking alembic upgrade head on Postgres. Added core/db_guards.py (dialect-aware drop_trigger/create_abort_trigger/drop_function), refactored 0005 to use it; wired the D-022 Postgres CI step (postgres:16 service, upgrade/downgrade/upgrade reversibility + `pytest -m pg`); added tests/core/test_pg_migrations.py (pg-marked append-only trigger + schema smoke). Validated locally: full migration round-trip + trigger fire on real Postgres 16. SQLite 153 pass, pg 2 pass.
- [done] Phase 3 complete + v0.1.0 promoted to main (merge commit 222a2ee, tagged, released); main==dev trees.
- [done] 4.1 Finance COA + fiscal periods (D-021/D-018): modules/finance package (constants/models/schemas/service[accounts,periods]/queries/router); fin_accounts + fin_account_groups (presentation hierarchy) + fin_fiscal_years + fin_fiscal_periods (migration 0008); account-type model (ASSET/LIAB/EQUITY/REV/EXP + cash_flow_category) that statements will project; period open/close service with the D-018 close seam for journal drafts; finance permissions registered; queries.py exposes find_period_for_date/get_period_status/account_exists; +41 tests (206 SQLite + 2 pg). Validated on Postgres.
- [done] 4.2 Universal journal (D-015/D-017/D-018): core/money.py (MoneyType/QuantityType/RateType TypeDecorators — NUMERIC on PG, INTEGER micro/nano-units on SQLite; quantize half-up; largest-remainder allocate); fin_journal_entries + fin_journal_lines (one-side CHECK, all CO dimensions, denormalized is_posted/posting_date/fiscal_period for header-join-free statements); two-flush post_entry (period check → gapless number claim → DRAFT→POSTED), reverse-only corrections, JournalEntryPosted/Reversed events; four per-dialect triggers (period-close ATLAS_PERIOD_CLOSED, balance ATLAS_UNBALANCED_ENTRY, header + line immutability ATLAS_POSTED_IMMUTABLE) via extended db_guards; idempotent post/reverse endpoints; widened period-close seam rejects drafts; migration 0009. +93 tests (258 SQLite + 11 pg) — all four triggers verified firing on BOTH engines.
- [done] 4.3 Multi-currency (D-019): models/fx.py (fin_currencies w/ partial-unique one-functional index, fin_exchange_rates SPOT/CLOSING, fin_posting_defaults purpose-keyed wiring, fin_fx_revaluation_runs) + is_monetary/currency_code on fin_accounts; constants RateKind/FxRunStatus + 5 FX posting purposes + finance.fx.manage/revalue perms. service/fx.py get_rate (direct-or-inverse w/ 10-dp round, same-currency=1, missing→422 finance.exchange_rate_missing) + functional_currency(_or_none) + translate + currency/rate mgmt; service/posting_defaults.py get/set (unmapped→422); service/fx_translation.py translate_entry_lines wired into post_entry (foreign entries translate at SPOT, residual cent absorbed via largest-remainder so functional Σdebit==Σcredit exactly — balance trigger passes on both engines; rate_override param, no header column so fin_journal_entries+its 4 triggers UNTOUCHED; reversals never re-translated); service/fx_revaluation.py run_fx_revaluation (per-account monetary-foreign-balance scope, CLOSING-rate delta vs functional carrying, balanced FX_REVAL entry + independent next-period auto-reversal linked 'revalues', next-period-open validated up front→422, re-run reverses prior COMPLETED run then reposts, tracked in fin_fx_revaluation_runs). fx_router.py (currencies/exchange-rates/posting-defaults guarded fx.manage; fx-revaluation-runs guarded fx.revalue + idempotent) mounted into router.py. queries.py exposes get_rate/functional_currency. migration 0010 (4 tables + batch_alter fin_accounts — non-trigger-bearing so NO trigger recreation needed; journal table deliberately not altered). +37 tests (295 SQLite + 12 pg); PG reversibility (downgrade base→upgrade head) + foreign-translation balance trigger verified on real Postgres 16. DEVIATIONS: rate override is a post_entry param not a JE column (avoids batch-altering the trigger-bearing journal table); auto-reversal is an independent swapped-line FX_REVAL entry (not reverse_entry of the adjustment) so the adjustment stays POSTED and re-runnable; realized FX + per-open-item revaluation bounded to AP/AR (4.4+), recorded 'partial' in parity doc.
- [done] 4.4 Tax engine: fin_tax_codes (rate/jurisdiction/inclusive-exclusive, payable+receivable account links; migration 0011); calculate_line_tax (inclusive net=gross/(1+rate)) + calculate_document_tax (group per code, allocate exact) with TaxDirection OUTPUT/INPUT; queries expose get_tax_code + calculate_line_tax for Sales/Procurement; tax-codes endpoints (tax_router.py). +17 tests (315 SQLite + 12 pg). Note: finance/router.py at the 400-line cap — next routes split.
- [done] 4.5 Accounts Payable (D-029/D-019): fin_vendor_bills/lines + fin_vendor_payments + allocations (migration 0012), partner_id opaque per D-029; create+post bill posts Dr expense/Dr input-tax/Cr AP-control journal with open_amount; payment clearing reduces open items + realized FX at payment (functional-at-bill-rate − functional-at-payment-rate to fx_realized_gain/loss, balanced via skip_translation clearing entry); payment-run batch; AP aging projection; VendorBillPosted/VendorPaymentPosted events; ap_router.py. +25 tests (351 SQLite + 13 pg incl. realized-FX on Postgres). D-030 records AP placement + journal clearing hook.
- [done] 4.6 Accounts Receivable (D-029/D-019): fin_customer_invoices/lines + fin_customer_receipts + allocations (migration 0013, down_revision 0012, partner_id opaque per D-029, no FK to a customer master; abbreviated 67→45-char allocation FK names for PG's 63-char cap); AP mirror sign-flipped — create+post invoice posts Dr AR-control/Cr revenue/Cr output-tax journal (AR_INVOICE) with open_amount=gross; receipt clearing Cr AR / Dr bank reduces open items + realized FX at receipt (functional-at-invoice-rate vs functional-at-receipt-rate to fx_realized_gain/loss, balanced via skip_translation clearing entry); AR aging projection; DUNNING (DUNNING_THRESHOLDS 7/30/60 days → levels 1/2/3; run_dunning advances dunning_level + last_dunned_date on overdue open invoices, posts NO journal, idempotent-ish per day); CustomerInvoicePosted/CustomerReceiptPosted events; queries get_open_customer_invoices + customer_open_balance (for Sales credit-limit block); ar_router.py mounted one-surface. Factored AP+AR realized-FX clearing into shared service/clearing_fx.py (ClearedItem tuples + control_is_debit flips AP Dr-control vs AR Cr-control); payment_clearing.py refactored to delegate. +29 tests (392 SQLite + 14 pg incl. realized-FX receipt on Postgres). ruff + pytest not-pg + alembic PG upgrade (zero autogen drift) + -m pg all green; finance stays bottom-dependency, partner_id opaque.
- [done] 4.6 Accounts Receivable (D-029/D-019/D-031): fin_customer_invoices/lines + fin_customer_receipts + allocations (migration 0013); invoice posts Dr AR/Cr revenue/Cr output-tax; receipts clear open items + realized FX (shared clearing_fx.py helper, control_is_debit flag — AP+AR one implementation); dunning levels (7/30/60-day thresholds, state-only no journal); AR aging; customer_open_balance query for Sales credit block; CustomerInvoicePosted/CustomerReceiptPosted events; ar_router.py. +29 tests (392 SQLite + 14 pg). D-031 records the shared clearing helper.
- [done] 4.7 Controlling (D-021): fin_cost_centers + fin_profit_centers (hierarchies) + fin_allocation_rules/targets + fin_allocation_runs (migration 0014); run_allocation distributes a source cost-center's period balance to targets via allocate (exact) posting a balanced CO entry tagged per cost_center dimension; journal create_draft_entry now validates cost_center/profit_center existence (service-level integrity, no FK on trigger-bearing fin_journal_lines); cost_center_balance query; AllocationPosted event; co_router.py. +22 tests (428 SQLite + 15 pg). CO is a projection of the universal journal.
- [done] 4.8 Financial statements (D-021 — THE universal-journal payoff): service/statements/ package derives Trial Balance, P&L, Balance Sheet, Cash Flow (indirect), cost-center report, margin-by-product ALL from ONE aggregate over fin_journal_lines (sum functional debit−credit, tenant+is_posted+date predicate) — zero stored totals; retained earnings computed on the fly as net-income-to-date synthetic equity line; cash flow reconciles to is_cash_equivalent movement (built-in is_reconciled self-check); TB and BS self-balance (is_balanced). statements_router.py (finance.statements.read). Migration 0015 promotes ix_fin_journal_lines_proj to covering (postgresql_include + partial where both dialects); line trigger survives (plain CREATE INDEX). +11 tests (438 SQLite + 16 pg). Phase 4 finance ~80% (8/10).
- [done] 4P.1 PERFORMANCE.md addendum integrated: committed at repo root, bound in CLAUDE.md (§6 checklist now part of endpoint DoD), Phase 4P retrofit tasks added to PLAN (query-count fixture, index audit, gzip, background-job core, ETag, perf suite), Phase 16/17 amended (volume seed tenant, deployment doc). v0.2.0 promotion now gated on 4P.2–4P.4 + perf smoke.
- [done] fix #25 (4P.3): AP/AR hot-list filter indexes — ix_fin_vendor_bills_list_filters + ix_fin_customer_invoices_list_filters (tenant_id, partner_id, status, date) in models + migration 0016; regression test asserts both exist in the migrated schema. Audit found all other FK/filter columns already covered (UNIQUE composites + tenant indexes). 440 SQLite + 16 pg.
- [done] 4P.2+4P.4 (closes #27): query_counter fixture (tests/conftest.py, after_cursor_execute on the per-test engine, transaction-control/PRAGMA excluded) + assert_query_budget helper (finance conftest, warm-up request so the D-009 RBAC cache is hot); ≤3-query assertions on all 17 list endpoints (accounts, account-groups, fiscal-years, fiscal-periods, journal-entries, currencies, exchange-rates, posting-defaults, fx-revaluation-runs, tax-codes, vendor-bills, vendor-payments, customer-invoices, customer-receipts, cost-centers, profit-centers, allocation-rules) + ≤4 on the 3 child-bearing details (journal-entry/vendor-bill/customer-invoice). Measured warm path: every list = 2 queries (user load + page select), every detail = 4 (user + header + refresh + lines) — NO N+1 found. GZipMiddleware (minimum_size=500) in create_app() between CORS and RequestId (tests: large gzipped, tiny passthrough, X-Request-ID survives). The six bare-list reference endpoints now cursor-paginated via paginate() + new core map_page() helper (account-groups sort_order/code, fiscal-years/periods start_date, currencies code, posting-defaults purpose, reval-runs created_at DESC — all + id tiebreaker); router.py refactored under the 400-line cap. +12 tests (452 SQLite + 16 pg). docs: finance.md endpoint shapes, core.md guarantee 6.
- [done] 4P.2 + 4P.4: QueryCounter fixture (after_cursor_execute on per-test engine, tx-control excluded) + warm-path budgets asserted on every list (≤3; measured 2) and child-bearing detail (≤4; measured 4 incl. deliberate refresh) endpoint — zero N+1 found; GZipMiddleware (≥500B) with request-id preserved; six reference endpoints cursor-paginated (closes #27); map_page helper keeps routers under cap. +12 tests (452 SQLite + 16 pg).
- [done] fix #30 (STRUCTURE §8.4 size caps): tests/modules/finance/conftest.py 466→127 lines — data builders (setup dataclasses, seed_small_coa/seed_fiscal_year, build_*_setup, create_finance_principal) extracted to tests/modules/finance/factories.py (394); conftest keeps only the thin fixtures (names/signatures unchanged, dataclasses re-exported so test imports keep working). app/modules/finance/constants.py 405→363 by condensing docstrings/comments only — no renames, no value changes, permission registration intact. ruff clean; 452 SQLite passed + 1 skipped (identical to pre-change).
- [done] 4P.5 Background-job core (PERFORMANCE §3, closes #26, D-032): core/jobs.py — core_jobs model (NOT audited; high-churn control rows), @register_job code-defined registry, submit_job (PENDING row commits inside the endpoint's uow atomically with the D-013 capture → a replayed Idempotency-Key returns the SAME job id), schedule_job post-commit via the JobScheduler Protocol seam (in-process asyncio runner v1, 4-concurrent cap via per-loop semaphores, wait_for_jobs() test helper; arq/celery swaps the one scheduler binding); runner opens its own session from the submitter-passed factory (prod = get_session_factory dependency, tests = per-test override), restores tenant_context + audit actor from submitted_by, marks RUNNING (commit), runs the handler in run_in_uow with COMPLETED+result in the same commit, FAILED+error on rollback. core/jobs_router.py GET /api/v1/jobs[/{id}] (auth, tenant-scoped, paginated; D-027 core-router precedent) + JobRead/JobSubmitted in core/schemas.py. Migration 0017_core_jobs (zero autogen drift both engines). #26 fix: POST /fx-revaluation-runs + POST /payment-runs now submit finance.fx_revaluation/finance.payment_run jobs and return 202 {job_id} (service functions unchanged; PaymentRunResult retired). +13 tests (465 SQLite + 16 pg incl. PG downgrade base→head); jobs suite proves PENDING→COMPLETED+result, FAILED rollback, tenant+actor context, cap, polling isolation; FX/AP endpoint tests assert the 202+poll contract with unchanged business outcomes. ruff + pytest -m 'not pg' + alembic PG reversibility + -m pg all green.
- [done] 4.9 Bank reconciliation (D-033): fin_bank_statements + fin_bank_statement_lines (migration 0018, down_revision 0017; statement is DocumentMixin with doc_number NULL — external document, no Atlas number; matched_journal_line_id opaque, cleared_journal_entry_id composite tenant FK; UNIQUE(tenant, statement, line_number) + (tenant, statement, status) + (tenant, value_date) indexes). CSV contract in service/bank_csv.py (header value_date,amount,description,counterparty_ref; ISO dates; signed decimal amounts, positive = money in; per-row 422 error report finance.statement_csv_invalid; closing==opening+Σlines or 422 finance.statement_unbalanced; account must be is_cash_equivalent). import_statement (service/bank_import.py) bulk-inserts lines via ONE ORM-enabled executemany (PERFORMANCE §2 — a 1200-line import = 6 SQL statements, query-counter asserted ≤10); POST /bank-statements runs ≤1000 lines inline (201) and submits finance.bank_statement_import job above (202 {job_id}, PERFORMANCE §3; handler calls the same import; replayed Idempotency-Key returns the same statement/job id). suggest_matches (service/bank_reconcile.py): two set-based rules — exact amount+date, then exact amount ±3 days nearest-wins — over candidate maps from two queries (posted journal lines on the bank account, consumed ids excluded tenant-wide via NOT-IN subquery); rule-3 description heuristic + partial matching documented as v1 boundary. confirm/reject transitions; clear_unmatched_line posts a real Dr/Cr bank vs contra entry (default = bank_unmatched_clearing posting default), JOURNAL type, docflow statement->'posts'->entry, line CLEARED; statement status derives IMPORTED -> PARTIALLY_RECONCILED -> RECONCILED from resolved (MATCHED|CLEARED) counts. Permissions finance.bank.read/.import/.reconcile; bank_router.py mounted one-surface; lists paginated ≤3-query budget (statements sort (statement_date DESC, id) — created_at seek keys repeat the boundary row on SQLite, core fix tracked). +32 tests (497 SQLite + 16 pg incl. zero autogen drift both engines). ruff + pytest -m 'not pg' + alembic PG reset/upgrade/check + -m pg all green.
- [done] 4.10 Asset accounting lite (parity scope: register + SL/DB depreciation runs; NO transfers/retirements/parallel areas — recorded laters): fin_assets + fin_depreciation_runs + fin_depreciation_entries (migration 0019, down_revision 0018, three tables + indexes, NO trigger-bearing alters; UNIQUE(tenant, asset, fiscal_period) on entries = the idempotency backbone; partial-unique (tenant, asset_number) WHERE NOT NULL — AST number claimed at ACTIVATION per D-012; three explicit-named composite FKs to fin_accounts to dodge the column-0 name collision). Lifecycle: create/update DRAFT (validates account TYPES — asset+accumulated postable ASSET, expense postable EXPENSE — salvage<cost, declining rate required (0,100] iff DECLINING_BALANCE, cost-centre dimension exists); activate claims AST-2026-NNNNN + status ACTIVE; capitalize=True ALSO posts Dr asset / Cr asset_acquisition_clearing posting default dated acquisition_date with docflow asset->'posts'->entry; capitalize=False = opening-balance assets. compute_depreciation(asset, period_index, prior_accumulated): STRAIGHT_LINE drift-free cumulative form (amount(n)=quantize(depreciable×n/life)−prior; the final period takes depreciable−prior, so 10000/12 → 833.33/833.34 alternating summing EXACTLY 10000); DECLINING_BALANCE NBV×rate/12 quantized, floored so the crossing period takes NBV−salvage landing EXACTLY on salvage (exhaust n>=life likewise). run_depreciation is SET-BASED (PERFORMANCE §2; constant statement count ~33 regardless of N, query-counter asserted <=35): NOT EXISTS anti-join selection, ONE grouped prior aggregate (count+sum per asset), ONE bulk executemany entry insert, ONE journal entry with lines GROUPED per (expense account, cost centre) / per accumulated account (document_type DEPRECIATION, DEP-2026-NNNNN, docflow run->'posts'->entry); assets at cost−salvage flip FULLY_DEPRECIATED; re-run returns the existing POSTED run (no-op); closed period → 422 finance.period_closed (journal period trigger = bypass-proof backstop); run_date must fall inside the period. POST /depreciation-runs: <=100 eligible inline 201, >100 submits finance.depreciation_run job → 202 {job_id} (D-013 idempotent both paths; replay returns the same run / SAME job id). asset_register(as_of) = pure projection (ONE statement: SUM(entries.amount) over periods ending <= as_of; the *_after columns are per-entry audit trail only, no stored NBV — D-021 spirit). Permissions finance.asset.read/.manage + finance.depreciation.run; assets_schemas.py/assets_router.py siblings mounted one-surface. constants.py hit the 400-line cap and split into the constants/ package (enums/documents/permissions; __init__ re-exports everything, zero import changes — D-034); test factories' _FINANCE_KEYS now registry-derived (auto-grants new finance keys). +29 tests (537 SQLite passed + 1 skipped, 16 pg incl. zero autogen drift + full downgrade base→head reversibility on Postgres 16). ruff + pytest -m 'not pg' + PG schema reset/upgrade/check + -m pg all green. PHASE 4 (Finance & Controlling) COMPLETE — 4.1–4.10 all done (4P.6 ETag + 4P.7 perf suite remain as the v0.2.0 promotion gate). STRUCTURE §9 self-check: no file over the 400-line cap, no orphans, no dependency-rule breaks, no term drift.
- [done] 4P.7 Perf smoke suite (PERFORMANCE §5): tests/perf/ (@pytest.mark.perf, excluded from default runs via -m "not pg and not perf") — session-scoped perf_dataset fixture bulk-seeds ONE mid-volume tenant (30-account COA via real services; 2,500 POSTED journal entries / 20,000 posted lines across 12 OPEN 2026 periods with denormalized is_posted/posting_date/fiscal_period_id; 1,500 mixed-status customer invoices spanning all aging buckets; full-finance principal for API timing) onto a copy of the D-025 SQLite template — or onto ATLAS_PERF_DATABASE_URL (real Postgres, fresh tenant per run) — using Core executemany inserts (PERFORMANCE §2; trigger-safe: 0009's period trigger fires on INSERT of POSTED and passes with periods OPEN, the balance trigger is UPDATE-only, line immutability is UPDATE/DELETE-only; debits==credits per entry by construction). Seeds in ~2s SQLite / ~5s PG. test_budgets.py: journal-entries list API (status filter, page 50) + trial balance/P&L/balance sheet/AR aging service calls + a volume/TB-balanced sanity pin; `timed` helper asserts the MEDIAN of 5 runs (1 warmup; stabler than p95 at n=5) at 2× the §5 budgets on SQLite (600ms/3s) and 1× on Postgres (300ms/1.5s), always printing [perf] report lines. Measured medians: SQLite 4.2ms list, 6.0/6.6/6.0ms statements, 10.8ms aging; Postgres 4.8ms list, 5.6/5.6/5.5ms statements, 8.1ms aging — all far under budget. CI: new NON-BLOCKING `perf-smoke` job (continue-on-error, NOT a required status check; `pytest -q -m perf -s`); backend SQLite step now `-m "not pg and not perf"`. Makefile `perf` target; `perf` marker in pyproject; suite documented in docs/modules/core.md Tests. Default suite unchanged (537 passed + 1 skipped); ruff clean; verified green on BOTH engines. Deviation: the list API is timed with its supported status filter — the journal list endpoint has no date-range filter parameter today (the §5 'journal list w/ filters' path is exercised via filtered keyset pagination).
- [done] 4P.6 Conditional requests on slow-changing reference data (PERFORMANCE §3, closes #28, D-035): new core flat file `core/conditional.py` — `collection_etag` (ONE cheap aggregate `COUNT(id)+MAX(updated_at)`, auto tenant-scoped by the D-007 listener since the `func` aggregate carries the model's mapper), `check_not_modified` (RFC 7232 weak comparison + `W/` prefix + `*`), `conditional_response` (returns a bare `304 {ETag}` WITHOUT awaiting the page builder, so `response_model=Page[T]` is bypassed and the page query never runs). ETag format `W/"<count>-<max_updated_micros>-<tenant8>-<request_fingerprint>"` — collection-level weak validator; the `tenant8` component makes a cross-tenant 304 impossible and the `request_fingerprint` (cursor+limit+filters) makes a wrong-slice 304 impossible. Applied to the seven slow-changing reference lists ONLY (accounts, account-groups, currencies, tax-codes, fiscal-years, fiscal-periods, posting-defaults); transactional lists carry none. The 304 path is cheaper than 200 (2 queries vs 3, query-counter pinned). Structure: journal-entry endpoints moved to a new sanctioned sibling `finance/journal_router.py` so `router.py` stays under the 400-line cap (now 322). +22 tests (14 core unit in tests/core/test_conditional.py + 8 endpoint in tests/modules/finance/test_router_conditional.py) → 559 passed, 1 skipped on SQLite; 16 pg-marked pass after reset+upgrade head (no new migration — D-035 adds no schema).
- [done] 4P.6 Conditional reference reads (PERFORMANCE §3, closes #28, D-035): core/conditional.py weak collection ETag = W/"<count>-<max(updated_at)micros>-<tenant8>-<request-fingerprint>" from ONE COUNT/MAX aggregate (tenant-scoped by the D-007 listener + tenant component = cross-tenant 304 impossible; request fingerprint via pagination.filter_fingerprint = cross-page 304 impossible); conditional_response returns 304 WITHOUT awaiting the page builder so revalidation is 2 queries vs 200-path 3. Applied to /accounts, /account-groups, /currencies, /tax-codes, /fiscal-years, /fiscal-periods, /posting-defaults only (never transactional lists). journal endpoints extracted to finance/journal_router.py (router.py back under cap). +22 tests (559 SQLite + 16 pg). No migration (head 0019).
- [self-check v0.2.0 promotion] STRUCTURE §9 + GITHUB-WORKFLOW §9: working tree clean on dev; all feature branches deleted after merge; both CI checks green on dev; perf-smoke green; zero open issues; no file over the 400-line cap; no orphans; dependency rules intact; no term drift. docker stack verified manually (compose binary absent in this env): backend image builds, `alembic upgrade head` applies all 19 migrations on Postgres 16, app boots and /api/v1/health returns 200, OpenAPI mounts auth/documents/finance/jobs route groups. Full suite 559 passed + 1 skipped (SQLite) / 16 pg / perf 6 passed both engines. PHASE 4 (Finance & Controlling) + Phase 4P (performance retrofits) COMPLETE — promoting dev → main as v0.2.0.
- [done] 5.1 Inventory item masters — NEW module `backend/app/modules/inventory/` (second business module, reads finance/queries downward; permissions registered at import). `constants.py` (single file): ItemType (STOCKED|NON_STOCKED|SERVICE), CostingMethod (MOVING_AVERAGE|FIFO), TrackingMode (NONE|LOT|SERIAL) + LotStatus/SerialStatus, six permission keys (item/category/uom × read/manage). NO number sequence — item_code is user-supplied unique-per-tenant (account-code precedent), required on create; masters carry no gapless document number (those come with moves in 5.2). `models.py` (six inv_ tables): ItemCategory (default_costing_method + THREE OPAQUE finance GL-account ids inventory/cogs/price_difference per D-029 — nullable, validated-in-finance when set, never a cross-module FK), Uom (code/name; base-ness is per item), Item (item_code UNIQUE(tenant,item_code), item_type, category_id+base_uom_id composite tenant_fks, costing_method defaulted-from-category-but-stored per D-020, tracking_mode, is_active, reorder_point/quantity QuantityType nullable; composite index (tenant,item_type,category_id,is_active) for the filtered list + (tenant,category_id) FK index), UomConversion (per-item alt_uom + factor_to_base QuantityType, CHECK factor>0, UNIQUE(tenant,item,alt_uom)), Lot + SerialNumber (master tables defined now, populated by receipts 5.2+; UNIQUE(tenant,item,code) each). **UoM convention chosen: per-item BASE + ALTERNATES** — each item has one base_uom_id; UomConversion(item, alt_uom, factor_to_base) where multiplying an alt-UoM qty by factor yields the base-UoM qty (base EA + BOX factor 12 ⇒ 1 BOX = 12 EA); base↔alt and alt↔alt both derive from the single per-alternate factor (the S/4 alternative-UoM shape). `service/` package (split at the 400-line cap per finance precedent — categories/uoms/items/conversions, __init__ re-exports so `service.create_item(...)` is unchanged): CRUD for all four masters + validations (code/item_code unique with friendly ConflictError; category+base_uom exist; costing_method defaults from category when omitted; tracking-only-on-STOCKED; conversion factor>0 + alt≠base; GL accounts validated via finance/queries) + pure `convert_quantity(qty,from,to,base,factors)=qty*factors[from]/factors[to]` quantized scale 6, unknown-UoM raises. `queries.py` (the only file other modules import): get_item/item_exists/get_costing_method/get_base_uom/get_category_accounts(item)->(inv,cogs,price_diff). `router.py` /api/v1/inventory: item-categories/uoms/items CRUD (paginated + collection ETag per D-035, ≤3-query budget) + nested GET/POST /items/{id}/uom-conversions, every endpoint permission-guarded; mounted in main.py after finance. NO events.py/handlers.py (masters drive no cross-module effects; no-stub rule — they arrive with moves in 5.2). Migration `0020_inventory_items` (down_revision 0019): six tables + FK/filter indexes, no trigger-bearing alters, portable both engines; zero autogenerate drift on PG, full downgrade base→head reversibility verified. **Sanctioned cross-module addition:** `finance/queries.account_exists_by_id(session,tenant,account_id)` (by-id companion to account_exists, for the category's opaque GL ids — D-029/STRUCTURE §5). docs/modules/inventory.md added. +46 tests (test_categories/test_uom/test_items/test_items_api) → 623 passed + 1 skipped SQLite, 16 pg. ruff + pytest -m "not pg and not perf" + PG reset/upgrade/downgrade-base/upgrade-head + -m pg all green; no stray atlas.db; no file over the 400-line cap.
