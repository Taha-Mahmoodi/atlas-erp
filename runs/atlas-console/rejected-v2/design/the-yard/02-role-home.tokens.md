# the yard — Surface 2 of 7 — Role Home — composition sidecar

**Composition anchor:** `dense-grid`
**Background mode:** `flat-surface`

One line: the seven-lane bento grid fills the canvas edge-to-edge inside the chrome margin —
rail and header are subordinate to it, not a competing focal point — on one solid substrate
with no image, gradient, or texture behind the cards; the categorical hue lives only in each
lane's badge and its cards, never in a background treatment.
