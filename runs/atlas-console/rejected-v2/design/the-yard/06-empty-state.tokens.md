# the yard — Surface 6 of 7 — Empty State (Filtered) — composition sidecar

**Composition anchor:** `centered-statement`
**Background mode:** `flat-surface`

One line: a single 480px column, dashed empty token-slot above a two-line message above one
verified-safe action, vertically and horizontally centered in a flat `substrate` field below
a sticky title band and a dismissible filter-chips band — deliberately one of the calmest,
most open compositions in the set (paired with the login screen), set against the
dense-grid table screens elsewhere in this concept's surface list.

**Files:**
- light: `design/the-yard/06-empty-state-light.md`
- dark: `design/the-yard/06-empty-state-dark.md`
